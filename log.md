---
title: Coderails Wiki Log
type: log
created: 2026-05-30
last_updated: 2026-07-13
sources: []
tags: [log]
---


# Log

Append-only chronological record. Format: `## [YYYY-MM-DD] operation | description`

---

## [2026-05-30] init | Wiki bootstrapped for coderails plugin. Seeded design, hooks, commands, investigations pages.

## [2026-05-31] ingest | verify-loop hardening session: meta-bullet exclusion, threshold <3→<1, string-coercion, weak-check deletion; new design/hook-exit-codes; session source recorded.

## [2026-05-31] lint | Clean: 0 contradictions, 0 orphans, 0 stale, 0 real dangling links. Coverage gaps noted (1/10 commands, 2/6 hooks, 0/4 skills documented) — expected, wiki is young.

## [2026-05-31] ingest | Documented 4 hooks: destructive_bash_gate, test_gate, inject_context, discipline_catchup.

## [2026-05-31] ingest | Prompting-doc alignment: --no-verify gate, agentic-loop context/delegate guidance, discipline scope+floor clauses, workflow parallel-calls.

## [2026-05-31] lint | 1 fix (index.md "three"→"four" discipline hooks). No contradictions, no orphans, no stale, 3 dangling (workflow-chain/init known forward-refs, wiki-links template-only). Schema discrepancy: hook pages use type:component vs template type:hook; agentic-loop uses type:skill (correct). Coverage: 0/10 commands, 6/6 hooks, 1/4 skills documented.

## [2026-06-01] ingest | check_verify_loop escalated to total enforcement: source-token regex + meta_pattern removed, any untagged DNV bullet blocks, (unverifiable: …) is the sole escape. Updated hook page, index trigger row, added session source.

## [2026-06-01] ingest | Both repos now hosted private under blueman82: plugin → github.com/blueman82/coderails (git init this session), wiki → github.com/blueman82/coderails-wiki. Added design/repo-hosting.md (clone locations, .gitignore rules); flagged stale "not a git repository" claim in CLAUDE.md.

## [2026-06-01] ingest | install.sh bash 3.2 bug: ${answer,,} (bash 4 case-mod) errored "bad substitution" on a macOS /bin/bash 3.2 machine at the overwrite prompt. Fixed in plugin repo (a312b28) with tr lowercase. Filed investigation, added bash-3.2 portability note to install-and-cache-trap.

## [2026-06-01] ingest+lint | CLAUDE.md "not a git repository" line corrected (plugin 9f15823); updated repo-hosting.md (resolved the stale-flag note, added logs/ to documented .gitignore after second-machine commit cc55b14). Lint: 6/6 today-links resolve, no dangling, no remaining stale git-absence assertions (3 grep hits are descriptive).

## [2026-06-01] ingest+lint | agentic-loop delegate-all-impl-to-sonnet (plugin commit 3c33f99): updated skills/agentic-loop.md (Phase 3 two-rung ladder, Phase 3a added, old "work directly" guidance removed, delegation rung section rewritten); created sources/session_2026-06-01_agentic-loop-delegate-all-impl.md; updated index.md sources list. Lint: all [[links]] added resolve to real .md files (verified: find + grep); no dangling; no contradictions introduced; pre-existing: log.md frontmatter last_updated frozen at 2026-05-30 (matches established pattern, not fixed).

## [2026-06-25] ingest | Agentic-loop upgrade arc (PRs #12–#18). Six sequenced specs + a cleanup: A clean-migration disposition fork (Phase 2.6, reviewer-as-gate, disposition-violations KPI); C1 progress.json lifecycle + presence/ownership Stop hook (loop_state_guard, agentic_loop_path.sh sole path authority); C2 declaration-based anti-stall Stop hook (loop_stall_guard, LOOP-STOP vocab, loop_state_common.sh shared lib); B slim 454→434 (Phases 7&8 stub, 16 war-stories compressed, six no-touch regions); D vendored coderails:test-driven-development (reversed A's reference→vendor); E spec.md+plan.md durable artifacts via coderails:writing-plans (Phases 2.7/2.8, ≥3-unit guard, E→D tie); #18 dropped tsh ssh from Phase 4/12 examples. Created: skills/test-driven-development, skills/writing-plans, hooks/loop_state_guard, hooks/loop_stall_guard, design/spec-plan-progress-artifact-chain, sources/session_2026-06-25_agentic-loop-upgrade-arc. Substantially updated skills/agentic-loop (new phases -2/0.5/2.6/2.7/2.8/3/3a, LOOP-STOP contract, artifact chain, progress.json schema growth). Index + log updated.

## [2026-06-25] lint | 3 fixes applied: (1) discipline-loop.md updated — stale check_verify_loop description replaced with total-enforcement behaviour, six-hook composition section added, last_updated bumped to 2026-06-25; (2) loop_stall_guard.md — added missing [[spec-plan-progress-artifact-chain]] cross-link; (3) log.md last_updated corrected. No contradictions found. 0 new dangling links (all 6 new page links resolve). 0 new orphans. Pre-existing known dangling: [[workflow-command-chain]], [[workflow-init]] (forward-refs), [[wiki-links]] (template-only). Six no-touch regions: consistent between agentic-loop.md and Spec B source; design page correctly does not duplicate them. Schema conformance: 6 new pages all pass frontmatter check; hook pages still use type:component (pre-existing, not new). Coverage: 0/10 commands documented, 8/8 hooks documented, 3/6 skills documented (handoff/planning-sequence/premortem not yet documented).

## [2026-06-25] ingest | Documented 8 previously-undocumented skills: handoff, improve-prompt, planning-sequence, premortem, wiki-ingest, wiki-init, wiki-lint, wiki-query. Skills coverage now 11/11. Pages created by parallel agents; index.md and log.md updated in this consolidation pass. index.md skill table expanded (8 new rows, [[wiki-link]] form); "What this plugin is" skills list updated to name all 11 skills.

## [2026-06-25] lint | 5 dangling [[workflow]] links found across 4 new skill pages (wiki-ingest x2, wiki-init, wiki-lint, wiki-query) — no commands/workflow.md exists in the vault (0/10 commands documented). Fixed: all 5 replaced with prose reference `/coderails:workflow` (link removed, prose retained). 0 other new dangling links — all 13 unique [[links]] across the 8 pages resolve (verified). Schema conformance: all 8 new pages pass frontmatter check (type: skill, created/last_updated present, sources non-empty, tags non-empty, confidence labels present). 0 new orphans, 0 contradictions. Pre-existing known dangling not re-reported: [[workflow-command-chain]], [[workflow-init]], [[wiki-links]] (template-only). Final skills coverage: 11/11.

## [2026-06-25] ingest | 10 command pages documented by parallel agents (workflow, prep, push, merge, init, test-gate-setup, assumptions, disconfirm, notchecked, verify). Commands coverage now 10/10. index.md commands table updated: all 10 rows replaced "Not yet documented" with [[wiki-link]]; stale /coderails:workflow-init row corrected to /coderails:init → [[init]]; stale absolute path fixed (/Documents/Github → /Github); last_updated bumped to 2026-06-25. 6 hook pages corrected type: component → type: hook (schema conformance).

## [2026-06-25] lint | Fixes applied: (1) index.md — workflow-init→init row reconciled (clears long-standing [[workflow-init]] dangling forward-ref), path corrected, 10 command links added; (2) commands/config-resolution.md — stale NO_CONFIG behaviour corrected (halt+prompt→MINIMAL MODE per current prep.md:11–16 + workflow.md:11–17), workflow-init references updated to init, last_updated bumped; (3) hook pages type:component→type:hook ×6 (schema conformance). [[workflow-init]] now resolved. Pre-existing known dangling not re-reported: [[workflow-command-chain]], [[wiki-links]] (template-only). Coverage: commands 10/10, hooks 8/8, skills 11/11.

## [2026-06-25] lint | Full-vault audit post 10-command-page ingest. Results: 0 contradictions, 0 stale pages (all dates ≥2026-05-30, within 30-day window), 0 orphans (every page has ≥1 inbound link). Schema conformance: all 10 new command pages pass (type:command, created/last_updated/sources/tags present); config-resolution.md in commands/ dir has type:design (pre-existing mismatch, judgement call — leave for human to decide whether to move to design/). Dangling links: [[workflow-command-chain]] (commands/config-resolution.md — pre-existing forward-ref, no target page exists); [[wiki-links]], [[page_name]], [[link]], [[links]], [[wiki-link]] (all in log.md or skills/wiki-lint.md prose/code-examples, not navigable links — benign); [[workflow-init]] CONFIRMED RESOLVED (only in log.md historical prose). No new dangling links introduced by the 10 new command pages. Cross-references: new command pages are well-linked (workflow 12, prep 11, push 12, merge 7, init 3, test-gate-setup 1 inbound links each); all discipline commands cross-reference their hook counterparts. One missing cross-ref noted for human judgement: [[test_gate]] and [[test-gate-setup]] are not linked from discipline-loop.md (which covers the six discipline hooks generally — test_gate is opt-in, different character, plausible omission). No action taken. Final coverage: commands 10/10, hooks 8/8, skills 11/11.

## [2026-06-25] lint | Two cleanups applied: (1) commands/config-resolution.md moved to design/config-resolution.md via git mv (type:design page now in correct directory; all 12 [[config-resolution]] inbound links still resolve — Obsidian resolves by filename globally); (2) [[workflow-command-chain]] in design/config-resolution.md (the sole content-page occurrence) repointed to [[workflow]]. Full-vault audit: 0 contradictions, 0 stale pages, 0 orphans, 0 real navigational dangling links. Remaining apparent-dangling are all benign: [[workflow-command-chain]]/[[workflow-init]] in log.md historical prose only; [[wiki-links]]/[[link]]/[[links]]/[[wiki-link]] are syntax-illustration prose in skills/wiki-lint.md; [[page_name]] is a citation-format example in skills/wiki-query.md prose; [["$answer"=="y"]] is a bash conditional in a fenced code block (false positive). Final coverage: commands 10/10, hooks 8/8, skills 11/11, design 7/7.

## [2026-06-25] ingest | Self-containment + plugin hardening cluster (PRs #19–#30). Thread 1: 12 superpowers skills vendored into coderails: namespace (using-coderails, using-git-worktrees, requesting-code-review, receiving-code-review, finishing-a-development-branch, dispatching-parallel-agents, systematic-debugging, verification-before-completion, writing-skills, subagent-driven-development, executing-plans, brainstorming); SessionStart inject_bootstrap.sh hook added (PR #23); agentic-loop Phase 3 rewired to coderails:subagent-driven-development + stale claude-guardrails refs fixed (PR #24); design spec at docs/superpowers/specs/2026-06-25-coderails-self-containment-design.md (PR #26). superpowers now uninstallable (only retained external dep: pr-review-toolkit for agentic-loop Phase 4b). Thread 2: Two phantom hooks made real — no_edit_on_main.sh (blocks code-file Write/Edit/MultiEdit on main/master) and enforce_pr_workflow.sh (blocks gh pr create/merge without prior /push or /review-pr). Thread 3: install.sh chmod derives from hooks.json (PR #28); discipline_common.sh shared lib extracted (PR #29). Created: design/self-containment.md; hooks/inject_bootstrap.md, hooks/no_edit_on_main.md, hooks/enforce_pr_workflow.md; 12 skill pages (using-coderails through brainstorming); sources/pr_19-30_self-containment-and-hardening.md. Updated: index.md (skills ×12 + hooks ×3 + design ×1 + source); enforcement-model.md (hook table expanded, new hook cross-refs); discipline-loop.md (discipline_common.sh section); skills/agentic-loop.md (PR #24 rewire documented). Final coverage: commands 10/10, hooks 11/11, skills 23/23, design 8/8.

## [2026-06-25] lint | Post-ingest lint for self-containment cluster. Fixes applied: (1) index.md — invalid YAML frontmatter (parenthetical in last_updated field) corrected; (2) discipline-loop.md — stale section heading "6 hooks total" corrected to "4 Stop hooks; 11 hooks total across all events"; (3) commands/push.md — added missing [[enforce_pr_workflow]] cross-ref (hook blocks gh pr create without prior /push); (4) commands/merge.md — added missing [[enforce_pr_workflow]] cross-ref (hook blocks gh pr merge without prior /review-pr); (5) commands/workflow.md — added [[enforce_pr_workflow]] cross-ref in See Also. 0 contradictions found. 0 stale pages (all dates ≥2026-05-30, within 30-day window). 0 orphans (all 17 new pages have ≥1 inbound link; minimum 1, maximum 11). Dangling links: 9 apparent; all confirmed benign (log.md historical prose, syntax-illustration, bash false-positives — same set as prior lint). Schema conformance: all 17 new pages pass (correct types, required frontmatter fields present, sources non-empty, tags non-empty). Final coverage: commands 10/10, hooks 11/11, skills 23/23, design 8/8, sources 6/6.

## [2026-06-25] ingest | agentic-loop slim v2 (reference-not-embody), PR #39 (merged 51414a8). The task aimed to shrink skills/agentic-loop/SKILL.md by replacing inline restatements with one-line references to vendored coderails: skills. Realistic delta turned out to be ZERO. PR #39 normalised 13 bare/unqualified skill refs (/agentic-loop, /planning-sequence, /premortem, /wiki-query, /workflow, /wiki-ingest, /wiki-lint, coderails:handoff, coderails:writing-plans, pr-review-toolkit) → fully-qualified /coderails: form — the entire actionable surface; prose-only, no C1/C2 frozen region touched, 3 agentic-loop hook suites green. Two independent passes (main context + a blind Explore agent) classified all 22 phases against a four-part test (generic + fully-covered + worker-facing + no-autonomy-delta) and BOTH converged on zero replaceable passages: every phase fails part 3 (orchestrator-facing) or part 4 (autonomy superset of the vendored skill), or is frozen. Last near-miss Phase 5 is KEEP (reproduce-before-fixing core = systematic-debugging, but wrapped in SOT-caching + hard-stop autonomy delta). Verdict: do not re-attempt the slim. Created: sources/pr_39_agentic-loop-slim-v2.md. Updated: skills/agentic-loop.md (new § "Slimming v2" + source in frontmatter); index.md (sources list).

## [2026-06-25] lint | Post-ingest lint for agentic-loop slim v2 (PR #39). New page sources/pr_39_agentic-loop-slim-v2.md: NOT orphan (3 inbound — index.md, log.md, skills/agentic-loop.md); both outbound links resolve ([[agentic-loop]], self); schema conforms (type:source, all required frontmatter present, sources + tags non-empty). 0 contradictions — the prior "Slimming (Spec B): 454→434 lines" and the new "Slimming v2" coexist (Spec B end-state 434; later Specs D/E/#24/#39 brought it to the current 459, verified via wc -l). 0 new stale pages, 0 new orphans, 0 new dangling links. No fixes required.

## [2026-06-25] ingest | PR #40 hook hardening (merged 2e31188). Three changes: (1) enforce_pr_workflow.sh — added `git merge` gate: only fires on main/master (Gate 4b branch check); conflict-resolution ops and --help/--dry-run exempted; reuses review-pr evidence scan same as `gh pr merge`. Closes local-merge bypass left by finishing-a-development-branch "merge locally" option. (2) inject_bootstrap.sh — clean-break jq refactor: escape_for_json() bash function removed entirely; single `jq -n --arg ctx ...` now handles all JSON escaping. Plugin-root path made portable (CLAUDE_PLUGIN_ROOT env var → two-level fallback). (3) New hooks/scripts/tests/run_all.sh aggregate runner: discovers *.test.sh, counts failures, zero-test guard exits 1 to prevent false-success. Created: sources/pr_40_hook-hardening.md. Updated: hooks/enforce_pr_workflow.md (logic summary rewritten, git-merge gate + Gate 4b + why-it-exists extended, finishing-a-development-branch cross-ref added); hooks/inject_bootstrap.md (logic summary + new jq-refactor section, sources updated); index.md (enforce_pr_workflow row updated to mention git merge, new source entry).

## [2026-06-25] lint | Post-ingest lint for PR #40 hook hardening. New page sources/pr_40_hook-hardening.md: NOT orphan — inbound links from index.md (sources list), log.md (this entry), hooks/enforce_pr_workflow.md (sources frontmatter), hooks/inject_bootstrap.md (sources frontmatter) = 4 inbound links (verified). Outbound links: [[enforce_pr_workflow]] resolves to hooks/enforce_pr_workflow.md (verified); [[inject_bootstrap]] resolves to hooks/inject_bootstrap.md (verified); [[no_edit_on_main]] resolves to hooks/no_edit_on_main.md (verified). Schema conformance: type:source, created/last_updated 2026-06-25, sources:[], tags non-empty — all fields present (verified). No contradictions: enforce_pr_workflow.md prior text stated two-gate model; updated to three-gate — consistent with source. inject_bootstrap.md logic summary updated to match current jq-based implementation; no prior claim contradicted. 0 new stale pages, 0 new orphans, 0 new dangling links introduced. No fixes required.

## [2026-06-25] ingest | PR #41 Phase 2.5 brainstorming xref (merged fd3e5e1). One-bullet additive change to skills/agentic-loop/SKILL.md Phase 2.5: design agent now applies [[brainstorming]]'s design-quality discipline (YAGNI, design-for-isolation, weigh viable approaches) without brainstorming's human-approval gates. Rationale: brainstorming is human-gated by construction; the autonomous loop reuses the design thinking by reference, not by invoking the gated skill. Same reference-not-embody pattern as PR #39. Durable point: "why doesn't the loop just invoke brainstorming?" → brainstorming blocks on a human at its approval gates; Phase 2.5 borrows its *criteria*, not its control flow. Created: sources/pr_41_phase25-brainstorming-xref.md. Updated: skills/agentic-loop.md (Phase 2.5 table row, new § "Phase 2.5 — brainstorming design-quality by reference", Cross-references entry, source in frontmatter); index.md (new source entry).

## [2026-06-25] lint | Post-ingest lint for PR #41. New page sources/pr_41_phase25-brainstorming-xref.md: NOT orphan — inbound links from index.md (sources list), log.md (this entry), skills/agentic-loop.md (frontmatter sources + Cross-references section) = 3 inbound links (verified). Outbound links: [[agentic-loop]] resolves to skills/agentic-loop.md (verified); [[brainstorming]] resolves to skills/brainstorming.md (verified). Schema conformance: type:source, created/last_updated 2026-06-25, sources:[], tags non-empty — all fields present (verified). 0 contradictions — the new Phase 2.5 note coexists cleanly with the existing Slimming v2 section and the reference-not-embody pattern. 0 new stale pages, 0 new orphans, 0 new dangling links introduced. No fixes required.

## [2026-06-25] ingest | PR #42 skills-hooks seam (merged fbe24f1). Four changes: (F4, headline bug) enforce_pr_workflow Gate 3 regex `\bgit +merge\b` also matched `git merge-base` — POSIX ERE `-` is a word boundary, so `\bmerge\b` fires inside `merge-base`. Fixed to `\bgit +merge([[:space:]]|$)` at both Gate 3 and subcommand-detection; new Case 14 test asserts git merge-base → allow. (F2) git-merge block-message hint reordered to lead with resolution (run /pr-review-toolkit:review-pr first), matching the adjacent gh pr merge hint. (F1) finishing-a-development-branch Option 1 gained a note that enforce_pr_workflow blocks git merge on main unless review-pr ran. (F3) New "Skills↔hooks seam convention" in CLAUDE.md: each direction has a cross-reference obligation (skill → name hook + resolution; hook → update instructing skills). wiki-ingest and wiki-lint gained accurate hook-awareness notes; subagent-driven-development deliberately untouched (uses only read-only git merge-base). Created: sources/pr_42_skills-hooks-seam.md; design/skills-hooks-seam.md. Updated: hooks/enforce_pr_workflow.md (merge-base exclusion section, regex before/after, reordered hint, new See Also entry, sources frontmatter); skills/finishing-a-development-branch.md (Hook-gated actions section, sources frontmatter); index.md (new design row + source entry).

## [2026-06-25] lint | Post-ingest lint for PR #42. New pages: sources/pr_42_skills-hooks-seam.md — NOT orphan: inbound links from index.md (sources list), log.md (this entry), hooks/enforce_pr_workflow.md (frontmatter sources), skills/finishing-a-development-branch.md (frontmatter sources) = 4 inbound (verified). Outbound: [[enforce_pr_workflow]] resolves (hooks/enforce_pr_workflow.md verified), [[finishing-a-development-branch]] resolves (skills/finishing-a-development-branch.md verified), [[skills-hooks-seam]] resolves (design/skills-hooks-seam.md verified). design/skills-hooks-seam.md — NOT orphan: inbound from index.md, log.md, hooks/enforce_pr_workflow.md (See also + seam-convention sections), sources/pr_42_skills-hooks-seam.md (Wiki pages updated), skills/finishing-a-development-branch.md (Hook-gated actions) = 5 inbound (verified). Outbound: [[enforce_pr_workflow]], [[finishing-a-development-branch]], [[skills-hooks-seam]] (self — benign in See also), [[enforcement-model]], [[pr_42_skills-hooks-seam]], [[wiki-ingest]], [[wiki-lint]], [[requesting-code-review]], [[no_edit_on_main]] — all resolve (verified via ls). Schema conformance: both new pages — type:source and type:design, created/last_updated 2026-06-25, sources non-empty (except pr_42 which has sources:[]), tags non-empty, confidence labels present (verified). No contradictions: enforce_pr_workflow.md consistently documents merge-base exclusion; all three touch-points (gate 3, subcommand detection, merge-base section) are coherent. finishing-a-development-branch.md hook note is additive — no prior claim contradicted. 0 new stale pages, 0 new orphans, 0 new dangling links. Design page count: 8→9. Final coverage: commands 10/10, hooks 8/8 (note: 11 hooks in hooks.json; 8 have dedicated pages — unchanged from prior lint), skills 23/23, design 9/9, sources 10/10.

## [2026-06-25] ingest | PR #43 rough edges (merged df4b372). Three fixes: (F1) templates/failure_log.md — 3 pre-filled dev-history rows removed; seeded template is now a clean empty table. (F2) README.md — GitHub-only constraint made explicit in Requirements bullet for /push / /merge. (F3) scripts/merge.sh — informational notice added before gh pr merge when no workflow.config.yaml exists, surfacing that enforce_pr_workflow is inactive and pointing to /coderails:init. Durable points: GitHub-only is a deliberate scope decision (require::repo only matches github.com); enforce_pr_workflow opt-in creates a silent no-enforcement gap that merge.sh now surfaces. Created: sources/pr_43_rough-edges.md. Updated: commands/merge.md (enforcement-gap notice in Design notes, sources frontmatter); design/enforcement-model.md (Scope Assumptions section — GitHub-only + opt-in enforcement gap, sources frontmatter, new [[merge]] cross-ref); index.md (new source entry).

## [2026-06-25] lint | Post-ingest lint for PR #43. New page sources/pr_43_rough-edges.md: NOT orphan — inbound links from index.md (sources list), log.md (this entry), commands/merge.md (sources frontmatter), design/enforcement-model.md (sources frontmatter) = 4 inbound (verified). Outbound links: [[merge]] resolves to commands/merge.md (verified); [[enforcement-model]] resolves to design/enforcement-model.md (verified); [[enforce_pr_workflow]] resolves to hooks/enforce_pr_workflow.md (verified). Schema conformance: type:source, created/last_updated 2026-06-25, sources:[], tags non-empty, confidence labels present (verified). No contradictions: enforcement-model.md Scope Assumptions section is additive; merge.md Design notes is additive — no prior claim contradicted. 0 new stale pages, 0 new orphans, 0 new dangling links introduced. No fixes required.

## [2026-06-26] ingest | PR #44 no_edit_on_main plugin-source gate + docs/superpowers removal (merged 8179b31). Change 1: no_edit_on_main.sh now gates `skills/*/SKILL.md` and `commands/*.md` on main/master like code files — plugin source carried in markdown is source, not docs (the old docs-only carve-out had let a SKILL.md edit land direct on main earlier the same session). Two `/`-anchored path arms added to the gated `case` (`*/skills/*/SKILL.md|skills/*/SKILL.md`, `*/commands/*.md|commands/*.md`); test suite 11→17 cases. Connected decision: `git push` deliberately NOT gated — edit-time is the correct seam; a push gate would be redundant (edit blocked + branch protection), break the workflow's feature-branch push, and is brittle to match. Also closed finding #4 of a 5-tension agentic-loop review (the #42 seam convention unapplied to the loop's own Phase 3/3a worker prompts, fixed in 4ac38bd); the other 4 tensions were owned tradeoffs, not defects. Change 2: removed historical `docs/superpowers` A–E + self-containment spec/plan tree (~4,500 lines, main-branch hygiene; [[self-containment]] unaffected). Created: sources/pr_44_no-edit-plugin-source.md. Updated: hooks/no_edit_on_main.md (gated-set logic, block condition, plugin-source + git-push-non-gating design notes, sources frontmatter, last_updated); index.md (no_edit_on_main hook row, new source entry, last_updated).

## [2026-06-26] lint | Post-ingest lint for PR #44. New page sources/pr_44_no-edit-plugin-source.md: NOT orphan — inbound from index.md (sources list) and hooks/no_edit_on_main.md (frontmatter sources) = 2 inbound (verified via grep). Outbound links [[no_edit_on_main]], [[skills-hooks-seam]], [[enforcement-model]], [[self-containment]] all resolve (verified via ls). hooks/no_edit_on_main.md updated links [[enforce_pr_workflow]], [[hook-exit-codes]], [[destructive_bash_gate]], [[discipline-loop]], [[enforcement-model]] all resolve (verified). Schema conformance: source page type:source, created/last_updated 2026-06-26, sources:[], tags non-empty, confidence labels present (verified/decision tags used). No contradictions: hook page gated-set widening is additive and internally coherent (logic summary, block condition, design note all agree skills/*/SKILL.md + commands/*.md are gated); git-push-non-gating note is a new decision, contradicts nothing prior. 0 new orphans, 0 new dangling links, 0 new stale pages. Sources 11→12.

## [2026-06-26] skip | PR #45 (`docs: sync CLAUDE.md and REFERENCE.md hook maps with #40/#42`, merged e23548e) — deliberately NOT ingested. It corrects stale prose to match hook behaviour the wiki already documents (the #40 `git merge` gate in [[pr_40_hook-hardening]]; the #42 `merge-base/merge-file/merge-tree` exclusion in [[pr_42_skills-hooks-seam]]); no new behaviour or decision to capture, so no source page or page updates. Recorded so a future curator doesn't re-flag #45 as an un-ingested gap (curator principle: document the decision, not redundant content).

## [2026-06-26] ingest | PR #46 gate git push on main/master (merged 7a4906c). enforce_pr_workflow.sh gains a fourth gated subcommand, `git_push`, mirroring the #40 `git merge` gate: a `git push` landing on main/master is blocked (when workflow.config.yaml exists / opt-in) unless `/pr-review-toolkit:review-pr` ran this session. Key design: Gate 4b for git_push keys off the push DESTINATION, not just the checked-out branch — it gates when (a) current branch is main/master OR (b) the command names an explicit main/master destination refspec (`HEAD:main`, `feature:master`, `:refs/heads/main`). Destination anchor `:(refs/heads/)?(main|master)([[:space:];&|)]|$)` accepts whitespace/EOL/shell-separator so `HEAD:main;echo` can't evade. Documented limitation: bare positional `git push origin main` from off-main is NOT parsed (low risk — pushes local main, which tracks origin/main); feature-branch pushes never gated (PR flow requires them). Process point: pr-review-toolkit multi-agent review caught a Critical silent false-allow in the first impl (current-branch-only let `HEAD:main` refspec pushes through), fixed by destination-refspec gating + a metachar-anchor follow-up, each TDD'd; enforce suite 14→27 cases. NOT a reversal of #44's "don't gate git push" — that was no_edit_on_main edit-seam protection; this is review-evidence enforcement in a different hook. Created: sources/pr_46_gate-git-push-on-main.md. Updated: hooks/enforce_pr_workflow.md (header, logic summary Gate 3/4b, block condition, new "git push gate: destination-refspec model" section, sources frontmatter, last_updated); design/skills-hooks-seam.md (new `git push` row in gated-actions table, sources frontmatter, last_updated); index.md (enforce_pr_workflow hook row, new source entry).

## [2026-06-26] lint | Post-ingest lint for PR #46. New page sources/pr_46_gate-git-push-on-main.md: NOT orphan — inbound from index.md (sources list), log.md (ingest entry), hooks/enforce_pr_workflow.md (frontmatter sources), design/skills-hooks-seam.md (frontmatter sources) = 4 inbound (verified via grep). Outbound links [[enforce_pr_workflow]], [[no_edit_on_main]], [[enforcement-model]], [[skills-hooks-seam]], [[pr_40_hook-hardening]], [[pr_44_no-edit-plugin-source]] all resolve (verified via find). hooks/enforce_pr_workflow.md new outbound [[pr_46_gate-git-push-on-main]] resolves. The `pr-review-toolkit` reference is intentionally plain text (no vault page — external plugin dep), avoiding a dangling link. Schema conformance: source page type:source, created/last_updated 2026-06-26, sources:[], tags non-empty, confidence labels present (verified/inferred). No contradictions: the #46 push gate is documented as distinct from #44's no_edit_on_main "don't gate git push" decision (different hook, different invariant — review-evidence vs edit-seam) on both the source page and the hook page; no prior claim reversed. 0 new orphans, 0 new dangling links, 0 new stale pages (all 4 touched pages last_updated 2026-06-26). Apparent dangling `[[:space:]]` in enforce_pr_workflow.md is a regex literal in prose (destination anchor `[[:space:];&|)]`), not a wiki-link — benign, same class as prior bash false-positives. Sources 12→13.

## [2026-06-26] ingest | PR #47 strictcode_skill config (merged f00ed54). `strictcode_skill` field added to `workflow.config.yaml`: [[init]] now prompts for it with language auto-detection (`go.mod` → `/strictcode-go`; `package.json`+`.ts` → `/strictcode-ts`; else `/strictcode-python`; "none" disables). [[push]] and [[workflow]] now read `config.strictcode_skill` (default `/strictcode-python`) instead of calling `/strictcode-python` directly. `workflow.md` `allowed-tools` frontmatter expanded to pre-authorise `/strictcode-go` and `/strictcode-ts`. Fully backward-compatible: absent/null falls back to `/strictcode-python`. Created: sources/pr_47_strictcode-skill-config.md. Updated: commands/init.md (strictcode_skill prompt + YAML schema), commands/push.md (pre-flight note + config fields table), commands/workflow.md (Phase 3 pre-flight + config fields table + Design notes), design/config-resolution.md (config fields table added, strictcode_skill row), index.md (source entry). Sources 13→14.

## [2026-06-26] lint | Post-ingest lint for PR #47. New page sources/pr_47_strictcode-skill-config.md: NOT orphan — inbound [[wiki-links]] from index.md (sources list), design/config-resolution.md (config fields table), commands/workflow.md (Design notes section), commands/init.md (note after config fields schema) = 4 inbound (verified); plus frontmatter-sources reference from commands/push.md. All 4 outbound links resolve: [[push]], [[workflow]], [[init]], [[config-resolution]] (verified). Schema conformance: type:source, created/last_updated 2026-06-26, sources:[], tags non-empty, confidence labels present (verified/inferred used). 0 new dangling links — all apparent dangling are pre-existing benign bash false positives ([[:space:]] regex literals in enforce_pr_workflow.md, skills-hooks-seam.md, pr_42/46 source pages; [["$answer"=="y"]] bash conditionals in log.md and investigation page; all confirmed same class as prior lints). 0 orphans (all 71 pages have ≥1 inbound link or are exempt). 0 contradictions — strictcode_skill is additive; no prior claim about strictcode reversed. Updated pages: commands/init.md (last_updated 2026-06-26), commands/push.md (last_updated 2026-06-26), commands/workflow.md (last_updated 2026-06-26), design/config-resolution.md (last_updated 2026-06-26), index.md (last_updated 2026-06-26). Sources 13→14.

## [2026-06-26] ingest | PR #50 planning-sequence gate in writing-plans flow (merged 03b5b11). `skills/writing-plans/SKILL.md` gains `## Stress-test before implementation (required)`: after the self-review gate and before implementation hand-off, the written plan runs through `/coderails:planning-sequence` (Pre-Parade → Premortem → Red Team), findings fold back into the plan inline, then hands to `subagent-driven-development`/`executing-plans`. `REFERENCE.md` writing-plans entry gains a matching "Next step (required)" line (no doc↔skill drift). Advisory, not mechanical — no hook enforces it (consistent with [[enforcement-model]]). Relationship reconciled: [[planning-sequence]] was framed as an optional UPSTREAM step before decomposition; it is now also a required DOWNSTREAM gate after the plan is written — both valid, the mandatory instance is post-plan. PR also relocated the engineering-principles spec/plan into `docs/coderails/{specs,plans}/` (conforming to REFERENCE.md's already-documented structure; files had drifted to `docs/` root) and landed design+plan docs for the not-yet-implemented strictcode→engineering-principles vendoring (no `engineering-principles` skill dirs exist yet; config still `strictcode_*`). Mixed-concern PR. Pre-existing dead `docs/superpowers` link rode along in relocated `skill-vendoring.md` (carried by 100%-rename, not fixed). Created: sources/pr_50_planning-sequence-gate.md. Updated: skills/writing-plans.md (gate section + See also + frontmatter), skills/planning-sequence.md (Relationship to writing-plans + See also reframe + frontmatter), index.md (source entry). Sources 14→15.

## [2026-06-26] lint | Post-ingest lint for PR #50. New page sources/pr_50_planning-sequence-gate.md: NOT orphan — 4 inbound links (index.md sources list, log.md ingest entry, skills/writing-plans.md frontmatter+body [[pr_50…]], skills/planning-sequence.md frontmatter+body) (verified via grep). All outbound [[links]] from the 3 touched pages resolve (verified via find): pr_50 source → [[enforcement-model]],[[planning-sequence]],[[writing-plans]]; writing-plans.md → [[agentic-loop]],[[planning-sequence]],[[pr_50_planning-sequence-gate]],[[spec-plan-progress-artifact-chain]],[[test-driven-development]]; planning-sequence.md → [[agentic-loop]],[[improve-prompt]],[[pr_50_planning-sequence-gate]],[[premortem]],[[writing-plans]]. 0 dangling. Contradiction check: the writing-plans↔planning-sequence relationship is reconciled, not contradictory — planning-sequence.md explicitly states the new required-downstream-gate role "complements — does not replace" its prior optional-upstream framing (idea-before-planning optional vs written-plan-before-building mandatory). 0 new orphans, 0 dangling links, 0 stale pages (all 3 touched pages last_updated 2026-06-26). Sources 14→15. No fixes required.

## [2026-06-26] ingest | PR #49 gate-function rename (merged 0426312). Replaced positional `# Gate N` early-exit comments with named bash functions across the three workflow/loop hook scripts. enforce_pr_workflow.sh: 7 functions (gate_has_command, gate_safe_passthrough, gate_in_scope, gate_config_present, gate_targets_main — headline rename of former Gate 4b, gate_have_transcript, enforce_required_step). loop_state_guard.sh + loop_stall_guard.sh: Gates 1–4 were byte-identical between them, extracted ONCE into new hooks/scripts/lib/loop_state_common.sh as als_gate_no_transcript, als_gate_stop_hook_active, als_gate_not_a_loop, als_load_progress; each guard keeps its own Gates 5–6. Design rationale: positional ordinals communicate position not purpose, break on insertion; named functions are semantic, greppable, stable — mirrors require::/pr:: idiom in git-common.sh. Known limitation documented (deferred): enforce_pr_workflow checks invocation evidence not completion (hollow invocation satisfies gate; substring false-positive possible); real guarantee is server-side branch protection. CLAUDE.md hook conventions paragraph rewritten: scoped to 3 scripts, corrected false claim about check_verify_loop.sh numbered gates (it has no numbered gates). .worktrees added to .gitignore. Test comments in 3 test files updated to name functions. Created: sources/pr_49_gate-function-rename.md. Updated: hooks/enforce_pr_workflow.md (named-function table, known-limitation section, sources frontmatter); hooks/loop_state_guard.md (shared-lib note, als_gate_* in logic summary, sources frontmatter, last_updated); hooks/loop_stall_guard.md (same); index.md (source entry, loop_state_common.sh description updated, last_updated). Sources 15→16.

## [2026-06-26] lint | Post-ingest lint for PR #49. New page sources/pr_49_gate-function-rename.md: NOT orphan — 6 inbound links (index.md sources list, log.md ingest entry, design/spec-plan-progress-artifact-chain.md frontmatter+body, hooks/enforce_pr_workflow.md frontmatter, hooks/loop_state_guard.md frontmatter, hooks/loop_stall_guard.md frontmatter) (verified via grep). All outbound [[links]] from pr_49 source resolve: [[enforce_pr_workflow]], [[loop_stall_guard]], [[loop_state_guard]], [[discipline-loop]], [[spec-plan-progress-artifact-chain]] (verified via find). Updated pages outbound links also resolve (no new dangling introduced). Contradictions: 0 (named-function refactor is additive, no behavioural claims reversed; check_verify_loop.sh gate-description in discipline-loop.md uses numbered steps to describe logic flow, not bash pattern claims — distinct from the CLAUDE.md correction, not a contradiction). Stale pages: 0 (no page last_updated before 2026-05-27). Orphan pages: 0. One fix: design/spec-plan-progress-artifact-chain.md shared-lib section was missing the als_gate_* named functions added by PR #49 — updated to include them, sources frontmatter updated, last_updated bumped to 2026-06-26. 0 dangling links introduced. Sources 15→16.

## [2026-06-26] ingest | PR #54 vendor strictcode → engineering-principles (merged 2f1ad1c). Brought the global strictcode family (coordinator + python/go/ts) into coderails as the `engineering-principles` family; stripped Scout (Phase 3) + SlimCode, kept Serena with a Grep fallback. Renamed config `strictcode_paths`/`strictcode_skill` → `engineering_principles_paths`/`engineering_principles_skill`, default `/strictcode-python` → `/engineering-principles-python` across push/workflow/prep/init + REFERENCE. 3 touchpoints: planning ([[writing-plans]] self-review gate + [[brainstorming]]), pre-push pre-flight (renamed), PR-review (new [[workflow]] Phase 3 `2b` /engineering-principles + `2c` /simplify). README 23→27 / four groups. Globals removed. Plan ran through `/coderails:planning-sequence` first (gate from [[pr_50_planning-sequence-gate]]); one finding self-disproved (branch name vs require::feature), two hardening items folded. Review clean (one a→an grammar nit fixed). Created sources/pr_54_engineering-principles-vendoring.md. Updated: commands/push|workflow|init + design/config-resolution|enforcement-model (config keys, default, prose renamed via safe patterns; last_updated 2026-06-26), sources/pr_47 (supersession note → pr_54), index.md (pr_54 entry). Preserved [[pr_47_strictcode-skill-config]] links + "Added PR #47" provenance (immutable history). Sources 15→16.

## [2026-06-26] lint | Post-ingest lint for PR #54. New page sources/pr_54_engineering-principles-vendoring.md: NOT orphan — 3 inbound (index.md sources list, log.md ingest entry, sources/pr_47 supersession note) (verified grep). All 10 outbound [[links]] resolve (verified find): [[brainstorming]],[[config-resolution]],[[enforcement-model]],[[init]],[[pr_47_strictcode-skill-config]],[[pr_50_planning-sequence-gate]],[[prep]],[[push]],[[workflow]],[[writing-plans]]. Live-state pages (commands/push|workflow|init, design/config-resolution|enforcement-model) renamed strictcode→engineering-principles via SAFE patterns that preserved the [[pr_47_strictcode-skill-config]] wiki-links, frontmatter source paths, and "Added PR #47" provenance. Contradiction check: pr_47 (immutable) carries a forward supersession note to pr_54; no prior claim rewritten — rename is additive-with-pointer. 0 orphans, 0 dangling, 0 stale (touched pages last_updated 2026-06-26). Sources 15→16. No fixes required.

## [2026-06-26] ingest | PRs #55 + #56 (skill-testing cluster). #55 (merged ce499d7): deleted orphaned `skills/planning-sequence/evals/evals.json` — a dormant skill-creator quality-eval artifact (`{skill_name, evals:[{prompt, expected_output}]}` schema) referenced by nothing in coderails and missing the `expectations[]` field skill-creator's grader scores; it falsely implied a test harness existed. #56 (merged 0f0969e): writing-skills doc fixes — stale `using-skills`→`using-coderails` naming example, and new `examples/CLAUDE_MD_TESTING.md` worked test-campaign closing a dangling reference in `testing-skills-with-subagents.md`. The two are paired: one removes a dead *automated*-looking eval, one completes the *manual*-testing docs. Key non-obvious finding captured in new investigation [[skill-testing-state_2026-06-26]]: coderails has ZERO automated skill testing (neither triggering nor behavioural); skill-creator is an EXTERNAL claude-plugins-official plugin with two eval systems (trigger `run_eval.py` query/should_trigger + agent-graded quality prompt/expected_output/expectations[]); decision NOT to build a runner (corpus=1 across 27 skills, only ~4–6 discipline skills pressure-sensitive, hooks already test the high-stakes layer); candidate reminder-hook deferred. Created: sources/pr_55_remove-dead-evals.md, sources/pr_56_writing-skills-using-coderails-example.md, investigations/skill-testing-state_2026-06-26.md. Updated: skills/writing-skills.md (skill-testing section + See also + sources + last_updated), index.md (1 investigation + 2 source entries). Sources 16→18.

## [2026-06-26] lint | Post-ingest lint for PRs #55 + #56. New pages NOT orphans (verified grep): pr_55_remove-dead-evals → 3 inbound (index, log, investigation), pr_56_writing-skills-using-coderails-example → 2 inbound (index, log), skill-testing-state_2026-06-26 → 5 inbound (index, log, both sources, writing-skills). All 14 outbound [[links]] across the 4 touched pages resolve (verified find): agentic-loop, discipline-loop, enforcement-model, no_edit_on_main, planning-sequence, pr_55_remove-dead-evals, pr_56_writing-skills-using-coderails-example, receiving-code-review, self-containment, skill-testing-state_2026-06-26, test-driven-development, verification-before-completion, wiki-ingest, writing-skills. Contradictions: 0 (additive — no prior claim reversed; the investigation explicitly corrects a within-session error about run_eval.py "plugging in", but no existing wiki page had asserted automated skill-testing existed). 0 orphans, 0 dangling, 0 stale (new/touched pages last_updated 2026-06-26). Sources 16→18. No fixes required.

## [2026-06-26] lint | Post-ingest lint for PRs #57–#62 cluster. 0 dangling links (all [[links]] across 9 touched pages resolve — verified Python link-resolution script). 0 orphans (pr_57-62 source page: inbound from index.md, log.md, hooks/check_confidence_labels.md, hooks/check_verify_loop.md, hooks/destructive_bash_gate.md, hooks/enforce_pr_workflow.md, hooks/no_edit_on_main.md, design/discipline-loop.md, design/enforcement-model.md = 9 inbound, verified). 0 new stale pages (all touched pages last_updated 2026-06-26). Fixes applied: (1) design/discipline-loop.md — stale summary table row "DNV bullets must not name unread source files" corrected to "Any untagged ## Did Not Verify bullet" (source-token era wording had survived two ingests); (2) sources/pr_57-62_subagent-enforcement-gate-hardening.md — added 5 confidence labels (verified/inferred) to factual claims. Pre-existing non-new findings deferred: (a) 3 old session sources use `origin:` instead of `sources:` frontmatter field — predates the schema, not introduced by this ingest; (b) 12 skill pages and 4 older source pages lack confidence labels — pre-existing, no new pages contributed to this count. Schema conformance: new source page passes (type:source, all required frontmatter fields present, confidence labels present). No contradictions found (SubagentStop additions are additive; file_count removal is documented correctly across check_verify_loop.md, discipline-loop.md, enforcement-model.md; allowlist inversion in no_edit_on_main.md is internally consistent). Sources 18→19. Final coverage: commands 10/10, hooks 11/11, skills 23/23, design 9/9. (verified: Python link/orphan audit script)

## [2026-06-26] ingest | Subagent enforcement + gate hardening cluster (PRs #57–#62). #57: SubagentStop discipline — check_confidence_labels.sh and check_verify_loop.sh wired to SubagentStop event; both read last_assistant_message directly (transcript_path on SubagentStop is the parent session transcript); dc_file_count() extracted to discipline_common.sh; loop guards remain Stop-only. #58: enforce_pr_workflow hardened — per-PR review evidence (gh pr merge N requires review-pr N as leading token), consume-on-use for git merge, positional git push origin main now gated, --dry-run/--help flag-boundary tightening, subagent transcript scanned for evidence; review-caught Critical (positional push bypass) fixed before merge. #59: destructive_bash_gate extended — git clean -f/--force (with dry-run/interactive allowance), find -delete, truncate -s/--size, shred added to blocklist; branch-aware in-Bash source-edit gate (sed -i/perl -i/redirects/tee/cp/mv/dd targeting .py/.ts/.tsx/.js/.jsx/.go or plugin source on main); review-caught Critical (git clean --force combined-flag bypass) fixed before merge. #60: no_edit_on_main code arm inverted to allowlist — previously a 6-extension blocklist, now everything except .md/.txt/.rst, .yaml/.yml/.json/.toml/.ini/.cfg, .gitignore, LICENSE is blocked on main; plugin-source markdown arm preserved. #61: check_verify_loop file_count gate removed on Stop path — DNV enforcement now independent of file edits (SubagentStop path already had no such gate). #62: docs consolidation — CLAUDE.md + REFERENCE.md hook-map synced; "Enforcement ceilings" section added to CLAUDE.md; design spec filed at docs/coderails/specs/; .gitignore allowlist over-match (*.gitignore → basename match) fixed TDD-first. Created: sources/pr_57-62_subagent-enforcement-gate-hardening.md. Updated: hooks/check_confidence_labels.md (SubagentStop path + events); hooks/check_verify_loop.md (SubagentStop path + file_count removal + generation 4 history); hooks/destructive_bash_gate.md (extended blocklist + in-Bash gate); hooks/enforce_pr_workflow.md (per-PR + consume-on-use + positional push + flag-boundary + subagent transcript); hooks/no_edit_on_main.md (allowlist model + .gitignore tightening); design/discipline-loop.md (SubagentStop composition + enforcement ceilings section); design/enforcement-model.md (hook map updated). Sources 18→19.

## [2026-06-26] lint | Post-ingest lint for PR #64. New page sources/pr_64_loop-review-via-skill.md: NOT orphan — 3 inbound links (index.md, skills/agentic-loop.md, hooks/enforce_pr_workflow.md) (verified). Outbound [[links]] from pr_64 source resolve: [[agentic-loop]], [[enforce_pr_workflow]] (verified). Outbound [[links]] from the two updated concept pages all resolve (verified): agentic-loop.md → [[pr_64_loop-review-via-skill]], [[enforce_pr_workflow]], [[skills-hooks-seam]], [[brainstorming]], [[spec-plan-progress-artifact-chain]], [[writing-plans]], [[test-driven-development]], [[subagent-driven-development]], et al; enforce_pr_workflow.md → [[pr_64_loop-review-via-skill]], [[agentic-loop]], [[skills-hooks-seam]], et al. Fix applied: enforce_pr_workflow.md "Accepted PR-review evidence" section closing line added [[pr_64_loop-review-via-skill]] body wiki-link (it was only in frontmatter sources; inbound count: 2→3). 0 new dangling links introduced. 0 orphans (all pages have ≥1 inbound). 0 stale pages (all dates ≥2026-05-30). 0 contradictions. Sources 20→21.

## [2026-06-26] ingest | PR #64 loop Phase 4b: invoke review-pr Skill instead of hand-rolling agents (ee44aa0). agentic-loop Phase 4b rewritten: the six reviewer `Agent` blocks are replaced by a single `/pr-review-toolkit:review-pr <PR#>` Skill invocation (PR number passed as arg). Root cause: enforce_pr_workflow.sh only recognises Skill invocations of review-pr as merge evidence — a hand-rolled agent fanout leaves no recognisable Skill record in the transcript, so the merge gate blocks. CLAUDE.md skills↔hooks seam convention paragraph extended with one sentence naming this constraint. Created: sources/pr_64_loop-review-via-skill.md. Updated: skills/agentic-loop.md (Phase 4b table row, new "Phase 4b — invoke the review-pr Skill" section, cross-refs + sources); hooks/enforce_pr_workflow.md (new "Accepted PR-review evidence" section, sources frontmatter). Sources 20→21.

## [2026-06-26] ingest | Removed orphaned `templates/failure_log.md` and all plumbing (PR #63, 18392fb). The file was the data input for claude-guardrails' weekly calibration ritual — a feature the plugin merge already removed ("no launchd, no calibration ritual"); consumer gone, no hook/skill/command read or wrote it, zero entries in two months. An automation plan (`/failure` capture + `/failure-review` recurrence consumer) was considered and **DECLINED**: zero entries from a willing maintainer over two months is a *demand* signal, not a *friction* signal — per YAGNI/reactive discipline, build capture when recurrence demands it, not speculatively. Key distinction recorded: caught failures (hooks already log to discipline.log, blocked=1) vs escaped failures (the file's actual target — wrong output that passed every hook, uncapturable by a hook by construction). Kept deliberately: docs/coderails-review.md (historical record) + README "no calibration ritual" line (accurate history). Created: sources/pr_63_remove-failure-log.md. Updated: design/discipline-loop.md (Founding Thesis citation re-attributed — quote preserved in-wiki, dead pointer to the deleted file fixed; sources list updated), index.md. Sources 19→20.

## [2026-06-29] ingest | PR #69 no_edit_on_main message → worktree + branch (86e701a). Prose-only reword of the deny `reason` string + header comment in no_edit_on_main.sh: now leads with "create an isolated worktree + branch first" (`/coderails:prep` or `git worktree add <path> -b <name>`) instead of "create a feature branch first". Block conditions, branch detection, allowlist, and the 20/20 test suite all unchanged (tests assert on ALLOW/DENY decision, not message text). Two PR-review agents clean. Created: sources/pr_69_no-edit-message-worktree.md. Updated: hooks/no_edit_on_main.md (new "Why the block message recommends worktree + branch (PR #69)" design note, last_updated + sources), index.md (source row + date). Recorded gotcha: scripts/merge.sh exits 128 on post-merge `git checkout main` when run from inside a linked worktree (merge still lands). Sources 21→22.

## [2026-06-29] lint | Post-ingest lint for PR #69. New page sources/pr_69_no-edit-message-worktree.md: NOT orphan — 3 inbound (index.md, log.md, hooks/no_edit_on_main.md) (verified). Outbound [[links]] resolve: [[no_edit_on_main]] → hooks/no_edit_on_main.md, [[prep]] → commands/prep.md (verified). New design-note link in hooks/no_edit_on_main.md ([[prep]]) resolves (verified). index.md source row [[pr_69_no-edit-message-worktree]] resolves (verified). 0 dangling links, 0 orphans, 0 stale pages (only templates/ YYYY-MM-DD placeholders match, out of scope), 0 contradictions (the no_edit_on_main page never quoted the deny-message text, so the reword supersedes nothing). Sources 21→22.

## [2026-06-29] ingest | PR #70 merged (e52e541): no_edit_on_main permission-file arm — blocks .claude/settings.json/.local.json edits on any branch (first non-main arm), closing the settings.json self-escape door. Collateral: ceiling_note.test.sh retargeted CLAUDE.md->AGENTS.md. Supersedes closed #68. Pages: hooks/no_edit_on_main.md, sources/pr_70_gate-settings-json-edits.md, index.md.

## [2026-06-29] lint | Post-ingest lint for PR #70. New page sources/pr_70_gate-settings-json-edits.md: NOT orphan — 3 inbound (index.md, log.md, hooks/no_edit_on_main.md) (verified). All 6 new outbound [[links]] resolve (enforcement-model, destructive_bash_gate, discipline-loop, no_edit_on_main, enforce_pr_workflow, pr_70 self) (verified). No contradiction: destructive_bash_gate.md / enforce_pr_workflow.md reference adding a Bash *permission rule* to settings.json (a deliberate human config change) — consistent with the new arm gating *agent edits* of the file via Write/Edit/MultiEdit, not the permission mechanism (verified). 0 dangling, 0 new orphans, 0 contradictions. Sources 22->23.

## [2026-06-29] ingest | PR #72 merged (3862614): fixed config.sh walk-up infinite-loop hang on symlinked paths (NO_CONFIG path; pwd -P + "/" floor). Root cause from #67 (walk-up) + #71 (extracted config.sh, no test). Was hanging enforce_pr_workflow.test.sh + run_all. Added config.test.sh (watchdog reproducer). Suite 14->15, 307 ok. Updated STALE design/config-resolution.md (documented the pre-#67 dual-path lookup that no longer exists). Pages: sources/pr_72_config-walkup-symlink-hang.md (new), design/config-resolution.md (rewritten lookup section + update-discipline note), index.md.

## [2026-06-29] lint | Post-ingest lint for PR #72. New page sources/pr_72_config-walkup-symlink-hang.md: NOT orphan — 3 inbound (index, log, config-resolution) (verified). All 4 new outbound [[links]] resolve (verified). STALENESS SWEEP (in-scope, same config-resolution drift as the PR): found + fixed pre-#67 "dual-path projects/<name>/" claims that survived #67/#71 un-ingested — design/config-resolution.md (lookup section rewritten to walk-up + symlink-fix note), commands/init.md (write path was projects/<name>/ → now $(pwd)/.claude walk-up, matching real init.md:15), and 4 stale "dual-path" link labels (init/prep/push/workflow) + index.md catalog row → "walk-up resolver". Remaining "dual-path" mentions are historical records only (log, source, the explicit superseded note). 0 dangling, 0 orphans, 0 remaining current-claim contradictions. Sources 23->24.

## [2026-06-29] ingest | PR #74 merged (55e0d63): config.test.sh watchdog stalled 5s/call. The #72 watchdog was correct but every resolve() paid the full 5s sleep (~25s of the ~45s suite; low CPU% was the tell). Root cause: `kill $w` orphans the watchdog's `sleep` child, which holds the $() pipe write-fd open, so $() blocks 5s — a pipe-fd lifetime issue, NOT the "ignores SIGTERM" the first comment claimed (empirically false: subshell dies in ~3ms). Fix: `pkill -P $w` kills the sleep directly (closes fd); `{ } 2>/dev/null` swallows the job-control notice. Suite ~45s→~21s, still 15/15, TIMEOUT path intact. Review: comment-analyzer caught the false comment (rewritten in f9a2fbc); code-reviewer self-retracted a wrong narrower-redirect proposal; simplifier ship-as-is; /security-review clean. Pages: sources/pr_74_config-test-watchdog-stall.md (new), sources/pr_72_config-walkup-symlink-hang.md (Follow-up backref), index.md.

## [2026-06-29] lint | Post-ingest lint for PR #74. New page sources/pr_74_config-test-watchdog-stall.md: NOT orphan — 3 inbound (index, log, pr_72 Follow-up) (verified). Outbound links: fixed 2× [[review-pr]] (no such page) → [[pr_64_loop-review-via-skill]] (the canonical PR-review-via-skill page); remaining links [[pr_72...]], [[config-resolution]], [[pr_64...]] all resolve (verified). No contradiction: the "ignores SIGTERM" string survives only in pr_74/index/log as the explicitly-corrected false claim — no current-claim page asserts it as truth (verified). 0 dangling, 0 new orphans, 0 contradictions. Sources 24→25.

## [2026-06-29] ingest | PR #75 merged (feaefe9): main() in scripts/lib/git-common.sh returned blank instead of falling back to literal `main` when `git symbolic-ref refs/remotes/origin/HEAD` was unset. Pipeline-exit-status bug: `git ... | sed ... || echo main` — the `||` binds to the pipeline's exit (sed's, 0 on empty input), so the fallback never fired. Fix: capture + `echo "${m:-main}"` (key off empty output). Latent — origin/HEAD is set by clone/gh, so only bare-`git init` test scaffolding hit the blank; starved sync::main_branch (added by un-ingested #73) into a silent no-op. Regression test added (marker-less repo → must yield `main`). 3 reviews (code/tests/silent-failure) clean, 0 findings. Same pipeline-mechanism family as #74. Pages: sources/pr_75_main-branch-fallback.md (new), index.md. Note: #73 (sync::main_branch origin) has no source page — never ingested.

## [2026-06-29] lint | Post-ingest lint for PR #75. New page sources/pr_75_main-branch-fallback.md: NOT orphan — 2 inbound (index, log) (verified). All 4 outbound [[links]] resolve: [[pr_74_config-test-watchdog-stall]], [[enforcement-model]], [[merge]], [[push]] (verified). Removed an invented [[pr_73]] link mid-ingest — #73 (sync::main_branch origin) was never ingested, so the page doesn't exist; #73 now referenced in prose only (verified: no pr_73 page, no other inbound to it). CONTRADICTION SWEEP (in-scope: main()/origin-HEAD claims): push.md:28 and merge.md:48 mention the helper but only describe *what it reads* ("base branch via git symbolic-ref", "default branch detection") — neither asserts the buggy fallback-works behaviour, so the fix contradicts nothing; no current-claim page was ever wrong (the fix made documented behaviour true, didn't change the interface). 0 dangling, 0 orphans, 0 contradictions, 0 edits to existing pages. Sources 25→26.

## [2026-06-29] lint | Post-ingest lint for PR #76. New page sources/pr_76_harden-hook-stdin-read.md: NOT orphan — 13 inbound links (index, log, all 10 hook pages, design/discipline-loop) (verified). All 12 outbound [[links]] resolve: [[check_confidence_labels]], [[check_verify_loop]], [[destructive_bash_gate]], [[discipline-loop]], [[discipline_catchup]], [[enforce_pr_workflow]], [[inject_context]], [[loop_stall_guard]], [[loop_state_guard]], [[no_edit_on_main]], [[pr_72_config-walkup-symlink-hang]], [[test_gate]] (verified via Python). 0 new dangling links introduced. 0 stale pages (all 12 touched pages last_updated 2026-06-29). 0 new orphans (all 7 apparent orphans from the audit script have ≥1 real content-page inbound link, confirmed). BENIGN FALSE-POSITIVES CONFIRMED (pre-existing, not new): [[:space:]] regex literals in enforce_pr_workflow.md, skills-hooks-seam.md, pr_42 source (bash regex prose); [[pr_50…]]/[[pr_64...]]/[[pr_72...]]/[[pr_73]]/[[review-pr]]/[[workflow-init]] in log.md historical prose only (not navigational). inject_bootstrap.sh correctly NOT updated — it reads a file via `cat`, not stdin from the hook payload (SessionStart event, different mechanism). No contradictions: the fan-incident / PR #72 root cause is distinct from the PR #76 stdin-block risk; the source page and enforce_pr_workflow hook page both document this distinction explicitly. Sources 26→27.

## [2026-06-29] ingest | PR #76 merged (37459a3): harden hook stdin read — replaced `input=$(cat)` with `IFS= read -r -d '' -t 5 input || true` in all 10 hook scripts. Added `hooks/scripts/tests/stdin_bounded_read.test.sh` (guard + fidelity tests, built TDD; 2 guard FAILs pre-fix → 6/6 PASS post-fix; suite 17/17). Updated AGENTS.md hook conventions. This is **independent defence-in-depth** for a latent stdin-blocking risk — NOT the fix for the fan incident (21 orphaned enforce_pr_workflow.sh processes at 8–10% CPU), which was a config.sh walk-up infinite loop, already fixed in PR #72. Key design: 5-second timeout fires only when the parent is dead → `input` empty → exit 0 (fail-open is safe: no live pipeline to gate). The `|| true` is mandatory (`read -d ''` returns exit 1 on normal EOF). Known gap: no automated test guards `min(hooks.json timeout) >= 5`. Security review: 6-agent review, CLEAN 9/10. Created: sources/pr_76_harden-hook-stdin-read.md. Updated: all 10 hook pages (frontmatter sources + stdin convention section), design/discipline-loop.md (new stdin convention section), index.md (source row). Sources 26→27.

## [2026-06-29] lint | Post-ingest lint for PR #77. New page sources/pr_77_agentic-loop-sync-docs-step.md: NOT orphan — 3 inbound (index.md, log.md, skills/agentic-loop.md) (verified). Outbound links: [[agentic-loop]] → skills/agentic-loop.md, [[enforcement-model]] → design/enforcement-model.md (both verified). All links in updated skills/agentic-loop.md resolve (21 links, all verified). 0 dangling links introduced. 0 stale pages (agentic-loop.md last_updated bumped to 2026-06-29). 0 new orphans. CONTRADICTION CHECK: no ⚠️ CONTRADICTION flags on content pages — log.md and wiki-lint.md occurrences are prose/schema, not flagged contradictions (confirmed). BENIGN FALSE-POSITIVES (pre-existing, not new): [[:space:]] POSIX regex literals in enforce_pr_workflow.md, skills-hooks-seam.md, pr_42 source — these appear as [[wiki-links]] to the parser but are bash regex in prose/code (same class as prior lint entries). No fixes required.

## [2026-06-29] ingest | PR #77 merged (cefb47c): agentic-loop Phase 9 gains `/sync-docs` docs-drift check at loop boundary. Key distinction: wiki ingest (external KB) and `/sync-docs` (in-tree docs) were previously conflated as "docs" at the loop boundary — now explicitly separated. `/sync-docs` runs even without Serena (omit `--semantic` for file-comparison audit); delegated to a spawned agent (same pattern as wiki step). Findings-triage discipline: fix only drift the loop's own PRs introduced; surface pre-existing drift to the user (not scope-creep inline fixes). Advisory only — no hook. Thematically distinct from PR #76 (hook stdin hardening); ingested separately. Created: sources/pr_77_agentic-loop-sync-docs-step.md. Updated: skills/agentic-loop.md (Phase 9 table row, new "Phase 9 — cluster wiki ingest + in-tree docs-drift check" section, Key architectural decisions bullet, Cross-references entry, sources frontmatter, last_updated 2026-06-26→2026-06-29), index.md (source row). Sources 27→28.

## [2026-06-29] ingest | PR #78 merged (ceabb75): hooks.json timeout-floor guard test — closes the known gap from PR #76. Added `hooks/scripts/tests/hooks_json_timeout_floor.test.sh` asserting `min(declared hooks.json timeout for hooks/scripts/ entries) >= 5` (the `read -t 5` in-process backstop floor). Fixes fractional-timeout silent-skip bug (bash `-lt` errors on floats — moved comparison to jq). Empty-result guard prevents vacuous-pass on broken jq filter or changed hooks.json structure. Suite 17/18 → 18/18. Created: sources/pr_78_hooks-json-timeout-floor.md. Updated: sources/pr_76_harden-hook-stdin-read.md (Known gap → Closed by PR #78 + backref link), design/discipline-loop.md (Stdin read convention section: known-gap clause replaced with timeout-floor invariant guard paragraph), index.md (source row; PR #76 entry updated). Sources 28→29.

## [2026-06-29] ingest | PR #79 merged (0bcb44e): docs: fix sync-docs drift — 4 pre-existing doc-drift findings surfaced by agentic-loop Phase 9 `/sync-docs` step (added PR #77). F1 (High): REFERENCE.md missing 4 engineering-principles skills + false "complete catalogue" — added section, count 23→27. F2 (Medium): README listed bare `truncate` in destructive_bash_gate blocklist — corrected to the flagged flag-form only. F3 (Low): README + AGENTS.md omitted inject_context.sh first-prompt discipline reminder — added. F4 (Low): AGENTS.md said "four core gate scripts" omitting test_gate.sh — corrected to "five". Docs-only, no runtime impact. Created: sources/pr_79_sync-docs-drift.md. Updated: hooks/inject_context.md (sources frontmatter), hooks/test_gate.md (sources frontmatter + new "Gate-pattern convention" section), index.md (source row). Sources 29→30.

## [2026-06-29] lint | Post-ingest lint for PRs #78 + #79 (cluster). NEW pages: sources/pr_78_hooks-json-timeout-floor.md — NOT orphan: 4 inbound (index, log, discipline-loop, pr_76) (verified). sources/pr_79_sync-docs-drift.md — NOT orphan: 4 inbound (index, log, test_gate, inject_context) (verified). Outbound links for pr_78: [[pr_76_harden-hook-stdin-read]] ×2, [[discipline-loop]] — all resolve (verified). Outbound links for pr_79: [[pr_54_engineering-principles-vendoring]], [[pr_57-62_subagent-enforcement-gate-hardening]], [[inject_context]], [[test_gate]], [[discipline-loop]] — all resolve (verified). UPDATED pages: pr_76 source (14 links OK), discipline-loop (18 links OK), test_gate (6 links OK), inject_context (7 links OK), index (110 links OK) — 0 new dangling introduced (verified via Python link scan). KNOWN-GAP CLOSURE verified: sources/pr_76_harden-hook-stdin-read.md "Known gap → Closed" updated in two places; discipline-loop.md "Stdin read convention" section updated; index.md PR #76 row updated. 0 new orphans. 0 stale pages (all updated pages carry 2026-06-29). CONTRADICTION CHECK: two "⚠️ CONTRADICTION" occurrences in log.md and wiki-lint.md — confirmed pre-existing benign false-positives (prose describing the lint check protocol, not flagged contradictions). "Known gap" references in log.md line 141 and pr_78 source are historical/quoted text, not unresolved gaps — immediately followed by closure entries. PRE-EXISTING BENIGN FALSE-POSITIVES (unchanged from prior lints): [[:space:]] bash/POSIX regex in code in enforce_pr_workflow.md, skills-hooks-seam.md, pr_42, pr_46 sources; [[wiki-links]]/[[link]]/[[page_name]] schema meta-text in log.md, wiki-lint.md, wiki-query.md; [[pr_73]]/[[review-pr]]/[[pr_50…]]/etc. historical log prose. 0 fixes needed. Inbox: empty.

## [2026-06-30] ingest | PR #80 merged (f5aacde): guard both halves of the timeout invariant — extends `hooks_json_timeout_floor.test.sh` with Half B. Closes test-analyzer S1 from PR #78's review. Half A (PR #78): `min(hooks.json declared timeout) >= 5`. Half B (this PR): exactly 10 hook scripts carry the bounded-read backstop, and every `read -t N` == 5. Together both halves make the full `read -t 5 <= min(hooks.json timeout)` invariant tamper-evident: a future PR that lowers the in-process `read -t` value alone OR a hook silently gaining/losing the backstop now fails the suite. Two review hardening fixes: dropped redundant `|| echo 0`; switched extraction to `grep -oE` (end-of-line-tolerant). Created: sources/pr_80_guard-read-t-floor.md. Updated: sources/pr_78_hooks-json-timeout-floor.md (Relationship section expanded to name Half A/B split + PR #80 closure; Follow-on section added; last_updated 2026-06-29→2026-06-30), index.md (source row; PR #78 entry updated to note both halves now guarded). Sources 30→31.

## [2026-06-30] lint | Post-ingest lint for PR #80. New page sources/pr_80_guard-read-t-floor.md: NOT orphan — 2 inbound (index.md, sources/pr_78_hooks-json-timeout-floor.md) (verified via Python link scan). Outbound [[links]] from pr_80: [[pr_78_hooks-json-timeout-floor]] ×2, [[pr_76_harden-hook-stdin-read]] ×1, [[index]] ×1 — all resolve (verified). Updated pages: pr_78 source (Relationship section, Follow-on section, frontmatter sources + last_updated), index.md (PR #78 entry updated + PR #80 row added). 0 new dangling links (verified). 0 orphans (verified). 0 stale pages in scope (install-cache-trap_2026-05-30 is 31 days but is a historical investigation, pre-existing, not introduced by this ingest). CONTRADICTION CHECK: two ⚠️ CONTRADICTION occurrences in log.md + wiki-lint.md — confirmed pre-existing benign schema prose (same class as prior lints). PRE-EXISTING BENIGN FALSE-POSITIVES (unchanged): [[workflow-command-chain]] in log.md historical prose only; [[:space:]] bash regex in enforce_pr_workflow.md, skills-hooks-seam.md, pr_42, pr_46; [[wiki-links]]/[[link]]/[[page_name]] schema meta-text; [[$answer=="y"]] bash conditional in fenced block. Inbox: empty. Sources 30→31.

## [2026-06-30] ingest | PRs #81–83 merged (cluster): independent-review truth seam — SHA-bound PR comment + fail-closed merge gate. #81: design spec (5 review rounds, planning-sequence stress-tested) + 9-task implementation plan. Universal Stop guard + ledger enforcement DEFERRED (ceremony tax). #82: engine — scripts/lib/review-artifact.sh (marker SSOT, exact-equality match), scripts/post_review.sh (validate + write-cache subcommands), commands/post-review.md (posts via gh api, captures URL/metadata), scripts/lib/git-common.sh (pr::head_sha + pr::has_coderails_review_for_head, exit codes 0/1/2), scripts/merge.sh (review artifact gate, fail-closed, no local fallback, two distinct error messages). #83: wiring — commands/workflow.md Phase 3 end, skills/agentic-loop/SKILL.md Phase 4b (loop symmetry), commands/prep.md optional Part 1b stub (non-blocking). Suite: 21/21 passes. Key design: review truth moves from ephemeral chat to GitHub-visible SHA-bound PR comment; progress.json.review is cache/index only, never authority. Honest ceiling: validates structure not provenance. Follow-up ordering constraint: enforce_pr_workflow review-arm demotion must NEVER precede gate verification. Created: sources/pr_81-83_review-artifact-seam.md, design/review-artifact-seam.md, commands/post-review.md. Updated: commands/merge.md (artifact gate section + new helper docs), commands/workflow.md (Phase 3 end step), skills/agentic-loop.md (Phase 4b table + section + cross-refs), commands/prep.md (Part 1b), design/index.md (new row + source row). Sources 31→32.

## [2026-06-30] lint | Post-ingest lint for PRs #81–83 cluster. New pages: sources/pr_81-83_review-artifact-seam.md — NOT orphan: 6 inbound (index, design/review-artifact-seam ×2, commands/post-review ×2, skills/agentic-loop ×2) (verified). design/review-artifact-seam.md — NOT orphan: 14 inbound (index, source ×2, prep ×2, workflow ×2, merge ×3, post-review ×2, agentic-loop ×2) (verified). commands/post-review.md — NOT orphan: 9 inbound (index, review-artifact-seam ×2, pr_81-83 ×2, workflow ×2, merge, agentic-loop) (verified). All outbound links from 3 new pages: 0 broken (verified). FIX (cluster-related): skills/agentic-loop.md Phase 4b cross-references section was missing [[post-review]] link — mentioned `/coderails:post-review` in Phase 4b prose but no navigational wiki-link; added `[[post-review]] — the /coderails:post-review command Phase 4b now invokes after review-pr`. 0 orphans (verified). STALENESS: install-cache-trap_2026-05-30 is 31 days — pre-existing historical investigation, not introduced by this cluster (unchanged). BENIGN FALSE-POSITIVES (pre-existing, unchanged): [[:space:]] POSIX regex literals in skills-hooks-seam.md, enforce_pr_workflow.md, pr_42 source; [[$answer=="y"]] bash conditional in install-bash32 investigation; [[wiki-links]]/[[link]]/[[page_name]] schema meta-text in log.md, wiki-lint.md, wiki-query.md, wiki-ingest.md; [[pr_73]]/[[review-pr]]/[[pr_50…]]/[[workflow-init]] in historical log prose only. 0 unresolved findings introduced by this cluster. Sources 31→32.

## [2026-07-01] ingest | PR #86 merged (b8a1958): agentic-loop hardening — 7 review-resolved decisions against `skills/agentic-loop/SKILL.md` (+ `AGENTS.md` + `hooks/scripts/lib/agentic_loop_path.sh`). (1) Stage-map: 5-row overview table added grouping 19+ phases into Setup/Pre-flight/Build/Review&Ship/Wrap-up. (2) Phase 2.7 + Phase 2.8 merged into one guard-gated Phase 2.7 with sub-steps 2.7a (spec.md)/2.7b (plan.md) — both phases restated the identical ≥3-work-unit guard. (3) Phase 4b: removed the orchestrator's ability to self-demote an independent reviewer's clean-break compat MERGE-BLOCKER; only fix-it or hard-stop-to-human remain, with a Phase-0-only quoted auto-demote-authority carve-out. (4) Phase 13: dropped the gameable "human turns approaching zero" scorecard; replaced with raw unscored LOOP-STOP category counts + a flat "decisions absorbed" list. (5) AGENTS.md "Enforcement ceilings" gains a bullet documenting `model: sonnet` as deliberately advisory, not hook-enforced. (6) Documented the single-loop-per-project-directory invariant (progress.json races on concurrent loops in the same checkout) in both SKILL.md and agentic_loop_path.sh, pointing at git worktrees as the resolution. (7) Replaced 3 stale/nonexistent feedback-memory-file citations in SKILL.md Phase 9 (×2) and Phase 4b (×1) with self-contained inline prose. Post-merge review (4 agents + security pass) caught and fixed, inside the same merge commit: 2 Critical stale cross-references (SKILL.md:435, :480, describing the pre-rewrite Phase 13 shape) + 1 Important gap (Phase 0 thinking-block template didn't force an explicit yes/no + quote on the new auto-demote carve-out). Created: sources/pr_86_agentic-loop-hardening.md. Updated: skills/agentic-loop.md (phase table, stage-map, 2.7 merge, new Phase 4b self-attestation section, new Phase 13 rewrite section, new model:sonnet advisory section, single-loop invariant, architectural decisions, cross-references), design/enforcement-model.md (new Phase 4b self-attestation cross-reference section, new model:sonnet advisory ceiling section, cross-references). Sources 33→34. Known deferred gap (unchanged from PR #86 itself, not fixed by this ingest): `docs/coderails-review.md:160`'s SKILL.md line-number citations are now stale (drifted from the stage-map + Phase 2.7/2.8 merge); flagged as a follow-up, not fixed — do not re-open as a fresh wiki finding.

## [2026-07-01] ingest | PR #87 merged (344a849): agentic-loop progress.json path collision — fixes the single-loop-per-directory race PR #86 had only documented as an accepted limitation (worktrees-as-workaround), same day. `hooks/scripts/lib/agentic_loop_path.sh` now keys the path on cwd **and session_id** (`<slug>/<session_id>/progress.json`, was `<slug>/progress.json`), so two concurrent `agentic-loop` sessions in the same directory get independent files automatically — no worktrees required for this specific collision (worktrees remain useful for working-tree isolation, a separate concern). Relies on a verified-not-derivable platform guarantee: Claude Code's `session_id` stays stable for one continuous conversation across its own compaction/restart, differs across separate conversations. `loop_state_guard.sh`'s session-mismatch check is retained but now only fires on file/path corruption, not the routine cross-session race (that no longer happens by construction). Same-PR follow-up review (5 reviewer dispatches + native `/security-review`) caught and fixed a Critical residual bug in the initial fix: both guard scripts' `jq -r '.session_id // "?"'` extraction and the path helper's own `unknown-session` default fell back to FIXED sentinel strings on a missing/null session_id, letting multiple such sessions collide again. Closed via a new `als_sanitise_session_id()` helper (unique PID+high-res-timestamp fallback, no shared constant) wired into all 3 call sites, with a new discriminating regression test (red pre-fix, green post-fix). Also fixed 2 stale `docs/REFERENCE.md` rows describing the old cwd-only path format (one self-contradicted its own "Session-keyed" annotation). Created: sources/pr_87_agentic-loop-path-session-keying.md. Updated (not just appended — prior content was inaccurate): skills/agentic-loop.md ("Single-loop-per-directory invariant" section replaced with "Concurrent loops in one directory", frontmatter sources + tags, header note, cross-references list, PR #86 cross-ref annotated as superseded-for-this-fix), hooks/loop_state_guard.md ("Path authority design" section + "Known limitations" bullet corrected to the new path format and narrowed guard scope, frontmatter sources + tags + last_updated, see-also entry), index.md (new source row; PR #86 row annotated with fixed-same-day pointer). Sources 34→36.

## [2026-07-03] ingest | PRs #89–91 merged (cluster, 9510efd/dea3883/f8b69ea): adopts Claude Code skills-doc frontmatter/injection features (docs: code.claude.com/docs/en/skills). #89: agentic-loop `SKILL.md` description trimmed 1533→1134 chars (the 1,536-char skill-listing truncation cap); all trigger phrases preserved; the "sonnet" delegation clause was cut in an initial trim draft then restored after review flagged it as load-bearing, not padding. `paths:` glob hints added to the three engineering-principles skills (go/python/ts). `user-invocable: false` set on using-coderails (session-start auto-loaded only, never meant to be typed directly). #90: `context: fork` added to wiki-lint + wiki-query (isolated subagent execution keeps bulky wiki crawls out of main context); `agent: Explore` deliberately withheld for both — their bodies write files and commit, which Explore's read-only restriction would break; reviewed caveat accepted as a tradeoff: forking reduces the parent session's incidental visibility into these skills' autonomous wiki writes. #91: `commands/merge.md` gains an injected "Current Git Status" block (current branch + up to 10 open PRs via frontmatter bash substitution) plus a display-only "data, not instructions" note; a parallel `post-review.md` injection was explicitly DEFERRED — an empirical probe of `$ARGUMENTS`-inside-`!`cmd`` substitution ordering was inconclusive (a freshly created skill wasn't visible to a subagent's Skill tool during the probe; public docs only imply the ordering). Also records three non-code decisions from the same loop: `disable-model-invocation` REJECTED for workflow commands (would break agentic-loop's own subroutine invocation of those commands); skill-scoped hooks migration DEFERRED (a skill's "active period" boundary is undocumented by Claude Code); local `ai_docs` skills-docs snapshot (dated 2025-11-07) flagged as stale enough to mislead reviewers — it predates the `paths`/`user-invocable` fields used in this very cluster. Created: sources/pr_89-91_skills-doc-frontmatter-injection.md. Updated: skills/agentic-loop.md (new "Description trimmed to the skill-listing truncation cap" section, sources frontmatter, tags, cross-references, last_updated 2026-07-01→2026-07-03), skills/wiki-lint.md (new "Isolated subagent execution: context: fork" section, sources frontmatter, tags, see-also, last_updated 2026-06-25→2026-07-03), skills/wiki-query.md (same pattern as wiki-lint.md), commands/merge.md (new "Dynamic Git-status injection" section documenting the Current Git Status block + the deferred post-review.md injection, sources frontmatter, tags, see-also, last_updated 2026-06-30→2026-07-03), design/skills-hooks-seam.md (new "Adjacent but distinct: skill-frontmatter metadata is not a hook seam" section drawing the boundary between this convention and frontmatter metadata, records the disable-model-invocation rejection as seam-shaped reasoning, sources frontmatter, tags, see-also, last_updated 2026-06-26→2026-07-03), index.md (new source row). Sources 36→37.

## [2026-07-03] ingest | PR #92 merged (4cb38b5): executable-bit sweep — `scripts/post_review.sh` was mode 100644 while `commands/post-review.md` invokes it as a direct path (`./scripts/post_review.sh validate/write-cache`), causing a real permission-denied failure observed when a review artifact briefly posted with unvalidated content (step 2's abort-on-fail validate gate couldn't execute). Also normalises 8 already-working `hooks/scripts/tests/*.test.sh` stragglers 100644→100755 for consistency with their 17 already-755 peers (cosmetic — invoked via `bash "$test_file"`, mode bit not strictly required there). Zero content changes; every touched blob byte-identical pre/post (reviewer-verified). Audited and correctly left at 100644: `hooks/scripts/lib/discipline_common.sh`, `scripts/lib/config.sh`, `scripts/lib/review-artifact.sh` (only ever sourced), `install.sh`/`uninstall.sh` (always invoked via `bash`, confirmed via a docs grep finding no stale claims). Created: sources/pr_92_exec-bit-sweep.md. Updated: commands/post-review.md (new exec-bit Preconditions bullet, sources frontmatter, last_updated), design/review-artifact-seam.md (new "Executability is now a codified invariant" section, sources frontmatter, last_updated), index.md (new source row). Known gap, not fixed: no test asserts the mode bit stays 100755 — a future regression fails silently until a live run hits it. Sources 37→38.

## [2026-07-03] ingest | Session source (not a coderails PR): ai_docs snapshot refresh (local `~/.claude` repo, commit dbb6822) + 4 empirical Claude Code mechanics findings from a temporary probe skill (since deleted). Snapshot: `~/.claude/commands/ai_docs/cc_skills_docs.md` rewritten from live code.claude.com/docs/en/skills, closing the staleness [[pr_89-91_skills-doc-frontmatter-injection]] flagged (old snapshot dated 2025-11-07 predated `paths`/`user-invocable`, had made a PR-89 reviewer confidently wrong). Findings: (1) `$ARGUMENTS` substitutes before `!`cmd`` injection runs — resolves the ordering question PR #91 left open, unblocking (not yet implementing) a `post-review.md` injection mirroring `merge.md`'s pattern; (2) skill-frontmatter `PostToolUse` hooks persist across turn boundaries within one session but are session-scoped; (3) subagent skill enumeration is fixed at spawn time — a skill created mid-flight is invisible only to already-running subagents, explaining PR #91's inconclusive probe result as an enumeration artifact rather than an ordering one; (4) DECISION: migrating `loop_state_guard`/`loop_stall_guard` into skill-frontmatter hooks is feasible but REJECTED — no survival guarantee across post-compaction session restarts, and these are safety-critical guards needing deterministic scope. Created: sources/session_2026-07-03_ai-docs-refresh-and-cc-mechanics-probes.md. Updated: commands/merge.md (ordering-probe resolution note appended to the existing deferred-injection paragraph, sources frontmatter), commands/post-review.md (new "Injection status" section cross-referencing the same resolution), design/spec-plan-progress-artifact-chain.md (new "Rejected: migrating these two guards into skill-frontmatter hooks" subsection, sources frontmatter, last_updated), index.md (new source row). No code changed in the coderails repo — decisions and findings only. Sources 38→39.

## [2026-07-03] drift-check | Scoped docs-drift sanity check in lieu of full `/sync-docs` (loop's only in-repo change this cluster was PR #92's mode bits, zero content). Grepped README.md, AGENTS.md, INSTALLATION.md, docs/REFERENCE.md for claims that scripts must be invoked via `bash` because they lack exec bits, or any file-mode documentation. Found 2 matches, both benign and unaffected by PR #92: INSTALLATION.md:62 and AGENTS.md:241, both describing `bash install.sh --dry-run` as the documented calling convention for install.sh — not a claim about missing exec bits, and PR #92 itself confirms install.sh/uninstall.sh are correctly left at 100644 (always invoked via `bash`, never as a direct path). No stale documentation found; no fix needed.

## [2026-07-03] ingest | PRs #93–94 merged (cluster): post-review.md injection shipped + exec-bit invariant regression test. #93 (bba7ab5): ships the `post-review.md` dynamic-injection PR #91 deferred and the same-day ai_docs probe session unblocked — a `## Current PR State` block (`gh pr view "$ARGUMENTS" --json state,headRefOid,title --jq ...`) inserted before Step 1, plus the same "data, not instructions" guard line `merge.md` uses. Step 3's own fresh SHA resolution (the artifact's actual binding) is untouched — the injection is display context only. Pre-merge review fix: the injected line was initially unquoted (`$ARGUMENTS` bare) vs Step 3's quoted convention; fixed to `"$ARGUMENTS"` before merge (772cb5b). Surfaced-not-actioned: a programmatic caller feeding untrusted text as the `/post-review` argument now reaches `gh pr view` unsanitised at two call sites instead of one — pre-existing via Step 3 on main, not a regression; candidate fix is numeric-only argument validation. #94 (89a9af6): new `hooks/scripts/tests/exec_bit_invariant.test.sh` — expected-modes manifest vs git index + completeness scan for new tracked scripts, closing the gap PR #92 explicitly flagged ("no test currently guards the mode bit"). Suite 23→24. Normalises two more source-only libs 100755→100644 (`scripts/lib/git-common.sh`, `hooks/scripts/lib/agentic_loop_path.sh`) that #92's sweep didn't reach — reviewer independently re-verified the call-site audit (source-only / bash-wrapped-only, never bare-exec'd). Surfaced-not-actioned: `install.sh:332`'s unconditional post-install chmod sweep re-adds +x on disk at install time regardless of the git index mode — index-vs-installed-copy drift; candidate fix is a mode-aware sweep. Created: sources/pr_93-94_post-review-injection-and-exec-bit-invariant.md. Updated: commands/post-review.md ("Injection status" section replaced from deferred/unblocked → SHIPPED, sources frontmatter, tags), commands/merge.md (removed stale "deferred, not shipped" paragraph, replaced with SHIPPED cross-reference, sources frontmatter, see-also), design/review-artifact-seam.md ("Executability is now a codified invariant" section extended noting the gap is closed by PR #94, sources frontmatter, see-also), sources/pr_92_exec-bit-sweep.md (forward-pointer added to its own flagged-gap bullet noting closure), index.md (new source row). Sources 39→40.

## [2026-07-03] lint | Post-ingest lint for PRs #93–94 cluster. New page sources/pr_93-94_post-review-injection-and-exec-bit-invariant.md — NOT orphan: 5 inbound links (index.md, commands/post-review.md, commands/merge.md, design/review-artifact-seam.md, sources/pr_92_exec-bit-sweep.md), all verified via grep of `[[pr_93-94_post-review-injection-and-exec-bit-invariant`. Outbound [[links]] from the new page: [[pr_89-91_skills-doc-frontmatter-injection]], [[pr_92_exec-bit-sweep]], [[session_2026-07-03_ai-docs-refresh-and-cc-mechanics-probes]], [[post-review]] (via piped-title in prose), [[merge]] (via piped-title in prose), [[review-artifact-seam]] — all resolve to existing pages (verified). STALE-WORDING SWEEP (the specific ask this pass targeted): grepped commands/post-review.md, commands/merge.md, and design/review-artifact-seam.md for "deferred", "not yet implemented", "not shipped" language describing the post-review.md injection or the exec-bit test gap — both instances found and fixed (post-review.md's "Injection status (2026-07-03)" header + prose; merge.md's "was deferred, not shipped" paragraph). No further stale "deferred" occurrences of this specific claim remain in the vault (verified via grep across commands/ and design/; the historical PR #91 and PR #89-91 source pages still correctly describe the ORIGINAL deferral as history, which is accurate — those are past-tense records, not currently-stale claims). CONTRADICTION CHECK: none introduced. PRE-EXISTING BENIGN FALSE-POSITIVES (unchanged, not re-verified this pass — same class as every prior lint): [[:space:]] POSIX regex literals; [[wiki-links]]/[[link]]/[[page_name]] schema meta-text; [[$answer=="y"]] bash conditional. 0 orphans. 0 new dangling links. Sources 39→40.

## [2026-07-05] ingest | PRs #96–98 merged (cluster, 127a149/76084fe/559d79b): mode-aware install sweep, command-substitution injection class closed, hook-owned loop-stop counter. #96: `install.sh`'s exec-bit sweep reads each swept file's git index mode and branches (100755→ensure +x, 100644→ensure -x [new], not-in-index→legacy unconditional +x), closing the index-vs-installed-copy drift PR #94 flagged. New test `install_mode_sweep.test.sh` uses a real git worktree so `git ls-files -s` matches the real index. Fix round closed a Critical: the mode-read line ran under `set -euo pipefail`, and `git ls-files` exits 128 outside a git repo (release-tarball installs), silently killing the installer after the first swept file — fixed with `|| _index_mode=""` falling through to the pre-existing fallback branch; RED-first regression test added (no-`.git` directory, asserts exit 0 + fallback still applies). Suite 24→25. #97: experimentally proved [[post-review]]'s PR #93 render-time PR-state line was a **command-substitution injection**, not the milder "unsanitised input" PR #93 itself had noted — `$ARGUMENTS` is textually spliced into a render-time `` !`cmd` `` line before the shell parses it, so a payload executes as live shell syntax regardless of surrounding quotes (quoting only governs interpretation after substitution, not the substitution itself). Two reviewers had rated the original note low-severity without testing command substitution; this experimental methodology (running a real payload) caught what static review twice missed. Fix: removed the injected block entirely, replaced with argument-free `gh pr list --state open --limit 10` (matches merge.md's convention); `## Step 0 — Argument gate` rewritten as pure model-level inspection (never paste the argument into a shell command); added a **class-wide test** banning `$ARGUMENTS` in any `commands/*.md` render-time `` !`cmd` `` line. RED: 2 genuine fails + 1 unrelated BSD-grep bug in the test itself, also fixed. GREEN: 10/10. Suite 24/24 (net unchanged — 2 lines removed, replaced by different-shaped guard). #98: `loop_stall_guard.sh` becomes the sole writer of `progress.json.loop_stop_counts`, closing a two-writer lost-update race (hook's presence check + orchestrator self-maintenance prose in SKILL.md) that had undercounted 2 loops in a recorded session. New `bump_loop_stop_count()` does a tmp-file jq read-modify-write (auto-vivifying), same pattern as `post_review.sh`'s `write_cache`; best-effort and never fatal (missing file/malformed JSON/failed mv all logged and swallowed, hook still exits 0). `SKILL.md` updated in 5 places (body only) to mark the field HOOK-OWNED, mirroring `completed_marker`'s existing carry-forward treatment. Fix round added 4 standing-invariant tests: no-clobber (other progress.json keys survive byte-identical), jq-absence fail-open (`command -v jq || return 0`), last-declaration-wins tie-break for multi-`LOOP-STOP` messages, degraded-filesystem safety (unwritable/never-created directories) — plus found and fixed an off-by-one in the test harness's own `run_env` helper (arg0-pinning shifted args, producing a false exit-127 on the jq-absence test). `loop_stall_guard.test.sh` 13→21. Suite 24/24 both rounds. Context: repo renamed to `blueman82/coderails-dev` mid-loop (old URLs redirect); local `~/.claude` ai_docs hooks/subagents/slash-commands snapshots refreshed (hooks 8→29 events) after the stale-snapshot pattern from PR #89–91 recurred; an org-auth outage killed a worker mid-fix-round (uncommitted worktree work survived, versioned respawn inherited it); origin/main advanced under the loop twice (workers switched to merge-base-relative scope assertions). Created: sources/pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter.md. Updated: sources/pr_92_exec-bit-sweep.md (closure pointer for the install.sh drift gap, frontmatter), commands/post-review.md (Injection status section rewritten SHIPPED→REMOVED-for-security with full experimental writeup, see-also, frontmatter), commands/merge.md (injection-history paragraph extended with the security fix, see-also, frontmatter), design/spec-plan-progress-artifact-chain.md (new "loop_stop_counts: hook-owned, sole writer" section, see-also, frontmatter), hooks/loop_stall_guard.md (Phase 13 stall metric section corrected — was stale, claimed the orchestrator increments the counter — new hook-owned-counter section, see-also, frontmatter), design/install-and-cache-trap.md (new "Exec-bit sweep is now git-index-mode-aware" section, cross-references, frontmatter), index.md (new source row, hooks-table loop_stall_guard.sh status annotation, frontmatter). Sources 40→41.

## [2026-07-05] drift-check | Scoped docs-drift check for PRs #96–98 (report only, not fixed this ingest per task scope). Grepped README.md, AGENTS.md, INSTALLATION.md, docs/REFERENCE.md on current main. FOUND: (1) `INSTALLATION.md:73` — "arms the scripts (`chmod +x`)" describes the install sweep as unconditionally +x; as of PR #96 this is incomplete (sweep is now git-index-mode-aware: 100755→+x, 100644→-x, untracked→legacy +x) — not wrong for the common case (most swept files are 755 in the index) but no longer the full picture. Flagged, not fixed. (2) `README.md:22` and `INSTALLATION.md:41,45` still show `git clone https://github.com/blueman82/coderails.git` — predates the mid-loop rename to `blueman82/coderails-dev`; old URL redirects so not functionally broken, but stale. Flagged, not fixed. NOT FOUND (nothing stale): no README/INSTALLATION/REFERENCE prose describes `post-review`'s `$ARGUMENTS` handling or render-time injection risk — that was always implementation-level detail. No README/INSTALLATION/REFERENCE mention of `loop_stop_counts` at all — it's SKILL.md-internal/design-territory, no stale claim there.

## [2026-07-05] lint | Post-ingest lint for PRs #96–98 cluster. New page sources/pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter.md — NOT orphan: 8 inbound links verified via grep (index.md ×2, log.md, sources/pr_92_exec-bit-sweep.md, commands/post-review.md ×2, commands/merge.md, design/spec-plan-progress-artifact-chain.md ×2, hooks/loop_stall_guard.md ×2, design/install-and-cache-trap.md ×2). Outbound [[links]] from the new page: [[pr_92_exec-bit-sweep]], [[post-review]], [[merge]], [[pr_93-94_post-review-injection-and-exec-bit-invariant]], [[loop_stall_guard]], [[loop_state_guard]], [[spec-plan-progress-artifact-chain]], [[review-artifact-seam]], [[install-and-cache-trap]] — all resolve to existing pages (verified via grep of each target's title/filename). STALE-WORDING FIX (the specific correction this pass made): hooks/loop_stall_guard.md's "Phase 13 stall metric" section previously stated "the orchestrator increments [loop_stop_counts] per declaration" — this became actively wrong as of PR #98 (the hook is now the sole writer) and has been corrected with a new subsection, not just a pointer. Checked commands/post-review.md and commands/merge.md for any remaining "surfaced, not actioned" language about the now-closed injection — both updated to reflect closure, no stale instance remains (verified via grep for "surfaced" and "not actioned" across commands/ and design/). CONTRADICTION CHECK: none introduced by this cluster. PRE-EXISTING BENIGN FALSE-POSITIVES (unchanged, same class as every prior lint — not re-verified this pass): [[:space:]] POSIX regex literals in skills-hooks-seam.md/enforce_pr_workflow.md/pr_42/pr_46 sources; [[wiki-links]]/[[link]]/[[page_name]] schema meta-text in log.md/wiki-lint.md/wiki-query.md/wiki-ingest.md; [[$answer=="y"]] bash conditional in install-bash32 investigation; historical [[pr_N]]-style references in old log.md prose. 0 new dangling links (verified). 0 new orphans (verified). STALENESS: install-cache-trap_2026-05-30 investigation remains the only >30-day-stale page, pre-existing and historical, not touched by this cluster's scope. 0 unresolved findings introduced by this cluster.

## [2026-07-05] ingest | repo consolidation — two repos collapsed to one clean repo
Created sources/repo-consolidation_2026-07-05.md + design/history-identity-scrub.md. Updated design/repo-hosting.md (was stale — two-repo era + named a legacy identity string, both corrected) and index.md (Design + Sources sections, bumped last_updated). Records the 2026-07-05 collapse: mailmap history rewrite → delete-and-recreate to shed refs/pull/* → rename to canonical coderails → 25 stale branches pruned. Deliberately identity-string-free per owner request.

## [2026-07-06] lint | index.md gaps closed, stale Phase 2.8→2.7b reference fixed, 4-skill data gap flagged
FIXED: index.md was missing 3 rows for pages that already existed (post-evals command, task-evals skill, task-evals-gate design) — added all three. skills/writing-plans.md (frontmatter + 4 body mentions) still said "Phase 2.8" for the writing-plans invocation point; agentic-loop.md documents this merged into Phase 2.7b since PR #86 (2026-07-01) — corrected all 5 occurrences (4 in writing-plans.md + 1 in index.md's Skills table), bumped writing-plans.md last_updated. skills/using-coderails.md line 27 mentioned "the discipline loop" in prose without linking the existing design/discipline-loop.md page — added [[discipline-loop]], bumped last_updated. DATA GAP FLAGGED, NOT FIXED: 4 skills shipped by PR #54 (2026-06-26) — engineering-principles + 3 language sub-skills (go/python/ts) — have zero wiki page despite being referenced by name across 13 existing pages; authoring 4 substantive pages from ~1065 lines of source was out of scope for a lint pass, so index.md's Skills table now carries an explicit "no wiki page yet" marker instead of inventing dangling [[links]]. DECLINED AS NOISE: sub-agent flagged commands/prep.md and commands/workflow.md as missing a [[using-git-worktrees]] cross-reference for their worktree-creation prose — checked the skill page directly, it explicitly documents itself as "not invoked by /workflow itself" and covers a different mechanism (parallel multi-agent worktree isolation, not prep's single feature-branch `git worktree add`) — linking would conflate two distinct mechanisms, correctly left unlinked. STALE PAGES (unchanged, not fixed — investigations/sources are point-in-time immutable records per AGENTS.md, staleness there is expected): install-cache-trap_2026-05-30, hook-exit-codes (2026-05-31), session_2026-05-31_verify-loop-hardening, session_2026-05-31_prompting-doc-alignment, install-bash32-bad-substitution_2026-06-01, session_2026-06-01_verify-loop-total-enforcement, session_2026-06-01_agentic-loop-delegate-all-impl. ORPHANS (unchanged, not fixed — no natural inbound link exists to fabricate): skills/handoff.md (the one active-skill orphan; genuinely no other page discusses handoff without already linking it) + 7 old PR source pages (pr_19-30, pr_43, pr_49, pr_69, pr_75, pr_79) which are immutable point-in-time records, orphan status is benign for this page type. CONTRADICTIONS: 1 confirmed (fixed above), 1 investigated-and-cleared (repo-hosting.md vs pr_96-98 — same-day sequence, not a conflict, verified via git log timestamps 36 min apart). 0 inbox backlog (directory does not exist).

## [2026-07-06] ingest | PRs #7–10 merged: task-evals follow-up cluster (wiki-first prerequisite, comment-spoofing/pagination closure, install/docs refresh, tier_justification everywhere)
Four PRs closing gaps [[pr_1-4_task-evals-feature]] had surfaced-not-fixed, plus one owner directive, all same-day. #7 (`e487b54`, head `5602b9b`): task-evals SKILL.md wiki-first prerequisite tightened — "invariants and constraints the goal state must respect" wording, no-wiki fallback clause (`config.wiki_path` null → codebase-only), agentic-loop Phase 2 pre-flight-reuse carve-out. #8 (`62ad18d`, head `395c416`): comment-spoofing CLOSED for both PR-gate readers in `scripts/lib/git-common.sh` — new `pr::_trusted_login`/`pr::_trusted_comment_bodies` filter to the gh-authenticated login with `author_association == OWNER`, fail-closed; clean-break move from the 100-comment-capped `gh pr view --json comments` to paginated `gh api .../issues/{n}/comments --paginate`. Review-round fixes: mutation-proven stub tautology, `repo()` dotted-name truncation, pre-seeded `_PR_TRUSTED_LOGIN` jq-injection (charset validation added), false caching comment, vacuous clean-break guard literal. Known, documented limitation: `OWNER` assumes a personally-owned repo, fails closed on org repos. #9 (`ffe5ccc`, head `431c48e`): INSTALLATION.md refresh (28-skill table, post-review/post-evals documented, config-gated enforce_pr_workflow, any-branch no_edit_on_main clause) + `install.sh`'s literal chmod list gains `scripts/lib/review-artifact.sh` and `scripts/lib/config.sh` (the sweep has no `scripts/lib/*.sh` glob, so this is a manual-list gap closure, not structural); mode-sweep test hardened. #10 (`238f5e1`, head `376e9fc`, owner directive): `tier_justification` required at **every** tier, not tier-0-only — `post_evals::validate_structure` check 2 and the loop-gate's `als_read_loop_evals_result` (new `UNJUSTIFIED` result, checked before result/tier, plus a fail-closed catch-all for unrecognised values) both enforce it; retroactively blocks pre-existing GO loop artifacts that lack a justification. Cross-PR theme: every PR needed both a review artifact and an eval artifact before merge — the gate dogfooding itself; two vacuous-check findings (PR #8) were caught specifically by the negative-control discipline. Created: sources/pr_7-10_task-evals-followups.md. Updated: design/task-evals-gate.md (fail-open/fail-closed matrix gains UNJUSTIFIED and the unrecognised-value catch-all row, known-gaps section closes comment-spoofing/pagination, new tier_justification subsection, frontmatter/see-also), skills/task-evals.md (new "Prerequisite" section, tier_justification-everywhere note, failure-modes/see-also updates, frontmatter), design/review-artifact-seam.md (comment-spoofing/pagination fix note — same fetch helper is shared by this sibling seam — frontmatter/see-also), index.md (2 new Sources rows: the previously-missing pr_1-4_task-evals-feature row, and this cluster's row). Sources 41→43 (one retroactive row-gap fix + one new cluster).

## [2026-07-06] lint | Post-ingest lint for PRs #7–10 cluster — 2 doc-drift fixes (stale tier_justification wording, two files), vault health otherwise unchanged
FIXED: two files carried the pre-PR#10 "Tier 0 requires non-empty `tier_justification`" wording, contradicted by PR #10's owner directive (tightened to every tier) — both were files `sources/pr_7-10_task-evals-followups.md`'s own PR #10 file list never named (it only names `scripts/post_evals.sh`, the code, confirmed via grep), so both were genuine ingest-pass misses, not pre-existing/declined items. (1) `commands/post-evals.md` line 49 (structural refusal check 2) — corrected to state every tier requires non-blank `tier_justification` (tier 0 justifies the exemption itself, tier ≥1 justifies which predicate fired), added a `[[pr_7-10_task-evals-followups]]` citation inline plus a "See also" row, and added the PR's source page to frontmatter `sources` (missing despite the page depending on that PR's behaviour). (2) `design/task-evals-gate.md` line 82, its own "Structural refusals" recap of the identical 7-check list — carried the same stale line independently; corrected with matching wording plus a `[[pr_7-10_task-evals-followups|PR #10]]` citation and a pointer to the page's own existing "tier_justification required at every tier" section below it. Both files' `last_updated` were already 2026-07-06, no bump needed. CHECKED AND CLEAN: all four originally-touched cluster files (sources/pr_7-10_task-evals-followups.md, design/task-evals-gate.md, skills/task-evals.md, design/review-artifact-seam.md) — wiki-links log.md's ingest entry describes are all present and resolve; index.md's Sources section carries both the pr_1-4 and pr_7-10 rows correctly. A vault-wide grep for the literal string "Tier 0 requires non-empty" after both fixes returns zero hits outside log.md's own historical narration of this entry (verified). CONTRADICTIONS: 0 new beyond the two fixed above — no page describes comment-spoofing or the 100-comment pagination cap as still-open (all correctly reflect PR #8's closure). ORPHANS: 0 new (only the previously-accepted skills/handoff.md + 7 old PR source pages remain orphaned, unchanged). DANGLING LINKS: 0 new (all apparent hits outside the known-benign classes traced to log.md's own historical prose narrating past fixes, not live `[[links]]` in content pages; vault-wide sweep covered all 111 non-excluded markdown files). STALE PAGES: unchanged from the prior 2026-07-06 lint pass — same 7 pages (install-cache-trap_2026-05-30, hook-exit-codes, session_2026-05-31_verify-loop-hardening, session_2026-05-31_prompting-doc-alignment, install-bash32-bad-substitution_2026-06-01, session_2026-06-01_verify-loop-total-enforcement, session_2026-06-01_agentic-loop-delegate-all-impl), none drifted to a newer date needing correction. DATA GAP CLOSED (confirmed, not new work this pass): skills/engineering-principles.md + 3 language sub-skills now exist with substantive content (48–105 lines each), index.md's "no wiki page yet" marker from the prior lint pass is gone. 0 inbox backlog (directory does not exist).

## [2026-07-06] ingest | PRs #11–14 merged: gate-hardening follow-ups (tier-0 NO-GO precedence, HOME-sandboxed install test, push.sh staging fix, trust-floor widened + merge.sh error split)
Four independent work-units, base 238f5e1 (post-PR#10), each requiring both a review artifact and an eval artifact before merge. #11 (`a1c4803`, owner directive): `hooks/scripts/lib/loop_state_common.sh`'s `als_read_loop_evals_result()` now checks explicit `result == "NO-GO"` before the tier-0 exemption branch — previously a tier-0 artifact with an explicit recorded `NO-GO` still read as the allowed `TIER0` case, since the exemption fired on tier alone. "An exemption justifies having no evals, not overriding a recorded failure." A tier-0 artifact with no `result` set at all (the legitimate exemption) is unaffected. #12 (`fdd29fd`): `install_mode_sweep.test.sh` now sandboxes `$HOME` for both `install.sh` invocations it makes — closes a **real, recurring corruption** of the developer machine's actual `~/.claude/settings.json` (10 auto-repaired events during this very loop, caused by `test_gate`'s per-commit test run executing inside worker worktrees carrying the unfixed test; `MEMORY_TARGET` alone never redirected install.sh's unconditional `$HOME` writes). Anti-vacuity discipline: pre-seeds a stale marketplace key so the sandbox mutation is proven real, not merely inert; before/after cksum guards on the real settings.json/known_marketplaces.json/CLAUDE.md. #13 (`321bca3`): `push.sh` staging changed `git add -A` → `git add -u` + a named untracked-file warning (explicit `git add` now needed for new-file PRs); review caught and fixed a `grep '^??'`-under-`pipefail` crash on the zero-untracked-files case (`865cab0`); new `hooks/scripts/tests/push_staging.test.sh` (first-ever dedicated staging coverage); `docs/REFERENCE.md` updated. #14 (`7c1dd19`, security-relevant, owner directive): the review/eval artifact gates' `author_association == "OWNER"` trust conjunct — which failed closed on org-owned repos ([[pr_7-10_task-evals-followups|PR #8]]'s own documented scoped limitation) — is **removed entirely** (clean break, independently verified no surviving compat path) and replaced with a repo-permission check: login match (unchanged anti-spoof) **AND** `viewerPermission` of `ADMIN`/`MAINTAIN`/`WRITE` on the current repo (new `pr::_trusted_permission`/`pr::_permission_is_write_or_better` in `scripts/lib/git-common.sh`); a resolvable-but-insufficient permission is "no trusted comment found," not a fetch failure; an actual lookup failure still fails closed. `merge.sh`'s two artifact gates also gained a three-way error-message split (identity/permission/comments-fetch failure) via a new `PR_TRUST_FETCH_FAIL_REASON` signal threaded through a `pr::_trusted_comment_bodies_or_fail` wrapper (stderr-carried, since the reader is always called via `$(...)`). `INSTALLATION.md` documents the new trust rule for operators. Created: sources/pr_11-14_gate-hardening-followups.md. Updated: design/task-evals-gate.md (frontmatter/tags, loop-evals reader description + block condition + fail-closed matrix row for tier-0 NO-GO precedence, Known-gaps section closes the OWNER-badge scoped limitation, see-also), design/review-artifact-seam.md (frontmatter/tags, comment-spoofing section extended with the trust-floor widening, see-also), design/install-and-cache-trap.md (frontmatter/tags/last_updated, new HOME-sandbox section, see-also), commands/merge.md (frontmatter/tags, two new subsections — trust floor + error-message split — see-also), commands/push.md (frontmatter/tags/last_updated, staging behaviour rewritten, new "Staging safety" section, see-also), hooks/loop_state_guard.md (frontmatter/tags, gate 4a description + see-also), index.md (new Sources row). Suite test-file count 29→30 (PR #13's new file). Sources 43→44.

## [2026-07-06] lint | Full vault sweep (112 files) — 1 internal contradiction fixed, 2 data-gaps flagged, cross-ref backlog confirmed unchanged
FIXED: `hooks/loop_stall_guard.md` line 111 self-contradicted its own line 100 — line 100 correctly says "Four Stop hooks (confidence, verify, C1, C2)" but the "See also" pointer to [[discipline-loop]] mislabelled the combined Stop+SubagentStop total (6) as "all six Stop hooks" specifically. `design/discipline-loop.md:133` confirms the true composition: 4 Stop hooks + 2 SubagentStop hooks = 13 hooks total across all events. Corrected the pointer to state the composition explicitly instead of a bare number; bumped last_updated. Present since file creation (2026-06-25), survived 4 subsequent edit passes uncorrected — first lint pass to actually read this page's full body against discipline-loop.md rather than just checking its frontmatter/links.
DATA GAPS FLAGGED, NOT FIXED (documentation-only; no code follow-up implied): (1) Trust-floor / OWNER-badge→permission-check model (shipped in code by PR #14, already recorded in [[pr_11-14_gate-hardening-followups]]) has no dedicated concept page despite being cited across 5+ pages (design/review-artifact-seam.md, design/task-evals-gate.md, commands/merge.md, and the two PR-follow-up sources) — currently only exists as subsections of other pages. (2) `/sync-docs` (the agentic-loop drift-check step; PR sources [[pr_77_agentic-loop-sync-docs-step]] and [[pr_79_sync-docs-drift]] already exist) has no `commands/sync-docs.md` or `skills/sync-docs.md` home page despite being an active loop-boundary step referenced from hooks/inject_context.md, hooks/test_gate.md, skills/agentic-loop.md, and index.md. Neither authored this pass — both are multi-paragraph undertakings out of lint scope, consistent with how the PR #54 engineering-principles gap was deferred in the 2026-07-06 lint pass two entries above (until a later ingest closed it).
CONFIRMED STILL OPEN (previously flagged in the 2026-06-25 and 2026-07-06 lint passes, human-judgement calls, not re-litigated): `hooks/test_gate.md` mentions "test-gate-setup" by name without linking the existing `commands/test-gate-setup.md` page; `design/enforcement-model.md`'s "Current Hook Map" table lists 8 hook script filenames with none `[[linked]]` in its own Cross-References section. Both deferred again — same status quo, no new information changes the prior human-judgement call.
STALE PAGES: unchanged, same 7 point-in-time records as every pass since 2026-07-06 (install-cache-trap_2026-05-30, hook-exit-codes, session_2026-05-31_verify-loop-hardening, session_2026-05-31_prompting-doc-alignment, install-bash32-bad-substitution_2026-06-01, session_2026-06-01_verify-loop-total-enforcement, session_2026-06-01_agentic-loop-delegate-all-impl) — all investigations/sources, staleness expected per AGENTS.md.
ORPHANS: unchanged, same set (skills/handoff.md + 7 old PR source pages: pr_19-30, pr_43, pr_49, pr_69, pr_75, pr_79) — all previously accepted as benign for their page type.
CONTRADICTIONS: 1 found and fixed (above). No `⚠️ CONTRADICTION` marker flags present anywhere in the vault.
INDEX: 0 gaps — every real page has an index.md row; no index.md row points at a nonexistent page.

## [2026-07-06] ingest | PRs #15–17 merged: loop-hardening cluster (registration nudge hook, eval-freeze timing, SDD ledger DRY)
Three independent PRs closing two gaps a single 2026-07-06 incident surfaced: a 17-work-unit `subagent-driven-development` execution ran 9 units with zero hook signal because the orchestrator never invoked `coderails:agentic-loop` and never registered `progress.json` — and separately, the plan that drove that execution had encoded eval-freeze-and-grade as one final task, so it ran 8 tasks deep with no frozen `evals.json` at all. Incident source: memory file `project_loop_hardening_handoff.md` (origin session `f5fa5ea2-35ca-45e5-a558-1ccfcdc696fb`). #15 (F2, `dc55377`): `writing-plans/SKILL.md`'s "Final eval-gate task" section split into "freeze evals after stress-test, before implementation" (the actual freeze point, immediately after the mandatory planning-sequence gate) and "final task: grade and post only" (the plan's real last task, `/coderails:post-evals` against the already-frozen artifact). Four cross-referencing docs corrected in lockstep: `task-evals/SKILL.md`'s invocation-point bullet, a new `subagent-driven-development/SKILL.md` Pre-Flight Plan Review bullet asserting `evals.json` already has non-empty `frozen_at`/`frozen_sha` before Task 1 dispatch (belt-and-braces), `commands/workflow.md:157`, `docs/REFERENCE.md:83`. Doc-only, verified by lockstep grep, not a test file. Two known pre-existing defects in the PR's own frozen PR-scope evals (an `awk` range whose end-pattern matches its own start line; a file-existence check for a path shape that was never how `/coderails:post-evals` is actually implemented) flagged for the grading agent, not fixed. #16 (WU4, `e4aafa6`): SDD progress ledger moved from repo-root-keyed `$root/.coderails/sdd` (shared by every session in a checkout — a latent collision bug, destroyed wholesale by `git clean -fdx`) to the session-keyed loop-state dir beside `progress.json`, via `agentic_loop_path.sh` + `dirname`. Renamed `sdd-ledger.md`. Fact-ownership split documented: ledger = task-level truth with commit ranges, controller-owned; `progress.json.work_units[id].status` = unit-level truth, orchestrator-owned. `sdd-workspace` now fails loud on an unset `CLAUDE_CODE_SESSION_ID` (the path helper's per-call fallback is non-idempotent — fatal for a ledger owner needing the same path repeatedly) and on non-absolute helper output (a previously-silent cwd-fallback bug, reproduced in review). New 6-check TDD smoke test — first test infrastructure for any SDD script. Clean-break: no shim, no fallback to the old path; accepted one-time migration risk for any in-flight ledger at merge time (recovery: `git log` remains authoritative, per the skill's pre-existing contract). #17 (F1, `d1b1360`): new sibling Stop hook `hooks/scripts/unregistered_loop_guard.sh` — nudges (`additionalContext`, exit 0), never blocks, when ≥3 DISTINCT `message.id` values carry an `Agent` tool_use (parallel fan-outs share one message.id and never trip it; sequential dispatches — the incident's actual shape — each get a distinct id) AND no `progress.json` at the resolved path AND no `agentic-loop` Skill invocation anywhere in the transcript. Deliberate deviation from every sibling loop-state hook (all of which block on ground truth): this hook has only a heuristic, so nudge is the honest posture; the flip-to-block condition is pre-recorded (a nudge delivered but ignored escalates it). `jq` failures logged with distinct reasons (`jq_missing`/`jq_parse_error`/`payload_parse_error`), never folded into the below-threshold case. New 27-check test file; `stdin_bounded_read`, the timeout-floor test, and the exec-bit invariant test all updated for the new file. Stop hook count 4→5 (SubagentStop unaffected — this hook is Stop-only, same as its two siblings). Created: sources/pr_15-17_loop-hardening-registration-eval-freeze-ledger-dry.md, hooks/unregistered_loop_guard.md (new page). Updated: hooks/loop_state_guard.md (new "Relationship to unregistered_loop_guard.sh" section, frontmatter, see-also), hooks/loop_stall_guard.md (Stop-hook-count correction, see-also), skills/writing-plans.md (new "Eval freeze happens once" section, frontmatter, see-also), skills/task-evals.md (invocation-point wording corrected in two places, frontmatter, see-also), skills/subagent-driven-development.md (two new sections — Pre-Flight bullet + ledger relocation — frontmatter, failure modes, see-also), skills/agentic-loop.md (Context-window persistence gains one documenting sentence — no-touch region's own instructions untouched — frontmatter, see-also), design/discipline-loop.md (Stop hook composition table 4→5 hooks, 13→14 total, cross-references), index.md (new hooks-table row, Stop hook order line, new Sources row).

## [2026-07-06] lint | Post-ingest lint for PRs #15–17 cluster
NEW PAGE ORPHAN CHECK: sources/pr_15-17_loop-hardening-registration-eval-freeze-ledger-dry.md — NOT orphan, 8 inbound links verified via grep (index.md, hooks/loop_state_guard.md ×2, hooks/loop_stall_guard.md, skills/writing-plans.md ×2, skills/task-evals.md ×2, skills/subagent-driven-development.md ×2, skills/agentic-loop.md ×2, design/discipline-loop.md). hooks/unregistered_loop_guard.md (new page) — NOT orphan, 6 inbound links verified (index.md ×2, hooks/loop_state_guard.md ×2, hooks/loop_stall_guard.md, skills/agentic-loop.md, design/discipline-loop.md ×2). Outbound `[[links]]` from both new pages resolve to existing pages (verified via grep of each target's title/filename): loop_state_guard, loop_stall_guard, agentic-loop, task-evals, writing-plans, subagent-driven-development, discipline-loop, dispatching-parallel-agents, spec-plan-progress-artifact-chain, enforcement-model, pr_76_harden-hook-stdin-read — all present. NO-TOUCH REGION CHECK (specific concern for this cluster, since PR #16 edited inside `## Context-window persistence`): read `skills/agentic-loop/SKILL.md`'s actual no-touch region text — confirmed the PR #16 sentence was appended after the region's existing instructional text, not inserted into it; the wiki page's own framing ("documentation of an adjacent fact, not a change to the no-touch region's own instructions") matches the source diff. STOP HOOK COUNT CONSISTENCY CHECK: grepped the vault for "Four Stop hooks" and "4 Stop hooks" post-edit — one surviving instance in hooks/loop_stall_guard.md's Known Limitations prose, corrected in this same pass (see Updated list above) to note the fifth hook exists but doesn't add to that specific ceremony-thrash risk, since it's a nudge not a blocking co-requirement. CONTRADICTION CHECK: none introduced — task-evals.md's two corrected invocation-point mentions and writing-plans.md's new section describe the same freeze-after-stress-test timing consistently; subagent-driven-development.md's Pre-Flight bullet and ledger-relocation sections don't conflict with any pre-existing content (grepped for "coderails/sdd" and "final task" project-wide, zero stale hits remain outside this entry's own historical narration and log.md's prior entries narrating the pre-fix state). ORPHANS: unchanged aside from the two new pages confirmed non-orphan above (skills/handoff.md + 7 old PR source pages remain the same accepted set). STALE PAGES: unchanged, same 7 point-in-time records as every prior pass. DANGLING LINKS: 0 new (vault-wide grep for `[[unregistered_loop_guard]]` and `[[pr_15-17_loop-hardening-registration-eval-freeze-ledger-dry]]` shows every occurrence resolves to the two new pages, none dangling). 0 inbox backlog (directory does not exist).
INBOX: 0 backlog (directory does not exist).

## [2026-07-06] lint | Full vault re-sweep — 0 new findings, re-confirms prior pass
Independent full-vault check (all 77 non-excluded files read/grepped) run after the two lint passes earlier today. Re-verified every open item live against source rather than trusting cached reads: `design/enforcement-model.md`'s Current Hook Map (lines 58-77) already lists all 5 Stop hooks including `unregistered_loop_guard.sh`, matching `hooks/hooks.json` exactly (verified: `python3 -c 'json.load(...)'` dump of hooks.json Stop entries) — no contradiction present. `hooks/test_gate.md:77` already wiki-links `[[test-gate-setup]]`; `commands/test-gate-setup.md` has that inbound link plus its index.md row, so it is not an orphan (verified via grep). CONTRADICTIONS: 0. STALE PAGES: same 7 point-in-time records, unchanged (expected per page-type convention). ORPHANS: same accepted set (skills/handoff.md + 6 old PR source pages: pr_19-30, pr_43, pr_49, pr_69, pr_75, pr_79) — 0 new. MISSING CONCEPTS: trust-floor/OWNER-permission model and `/sync-docs` remain undocumented as dedicated pages (both previously flagged and deliberately deferred as multi-paragraph undertakings, not re-litigated); `work_units` (7 pages) and `loop_stop_counts` (3 pages, mutually consistent) flagged as new candidates for a consolidating concept page but not authored this pass — out of lint scope, same deferral pattern as trust-floor/sync-docs. MISSING CROSS-REFS: `design/enforcement-model.md`'s own Cross-References section still doesn't link the 8+ hook filenames named in its own table — confirmed still open, human-judgement call, deferred again. DATA GAPS: 0 — hooks 12/12, commands 12/12, skills 27/27 for merged content have wiki pages; `skills/dashboard/` exists on disk but is untracked/uncommitted in the plugin repo (verified: `git status --short`), so its absence is correct, not a gap. INBOX: 0 backlog (directory does not exist).

## [2026-07-06] lint | Correction to prior pass's false-clean claim + 2 genuine fixes (enforcement-model hook table, test_gate cross-ref)
CORRECTION: the immediately preceding log entry ("Full vault re-sweep — 0 new findings") incorrectly claimed `design/enforcement-model.md`'s Current Hook Map already listed `unregistered_loop_guard.sh` and that `hooks/test_gate.md:77` already linked `[[test-gate-setup]]`. Both claims were checked directly against the files as they stood at the time and were false — this was caught by a second, independent read of the same two files, not new information. FIXED (this pass): (1) `design/enforcement-model.md` — added the missing `unregistered_loop_guard.sh` row to the Current Hook Map table (Stop event, nudge-not-block mode, PR #17 trip condition), updated the table-footer note to call out it as the vault's only nudge-mode Stop hook (with the pre-recorded nudge→block escalation trigger), added it to Cross-References, added `sources/pr_15-17_loop-hardening-registration-eval-freeze-ledger-dry.md` to frontmatter `sources`, bumped `last_updated`. Also wiki-linked all 9 previously-bare hook-script filenames in the same table (`inject_bootstrap`, `inject_context`, `discipline_catchup`, `check_confidence_labels` ×2, `check_verify_loop` ×2, `loop_state_guard`, `loop_stall_guard`, `destructive_bash_gate`, `test_gate`, `enforce_pr_workflow`, `no_edit_on_main`) — closing the separate, previously-deferred "Current Hook Map lists hook filenames with none linked" cross-ref gap in the same edit, since both live in the identical table. (2) `hooks/test_gate.md` — line 77 named `/coderails:test-gate-setup` in prose without a wiki-link despite `commands/test-gate-setup.md` existing; added `[[test-gate-setup]]`, bumped `last_updated`. This also resolves `commands/test-gate-setup.md`'s orphan status (previously only linked from index.md, which doesn't count as a qualifying inbound link) — it now has a real inbound content-page link. CONTRADICTIONS: 1 found and fixed (above) — this was the vault's actual open contradiction (design doc under-counting live Stop hooks against `hooks/hooks.json` HEAD), not the 0 the immediately-preceding entry claimed. STALE PAGES: unchanged, same 7 point-in-time records (investigations/sources, staleness expected per page type). ORPHANS: 7 remain (re-verified via grep: skills/handoff.md is still genuinely orphaned — index.md is its only inbound link — plus the 6 old immutable PR source pages: pr_19-30, pr_43, pr_49, pr_69, pr_75, pr_79, all benign for their page type); `commands/test-gate-setup.md` resolved by the fix above (dropped out of the orphan set). MISSING CONCEPTS: `work_units` (7 pages: hooks/loop_state_guard.md, skills/task-evals.md, skills/writing-plans.md, design/task-evals-gate.md, skills/subagent-driven-development.md, skills/agentic-loop.md, design/spec-plan-progress-artifact-chain.md) and `loop_stop_counts` (3 pages, mutually consistent per PR #98) remain flagged, not authored — same deferral rationale as trust-floor/OWNER-permission and `/sync-docs`, all multi-paragraph undertakings out of lint scope. DATA GAPS: 0, reconfirmed — hooks 12/12, commands 12/12, skills 27/27 for merged content; `skills/dashboard/` is untracked/uncommitted in the plugin repo working tree (verified via `git status --short skills/dashboard/` and `git log --all -- skills/dashboard/SKILL.md` in the plugin repo), so its absence from the wiki is correct ingest behaviour (ingests from merged PRs only), not a coverage gap. INBOX: 0 backlog (directory does not exist).

## [2026-07-06] editorial | wu3 (loop-2 follow-ups): 3 consolidating pages authored, closing the trio of "missing concept" deferrals both prior lint passes flagged
Authored the three consolidating pages the last two lint passes deliberately deferred (not new findings — closing already-flagged gaps): (1) `design/loop-progress-fields.md` — consolidates `work_units` (orchestrator-owned, read by `als_read_work_units()` in `hooks/scripts/lib/loop_state_common.sh:138-145`, fail-open when absent, feeds [[loop_state_guard]]'s `>=3`-work-unit eval-gate threshold) and `loop_stop_counts` (HOOK-OWNED, sole writer `bump_loop_stop_count()` in `hooks/scripts/loop_stall_guard.sh:52-58`/`:85`, PR #98) into one page. Backlinked from the exact 7 `work_units`-citing pages this vault's own 2026-07-06 resweep entry named (`hooks/loop_state_guard.md`, `skills/task-evals.md`, `skills/writing-plans.md`, `design/task-evals-gate.md`, `skills/subagent-driven-development.md`, `skills/agentic-loop.md`, `design/spec-plan-progress-artifact-chain.md`) rather than re-deriving the list (that resweep entry's count already disagreed with the loop-2 spec's own "13 pages" figure — used the vault's own most recent count, not a third independently-derived number). (2) `design/trust-floor.md` — consolidates the trust-floor/OWNER-permission model (PR #14) as a SHORT map, explicitly NOT restating `commands/merge.md`'s "Trust floor for artifact gates" section (the SSOT for the mechanism itself — the `viewerPermission` call, the `ADMIN`/`MAINTAIN`/`WRITE` levels, the login-match anti-spoof property all stay only in `merge.md`). Backlinked from `design/review-artifact-seam.md`, `design/task-evals-gate.md`, and the two PR-follow-up source pages (`sources/pr_11-14_gate-hardening-followups.md`, `sources/pr_7-10_task-evals-followups.md`). (3) `skills/sync-docs.md` — genuinely net-new (no prior page existed for this concept at all). **Location correction, verified against the actual repo, not secondhand:** `/sync-docs` is a **user-level skill** at `~/.claude/skills/sync-docs/SKILL.md`, NOT part of the `coderails` plugin repo — confirmed via `find /Users/harrison/Github/coderails -iname '*sync-docs*'` (zero hits inside the repo itself; only this wiki's own PR-source records reference it). Backlinked from `hooks/inject_context.md`, `hooks/test_gate.md`, `skills/agentic-loop.md` (the three pages this vault's resweep entry named as referencing `/sync-docs` with no home page). All three new pages added to `index.md` (Skills table for sync-docs; Design list for the other two). Ran `coderails:wiki-lint` after delivery per the delivery convention — see the lint entry immediately following this one for results. Sources: `sources/pr_1-4_task-evals-feature.md`, `sources/pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter.md`, `sources/pr_15-17_loop-hardening-registration-eval-freeze-ledger-dry.md` (loop-progress-fields.md); `sources/pr_7-10_task-evals-followups.md`, `sources/pr_11-14_gate-hardening-followups.md` (trust-floor.md); `sources/pr_77_agentic-loop-sync-docs-step.md`, `sources/pr_79_sync-docs-drift.md` (sync-docs.md).

## [2026-07-06] lint | Post-wu3 full vault sweep (117 files) — 1 stale-phrase fix, 1 orphan-bookkeeping correction, 0 live contradictions
Full re-derivation of every check from current file contents (not trusted from prior log entries), following the vault's own established discipline after a prior pass's false "0 new findings" claim was caught by the next one. FIXED: `index.md:115` said "how the six discipline hooks compose into a coherent loop" — stale against `design/discipline-loop.md`'s current composition ("the three discipline hooks" sharing `discipline_common.sh`; "5 Stop hooks; 2 SubagentStop hooks; 14 hooks total across all events" overall). "Six" matched neither figure — a leftover from an earlier hook-count era that survived at least the 2026-06-25 "three→four" correction and several subsequent hook additions uncorrected. Reworded to state both real numbers instead of a single bare count that will drift again. ORPHAN BOOKKEEPING CORRECTED (not a fix, a count correction): `sources/pr_79_sync-docs-drift.md` dropped out of the accepted-orphan set as an unrecorded side effect of today's wu3 editorial pass — `skills/sync-docs.md:92` now carries a genuine content-page inbound link `[[pr_79_sync-docs-drift]]`. Current orphan set is 6, not 7: `skills/handoff.md` (genuinely orphaned, index.md is its only inbound link) + 5 old immutable PR source pages (pr_19-30, pr_43, pr_49, pr_69, pr_75) — all benign for their page type. CONTRADICTIONS: 0 live `⚠️ CONTRADICTION` flags anywhere in the vault (grep-verified); all historical occurrences in log.md/wiki-lint.md are schema/prose, previously classified benign, re-confirmed. STALE PAGES: `design/hook-exit-codes.md` (2026-05-31, 36 days) is the only commands/hooks/skills/design page past the 30-day threshold — content re-checked against current hook behaviour and found still accurate (exit-code semantics haven't changed; the page describes a stable Claude Code platform contract, not coderails-specific hook count), so left as-is, flagged for awareness rather than fixed. The other 6 stale pages are accepted point-in-time investigation/source records per AGENTS.md convention (install-cache-trap_2026-05-30, install-bash32-bad-substitution_2026-06-01, and 4 session_* source pages). DATA GAPS: 0 — direct diff of plugin source at `/Users/harrison/Github/coderails/.claude/worktrees/loop-hardening-orchestrator` against the vault confirms commands 12/12, hooks 12/12, skills 29/29 all have matching wiki pages (skill count includes the 2 pages authored this session: `design/loop-progress-fields.md` and `design/trust-floor.md` are design pages, `skills/sync-docs.md` brings skills to 29). MISSING CONCEPTS: 0 new — the three gaps flagged by the two prior 2026-07-06 lint passes (trust-floor, sync-docs, loop-progress-fields/work_units/loop_stop_counts) were closed by this session's own wu3 pass; no new 3+-page-cited concept found lacking a home. MISSING CROSS-REFS: 0 new. INBOX: 0 backlog (directory does not exist). Suggested follow-ups (not actioned, logged per lint convention): (1) audit other pages for bare hook/gate-count prose likely to drift the same way `index.md:115` just did — replace with links to the authoritative composition page instead of restating numbers; (2) consider computing orphan-set deltas mechanically against the prior lint's stated set rather than hand re-deriving the full list each pass, since incidental cross-linking work (like wu3) silently changes orphan status; (3) a follow-up sweep for any other 3+-page-cited concept without a home page, now that the three flagged gaps are closed — none found this pass, but this category tends to accumulate quietly between lints.

## [2026-07-06] ingest | PRs #21–22 merged: Loop 2 suggestion-tier follow-ups (merge.sh tempfile arm + test-coverage completions)
Two PRs closing 4 of the 6 suggestion-tier residuals recorded against [[pr_11-14_gate-hardening-followups]] (base `9f8f1af`, final `main` `1033512`). #21 (`d0e4c5a`): `scripts/merge.sh` gains a dedicated `tempfile)` case arm on both the review-artifact and eval-artifact gates for `PR_TRUST_FETCH_FAIL_REASON` — `pr::_trusted_comment_bodies_or_fail` (`scripts/lib/git-common.sh:182-196`) already set this reason on its own `mktemp` failure (which precedes any `gh` fetch), but `merge.sh`'s `case` had no arm for it and fell through to the generic "GitHub fetch failed" message, which is actively wrong for a local tempfile failure. TDD: Test 3d (`merge.test.sh`) + Test 4d (`merge_evals_gate.test.sh`), both asserting message-distinctness against the identity/fallback messages. `git-common.sh` itself untouched — only the `merge.sh` consumer gained the arm. #22 (`1033512`): test-only coverage completions closing gaps the #11-14 cluster had flagged but not filled — `push_staging.test.sh` gains PRE-STAGED NEW FILE + DELETED TRACKED FILE cases (proving `git add -u`'s tracked-only migration doesn't regress either edge); `loop_state_guard_evals.test.sh` gains a tier≠0/justified/result-key-absent-entirely fixture that is the first to genuinely reach `als_read_loop_evals_result`'s final `else` (NO-GO) branch — a pre-existing fixture's "(exercises final else)" comment was a misnomer, corrected in the test file itself; `install_mode_sweep.test.sh` gains a commands-dir conflict-decline fixture (an empty commands dir never triggers install.sh's conflict prompt, so the decline path was previously untested) plus a NOGIT-sandbox pre-seed exercising real `jq` mutation logic. All new assertions mutation-tested (5 mutations, each caught by exactly its intended check). A sixth residual — a further symmetry refinement to the eval-gate's tempfile message — was identified during Loop 2 planning and deliberately left unshipped; recorded as still-open in the source page. Created: `sources/pr_21-22_loop2-suggestion-tier-followups.md`. Updated: `commands/merge.md` (tempfile case-arm section + `sources`/`tags` frontmatter), `design/trust-floor.md` (new "Failure-reason vocabulary" section extending `identity`/`permission`/`comments` to include `tempfile`, frontmatter, see-also), `design/task-evals-gate.md` (see-also cross-reference only, no mechanism restated — matches the page's own established consolidation discipline), `index.md` (new Sources row, trust-floor Design-list line updated). `skills/sync-docs.md` checked — no update needed, neither PR touches anything it documents.

## [2026-07-06] lint | Post-ingest lint for PRs #21–22 cluster
NEW PAGE ORPHAN CHECK: `sources/pr_21-22_loop2-suggestion-tier-followups.md` — NOT orphan, inbound links verified via grep: `index.md`, `commands/merge.md`, `design/trust-floor.md` ×2, `design/task-evals-gate.md`. Outbound `[[links]]` from the new source page resolve to existing pages (verified via grep of each target's filename): `merge`, `trust-floor`, `task-evals-gate`, `pr_11-14_gate-hardening-followups` — all present, none dangling. CONTRADICTIONS: 0 introduced — `merge.md`'s new tempfile-arm prose and `trust-floor.md`'s new vocabulary section describe the same mechanism consistently (both state the `mktemp`-precedes-`gh`-fetch ordering and the deliberate non-"GitHub fetch failed" wording); `task-evals-gate.md`'s addition is a pointer only, restates nothing. DUPLICATE-CONSOLIDATION CHECK (specific concern for this cluster): the originating task brief requested a *new* `design/trust-model.md` page and a *new* `commands/sync-docs.md` page. Both already exist under different, correct names — `design/trust-floor.md` (authored in the prior wu3 pass specifically as the trust-model consolidating page, explicitly scoped as "a map, not a second spec" with `merge.md` as SSOT) and `skills/sync-docs.md` (also wu3, correctly typed `skill` not `command` since `/sync-docs` is a user-level skill outside the coderails repo, confirmed via `find` returning zero hits). Creating second pages under the requested names would have forked the SSOT the vault's own curator principle (`index.md` "add only what's new and non-obvious") exists to prevent. Extended the existing pages in place instead. ORPHANS: unchanged from the prior pass's set (`skills/handoff.md` + 5 old immutable PR source pages) — 0 new. STALE PAGES: unchanged, same 7 point-in-time records plus `design/hook-exit-codes.md` (flagged prior pass, still accurate, not re-flagged as a new finding). MISSING CONCEPTS: 0 new. MISSING CROSS-REFS: 0 new. DATA GAPS: 0 — this cluster is test-file + single-script content, no new command/hook/skill surface introduced. INBOX: 0 backlog (directory does not exist).

## [2026-07-06] ingest | PRs #23–24 merged: Loop 2's second follow-ups cluster (hook-lib failure observability + repo-keyed loop state)
Two PRs treated as one cluster (wu1 + wu2 of a 3-unit follow-ups loop; wu3's three editorial pages were delivered in a separate pass, already logged above). #23 (`9837c15`): `als_count_invocations` (`hooks/scripts/lib/loop_state_common.sh`) no longer logs a jq-failure reason directly via `als_log` on every call — under `als_stable_invocations`'s retry loop this made a transient failure that recovered on a later attempt indistinguishable from one that never recovered, and a sustained failure logged once per attempt with no final verdict (reproduced empirically: a persistently-malformed transcript produced 2 identical lines at `MAX_ATTEMPTS=3`). Fix: the function now signals `jq_missing`/`jq_parse_error` on **stderr** instead (a global can't cross the retry caller's command-substitution subshell boundary); `als_stable_invocations` captures stderr per attempt via a scratch file (`mktemp`, fails open per the PR #21 tempfile lesson) and logs exactly one summary line after the retry loop exits (`reason=X attempts=N outcome=recovered|exhausted`), never per-attempt. One-shot caller `unregistered_loop_guard.sh`'s `ulg_has_skill_invocation` now explicitly discards the new stderr signal (`2>/dev/null`), preserving its prior silent-on-parse-failure behaviour as an explicit choice. Also fixed a real, separate `als_log` bug: a trailing `2>/dev/null` on `printf` alone does not suppress a redirection-open error (e.g. missing `LOG_FILE` parent) — fixed via a brace-group wrapping the whole statement; accepted tradeoff documented (a missing LOG_FILE parent still silently swallows a caller's own failure-reporting line too, only reachable via a misconfigured `CLAUDE_DISCIPLINE_LOG` override). Verified: gate outcomes proven bit-identical to the pre-change build via a cross-SHA consumer exit-code matrix (3 consumers × 4 fixtures). #24 (`14c7f1c`): `agentic_loop_path.sh`'s slug is now derived from `git -C "$cwd" rev-parse --path-format=absolute --git-common-dir` when cwd is in a git repo, instead of the raw cwd — all worktrees of one repo now share one `progress.json`, closing a real, live-observed bug where a mid-loop `EnterWorktree` hop (now a prescribed part of the shared-checkout workflow per the 2026-07-06 user directive) silently orphaned a loop's state onto a new cwd-keyed path. Critical fix caught by review: a git older than 2.31 doesn't recognise `--path-format`, echoes it back verbatim alongside a RELATIVE path, and exits 0 — trusting any non-empty stdout would have collapsed every old-git-host repo onto one garbage shared slug; fixed by validating the captured value is actually an absolute path before use, falling through to the cwd-slug fallback otherwise (same as an outright git failure). Sanitisation collisions (`"foo/bar"`==`"foo_bar"`) documented as an accepted tradeoff (replacement over fresh-fallback, since session_id is harness-owned not attacker-controlled) at both duplicate transform sites, kept intentionally un-shared (`agentic_loop_path.sh` stays dependency-free), with a pinning test locking the collision itself. Clean-break, no compat flag — old cwd-keyed state dirs left untouched on disk; dashboard's slug-globbing collector verified to still pick up both shapes. **Supersedes [[pr_86_agentic-loop-hardening|PR #86]] point 6's 2026-07-01 rejection** of locking machinery for the single-loop-per-directory race — the workflow shift (prescribed mid-session `EnterWorktree`) changed the calculus; this is a ~15ms keying fix, not the rejected machinery, so the two decisions aren't in tension. **Live proof**: the fix re-keyed this very loop's own `progress.json` mid-flight, from the worktree-cwd slug to the repo-keyed slug, the moment it merged. Created: `sources/pr_23-24_hook-lib-observability-and-repo-keyed-loop-state.md`, `design/agentic-loop-path-keying.md` (new page). Updated: `hooks/loop_state_guard.md` (path-authority section rewritten for the repo-keyed scheme, frontmatter, see-also), `hooks/unregistered_loop_guard.md` (new Known-limitations bullet for the stderr-discard carve-out, frontmatter, see-also), `sources/pr_86_agentic-loop-hardening.md` (superseded-by note appended to point 6, historical text preserved, `last_updated` bumped), `index.md` (new Design-list row, new Sources row).

## [2026-07-06] lint | Post-ingest lint for PRs #23–24 cluster
NEW PAGE ORPHAN CHECK: `sources/pr_23-24_hook-lib-observability-and-repo-keyed-loop-state.md` — NOT orphan, inbound links verified via grep: `index.md`, `hooks/loop_state_guard.md`, `hooks/unregistered_loop_guard.md`, `sources/pr_86_agentic-loop-hardening.md`, `design/agentic-loop-path-keying.md` = 5 inbound. `design/agentic-loop-path-keying.md` (new page) — NOT orphan, inbound links verified via grep: `index.md`, `hooks/loop_state_guard.md`, `sources/pr_86_agentic-loop-hardening.md`, `sources/pr_23-24_hook-lib-observability-and-repo-keyed-loop-state.md` = 4 inbound. Outbound `[[links]]` from both new pages resolve to existing pages (verified via grep of each target's filename): `loop_state_guard`, `unregistered_loop_guard`, `pr_86_agentic-loop-hardening`, `pr_87_agentic-loop-path-session-keying`, `loop-progress-fields`, `using-git-worktrees`, `pr_21-22_loop2-suggestion-tier-followups` — all present, none dangling. CONTRADICTION CHECK: `sources/pr_86_agentic-loop-hardening.md` point 6's original 2026-07-01 text is preserved verbatim above the new superseded-by note (not deleted or rewritten) — the two coexist as a historical-decision-then-reversal record, matching the wiki's established pattern for superseded content (e.g. [[pr_87_agentic-loop-path-session-keying]] superseding part of #86's point 6 already, now further extended); no live contradiction introduced. `hooks/loop_state_guard.md`'s path-authority section and `design/agentic-loop-path-keying.md` describe the same mechanism consistently (both state the git-common-dir derivation, the absolute-path guard, and the cwd fallback conditions identically). `hooks/unregistered_loop_guard.md`'s new bullet and PR #23's stderr-discard change agree (both state the hook explicitly discards stderr, preserving prior silent behaviour). ORPHANS: unchanged from the prior pass's set (`skills/handoff.md` + 5 old immutable PR source pages) — 0 new. STALE PAGES: unchanged, same set as the prior pass. MISSING CONCEPTS: 0 new — this cluster's repo-keying mechanism was previously only documented inline inside `hooks/loop_state_guard.md`'s header-comment citation; it now has its own dedicated design page, closing a gap before it was ever separately flagged. MISSING CROSS-REFS: 0 new. DATA GAPS: 0 — no new command/hook/skill surface introduced (both PRs touch existing hook-lib internals only). INBOX: 0 backlog (directory does not exist).

## [2026-07-06] ingest | PR #25 merged: observability dashboard (sub-project 1 of 5, agentic-OS evolution)
New `coderails:dashboard` skill: a Next.js/React Three Fiber web HUD plus a native Obsidian command-centre plugin, both reading state the kernel already produces (session dirs, agentic-loop `progress.json` via the existing `agentic_loop_path.sh` SSOT, `gh`-polled PR artifact markers parsed with the same marker grammar as `review-artifact.sh`/`eval-artifact.sh`, hook logs, wiki/memory mtimes) — no new services, no telemetry leaving the machine. Seven panels (SYSTEM VITALS, DIRECTIVES, DOCUMENTS/MEMORY.TRAIL, COMMAND DECK, PR GATES, bottom-centre hero, reserved assistant-agent slot). One `POST /api/run` endpoint, token- and Origin-guarded, spawns headless `claude -p` runs through a single profile→flag mapping (`buildArgv` in `src/lib/argv.ts`: read-only/standard/bypass) — the Obsidian plugin reuses it rather than re-implementing. Flag-smuggling closed by two independent layers found during review: a leading-dash `input` value is rejected outright (a `claude -p "--version"` empirical test confirmed the CLI really does parse a leading-dash argument as a flag, not literal text) AND a literal `--` end-of-options sentinel is always inserted before `input` regardless — neither layer alone was judged sufficient. The Obsidian plugin's `agentic-os` code-block processor writes intent files to `~/.claude/coderails-dashboard/queue/`, a deliberately frozen seam for the not-yet-built routines sub-project (2 of 5), with an interim direct-exec path meanwhile; ships a committed reproducible `dist/main.js` build. Process record: 16 SDD tasks each independently reviewed; 5 fix loops (lock TOCTOU Critical, marker grammar drift, `hooksFired` today-scoping, single-SSE-provider requirement, flag-smuggling scanner High). The frozen Tier-2 eval suite (10 evals, required since the PR is both ≥3 work-units and touches an outward/irreversible surface) caught **two production bugs every review round missed**: the launch script reported false-success against a squatted port, and a statically-prerendered page baked an empty button config into the HTML (fixed by forcing the route dynamic) — the first concrete demonstration in this wiki of the task-evals gate catching a real bug in production rather than only exercising its own mechanics. Eval artifact GO 10/10 and review artifact both posted SHA-bound on the PR; `/coderails:merge`'s two gates each consumed their respective artifact. The loop this PR ran under also registered its own loop-scope `evals.json` (GO 4/4, Phase 2.7c). Created: `skills/dashboard.md` (new skill page), `sources/pr_25_observability-dashboard.md`. Updated: `index.md` (new skills-table row, new plugin-skills-list entry, new sources row), `skills/agentic-loop.md` (see-also cross-link), `skills/task-evals.md` (see-also cross-link), `hooks/unregistered_loop_guard.md` (see-also cross-link).

## [2026-07-06] lint | Periodic full-vault health check (127 pages: 12 commands, 12 hooks, 29 skills, 15 design, 3 investigations, 47 sources, 6 templates + index.md/log.md)
ORPHANS: 0 true orphans (verified programmatically — every content page has >=1 inbound `[[link]]`, crediting `index.md`'s catalog entries as valid inbound references; the prior session's naive scan under-counted 6 pages, `skills/handoff.md` + 5 PR sources, as orphans because it excluded index.md's own outbound links from the inbound tally — corrected here). BROKEN LINKS: 0 real. All flagged occurrences are false positives from the `[[...]]` link regex matching non-wiki-link syntax: bash POSIX character classes `[[:space:]]` in prose/code across `hooks/enforce_pr_workflow.md`, `design/skills-hooks-seam.md`, `sources/pr_42_skills-hooks-seam.md`, `sources/pr_46_gate-git-push-on-main.md`; bash `[[ -n ... ]]` test syntax in `investigations/install-bash32-bad-substitution_2026-06-01.md` and `sources/pr_11-14_gate-hardening-followups.md`; and illustrative placeholder syntax `[[link]]`/`[[page_name]]` inside `skills/wiki-lint.md`/`skills/wiki-query.md`'s own template prose (these are the skill files documenting the `[[wiki-link]]` convention itself). Same benign set already on record in prior lint entries (e.g. 2026-06-29 PR #77 entry) — no new false positives, no real dangling links. STALE PAGES: only `sources/` (immutable PR records) and `investigations/` (explicitly point-in-time, may be superseded per schema) pages exceed the 30-day `stale_days` threshold — 7 pages, oldest 37 days (`investigations/install-cache-trap_2026-05-30.md`) — all expected by design, not gaps. One evergreen `design/` page is stale by date, `design/hook-exit-codes.md` (36 days, last_updated 2026-05-31): read in full and cross-checked against the current hook event map in AGENTS.md — content (exit-2 semantics per event, the two-block-mechanism table, the `discipline_catchup.sh` UserPromptSubmit rationale) is still accurate against all hooks added since, including the three newest (`loop_state_guard`, `loop_stall_guard`, `unregistered_loop_guard`) which follow the same documented pattern. No update needed; date reflects "last touched," not "last valid." CONTRADICTIONS: 0 active `⚠️ CONTRADICTION` flags on any content page (grepped full vault). MISSING CONCEPTS: 0 — spot-checked recurring terms ("enforcement ceiling", "freeze-before-build", "anti-pollution") against the two same-day consolidation passes that already closed `design/loop-progress-fields.md`, `design/trust-floor.md`, `skills/sync-docs.md`; each correctly redirects to an existing canonical page. MISSING CROSS-REFS: 0 — the nine 2026-07-05/06 design pages (`enforcement-model`, `trust-floor`, `review-artifact-seam`, `task-evals-gate`, `spec-plan-progress-artifact-chain`, `skills-hooks-seam`, `discipline-loop`, `loop-progress-fields`, `agentic-loop-path-keying`) each carry an explicit "See also" section linking every sibling concept named in body text. DATA GAPS: 0 — direct diff of plugin source against wiki pages: `hooks/scripts/*.sh` (12) vs `hooks/*.md` (12) exact match; `commands/*.md` (12) vs `commands/*.md` (12) exact match; `skills/*/SKILL.md` (29 dirs) vs `skills/*.md` (30 pages) — the one extra, `skills/sync-docs.md`, correctly documents a user-level skill living outside the coderails plugin repo (verified against filesystem: no `coderails/skills/sync-docs/` directory exists). INBOX: no `inbox/` directory exists in this personal (non-worktree) wiki — 0 backlog. No fixes required this pass; wiki is in clean state.

## [2026-07-06] lint | Full periodic health-check: 0 contradictions, 0 orphans (methodology-corrected: earlier apparent orphans credit inbound links from index.md), 0 broken links (known [[:space:]] regex + template-placeholder false positives only), 0 missing concepts, 0 missing cross-refs (9 recent design pages fully cross-linked), 0 data gaps (12 hooks / 12 commands / 29 skills exact 1:1 source↔wiki coverage; `skills/sync-docs.md` correctly documents an external user-level skill), 1 stale evergreen page: `design/hook-exit-codes.md` (36 days, last_updated 2026-05-31) — flagged for a refresh pass, not fixed this session (unrelated to current PR work). 6 stale sources/investigations pages (>30 days) are expected by schema (immutable/point-in-time). No inbox directory in use.

## [2026-07-06] lint | Follow-up fix: 3 missing cross-references to design/hook-exit-codes.md
The periodic health-check pass above flagged, but didn't confirm, whether the three newest loop hooks should backlink `design/hook-exit-codes.md`. Confirmed by grep: none of `hooks/loop_state_guard.md`, `hooks/loop_stall_guard.md`, `hooks/unregistered_loop_guard.md` linked it despite each page's block/nudge mechanism being exactly what that design page's exit-code table documents. Fixed: added a `[[hook-exit-codes]]` line to each page's `## See also` section (`loop_state_guard.md` and `loop_stall_guard.md` — "blocks via plain exit 2 on Stop"; `unregistered_loop_guard.md` — "nudges via exit-0 additionalContext rather than exit 2," the deliberate non-blocking contrast the design page explains). Added the reverse links to `design/hook-exit-codes.md`'s own `## Related` section and bumped its `last_updated` to 2026-07-06 (content itself was unchanged — still accurate, per the prior pass's review — only the cross-reference set changed). This closes the one open thread from the prior lint pass; 0 further findings.

## [2026-07-07] ingest | workflow-audit skill (PRs #27, #30, #37, #39)

Ingested the workflow-audit cluster as one source page (`sources/pr_27-39_workflow-audit-skill.md`) covering scan_transcripts.sh, cluster_ngrams.sh, judge-contract.md, SKILL.md orchestration, and the e2e sentinel privacy test. New skill page `skills/workflow-audit.md`. New investigation `investigations/claude-code-transcript-schema_2026-07-07.md` — first wiki coverage of Claude Code transcript JSONL record-type schema (13 types catalogued; only user/assistant/system carry conversation; last-prompt-is-a-resume-cache mtime trap; bare-string content gotcha; non-string input type-guarding). Cross-linked writing-skills, skill-testing-state_2026-06-26, and dashboard. index.md updated (Skills table, Investigations, Sources).

## [2026-07-07] query-file-back | queue contract cross-PR audit — workflow-audit, dashboard, send-gate, PR #31

Answered a query spanning workflow-audit, "the dashboard approval pipeline / queue contract (PR #31)", and send-gate integration. Found: PR #31 ("feat/wu5 approve button", merged `6fa5e34`) shipped the Approve/Deny button + `collectQueue`/`resolveQueueEntry`/`/api/queue` deferred by the normative spec `docs/coderails/specs/2026-07-06-assistant-link-panel-design.md` — but had **no wiki source page** until now. Confirmed the `QueueFileEntry` shape is shared, verbatim-matching, between two independent producers: assistant-agent's send-gate (`gate/surfaces/queue.ts`, documented in the separate assistant-agent-wiki's `capabilities/send-gate.md`) and workflow-audit's not-yet-built queue-mode proposal path (WU1–WU3, still pending as of this query). workflow-audit's shipped `SKILL.md` still uses synchronous `AskUserQuestion` only. Also found PR #36 (routines/wu1-dashboard-lib, sub-project 2) has no wiki page. Filed as `investigations/queue-contract-cross-pr-audit_2026-07-07.md`; index.md updated (Investigations section).

## [2026-07-07] ingest | ASSISTANT.LINK send-gate cluster (coderails PRs #28, #31 + assistant-agent PRs #3, #4, #5/wu3)
Ingested sub-project 4's send-gate cluster as one pass. New source pages `sources/pr_28_assistant-link-queue-contract-and-panel-spec.md` (the normative `QueueFileEntry` contract + ASSISTANT.LINK panel spec, doc-only) and `sources/pr_31_assistant-link-approve-button.md` (the dashboard-side implementation, including a Critical path-traversal fix caught and closed pre-merge — a `hash` parameter used unvalidated in a file-path join, closed with a strict `/^[0-9a-f]{64}$/` check at two layers). New design page `design/assistant-link-send-gate-architecture.md` covering the cluster theme: the send-gate architecture decision (host-process SDK `PreToolUse` hook, chosen because a bash hook would be forgeable by the agent it constrains, combined with the gate's own fail-closed self-wrap because the SDK is fail-open on hook throw/timeout — neither fact alone is sufficient), the `~/.claude/coderails-dashboard/queue/<hash>.json` seam connecting assistant-agent's gate to the dashboard's ASSISTANT.LINK panel, and the stale-plugin-cache finding (assistant-agent's installed coderails plugin sat at v1.0.0 for ~2 weeks, missing several hooks source already had; self-healed via autoUpdate 2026-07-07; detection now lives permanently in assistant-agent's own test suite via dynamic path resolution rather than a hardcoded version string). Updated `skills/dashboard.md` (the ASSISTANT.LINK "reserved slot" language is now a real panel) and `design/install-and-cache-trap.md` (new section: the marketplace-consumer analogue of the repo-vs-cache trap, distinct from that page's own local-directory-checkout scope) and `design/enforcement-model.md` (cross-reference: the host-process-hook precedent applied outside coderails itself). `index.md` updated (Design, Sources). Lint pass on this cluster only (see next entry) — 0 findings introduced by this ingest.

## [2026-07-07] lint | Cluster-scoped pass on the ASSISTANT.LINK ingest above
Checked only what this cluster touched (per-cluster scope, not a full-vault pass — see `sources/pr_27-39_workflow-audit-skill.md`'s entry above, itself mid-flight in this same session's shared checkout, for the most recent full context). New pages (`assistant-link-send-gate-architecture.md`, both PR sources) each carry frontmatter (`sources`, `tags`, `created`/`last_updated` both 2026-07-07) and a `## See also`/`## Wiki pages updated` section cross-linking every sibling page named in body text — 0 orphans among the 3 new pages (each has >=1 inbound link from `index.md` plus at least one sibling page). 0 broken `[[link]]` targets (every `[[...]]` in the 3 new/2 updated pages resolves to an existing page or another page created in this same pass). 0 contradictions — cross-checked the new design page's SDK fail-open claim and the `QueueFileEntry` shape against the two PR source pages; consistent. `skills/dashboard.md` and `design/install-and-cache-trap.md`/`enforcement-model.md` `last_updated` bumped to 2026-07-07 to match the actual edit date. Not checked (out of this cluster's scope, left for whoever owns it): the pre-existing stale `design/hook-exit-codes.md` flag from the 2026-07-06 health-check passes above, and any findings specific to the concurrently in-flight `workflow-audit` ingest.

## [2026-07-07] ingest | Addendum: loop-state re-keying gap from the same 1.0.0→1.1.0 plugin update
Orchestrator-verified finding, folded into the ASSISTANT.LINK cluster's existing pages rather than a new page (small, single-mechanism addition). The 1.0.0 cache predates [[agentic-loop-path-keying|PR #24]] entirely (verified: no `agentic_loop_path.sh` file exists in that cache directory at all — the older cwd-only slug scheme was all that version had), so the same overnight auto-update that closed the stale-hooks gap ([[assistant-link-send-gate-architecture]]) also silently switched any pre-existing session's `progress.json` path from the legacy cwd-slug to the git-common-dir-keyed slug PR #24 introduced. Unlike the same-version mid-loop re-key case [[agentic-loop-path-keying]] already documented, a plugin-version upgrade gives the active session no advance signal — a maintained loop was reported unregistered by the newer guards post-update. Workaround applied live: a symlink from the new slug to the old state directory. Added as a new caveat to `design/agentic-loop-path-keying.md` (candidate fixes: legacy-slug fallback, or a plugin-update migration step — filed for the owner, not actioned), with matching cross-references added to `design/assistant-link-send-gate-architecture.md` and `design/install-and-cache-trap.md`. This is a migration gap, not a bug fix — nothing in this pass changes `agentic_loop_path.sh`'s behaviour.

## [2026-07-07] ingest | workflow-audit × dashboard queue-mode integration (PRs #43/#44/#46, merged f1212a31/0468bc90/675e63bc)

Ingested the queue-mode cluster this session's own team-lead flagged as a loop-boundary wiki pass, concurrently with the ASSISTANT.LINK send-gate cluster above (same shared checkout, different session). This closes the exact gap `investigations/queue-contract-cross-pr-audit_2026-07-07.md` had surfaced earlier the same day ("workflow-audit's queue-mode approval path — WU1–WU3, still pending, not yet PR'd") — and, as of the merge reconciling this entry with the concurrent session's work, that investigation's separate Gap #1 (PR #31 had no source page) is also closed by the other session's `pr_31_assistant-link-approve-button.md`. WU1 (PR #43): `write_queue_entry.sh` writes one `QueueFileEntry` per judge `verdict:"propose"` output, reusing the existing envelope verbatim (`toolName: "workflow-audit:propose-skill"`, six D2-whitelisted fields, sha256 hash, `0700`/`0600` perms) — a third producer joining send-gate and PR #31's generic path. WU2 (PR #44): `AssistantLinkPanel.tsx` gains a type-guarded readable render branch for this `toolName`, falling back to the prior opaque render for every other producer; Approve/Deny buttons and `/api/queue` untouched. WU3 (PR #46): the consumption-seam contract for the not-yet-built routines runner — filter on `status:"approved"`, mandatory hash re-validation before acting, distinct logged rejections for hash-mismatch vs unparseable entries, only then drives `writing-skills`'s full PR-gated create step. Explicit "Honesty requirement" section pinned: approving today only flips `status`; no skill/branch/PR is created until the runner exists — a deliberate seam-only decision, not an oversight. Created: `sources/pr_43-44-46_workflow-audit-queue-seam.md` (disambiguated from unrelated pre-existing `pr_43_rough-edges.md`/`pr_44_no-edit-plugin-source.md`/`pr_46_gate-git-push-on-main.md`, same PR numbers, different feature, no relation). Updated: `skills/workflow-audit.md` (new queue-mode section + Related links), `skills/dashboard.md` (new AssistantLinkPanel per-producer-rendering section + Related links, merged with the concurrent session's own ASSISTANT.LINK/panel edits to the same file), `investigations/queue-contract-cross-pr-audit_2026-07-07.md` (closure banner, both Gap #1 and Gap #2 struck through and marked closed, renumbered remaining gaps, "two producers" language updated to three, Related section updated), `index.md` (new source entry, investigation summary updated to note both closures, merge-reconciled with the concurrent session's own `pr_28`/`pr_31` entries).

## [2026-07-07] lint | Post-ingest lint for the workflow-audit queue-seam cluster

New page `sources/pr_43-44-46_workflow-audit-queue-seam.md`: NOT orphan — inbound from `index.md` (sources list), `log.md` (this entry), `skills/workflow-audit.md` (frontmatter sources + Related), `skills/dashboard.md` (frontmatter sources + Related), `investigations/queue-contract-cross-pr-audit_2026-07-07.md` (closure banner + gap note + Related) = 5 inbound (verified via grep). Outbound links `[[pr_43_rough-edges]]`, `[[pr_44_no-edit-plugin-source]]`, `[[pr_46_gate-git-push-on-main]]`, `[[workflow-audit]]`, `[[dashboard]]`, `[[queue-contract-cross-pr-audit_2026-07-07]]` all resolve (verified via `ls`/`find`). Schema conformance: type:source, created/last_updated 2026-07-07, sources:[], tags non-empty, confidence labels present on every substantive claim in the Caveats section (verified/none guessed). No contradictions introduced: the closure banner and gap strikethrough on the investigation page are additive corrections of a since-resolved "pending" status, not a reversal of any still-true claim (the two-vs-three-producer count, the send-gate/PR#31 findings, and the PR#36/routines-runner gap all remain accurate as originally written). The PR-number collision with three unrelated pre-existing source pages (#43/#44/#46) was checked before naming this page and is called out explicitly in both the new source page and this log entry to prevent future conflation. Merge with the concurrently-pushed ASSISTANT.LINK send-gate cluster (log entries above) reconciled cleanly: only `skills/dashboard.md`'s `## See also` section and `index.md`'s source list needed manual reconciliation (both append-only, no overlapping claims); `investigations/queue-contract-cross-pr-audit_2026-07-07.md` merged with no conflict markers, so its Gap #1 line was updated separately in this same pass once the concurrent session's `pr_31` page was visible. 0 new orphans, 0 new dangling links, 0 new stale pages (all touched pages carry `last_updated: 2026-07-07`). Sources directory now 50 files total (verified via `ls sources/*.md | wc -l`) — prior log entries' "Sources N→N+1" phrasing undercounts the true total and is not continued here.

## [2026-07-07] ingest | approvals-dir move (coderails PR #62 + assistant-agent PR #10)

Orchestrator cross-repo verification pass: both PRs' "merged" claims re-derived from `origin/main` on each repo rather than taken on report (git show, git grep, full test suites re-checked green at the merge SHAs), then ingested. The move: the send-gate's own approval files (`gate/surfaces/queue.ts`'s `DEFAULT_QUEUE_DIR`, and the dashboard's matching `api/events`/`api/queue` route constants) moved from `~/.claude/coderails-dashboard/queue/` to a new sibling `approvals/` directory. Root cause captured for the first time in the wiki: `queue/` had grown two other consumers — the routines runner sweep and workflow-audit's `propose-skill` proposal writer ([[pr_43-44-46_workflow-audit-queue-seam]]) — both of which glob every `.json` file in the directory and quarantine/reject anything that isn't their own expected shape; a `QueueFileEntry` approval file looked like malformed input to both, so the send-gate's approval traffic needed its own directory. Clean-break, no dual-dir fallback, nothing to migrate (no prior producer ever wrote approval-shaped files into `queue/`). Created `sources/pr_62_10_approvals-dir-move.md`. Updated: `design/assistant-link-send-gate-architecture.md` (queue-seam section's path corrected to `approvals/` with a moved-from note, frontmatter sources+tags updated, See-also link added), `investigations/queue-contract-cross-pr-audit_2026-07-07.md` (this page is an explicitly frozen point-in-time record per its own existing convention — left the body prose as-written and added a second banner, matching the style of its first closure banner, distinguishing which of its `queue/` mentions describe the now-moved send-gate path vs. the still-accurate workflow-audit path), `sources/pr_43-44-46_workflow-audit-queue-seam.md` (one-sentence parenthetical added at its own `queue/` mention, clarifying that path is unaffected by this move), `index.md` (design-page summary path corrected + moved-from note; new source-list entry for this PR pair). Historical PR pages describing what was specified/shipped *at the time* (`pr_28_assistant-link-queue-contract-and-panel-spec.md`'s `queue/` path, `pr_31`'s) were deliberately left unedited — they are accurate records of what those PRs actually did, not descriptions of current state.

Also flagged, not fixed (outside this ingest's scope — a doc-only prose line in a different repo, filed as a residual in the new source page's own "Residual doc drift" section): assistant-agent's `AGENTS.md` line 96 still names the old `queue/` path and was not updated by PR #10; that PR's diff was exactly the one-line `DEFAULT_QUEUE_DIR` constant. The frozen eval artifact `.claude/evals-wu2-pr.json` also names the old path but is an intentional point-in-time snapshot, not live documentation — not drift, no action needed.

## [2026-07-07] lint | Cluster-scoped pass on the approvals-dir move ingest above

Checked only what this cluster touched (per-cluster scope). New page `sources/pr_62_10_approvals-dir-move.md`: NOT orphan — inbound from `index.md` (new source entry), `log.md` (this entry + ingest entry above), `design/assistant-link-send-gate-architecture.md` (frontmatter sources + See-also), `investigations/queue-contract-cross-pr-audit_2026-07-07.md` (new banner), `sources/pr_43-44-46_workflow-audit-queue-seam.md` (parenthetical) = 5 inbound (verified via grep). Outbound links `[[pr_28_assistant-link-queue-contract-and-panel-spec]]` (not linked — checked, this page correctly has no direct link to it since #28 predates and isn't touched by the move), `[[assistant-link-send-gate-architecture]]`, `[[pr_43-44-46_workflow-audit-queue-seam]]`, `[[queue-contract-cross-pr-audit_2026-07-07]]`, `[[dashboard]]` all resolve (verified via `ls`). Frontmatter: type:source, created/last_updated 2026-07-07, sources:[], tags non-empty. No contradictions introduced: this ingest corrects a stale path in pages describing *current* architecture while explicitly leaving frozen/historical pages (`queue-contract-cross-pr-audit`'s body, `pr_28`, `pr_31`) as accurate-for-their-time records — verified each edited page's surrounding claims (writer/reader split, `QueueFileEntry` shape, path-traversal fix, honesty-requirement seam-only decision) remain true and untouched by this pass. 0 new orphans, 0 new dangling links. `last_updated` bumped only on pages whose body content changed (`assistant-link-send-gate-architecture.md`) or that gained a new banner/entry (`queue-contract-cross-pr-audit_2026-07-07.md` frontmatter already 2026-07-07, unchanged; `index.md` has no per-entry frontmatter to bump).

## [2026-07-07] ingest | PR 36/41/33/53/65 — verified routines (sub-project 2 of 5)

Cluster source page `sources/pr_36-41-33-53-65_verified-routines.md` (created by a prior session, verified pushed) documents the intent-queue/runner contract, the routines scheduling convention, and one shipped routine's skill. This session added the `index.md` entry and four new pages, closing the "routines sub-project 2 has no wiki page" gap [[dashboard]] and [[queue-contract-cross-pr-audit_2026-07-07]] both flagged. New pages, each cross-verified against live repo source (`skills/dashboard/lib/`, `skills/dashboard/runner/src/`, `skills/memory-consolidation/SKILL.md`, `docs/routines.md`) rather than taken solely from the source page's summary: `design/intent-queue-runner-contract.md` (the `Intent` schema, queue/processing/archive/quarantine lifecycle, explicit note distinguishing this from the sibling `approvals/` dir PR #62 moved), `design/dashboard-runner.md` (sweep mechanics, per-intent failure boundary, artifact-gate predicate kinds, escalation taxonomy table, exit-code contract 0/1/2), `design/routines.md` (cadence due-ness thresholds, `RoutineDef` field contract with worked example, launchd wiring, the empirically-verified `PreToolUse`-does-not-fire-under-`claude -p` security finding), `skills/memory-consolidation.md` (skill phases, the unconditional-report-write mechanism that makes it artifact-gateable). Updated existing pages: `skills/dashboard.md` (Obsidian command-centre section — routines sub-project now shipped but plugin's interim direct-exec path still not removed, See Also link), `design/assistant-link-send-gate-architecture.md` (routine-runs panel-slot dependency closed, though panel-side render work itself not confirmed done), `investigations/queue-contract-cross-pr-audit_2026-07-07.md` (new UPDATE banner + Gap #3 struck through as closed + repo/wiki-mismatch table row updated), all three frontmatter `sources:` lists gained the new source page.

## [2026-07-07] lint | Scoped pass on the routines-cluster ingest above

Checked what this cluster touched. All 5 new/updated content pages (4 new + source page) verified NOT orphans: each has 5-7 inbound links (index.md, log.md, and cross-references among the cluster's own pages plus `skills/dashboard.md`, `design/assistant-link-send-gate-architecture.md`, `investigations/queue-contract-cross-pr-audit_2026-07-07.md`) — confirmed via grep across the vault. Outbound `[[links]]` from the 4 new pages: all resolve to an existing page file, with one flagged-then-confirmed-benign exception — `design/routines.md` contains literal prose `` [[wiki-links]] `` describing the schema convention itself (not a navigational link), the same class of false positive already documented repeatedly in this log for `wiki-lint.md`/`wiki-query.md`; no fix needed. Frontmatter: all 4 new pages conform (title/type/created/last_updated/sources/tags all present and non-empty, dates 2026-07-07, confidence labels present in every page: 8/8/7/3 `(verified)`/`(inferred)` tags respectively). Full-vault broken-link scan (Python-free `comm` diff of all `[[link]]` targets against all page basenames) surfaced only pre-existing, previously-documented benign false positives — bash regex/conditional literals in code fences, schema meta-text (`wiki-links`/`link`/`page_name`), historical log prose (`pr_73`, `workflow-init`, etc.), and `capabilities/send-gate` which is a correctly-non-cross-linkable reference to the separate assistant-agent-wiki vault (explicitly parenthesised as such in the source text). Nothing new introduced. 0 contradictions: verified this cluster's claims (queue lifecycle, escalation taxonomy, cadence thresholds, the `PreToolUse`-hooks-don't-fire-under-`-p` finding) against live repo source directly (not just the source page's summary) and found no divergence. 0 stale pages among touched pages (all `last_updated` 2026-07-07). Did not audit the rest of the vault beyond touched/cross-referenced pages (per-cluster scope, consistent with this log's established convention).

## [2026-07-07] ingest | PRs #55/#59/#60/#64/#66/#67 — Approve-click builder pipeline (loop 2)

Ingested the six-PR "Approve-click → skill-creator builder pipeline" cluster (L2-WU1/WU2/WU2-followup/WU4/WU3/WU5), which closes the gap the earlier same-day cluster ([[pr_43-44-46_workflow-audit-queue-seam]]) explicitly named as pending: clicking Approve on a `workflow-audit:propose-skill` queue entry now spawns a wrapper-owned (`run-builder.sh`) build that authors a skill via skill-creator/writing-skills and opens a PR, rather than only flipping the entry's `status` field. Verified against all six PRs' `gh pr view` bodies + file lists directly, not summarised from the team-lead's brief alone. Key mechanisms recorded: the claim/spawn seam (`claimAndSpawnBuild`, EEXIST-safe hash claim), the wrapper's artifact-derived terminal state with a guaranteed-terminal `EXIT` trap, the injection-fenced prompt template (six judge-authored fields in one fence), the empirically-verified `--disallowedTools` merge denial surviving `--dangerously-skip-permissions`, a same-day path-bug self-correction (`__dirname`-anchored wrapper resolution replacing a cwd-relative one), a real XSS fix (`safePrUrl`, https-only `<a href>`), and the L2-WU5 `queue/`→`approvals/` path correction needed after concurrent PR #62 ([[pr_62_10_approvals-dir-move]]) split those directories. Created `sources/pr_55-60-64-66-67_approve-build-runner.md`. Updated: `skills/workflow-audit.md` (queue-mode section's "status flip only" claim corrected + frontmatter sources/tags), `skills/dashboard.md` (AssistantLinkPanel section extended with build-state visibility + XSS fix + frontmatter sources/tags), `sources/pr_43-44-46_workflow-audit-queue-seam.md` (superseded-claim banner added above its "Honesty requirement" section, body left as-written per the vault's established historical-record convention, one caveat bullet added), `index.md` (new source-list entry). `design/assistant-link-send-gate-architecture.md` checked but not edited — its `approvals/` path description (from PR #62) is unaffected by and already consistent with this cluster's WU5 fix.

Noted, not separately actioned: E5 (one real, manual build run end-to-end through the pipeline) was still pending as of this ingest per the team-lead's brief — recorded as a caveat on the new source page and in `workflow-audit.md`'s updated section, not treated as a blocker to ingesting the six merged PRs themselves.

## [2026-07-07] lint | Cluster-scoped pass on the approve-build-runner ingest above

Checked only what this cluster touched. New page `sources/pr_55-60-64-66-67_approve-build-runner.md`: NOT orphan — inbound from `index.md` (new source entry), `log.md` (this entry + ingest entry above), `skills/workflow-audit.md` (frontmatter sources + body reference + See-also link), `skills/dashboard.md` (frontmatter sources + body reference + See-also link), `sources/pr_43-44-46_workflow-audit-queue-seam.md` (banner + caveat bullet) = 6 inbound (verified via grep). Outbound `[[links]]` from the new page: `[[pr_43-44-46_workflow-audit-queue-seam]]`, `[[pr_62_10_approvals-dir-move]]`, `[[workflow-audit]]`, `[[dashboard]]`, `[[assistant-link-send-gate-architecture]]` all resolve (verified via `ls`/file existence checks). Frontmatter: type:source, created/last_updated both 2026-07-07, sources:[] (consistent with other source pages in this vault, which leave this field empty and rely on body cross-references instead), tags non-empty. Confidence labels present: 4 explicit `(verified)`/`(inferred)` tags in the Caveats section, consistent with this vault's per-page labelling convention for non-trivial or time-bound assertions (the body's architecture/decisions sections are drawn directly from `gh pr view` output, treated as established fact rather than separately re-labelled per sentence, matching the density convention of sibling source pages e.g. `pr_36-41-33-53-65_verified-routines.md`). No contradictions introduced: the superseded-claim banner on `pr_43-44-46_workflow-audit-queue-seam.md` explicitly reconciles the one direct conflict this cluster creates (the old page's "status flip only" claim vs. the new page's "triggers a real build" claim) rather than leaving both as unflagged, silently-conflicting assertions. 0 new orphans, 0 new dangling links found among the touched/created pages. Did not audit the rest of the vault beyond touched/cross-referenced pages (per-cluster scope, consistent with this log's established convention).

## [2026-07-07] ingest | PR #70 + #71 — dashboard input delivery fix + voice announcements

Ingested a two-PR cluster (both merged 2026-07-07): PR #70 fixes a real, long-standing bug in the dashboard's button-input delivery (`buildArgv()` split `command` and `input` into two argv positionals, but the `claude` CLI only consumes one, so **every `inputAllowed` button silently dropped user input since the button model's inception** — caught by an intake smoke test, not by any prior argv-shape review round or unit test) plus a new "ask" button pattern it enabled. PR #71 adds a new observe-only Stop hook, `voice_announce.sh`, speaking loop lifecycle events (complete/waiting/stopped/stall) via macOS `say`. Verified both PRs directly against their merge-commit diffs (`git show e87877c --stat`, `git diff ebfee7b 8ddf64c -- skills/dashboard/app/src/lib/argv.ts`, `git show 18094dd:hooks/scripts/voice_announce.sh`) rather than relying solely on the team-lead's brief. Created `sources/pr_70-71_2026-07-07_dashboard-input-fix-and-voice-announcements.md` and `hooks/voice_announce.md`. Updated: `skills/dashboard.md` (argv/button-model section corrected — the old two-token model was documented as current fact and is now marked fixed; new ask-button pattern documented; frontmatter sources/tags), `design/discipline-loop.md` (Stop hook composition 5→6, `voice_announce` added first in the ordering list and cross-references, frontmatter sources + `last_updated`), `index.md` (new hook table row + Stop-hook-order sentence + new source-list entry).

**Real PR-number collision found and flagged, not silently worked around:** this repo's actual PR #69/#70 (`gh pr view`, merged 2026-07-07) are unrelated to two pre-existing wiki pages, `sources/pr_69_no-edit-message-worktree.md` and `sources/pr_70_gate-settings-json-edits.md` (merged 2026-06-29). Root cause: the 2026-07-05 repo delete-and-recreate ([[history-identity-scrub]]) reset GitHub's PR-number sequence, so old and new PRs of the same number now coexist across the recreation boundary with no repo-side disambiguation. Both old pages remain individually accurate — they document real, still-true work — but a bare `pr_<N>` citation is no longer a unique key. Documented the mechanism and the risk in a new section of `design/repo-hosting.md` (frontmatter sources + `last_updated` bumped), and used a date-qualified filename for this ingest's own source page (`pr_70-71_2026-07-07_...`) rather than the collision-prone bare `pr_70_...` form. Left the two older pages' filenames unchanged (out of this cluster's scope) but flagged renaming them as a follow-up in both `repo-hosting.md` and the new source page's banner.

## [2026-07-07] lint | Cluster-scoped pass on the dashboard-input-fix + voice-announcements ingest above

Checked only what this cluster touched (per-cluster scope, consistent with this log's established convention). New pages verified NOT orphans via grep: `sources/pr_70-71_2026-07-07_dashboard-input-fix-and-voice-announcements.md` has inbound links from `index.md` (new source entry), `log.md` (this entry + the ingest entry above), `skills/dashboard.md` (frontmatter sources + body reference + See-also), `design/discipline-loop.md` (frontmatter sources + ordering-list reference), `design/repo-hosting.md` (frontmatter sources + body reference + Related section), `hooks/voice_announce.md` (frontmatter sources + See-also) = 6 inbound. `hooks/voice_announce.md` has inbound from `index.md` (new hook table row), `log.md` (this entry), `design/discipline-loop.md` (ordering list + See-also section, two links), `skills/dashboard.md` (See-also) = 4 inbound. Outbound `[[links]]` from both new pages resolve: `[[repo-hosting]]`, `[[dashboard]]`, `[[voice_announce]]`, `[[loop_stall_guard]]`, `[[loop_state_guard]]`, `[[discipline-loop]]`, `[[hook-exit-codes]]`, `[[pr_76_harden-hook-stdin-read]]`, `[[destructive_bash_gate]]`, `[[push]]` all confirmed to exist via `ls` against the vault (one, `[[push]]`, is referenced only as a parenthetical "see follow-up" in the source page's landing-note paragraph, and the commands/push.md page exists). Frontmatter on both new pages: title/type/created/last_updated/sources/tags all present and non-empty, dates 2026-07-07. Confidence labels: the new source page and hook page both use the vault's established density convention (verified-tagged for the empirically re-derived facts — argv diff, merge SHAs, PR metadata — with the surrounding architecture narrative treated as established fact drawn directly from source, matching sibling pages like `pr_36-41-33-53-65_verified-routines.md`). No contradictions introduced beyond the one intentionally surfaced (the PR-number collision, reconciled with an explicit banner + `repo-hosting.md` section rather than left as a silent ambiguity). 0 new unintentional orphans, 0 new dangling links among touched/created pages. Did not re-audit the rest of the vault beyond touched/cross-referenced pages.

## [2026-07-07] lint | Re-verification pass, same cluster (dashboard-input-fix + voice-announcements)

Independent re-run of the scoped lint above, from a fresh session, prompted by an argument request to lint PR #70/#71. Found the prior lint entry already covered this cluster completely — re-derived its inbound-link counts (6 for the source page, 5 for `hooks/voice_announce.md` via a fresh grep, one more than the prior entry's count of 4 because `skills/dashboard.md`'s See-also link was counted separately from its body reference this pass) and confirmed both still hold. Additionally re-verified the two underlying source-code claims directly against the merge SHAs rather than trusting the source page's citations: `git show e87877c:skills/dashboard/app/src/lib/argv.ts` confirms the merged-prompt line (`btn.command ? \`${btn.command} ${input}\` : input`) is present at the merge commit and unchanged on current `HEAD`; `git show 18094dd:hooks/scripts/voice_announce.sh` confirms the `hard-stop` case arm and `extract_failed` silence path are both present at the merge commit and unchanged on current `HEAD`. Zero corrections needed — no page content, cross-reference, or frontmatter field required a change. No new orphans, no new dangling links, no new stale pages, no new contradictions found in this cluster.

## [2026-07-07] query-file-back | run/events/run-log architecture and streaming output-viewer gap

Query: what does the wiki cover about the dashboard's `/api/run` route, `/api/events` SSE stream, and the `runs.jsonl`/`runs/<runId>.log` artifact model, and what would a plan to add a streaming output-viewer need to know that isn't documented. Wiki coverage was architectural-only (`skills/dashboard.md` + its 5 source pages) — no page documented the run-log artifact model as its own topic, and none addressed a streaming viewer (no prior plan, no rejection). Read current source directly (`route.ts`, `events/route.ts`, `runlog.ts`, `collect/index.ts`, `useRunLifecycle.ts`, the design spec) rather than trusting wiki summaries, since this was a "what would a plan need to know" question requiring ground truth. Verified finding: the per-run `.log` file is write-only in the entire codebase — `route.ts` appends `stdout+stderr` to it exactly once, post-exit, inside the `execFile` callback; `RunRecord.outputPath` is stored in `runs.jsonl` but never read anywhere (grep confirms exactly 2 non-test hits: the write site and the type definition); the frontend's only `RunRecord` consumer (`useRunLifecycle.ts`) uses it purely for sphere hue/progress, never renders output text. Also verified the aggregator's recursive `fs.watch` on `runsDir` does fire on `.log` writes but only triggers a `runs.jsonl` metadata re-read — no log bytes ever reach the `Snapshot` or an SSE frame. Filed [[dashboard-run-log-streaming-viewer-gap_2026-07-07]] documenting this plus 6 cross-PR constraints a streaming-viewer plan must respect (Origin/Host guard, strict-ID-validation precedent from the [[pr_31_assistant-link-approve-button]] path-traversal fix, the single-SSE-provider requirement from PR #25's fix-loop, collector never-throw discipline, token/CSRF non-leakage, and the long-lived-POST/lock-lifetime coupling) and 5 assumptions a plan should not make. Updated `index.md`'s Investigations section.

## [2026-07-08] ingest | PRs #80–82 — dashboard streaming output-viewer (closes the 2026-07-07 gap)

Ingested a three-PR cluster (all merged 2026-07-08) as ONE source page, per the cluster's own theme: a real streaming output-viewer for the dashboard, closing [[dashboard-run-log-streaming-viewer-gap_2026-07-07]] the day after that investigation was filed. PR #80 (backend, `2a49d05`): `POST /api/run` now spawns `claude` with `--output-format stream-json --include-partial-messages --verbose`, streams stdout/stderr chunks incrementally to `runs/<runId>.log` (clean break from the old single post-exit write the investigation documented) and publishes each chunk on a new in-process pub/sub `runOutputBus.ts`, re-emitted by the aggregator as a new `"run-output"` SSE event on the existing `/api/events` connection — explicitly not a second SSE endpoint, honouring the investigation's single-SSE-provider constraint. New non-throwing, forward-compatible `streamJson.ts` parser (no event-type allowlist; parse failures never affect what's appended/published). Review caught and fixed a Critical: no `child.on("error", ...)` handler, so a spawn failure would hang the request forever and leak the per-button lock. PR #81 (frontend, `f2371ea`): new `OutputViewerPanel.tsx` in the COMMAND DECK — live-streams an active run from the SSE-accumulated buffer, fetches a finished run's settled output once via new `GET /api/run/output`, which validates `runId` against `/^[0-9a-f]{16}$/` and — going further than the PR #31 path-traversal precedent the investigation cited — never joins `runId` into a path at all, instead reading the matching `RunRecord`'s own server-written `outputPath`. Review caught and fixed a Critical: `fetchSettledOutput` silently swallowed all fetch/parse errors; fixed with a discriminated result type, a distinct 409/in-progress case, a visible retry button, and pruning of the `runOutput` accumulator for runs aged out of run-history. PR #82 (docs, `a383f36`): `docs/REFERENCE.md` + `skills/dashboard/SKILL.md` updated to describe the viewer. Verified all claims directly against `git show origin/main:<path>` for `runOutputBus.ts`, `streamJson.ts`, `api/run/route.ts`, `api/run/output/route.ts`, `OutputViewerPanel.tsx`, `useDashboardState.ts`, `collect/index.ts`, `docs/REFERENCE.md`, and `SKILL.md` — not summarised from a brief alone. Created `sources/pr_80-82_dashboard-stream-run-output-viewer.md`. Updated: `skills/dashboard.md` (new "Run Output viewer" subsection under Architecture, frontmatter sources/tags, See-also), `wiki/investigations/dashboard-run-log-streaming-viewer-gap_2026-07-07.md` (CLOSED banner + `status: closed` frontmatter + point-by-point Resolution section mapping each of the investigation's 7 findings/constraints to how this cluster satisfied it; investigative content left unchanged as a historical record), `index.md` (investigation bullet marked CLOSED, new Sources entry, frontmatter `last_updated`).

## [2026-07-08] lint | Cluster-scoped pass on the dashboard-stream-run-output-viewer ingest above

Checked only what this cluster touched (per-cluster scope, consistent with this log's established convention). New page `sources/pr_80-82_dashboard-stream-run-output-viewer.md`: NOT orphan — inbound from `index.md` (new Sources entry), `log.md` (this entry + the ingest entry above), `skills/dashboard.md` (frontmatter `sources` + body reference in the new subsection + See-also), `wiki/investigations/dashboard-run-log-streaming-viewer-gap_2026-07-07.md` (CLOSED banner + Resolution section + See-also) = 6 inbound, verified via grep. Outbound `[[links]]` from the new source page — `[[dashboard-run-log-streaming-viewer-gap_2026-07-07]]`, `[[pr_31_assistant-link-approve-button]]` — both resolve to existing pages (verified via `ls`). Frontmatter on the new page: title/type/created/last_updated/sources/tags all present and non-empty, dates 2026-07-08, tags non-empty; `sources: []` matches this vault's established convention for source pages (body cross-references instead of a frontmatter list). `skills/dashboard.md`: frontmatter `sources` and `tags` both extended, `last_updated` bumped 2026-07-07→2026-07-08; new subsection's two `[[links]]` (`dashboard-run-log-streaming-viewer-gap_2026-07-07`, `pr_31_assistant-link-approve-button`) both resolve. Investigation page: frontmatter gained `status: closed` and `last_updated` bumped; new Resolution section's one outbound link (`pr_80-82_dashboard-stream-run-output-viewer`) resolves; original investigative content (all "Finding:" sections, the cross-PR constraints list, the "should NOT assume" list) verified byte-unchanged except the frontmatter/banner/Resolution additions — the page remains an accurate historical record, not rewritten. CONTRADICTIONS: 0 new — the investigation's pre-fix claims (write-only log, dead `outputPath`, `fs.watch` red herring) are explicitly framed in both the banner and the source page as *then-current, now-resolved* facts, not left as unflagged conflicting present-tense claims alongside the new page's *now-current* description of the same code paths. 0 new orphans, 0 new dangling links among touched/created pages. STALE PAGES / full-vault scope: not re-audited this pass — per-cluster scope only, consistent with this log's established convention for prior clusters.

## [2026-07-08] lint | Full-vault sweep (144 files, mechanical link/frontmatter parse + manual verification of every flagged candidate)

FIXED: `wiki/investigations/dashboard-run-log-streaming-viewer-gap_2026-07-07.md` was filed under a stray `wiki/investigations/` subdirectory instead of the schema's top-level `investigations/` — moved via `git mv` to `investigations/dashboard-run-log-streaming-viewer-gap_2026-07-07.md`; removed the now-empty `wiki/` directory. Inbound `[[links]]` from `index.md`, `log.md`, `sources/pr_80-82_dashboard-stream-run-output-viewer.md`, and `skills/dashboard.md` were unaffected (basename-matching wiki-links don't encode directory) — verified via grep after the move. DATA GAP FOUND, NOT FIXED: `hooks/scripts/comment_citation_gate.sh` is a live `PreToolUse` hook wired in `coderails/hooks/hooks.json` (confirmed via grep, added commit `125aec7`) with **zero** wiki coverage — no `hooks/comment_citation_gate.md` page, and not mentioned in `index.md`'s Hooks table, its Stop/PreToolUse ordering prose, or anywhere else in the vault (grep for `comment_citation_gate`/`comment-citation` across all 144 files returns nothing). Plugin hook-script count is 14; wiki hooks pages are 13. Authoring the page was out of scope for a lint pass — flagged for a follow-up ingest. STRUCTURAL RISK FLAGGED, NOT FIXED: `dashboard-runs/wiki-lint.md` (a routine-runner operational note, explicitly non-wiki content per AGENTS.md's page-type table) shares a basename with `skills/wiki-lint.md` (a real wiki page). Every `[[wiki-lint]]` link in the vault (index.md, design/skills-hooks-seam.md, skills/agentic-loop.md, skills/wiki-ingest.md, skills/wiki-init.md, skills/wiki-query.md, 2 source pages — 9 link sites total) is ambiguous under basename resolution between the two files; Obsidian's own resolver picks one deterministically but a naive script (mine, initially) can't tell which was intended. No fix applied — renaming the operational file is a routines/dashboard-runner concern, not a wiki-content one, and out of this skill's scope. VERIFIED CLEAN (re-derived from current file contents, not trusted from prior entries): 0 live `⚠️ CONTRADICTION` flags (the one `skills/wiki-lint.md` hit is the skill's own description of the check, not a real flag). 0 broken `[[wiki-links]]` — an initial regex pass flagged ~30 candidates, all resolved as false positives on manual check: bash/POSIX code fragments inside code blocks (`[[ -n ... ]]`, `[[:space:]]`), `log.md`'s own append-only prose using `[[link]]`/`[[page_name]]` as generic placeholder text (log.md is explicitly excluded from cross-reference rules), a historical log entry citing the pre-rename filename `pr_69_no-edit-message-worktree` (the file was later renamed to `pr_69_2026-06-29_no-edit-message-worktree.md` per the PR-collision fix; log.md is append-only and correctly left unedited), and `[[capabilities/send-gate]]` which explicitly targets the sibling `assistant-agent-wiki` vault (verified the target file exists on disk) — documented as such in the same investigation page. 1 genuine orphan (unchanged from prior lints): `skills/handoff.md`, linked only from `index.md`'s catalog table which doesn't count as a qualifying inbound link. 6 stale pages (`last_updated` > 30 days), all `sources/`/`investigations/` point-in-time records — expected per page-type convention, not fixed (per AGENTS.md, these are immutable/point-in-time by design). 0 inbox backlog (`inbox/` doesn't exist). Commands 12/12, hooks 13/14 (gap above), skills 31/31 (30 coderails-owned + `sync-docs` correctly self-documented as an external user-level skill, not vendored) all otherwise match plugin source. Missing-concepts/missing-cross-refs backlog from prior lints (trust-floor, work_units, loop_stop_counts) already closed by intervening sessions — re-verified `design/trust-floor.md` exists and is populated; no new 3+-page-cited concept found lacking a home this pass.

## [2026-07-08] ingest | gate security-hardening cluster (substitution/force-with-lease/option-tolerance/plugin-anchor)

Ingested 5 merged PRs (#69, #72, #75, #84, #92 — all 2026-07-08, all landing in [[destructive_bash_gate]]) as 4 new source pages (`sources/pr_69_2026-07-08_substitution-bypass-audit.md`; `sources/pr_72-75_2026-07-08_force-with-lease-allowlist.md`, shared page for the same carve-out arc; `sources/pr_84_2026-07-08_git-global-option-bypass.md`; `sources/pr_92_2026-07-08_reference-drift-and-lookalike-fp.md`). **PR-number collision, deliberately avoided:** this repo already had older, unrelated PRs #69/#72/#75/#92 (2026-06-29/2026-07-05) documented at `sources/pr_69_2026-06-29_no-edit-message-worktree.md`, `sources/pr_72_config-walkup-symlink-hang.md`, `sources/pr_75_main-branch-fallback.md`, `sources/pr_92_exec-bit-sweep.md` — all four left byte-untouched (verified via `ls`/`git status` before and after); the new pages use a date-disambiguated slug (`_2026-07-08_`) and each carries an explicit `⚠️ PR-number collision` banner cross-linking the older page. Updated `hooks/destructive_bash_gate.md` (`last_updated` 2026-06-29→2026-07-08): rewrote the `git push` force-detection section (carved out of the main regex, no longer part of it), added three new sections (force-with-lease allowlist carve-out, command-substitution detection, `plugin_src` path-boundary anchor), added a "2026-07-08 adversarial-hardening arc" cross-cutting-theme section, extended `sources:` frontmatter and the Related list with all 4 new source pages. Updated `design/enforcement-model.md` (`last_updated` 2026-07-07→2026-07-08): added a new "Positive-control testing for gates with a conditional-allow path" section generalising the PR #72/#75 incident lesson beyond this one hook, cross-linked from Cross-References. Updated `index.md`: hooks-table row for `destructive_bash_gate.sh` extended to name the 2026-07-08 arc, 5 new Sources entries appended (chronologically last, after `pr_80-82`). No contradictions found — REFERENCE.md's Hook Activation Matrix (`docs/REFERENCE.md`, read from the plugin repo as ground truth) already matched the shipped gate exactly (PR #92 itself fixed the drift), so the wiki's new content was checked against current source, not stale docs.

## [2026-07-08] ingest | PR #88 + #93 — dashboard launchd agent (reboot survival)

Ingested a two-PR cluster (both merged 2026-07-08) as one source page, theme "dashboard reboot survival via launchd." PR #88 (`ec46132`) gives the dashboard a launchd LaunchAgent — new `launchd/com.coderails.dashboard.plist` (`RunAtLoad`+`KeepAlive`+`ThrottleInterval 60`), a thin exec wrapper `skills/dashboard/runner/bin/dashboard-server.sh` (execs `npm run start` in the foreground; deliberately duplicates ~13 lines of `start-dashboard.sh`'s build-if-stale logic rather than sharing it, extended with a fail-safe dependency/config staleness check and a missing-src-dir rebuild trigger a daemon needs and an interactive script doesn't), `launchd/install-dashboard-agent.sh`/`uninstall-dashboard-agent.sh`, and a mutation-validated static guard suite `hooks/scripts/tests/dashboard_agent.test.sh`. The installer reuses, rather than rediscovers, the boot-persistence fix [[routines]] already documents for the routine-sweeper agents: copy the plist into `~/Library/LaunchAgents/` and bootstrap from that copy, not the repo path — a repo-path bootstrap silently unloads on the next reboot (the routine-sweeper agents hit this live on 2026-07-08: 03:00 run fine, reboot at 07:34, every `com.coderails` job gone). PR #93 (`f630a98`) fixes an uninstall-time race found the same day: `launchctl bootout` is asynchronous for a running `KeepAlive` job (~2s observed to actually unload) — the uninstaller now polls up to 10×1s before declaring failure instead of checking once immediately, and only removes the `~/Library/LaunchAgents/` copy after the loaded-check passes (previously at risk of removing the copy on a still-loading false positive). Verified both PRs directly against `gh pr view --json title,body,files,mergedAt,mergeCommit` and `gh pr diff` rather than relying solely on the team-lead's brief. Design-fork record (thin-exec-wrapper vs. plist-invokes-start-dashboard.sh vs. a `--foreground` flag) captured in the new source page per the brief's own resolved-forks summary. Created `sources/pr_88_93_dashboard-launchd.md`. Updated: `skills/dashboard.md` (new "Surviving reboots (launchd)" subsection under Starting/stopping, frontmatter `sources`/`tags` extended), `design/routines.md` (cross-reference paragraph noting the dashboard reused this page's boot-persistence fix rather than rediscovering it, frontmatter `sources` extended, `last_updated` bumped to 2026-07-08), `index.md` (new Sources entry, chronologically last).

## [2026-07-08] lint | Cluster-scoped pass on the dashboard-launchd ingest above

Checked only what this cluster touched (per-cluster scope, consistent with this log's established convention). New page `sources/pr_88_93_dashboard-launchd.md`: NOT orphan — inbound from `index.md` (new Sources entry), `log.md` (this entry + the ingest entry above), `skills/dashboard.md` (frontmatter `sources` + body reference in the new subsection), `design/routines.md` (frontmatter `sources` + body cross-reference paragraph + See-also entry) = 5 inbound, verified via grep. Outbound `[[wiki-links]]` from the new source page — `[[routines]]`, `[[dashboard]]` — both resolve to existing pages (verified via `ls`). No PR-number collision: grep of `sources/` for a prior bare `pr_88_` or `pr_93_` filename found none, so no date-disambiguation or collision banner was needed (unlike several same-day clusters earlier in this log). Frontmatter on the new page: title/type/created/last_updated/sources/tags all present and non-empty, dates 2026-07-08; `sources: []` matches this vault's established convention for source pages (body cross-references instead of a frontmatter list). `skills/dashboard.md`: `last_updated` already 2026-07-08 from an earlier same-day ingest, left as-is (accurate); new subsection's `[[pr_88_93_dashboard-launchd]]` and `[[routines]]` links both resolve. `design/routines.md`: `last_updated` bumped 2026-07-07→2026-07-08, frontmatter `sources` extended, new cross-reference paragraph's implicit reference and the new See-also bullet's `[[pr_88_93_dashboard-launchd]]` link resolve. CONTRADICTIONS: 0 — the new page's claim that the dashboard reused (not duplicated-by-independent-discovery) the routines boot-persistence fix is consistent with both pages' framing; no page asserts the two mechanisms diverge. 0 new orphans, 0 new dangling links among touched/created pages. STALE PAGES / full-vault scope: not re-audited this pass — per-cluster scope only, consistent with this log's established convention for prior clusters.
## [2026-07-08] ingest | PR #95 slash-command loop detection + evals-gate enforcement-gap investigation

## [2026-07-08] lint | Cluster-scoped pass on the gate security-hardening ingest (#69/#72+75/#84/#92, substitution/force-with-lease/option-tolerance/plugin-anchor)

Checked only what this cluster touched (per-cluster scope, consistent with this log's established convention). Files audited: `sources/pr_69_2026-07-08_substitution-bypass-audit.md`, `sources/pr_72-75_2026-07-08_force-with-lease-allowlist.md`, `sources/pr_84_2026-07-08_git-global-option-bypass.md`, `sources/pr_92_2026-07-08_reference-drift-and-lookalike-fp.md`, `hooks/destructive_bash_gate.md`, `design/enforcement-model.md`, `index.md`, `log.md`. WIKI-LINKS: extracted 32 unique outbound `[[links]]` across the 6 content pages, all resolve to existing vault pages (verified against a 148-page basename index) — 0 dangling. The one mechanically-flagged `[[:space:]]` is a POSIX character class inside backtick/code-fence regex, not a wiki-link (Obsidian ignores `[[...]]` inside code spans); confirmed all occurrences are code-wrapped. `index.md`'s 4 new Sources bullets and their 4 collision cross-links all resolve; the 4 older date-collision pages (`pr_69_2026-06-29_no-edit-message-worktree`, `pr_72_config-walkup-symlink-hang`, `pr_75_main-branch-fallback`, `pr_92_exec-bit-sweep`) exist and their titles/merge-dates match each new page's collision banner exactly (2026-06-29/2026-06-29/2026-06-29/2026-07-03). FRONTMATTER: all 6 pages carry title/type/created/last_updated/sources/tags, all non-empty; `type` values valid (source×4, hook, design); all `last_updated` = 2026-07-08 (nothing stale). ORPHANS: 0 — each new source page has ≥2 inbound (index.md + destructive_bash_gate.md at minimum; #69/#72-75/#84 additionally inter-link within the arc). CONTRADICTIONS: 0. Merge commits agree in short/full form across source pages and index.md (ebfee7b/8332509/e63d3d1/7c4f12f/652c5f8). The `cmd_flat` (substitution block, #69) vs `force_cmd_flat` (force-push block, #72/#75) distinction is kept distinct on every page — no conflation. Vault-wide sweep for pages asserting force-push is "always denied / no carve-out" (which the new allowlist would contradict) found none; the hook page's "no approval path" line is correctly scoped to the main-regex blocklist while the carve-out is flagged separately in the intro and its own section. MINOR (not fixed, reported to caller): `hooks/destructive_bash_gate.md:95` mentions "PR #97" as bare prose where a link `[[pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter|PR #97]]` would resolve — contextual color about the `$ARGUMENTS` injection class, not core to this cluster; left as-is per surgical scope. STALE PAGES / full-vault scope: not re-audited this pass — per-cluster scope only.

## [2026-07-08] ingest | PRs #96-98 evals-gate uniform enforcement cluster (hook-level gh-pr-merge eval gate + systematic-debugging routing + coverage-boundary docs) — closes evals-gate-enforcement-gap_2026-07-08 same day. New source page sources/pr_96-98_evals-gate-uniform-enforcement_2026-07-08.md (date-suffixed to disambiguate from the unrelated, earlier PR-number-collision page pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter.md). Updated investigations/evals-gate-enforcement-gap_2026-07-08.md (Resolution), design/task-evals-gate.md (Known gaps + Merge gate placement), hooks/enforce_pr_workflow.md (new Gate 7 section, block conditions, named-gate table), design/enforcement-model.md (stale hook-map row fixed), index.md (Sources + Investigations entries)

## [2026-07-08] ingest | PR #99 unregistered_loop_guard nudge-once

Ingested PR #99 (merged 2026-07-08, `95479de`; commits `1ccefa7` nudge-once + `88af636` grep-metachar escape), the single merged PR for this phase. Fixes `unregistered_loop_guard.sh` re-emitting its "3+ Agent dispatches, no agentic-loop registration" nudge on EVERY Stop for a non-loop session with no per-session termination — self-perpetuating for a genuinely one-off dispatch sequence (nudge -> honest "no action needed" turn -> Stop -> nudge again, observed live 2026-07-08). Fix greps the existing discipline log (als_log's ledger, already written on the emit path) for a prior `nudged=1` line for the current session_id before emitting again; first nudge unaffected; missing/unreadable log still fails open. Second commit hardens the check: session_id is escaped (`sed 's/[.[\*^$\\]/\\&/g'`) before interpolation into the grep BRE pattern, closing a wildcard-match false-suppression regression (a literal `.` in a session id could otherwise match an unrelated session's logged line). Reviewed (code-reviewer 0 critical/0 important/1 suggestion, applied; pr-test-analyzer confirmed the regression test fails pre-fix, passes post-fix); eval artifact GO tier 1; suite 37/37. Created sources/pr_99_unregistered-loop-guard-nudge-once.md. Updated design/discipline-loop.md (Stop hook composition bullet #6 + Cross-References, frontmatter sources + last_updated), hooks/unregistered_loop_guard.md (new "Nudge-once-per-session" section, Known limitations, Test coverage count, frontmatter sources/tags/last_updated), index.md (hooks table row + new Sources entry). Sync-docs check: README.md, AGENTS.md, docs/REFERENCE.md all document the hook's trip condition but none described per-session nudge repetition/suppression before or after this PR -- no in-tree doc contained a claim this PR contradicts, so this is drift-by-omission only, not corrected factual drift; not fixed (out of this phase's scope, and each of those docs' hook-table rows remains accurate as far as it goes).

## [2026-07-08] lint | Full-vault health pass

Full audit (not cluster-scoped): 129 pages — 12 commands, 13 hooks, 32 skills, 19 design, 5 investigations, 56 sources, plus index/log. Findings:

- **Coverage gap (actioned as report, not authored):** `comment_citation_gate` is a live PreToolUse (Write|Edit|MultiEdit) hook — wired in `hooks/hooks.json`, script at `hooks/scripts/comment_citation_gate.sh`, added 2026-07-07 (commit 125aec7) — but has ZERO wiki coverage: no `hooks/comment_citation_gate.md`, absent from `index.md`'s hook table, unmentioned in any page (the single log.md hit is coincidental grep-output quoted in an unrelated lint entry). All other 13 hooks, all 12 commands, all 31 non-coordinator skills have pages. Root cause: the PR that added the gate was never wiki-ingested. Fix belongs to `/wiki-ingest` (author `hooks/comment_citation_gate.md` + index row), not lint. AGENTS.md's own Part-1 hook event map also omits it (source-side gap, flagged not fixed).
- **Dangling link FIXED:** `sources/pr_23-24_hook-lib-observability-and-repo-keyed-loop-state.md` linked `[[design/agentic-loop-path-keying]]` with a `design/` path prefix Obsidian shortlinks don't resolve; the canonical form used in 8+ other pages is `[[agentic-loop-path-keying]]`. Corrected in place.
- **Cross-wiki link noted, not a defect:** `investigations/queue-contract-cross-pr-audit_2026-07-07.md` uses `[[capabilities/send-gate]]`, explicitly labelled inline as an assistant-agent-wiki page — an intentional external reference, not a local dangle.
- **Contradictions: 0 open.** All 4 `⚠️` markers outside index/log are already-resolved supersession/PATH-UPDATE banners (config-resolution, queue-contract-audit, pr_70-71, pr_43-44-46), each reconciling its conflict explicitly rather than leaving a live contradiction.
- **Orphans: 0.** Every non-index/log page has ≥1 inbound link.
- **Stale (>30d, before 2026-06-08): 6, all benign** — all are `source` or `investigation` pages (immutable PR/session records and point-in-time analyses), whose old `last_updated` is correct by schema. No evergreen command/hook/skill/design page is stale.
- **Inbox: n/a** (no `inbox/` dir).

Suggestions: (1) run `/wiki-ingest` for commit 125aec7 to close the `comment_citation_gate` gap — highest-value next action; (2) consider renaming the pre-recreation `pr_69`/`pr_70` source pages to date-qualified forms to resolve the PR-number collision already flagged in `repo-hosting.md`; (3) add `comment_citation_gate` to AGENTS.md's Part-1 hook event map (source edit, owner's call).

## [2026-07-08] ingest | loop-lib residuals cluster (PRs #91/#107/#86/#87/#94) + comment_citation_gate coverage-gap fix

Ingested a five-PR cluster (all merged 2026-07-08) as one source page,
`sources/pr_86-107_2026-07-08_loop-lib-residuals.md`. Main thread: PR #91
(`f1db15c4`) makes the shared `hooks/scripts/lib/loop_state_common.sh`
transcript parse malformed-line-tolerant (per-line `jq -R 'fromjson? //
empty'` pre-filter ahead of the aggregating `jq -s`, dropped lines reported
as `skipped_malformed=N` on stderr) — a single bad JSONL line previously
collapsed the whole invocation count to empty/0, making
`als_gate_require_active_loop` treat a genuinely active loop as "not a loop"
and defeating [[loop_state_guard]], [[loop_stall_guard]], [[voice_announce]],
and (via its delegate call) [[unregistered_loop_guard]]. PR #107 (`d4256542`,
same day, a post-merge 4-reviewer verification pass) corrected two
failure-attribution regressions the tolerant parse had introduced: a
`read_error` tag for an unreadable transcript or a stage-1 `jq` binary death
(previously indistinguishable from "clean"), and an `all_lines_malformed` tag
distinguishing total parse loss (every line malformed) from a benign partial
skip — the distinction gates the `als_stable_invocations` retry/settle loop,
restoring the 5-attempt flush-race window a mis-attributed "clean" result
would have short-circuited. `unregistered_loop_guard.sh`'s own separate
`jq -s` slurp (`ulg_count_dispatch_turns`) and `discipline_common.sh`'s
`dc_extract_last_text` (a different lib family) both carry the identical bare
`jq -s` fragility and were explicitly NOT touched — both recorded as standing
residuals on the source page and cross-linked from
[[unregistered_loop_guard]]'s own page. Smaller, unrelated fixes swept into
the same cluster: PR #86 (`9b300355`) gives the Obsidian command-centre
plugin's `pressButton` (`exec.ts`) a try/catch around `buildArgv`, matching
`route.ts`'s existing parity; PR #87 (`81a705f6`) investigated a reported
nested-worktree test-failure concern in `install_mode_sweep.test.sh` and did
NOT reproduce it (documented as a header comment, no code change); PR #94
(`f4e05ee9`) removes a third-occurrence leaked root `evals.json` and adds a
root-anchored `/evals.json` gitignore entry that doesn't shadow the tracked
dashboard test fixture. **PR-number collision, deliberately avoided:** this
cluster's #86/#87 are unrelated to the older, pre-recreation
`sources/pr_86_agentic-loop-hardening.md` /
`sources/pr_87_agentic-loop-path-session-keying.md` (both merged 2026-07-01)
— left byte-untouched; the new page's slug carries the cluster theme, not a
bare `pr_86_`/`pr_87_` name.

Updated: `hooks/loop_state_guard.md` (new "Malformed-transcript tolerance"
section, frontmatter sources/tags, see-also, `last_updated` 2026-07-06→
2026-07-08), `hooks/loop_stall_guard.md` (cross-reference note in its
skip-gates list, frontmatter sources/tags, see-also, `last_updated`
2026-07-06→2026-07-08), `hooks/voice_announce.md` (cross-reference note in
its silence-conditions section, frontmatter sources/tags, see-also,
`last_updated` 2026-07-07→2026-07-08), `hooks/unregistered_loop_guard.md`
(new "Known residual: this hook's own jq -s slurp is untouched" section,
extended design-decisions bullet, frontmatter sources/tags, see-also,
`last_updated` 2026-07-06→2026-07-08), `skills/dashboard.md` (new
`pressButton` buildArgv-parity paragraph under the Obsidian command centre
subsection, frontmatter sources), `design/install-and-cache-trap.md` (new
"Nested-worktree concern investigated, did not reproduce" section, frontmatter
sources/tags, see-also, `last_updated` 2026-07-07→2026-07-08), `index.md`
(hooks-table rows for `loop_state_guard.sh`/`loop_stall_guard.sh`/
`unregistered_loop_guard.sh` annotated with the malformed-transcript fix, new
Sources entry).

**Also closed the `comment_citation_gate` coverage gap** the full-vault health
pass above flagged (highest-value suggestion #1): authored
`hooks/comment_citation_gate.md` from the script
(`hooks/scripts/comment_citation_gate.sh`) and its `hooks.json` registration
(read directly, not summarised) — a `PreToolUse (Write|Edit|MultiEdit)` hook
denying new code comments that cite a session-artifact label (`E#:`, `F# fix`,
`CHANGE B#/C#`, `Task A#`, `TA-I#`, "reviewer finding", `eval E#`, `WU#:`,
`C2`, "per the plan/design/session"); `.md` files out of scope; `PR #NN` a
documented survivor. Created `sources/pr_50_2026-07-07_comment-citation-gate.md`
(PR #50, merged 2026-07-07T12:22:08Z, `6cdeb9c8`) — **PR-number collision,
deliberately avoided**: this repo already has an older, unrelated
`sources/pr_50_planning-sequence-gate.md` (merged 2026-06-26, pre-recreation);
the new page uses the date-qualified slug per [[repo-hosting]]'s convention.
Both PR #50's and PR #107's `gh pr view --json body` fields return a bare
commit-log dump rather than written prose — both source pages' Summary
sections are reconstructed from direct source-diff/script reads, flagged
inline as such. Updated `index.md` (new hooks-table row for
`comment_citation_gate.sh`, new Sources entry).

## [2026-07-08] lint | Full-vault sweep, no cluster lint had run since the loop-lib-residuals ingest above

138 content pages (12 commands, 14 hooks, 32 skills, 19 design, 7
investigations, 66 sources, plus index/log). Findings:

- **Coverage gap (flagged, not authored — belongs to `/wiki-ingest`):**
  `hooks/scripts/offload_push_guard.sh` is a live `Stop`/`SubagentStop` nudge
  hook — registered in `hooks/hooks.json` (commit `580b734`, merged
  2026-07-08 15:01) — with **zero** wiki coverage: no `hooks/offload_push_guard.md`,
  absent from `index.md`'s hook table, unmentioned anywhere in the vault
  (grep-verified). It nudges (never blocks) when a session's final message
  tells the user to run `git push` to main/master themselves instead of
  clearing `enforce_pr_workflow.sh`'s push gate in-session — sibling to
  `unregistered_loop_guard.sh` (same nudge-once-per-session ledger idiom,
  same `additionalContext`-on-stdout delivery), unrelated in subject. This
  is the same failure shape as the `comment_citation_gate` gap the prior
  full-vault pass closed: a hook shipped and merged same-day but never
  wiki-ingested. Plugin hook-script count is now 15; wiki hook pages are 14.
  AGENTS.md's own Part-1 hook event map also omits it (source-side gap,
  flagged not fixed, same as the comment_citation_gate precedent).
- **Coverage gap (flagged, not authored — belongs to `/wiki-ingest`):**
  `skills/verify-merged-pr/SKILL.md` (created 2026-07-08 14:31, commit
  `984ee95`, committed clean on main) has **zero** wiki coverage: no
  `skills/verify-merged-pr.md`, absent from `index.md`'s skills section,
  unmentioned anywhere in the vault. Plugin skill count is 33 (32 vendored/
  coderails-owned + `sync-docs` as a documented external skill); wiki skill
  pages remain 32 — the count coincidentally matched pre-this-check because
  `sync-docs` occupies a slot `verify-merged-pr` doesn't, masking the gap
  until a name-by-name diff was run instead of a bare count comparison.
- **New source page re-verified (2026-07-08 ingest, no lint had followed it):**
  `sources/pr_86-107_2026-07-08_loop-lib-residuals.md` — NOT orphan: 9+
  inbound links (index, log, design/install-and-cache-trap, hooks/
  unregistered_loop_guard, hooks/loop_state_guard, hooks/loop_stall_guard,
  hooks/voice_announce, skills/dashboard) (verified). All 7 outbound
  `[[links]]` resolve: `[[dashboard]]`, `[[loop_stall_guard]]`,
  `[[loop_state_guard]]`, `[[repo-hosting]]`, `[[task-evals]]`,
  `[[unregistered_loop_guard]]`, `[[voice_announce]]` (verified). Frontmatter
  conforms (`sources: []` is correct for a `type: source` page — nothing
  informed the origin record itself, consistent with all other source
  pages). `index.md` and touched hook/design/skill pages already updated by
  the ingest — no further edits needed.
- **Contradictions: 0 open.** No live `⚠️ CONTRADICTION` markers outside
  index/log; all historical hits are already-resolved supersession banners
  or the skill's own description of the check (verified, same set as prior
  lints).
- **Orphans: 0 new.** `skills/handoff.md` remains the sole genuine orphan
  (index.md is its only inbound link) — unchanged, previously accepted.
  Basename cross-check against all 178 raw `[[link]]` occurrences found no
  other page with zero inbound links.
- **Dead links: 0 new.** Re-verified the standing benign set (`[[:space:]]`
  POSIX regex literals in code blocks; `[[wiki-links]]`/`[[link]]`/`[[links]]`/
  `[[page_name]]`/`[[wiki-link]]` schema meta-text in log.md/wiki-lint.md/
  wiki-query.md prose; `[[pr_73]]`/`[[review-pr]]`/`[[workflow-init]]`/
  `[[workflow-command-chain]]`/`[[pr_64...]]`/`[[pr_72...]]`/
  `[[pr_69_no-edit-message-worktree]]` in log.md historical prose only;
  `[[capabilities/send-gate]]` an intentional cross-vault reference to
  assistant-agent-wiki) — all confirmed still benign, no new dangling
  navigational links introduced since the last full sweep.
- **Stale (>30d, before 2026-06-08): 6, all benign** — same set as the prior
  full-vault pass (`install-cache-trap_2026-05-30`,
  `session_2026-05-31_prompting-doc-alignment`,
  `session_2026-05-31_verify-loop-hardening`,
  `install-bash32-bad-substitution_2026-06-01`,
  `session_2026-06-01_agentic-loop-delegate-all-impl`,
  `session_2026-06-01_verify-loop-total-enforcement`) — all `source`/
  `investigation` pages, immutable/point-in-time by schema convention. No
  evergreen command/hook/skill/design page is stale.
- **Inbox: n/a** (no `inbox/` directory).

Suggestions: (1) run `/wiki-ingest` for `offload_push_guard.sh` (commit
`580b734`) — same shape and same fix as the `comment_citation_gate` gap
closed by the prior ingest, highest-value next action; (2) run
`/wiki-ingest` for `skills/verify-merged-pr` (commit `984ee95`); (3) add
`offload_push_guard.sh` to AGENTS.md's Part-1 hook event map alongside the
still-outstanding `comment_citation_gate` addition (source edit, owner's
call); (4) when comparing plugin↔wiki coverage, diff by name (as this pass
did) rather than by bare count — the `sync-docs`/`verify-merged-pr` slot
coincidence in skills is a reminder that counts can silently mask a 1-for-1
gap.

## [2026-07-08] ingest | Approve→build pipeline live-fire (PRs #89/#100/#104/#106)

Loop-boundary ingest of the four-PR cluster that closes E5 (the first real
Approve→build run) for the [[pr_55-60-64-66-67_approve-build-runner|builder
pipeline]]. One source page for the cluster: `sources/pr_89-100-104-106_approve-build-e5-live.md`.

- **New:** `sources/pr_89-100-104-106_approve-build-e5-live.md`;
  `skills/verify-merged-pr.md` (the first skill the pipeline built — PR #104).
  Creating this skill page **closes suggestion (2) of the 2026-07-08 wiki-lint
  pass** (`skills/verify-merged-pr/SKILL.md` existed in the plugin with no wiki
  page).
- **Updated:** `skills/dashboard.md` (AssistantLinkPanel: live phase / elapsed
  timer / heartbeat + `PR resolved` after-merge reconciliation, PR #106);
  `skills/workflow-audit.md` (pipeline-outcome note — E5 done, first skill
  built); `sources/pr_55-60-64-66-67_approve-build-runner.md` (E5-pending
  caveat resolved, both occurrences); `index.md` (new source + skill rows,
  approve-build-runner E5 note flipped to done).
- **PR-number collision flagged:** this cluster's #89/#100 are unrelated to the
  pre-existing [[pr_89-91_skills-doc-frontmatter-injection|#89]]; resolve by
  merge SHA (`91d3802`/`a6f40ec`/`c30fcbf`/`23a73bd`).

## [2026-07-08] lint | post-E5-ingest health pass — clean, one fix

Focused lint of the seven pages the Approve→build E5 ingest touched. Verdict:
the E5-pending→done flip is applied consistently and no dead links were
introduced.
- **Contradictions:** 0. The E5-pending→done flip is complete across
  [[pr_55-60-64-66-67_approve-build-runner]] (both occurrences), [[index]],
  and [[workflow-audit]]; a vault-wide grep for any surviving "E5 …
  pending/not yet" claim found only text describing the *resolution*.
- **Dead wiki-links:** 0. All 8 distinct ingest-introduced link targets
  (`verify-merged-pr`, `pr_89-100-104-106_approve-build-e5-live`,
  `pr_89-91_skills-doc-frontmatter-injection`,
  `verification-before-completion`, etc.) resolve to real pages.
- **Orphans:** 0. Both new pages are inbound-linked — the source page from
  index/log/runner/dashboard/workflow-audit/verify-merged-pr; the skill page
  from index/source/runner/workflow-audit/dashboard.
- **Fixed:** this log's own ingest entry (above) called the cluster a "two-PR
  cluster" — it is four PRs (#89/#100/#104/#106); corrected to "four-PR".
- **PR #100 SHA resolved (post-lint):** the source page and index initially
  carried PR #100's merge identifier as a branch-name placeholder; resolved to
  the real merge SHA `a6f40ec` (merged 2026-07-08T13:24:47Z) via `gh` after the
  lint pass. No placeholder remains.

## [2026-07-08] ingest | dashboard lockfile emnapi drift finding (closed-not-merged, already resolved on main)

## [2026-07-08] lint | Full-vault health pass (160 files — 12 commands, 15 hooks, 33 skills, 19 design, 8 investigations, 71 sources)

Full audit, not cluster-scoped. Findings:

- **Coverage gap (report only, not authored — same pattern as `comment_citation_gate` in the prior full-vault pass):** `offload_push_guard.sh` is a live `Stop`+`SubagentStop` hook — wired in `hooks/hooks.json`, script at `hooks/scripts/offload_push_guard.sh`, shipped in PR #108 (merged 2026-07-08) — and the wiki side is fully covered (`hooks/offload_push_guard.md` exists, `index.md`'s hook table and Stop-order prose both list it, `sources/pr_108_2026-07-08_offload-push-guard.md` exists). But `coderails/AGENTS.md`'s own Part-1 "Hook event map" table (the source-of-truth working guide, a *different* repo from this vault) omits it — the table stops at `comment_citation_gate` (added by PR #50/#92's docs fix, merged earlier today) and was never updated for #108. Source-side gap, flagged not fixed (wiki-lint documents the raw-source repo but doesn't edit it).
- **Dangling links: 0 real.** Full regex sweep over all `[[...]]` found ~175 raw hits; all resolved to previously-documented benign false positives — `[[:space:]]`/`[[...]]`/`[["$answer"=="y"]]`-shaped POSIX/bash syntax quoted inside fenced code blocks (`enforce_pr_workflow.md`, `destructive_bash_gate.md`, `skills-hooks-seam.md`, several `pr_*` sources), `[[wiki-links]]`/`[[link]]`/`[[page_name]]` schema meta-text describing the link syntax itself (`design/routines.md`, `skills/wiki-lint.md`, `skills/wiki-query.md`), `[[capabilities/send-gate]]` explicitly labelled as an assistant-agent-wiki cross-vault reference (`investigations/queue-contract-cross-pr-audit_2026-07-07.md`), and historical `log.md` prose (`[[pr_73]]`, `[[review-pr]]`, `[[pr_50…]]`, `[[pr_69_no-edit-message-worktree]]`, etc.) quoting pre-fix link states from earlier lint entries, already flagged as benign in those same entries.
- **Orphans: 0.** Every non-index/log page (160 total) has ≥1 inbound `[[wiki-link]]`, verified by full Python scan.
- **Stale (>30d, before 2026-06-08): 6, all benign** — `install-bash32-bad-substitution_2026-06-01`, `install-cache-trap_2026-05-30`, and 4 `session_*` sources from 2026-05-31/06-01. All are `source`/`investigation` pages (immutable PR/session records and point-in-time analyses) whose old `last_updated` is correct by schema; no evergreen command/hook/skill/design page is stale. Same 6 as the last full-vault pass — no new staleness.
- **Contradictions: 0 live.** No `⚠️ CONTRADICTION` markers outside `log.md`/`skills/wiki-lint.md` (the latter is schema prose describing the check itself).
- **Inbox: n/a** (no `inbox/` dir).
- **Command/hook/skill parity vs the plugin repo: 12/12 commands, 15/15 hooks, 31/31 shipped skills all have wiki pages** (wiki also carries one extra skill page, `sync-docs`, not shipped in this plugin repo — pre-existing, not a gap).
- **Staged-but-uncommitted content reviewed:** `hooks/offload_push_guard.md`, `sources/pr_108_2026-07-08_offload-push-guard.md`, `sources/pr_112-113_2026-07-08_jq-slurp-residuals-round2.md`, and diffs to `hooks/enforce_pr_workflow.md`/`hooks/unregistered_loop_guard.md`/`skills/agentic-loop.md`/`skills/verify-merged-pr.md` — all internally consistent, correctly cross-linked, and the `unregistered_loop_guard.md` "Known residual" section was correctly flipped to "Resolved residual" with a link to the closing PR #113.
- **Process note, not a defect:** the "Sources N→M" running-count convention log entries used to append (last seen "Sources 43→44" for PR #11-14) was dropped partway through the log's history (around the 2026-07-07 cluster-ingest entries) and never resumed; `sources/` has grown from 44 to 71 since. Cosmetic — doesn't affect page content or lint validity, just a discontinued log-annotation habit. Not fixed here since `log.md` is append-only/historical.

Suggestions: (1) add `offload_push_guard` to `coderails/AGENTS.md`'s Part-1 hook event map (source edit, owner's call, mirrors the `comment_citation_gate` fix pattern); (2) no wiki-side fixes needed this pass — 0 orphans, 0 dangling links, 0 open contradictions.

## [2026-07-08] lint | PR #11 cluster re-verify (offload_push_guard, verify-merged-pr, jq-slurp round-2) + full-vault confirm

Requested re-lint scoped to the pending PR #11 branch cluster
(`wiki/offload-guard-verify-merged-pr-jq-slurp-round2`). State check first: local
`main` and the PR #11 branch point to the **same commit** (`4c6e29d`), one ahead of
`origin/main` (`bb74d3d`) — so the entire PR #11 content set is already in the
working tree on `main` and was linted by the immediately-prior entry above. Working
tree clean; no uncommitted content. Re-ran an independent full-vault scan (160 files
— 12 commands, 15 hooks, 33 skills, 19 design, 8 investigations, 71 sources) and it
reproduces the prior pass exactly:

- **PR #11 cluster pages all valid:** `hooks/offload_push_guard.md` (well-formed, cross-linked to [[enforce_pr_workflow]]/[[unregistered_loop_guard]]/[[discipline-loop]] + its PR #108 source), `skills/verify-merged-pr.md`, and both sources (`pr_108_2026-07-08_offload-push-guard.md`, `pr_112-113_2026-07-08_jq-slurp-residuals-round2.md`) present with correct frontmatter. `unregistered_loop_guard.md`'s "Resolved residual" section (line 73) correctly links the closing PR #113. Verified against the raw plugin source: `offload_push_guard.sh` exists and is registered on both `Stop` and `SubagentStop` in `hooks.json`; `skills/verify-merged-pr/SKILL.md` exists.
- **Dead links: 0 real** (37 raw `[[...]]` regex hits, all benign — POSIX/bash syntax in fenced code, `[[wiki-links]]`/`[[page_name]]` schema meta-text, the `[[capabilities/send-gate]]` cross-vault reference explicitly labelled as assistant-agent-wiki, and historical `log.md` prose quoting pre-fix link states).
- **Orphans: 0. Missing frontmatter: 0. Bad/missing `last_updated`: 0.**
- **Stale (>30d): same 6 benign** immutable `source`/`investigation` pages from 2026-05-30…06-01; no evergreen page stale; no new staleness.
- **Contradictions: 0 live** — 14 pages contain the literal string but all are `log.md`/schema prose (`skills/wiki-lint.md`), source records documenting the contradiction-detection feature, a superseded-history `⚠️` note in `design/config-resolution.md:21`, or a `⚠️ PR-number collision` section heading in `comment_citation_gate.md:111`.
- **The one open finding is unchanged and out of wiki-lint's write scope:** `coderails/AGENTS.md`'s Part-1 "Hook event map" table still omits `offload_push_guard` (source-repo working guide, a different repo from this vault; wiki-lint documents raw source but does not edit it). Report-only, owner's call — same disposition as the prior pass.

Suggestions: (1) still-standing — add `offload_push_guard` to `coderails/AGENTS.md`'s Part-1 hook event map (source edit); (2) no wiki-side fixes needed — vault is clean.

## [2026-07-09] ingest | PR #118-123 Self-Improving Loops cluster (retro gate + dormant v2 promotion pipeline)

Created [[pr_118-123_self-improving-loops]] source page. Updated [[loop_stall_guard]] (retro-gate section), [[agentic-loop]] (Phase 13 write-contract + Phase 2 read-back + dormant v2 section), and index.md sources. Retro gate LIVE on main (643e0ad); v2 pipeline ships dormant behind graduation predicate. 6 PRs, single-loop session 15f29276.

## [2026-07-09] ingest | PR #124 dashboard Run Output panel — parse result instead of raw stream-json dump

Created [[pr_124_dashboard-run-output-result-extraction]] source page. Updated [[dashboard]] (Run Output panel section, sources list) and [[pr_80-82_dashboard-stream-run-output-viewer]] (caveat marked closed inline). Closes the "not consumed for anything beyond well-formed verification" caveat that source page flagged 2026-07-08 — a real user report (correct answer unfindable in the raw dump) supplied the case it predicted. Tier-1 task-evals GO (3 P0 + 2 P1); two independent review agents (code-reviewer + pr-test-analyzer) converged unprompted on the same two coverage gaps (multi-result last-match, non-string-result fallback), both closed same session. Single-fix session, PR #124 (c9db6da).

## [2026-07-10] lint | full-vault health pass — 2 uncovered shipped skills found (fable-mode, loop-retro-promotion)

Full independent re-scan (162 files: 12 commands, 15 hooks, 33 skills, 19 design,
8 investigations, 73 sources) — orphans, dangling links, staleness, and
contradictions all re-verified by direct scan (not just trusting prior log
entries) and reproduce the last full-vault pass exactly:

- **Orphans: 0.** Every non-index/log page has >=1 inbound `[[wiki-link]]` (Python scan).
- **Dangling links: 0 real.** ~193 raw `[[...]]` regex hits, all resolved to the
  same benign classes as before: bash `[[ ]]`/`[[:space:]]` test syntax inside
  fenced code blocks, `[[wiki-links]]`/`[[page_name]]` schema meta-text describing
  the link syntax itself, historical `log.md` prose quoting pre-fix link states,
  and the one explicitly cross-vault `[[capabilities/send-gate]]` reference to
  assistant-agent-wiki.
- **Stale (>30d): same 6 benign** — `install-cache-trap_2026-05-30`,
  `install-bash32-bad-substitution_2026-06-01`, and 4 `session_*` sources from
  2026-05-31/06-01. All immutable `source`/`investigation` records; no evergreen
  command/hook/skill/design page is stale.
- **Contradictions: 0 live** outside `log.md`/`skills/wiki-lint.md` schema prose.

**New finding — skill-catalog coverage gap (not present in prior passes):**
Comparing the wiki's `skills/*.md` pages against the plugin's actual
`coderails/skills/` directories found two shipped skills with **zero wiki
documentation**:
- `fable-mode` (shipped 2026-07-08, commit `ee8f6ed`) — not in `index.md`'s
  skill table, not in its "what this plugin is" skill list, no
  `skills/fable-mode.md` page, and zero mentions anywhere in the vault.
- `loop-retro-promotion` (shipped 2026-07-09, commit `1ff2b4e`, part of
  [[pr_118-123_self-improving-loops]]) — named in `index.md`'s design-section
  prose, the PR #118-123 source page, and a `dashboard-runs/` routine-run note,
  but has no dedicated `skills/loop-retro-promotion.md` page and is absent from
  `index.md`'s skill table. The source page itself says PR #123's docs sync
  brought the skill count to "34 skills," but the wiki's own skill table was
  never updated to 34 rows.

Both are real gaps (verified against `coderails/skills/` on disk, not
inferred) — distinct from the intentional `sync-docs` entry (external,
documented as such). Command/hook parity is unaffected: 12/12 commands,
15/15 hooks still fully covered.

**Still-open, unchanged, out of wiki-lint's write scope:** `coderails/AGENTS.md`'s
Part-1 "Hook event map" table still omits `offload_push_guard` (source-repo working
guide, a different repo from this vault; wiki-lint documents raw source but does
not edit it) — same disposition as the last two passes, re-verified by direct grep.

Suggestions: (1) create `skills/fable-mode.md` and `skills/loop-retro-promotion.md`
using `templates/skill.md`, add both rows to `index.md`'s skill table (33->35,
matching the plugin's actual 34 shipped skills + external `sync-docs`), and
cross-link `loop-retro-promotion` from [[pr_118-123_self-improving-loops]] and
[[agentic-loop]] where it's already named in prose; (2) still-standing — add
`offload_push_guard` to `coderails/AGENTS.md`'s Part-1 hook event map (source edit,
owner's call).

## [2026-07-10] ingest | PR #128 dashboard "auto" permission profile for the ask button

Created [[pr_128_dashboard-ask-button-auto-profile]] source page. Updated [[dashboard]]
(profile→flag mapping table gains `auto`; "ask" button description updated from
`profile: "standard"` to `profile: "auto"` with the tool-call non-determinism caveat)
and [[pr_124_dashboard-run-output-result-extraction]] (forward-pointer noting this PR
as the third occurrence of independent-reviewer convergence). PR #128 (`7f60d96`) adds
a fourth `PermissionProfile` value, `"auto"`, mapped to `--permission-mode auto`, fixing
the "ask" button's headless permission-block hang on tools needing a fresh grant (e.g.
`WebSearch`) — but explicitly does **not** fix the separate, pre-existing non-determinism
of whether the model attempts a tool call at all (1/3 real tool calls in this session's
own empirical re-test, matching the PR's own `argv.ts:40-49` code-comment finding). Two
independent PR review agents (code-reviewer + pr-test-analyzer) converged unprompted on
the same missing auto+free-text test-coverage gap — third such occurrence in this
project's history, after [[pr_39_agentic-loop-slim-v2]] and
[[pr_124_dashboard-run-output-result-extraction]]. `pr_70-71_2026-07-07...` source page
left unmodified per the immutable-record convention (it documented the true state at its
own merge time); the superseding claim lives only on the living [[dashboard]] page.

## [2026-07-10] lint | scoped pass on PR #128 ingest — clean, 0 findings

Verified the PR #128 ingest ([[pr_128_dashboard-ask-button-auto-profile]], updated
[[dashboard]], updated [[pr_124_dashboard-run-output-result-extraction]], updated
`index.md`) is structurally sound. All `[[wiki-links]]` introduced across the 4 touched
files resolve to real pages (checked every link in the new source page, all links in
[[dashboard]], all links in [[pr_124_dashboard-run-output-result-extraction]], and the
two new index.md lines). No new orphans — the new page has 4 inbound links (index.md,
log.md, [[dashboard]], [[pr_124_dashboard-run-output-result-extraction]]). No
contradictions: [[dashboard]]'s two edited sections (profile→flag table + the new "auto
closes a headless-only hang" paragraph + the "ask" button paragraph) are mutually
consistent and correctly frame the switch as historical progression
(`profile: "standard"` → `profile: "auto"`), not a live conflict. The
[[pr_70-71_2026-07-07_dashboard-input-fix-and-voice-announcements]] page's now-superseded
`profile: "standard"` claim is untouched per the immutable-source-record convention, and
the superseding fact is correctly carried only on the living [[dashboard]] page and noted
explicitly in this session's own log entry above — reads as historical, not a
contradiction. Diff scope matched exactly the 5 files expected (index.md, log.md,
skills/dashboard.md, sources/pr_124_dashboard-run-output-result-extraction.md,
sources/pr_128_dashboard-ask-button-auto-profile.md). Out of scope, not re-checked here:
the fable-mode / loop-retro-promotion missing-page gaps already flagged by the
2026-07-10 full-vault pass above.

## [2026-07-11] lint | Full-vault health pass (163 files: 12 commands, 15 hooks, 33 skills,
19 design, 8 investigations, 74 sources) — 1 new contradiction, 8-PR ingest backlog, 2 known gaps unchanged

**NEW — CONTRADICTION, `skills/sync-docs.md` (top finding, distinct from the coverage
backlog below):** the page asserts, as verified fact with an inline "confirmed" citation
(lines 19-29, 82-84), that `/sync-docs` is "a user-level skill... **not** part of the
`coderails` plugin repo" and that "coderails does not vendor or ship it." This was true
when written (2026-07-06) but is now **false**: PR #129 ("import sync docs skill", merged
2026-07-10T20:00:52Z — same day as the last full-vault lint, before that pass's own
timestamp window closed) added `skills/sync-docs/SKILL.md` directly into the coderails
plugin repo (verified: `find /Users/harrison/Github/coderails -iname '*sync-docs*'` now
returns `coderails/skills/sync-docs` on disk; `gh pr view 129` confirms the single file
`skills/sync-docs/SKILL.md` added, commit `f488ca5`, title "Import sync-docs as a
coderails plugin skill"). The page's own "Relationship"/"See also" sections and the
index.md skill-table footnote calling out sync-docs as "external (30 coderails-owned +
sync-docs)" likely need the same correction. Not fixed in this pass — page-content
rewrites are ingest's job (per Step 7, lint doesn't chain into ingest); flagging for a
PR #129 ingest to pick up, since #129 has no source page yet (see next finding).

**NEW — 8-PR ingest backlog, none previously flagged:** `gh pr list --state merged`
shows PRs #129-136 all merged 2026-07-10, none with a `sources/pr_<N>_*.md` page or a
log.md ingest entry (checked all 8 individually — zero hits). #129 is the sync-docs
vendoring above. #130-136 is the "dashboard UX loop" cluster (button state diff, label
wrapping, agentic-loop retry-until-green, panel separation, output viewer context, run
history status glyph, input affordance) — per this session's own MEMORY.md handoff notes
these shipped as 7 independently-verified PRs, but that verification never produced wiki
source pages. Missing-concepts/missing-cross-references checks deferred pending this
ingest — new terminology from an 8-PR unread backlog can't be meaningfully assessed
against existing pages yet; re-run those checks after #129-136 land.

**Unchanged from 2026-07-10 pass (re-verified, still open):** `skills/fable-mode.md` and
`skills/loop-retro-promotion.md` still don't exist (plugin `skills/` dir has 34 entries,
wiki skill table has 33; diffed both listings directly). `coderails/AGENTS.md`'s Part-1
hook map still omits `offload_push_guard` — source-repo edit, out of wiki-lint's write
scope, owner's call.

**Re-verified clean (direct scan, not trusted from prior entries):** 0 orphans (Python
inbound-link scan over all 163 non-index/log/AGENTS pages). ~194 raw `[[...]]` regex hits,
all resolved to the same benign classes documented in every prior pass: bash `[[ ]]`/
`[[:space:]]` test syntax in fenced code blocks, `[[wiki-links]]`/`[[link]]`/`[[page_name]]`
schema meta-text, historical log.md prose quoting pre-fix link states, and the one
cross-vault `[[capabilities/send-gate]]` reference (explicitly labelled as targeting
assistant-agent-wiki, confirmed the target file exists there). 0 dangling links. 6 stale
pages, same set as every prior pass (`install-cache-trap_2026-05-30`,
`install-bash32-bad-substitution_2026-06-01`, 4 `session_*` sources from 2026-05-31/06-01)
— all immutable `source`/`investigation` records, expected per page-type convention, not
fixed. 0 live `⚠️ CONTRADICTION` flags outside log.md/wiki-lint.md schema prose (the new
sync-docs contradiction above has no such flag emoji in-page; found by direct claim
verification, not by grepping the marker). Commands 12/12, hooks 15/15 parity confirmed
against plugin source (`diff` of both directory listings, both empty). Inbox: doesn't exist.

Suggestions: (1) run `/wiki-ingest` for PR #129 (sync-docs vendoring) and the #130-136
dashboard-UX cluster — 8 source pages, plus the sync-docs.md rewrite and index.md skill-
table/count update as part of #129's ingest; (2) author `skills/fable-mode.md` and
`skills/loop-retro-promotion.md` per `templates/skill.md` (still pending since 2026-07-10);
(3) once #129-136 are ingested, re-run missing-concepts/missing-cross-references checks
against the new content — deferred this pass for the reason above; (4) still-standing,
owner's call — add `offload_push_guard` to `coderails/AGENTS.md`'s hook event map.

## [2026-07-11] ingest | Three-source cluster ingest for the 2026-07-10/11 session (closes the #130-136 suggestion from the prior lint pass; #129 sync-docs vendoring remains open — not part of this session's own work). (1) PRs #130-136: six independent dashboard right-rail UX fixes (panel separation, input affordance, label wrapping, run-history status glyphs, output-viewer header, button-state completion flash) shipped under a registered agentic-loop run, with a design-agent scope-drift incident (2 unscoped substitutions caught and rejected via user forced-choice) worth keeping. (2) PR #134: a mid-loop agentic-loop skill edit — retry-until-green bounded auto-fix cycle, systematic-debugging/dispatching-parallel-agents/disconfirm wiring, Phase 13 hardening, file-size extraction to retry-until-green.md — triggered by an explicit user request for zero-question autonomy that the orchestrator narrowed rather than granted in full (refused to remove the skill's 4 hard-stops). (3) PR #138: removes `docs/coderails/specs/` and `docs/coderails/plans/` (20 files) from repo tracking entirely, superseding the "permanent record" framing this wiki and `docs/REFERENCE.md` previously documented for both directories — a genuine convention reversal, not an addition. Created: sources/pr_130-136_dashboard-right-rail-ux.md, sources/pr_134_agentic-loop-retry-until-green.md, sources/pr_138_remove-specs-plans-tracking.md. Updated: skills/dashboard.md (new "Right-rail UX cluster" section, a stale-build-gotcha note from live observation this session, design-spec/plan lines corrected to point at PR #138, See Also ×3), skills/agentic-loop.md (Stop-conditions section gains retry-until-green subsection, Phase 13 section gains a concrete SO-2/SO-3 example, See Also ×3), skills/brainstorming.md (⚠️ supersession box + step 6 rewrite: spec now session-local, not committed; isolation step removed), skills/writing-plans.md (direct-invocation plan storage superseded; loop-scope `plan.md` explicitly noted as unaffected), index.md (3 new source entries, 3 last_updated bumps). Sources 40→43 (approximate; not recounted from a fresh directory listing this pass).

## [2026-07-11] lint | Post-ingest lint of the #130-136/#134/#138 cluster. 2 findings, both fixed same pass. (1) `skills/dashboard.md` line 25 still read "see the design spec's decisions log for the other four" — a live present-tense pointer to `docs/coderails/specs/2026-07-06-observability-dashboard-design.md`, the exact file the supersession box three lines below (added by the same ingest pass) says no longer exists. Contradiction introduced by the fixing edit itself; reworded to point at the sub-project pages instead of the removed spec's decisions log. (2) `skills/task-evals.md` (not one of the 4 pages the ingest pass touched) has two "Design spec: `coderails/docs/coderails/specs/2026-07-03-task-evals-design.md`" pointers (a "Source" line near the top, and a "## Source" section near the bottom) presenting the same PR #138-removed file as a live, currently-existing path — the exact dangling-reference pattern PR #138 fixed in `docs/REFERENCE.md` and the other skill pages, missed here because the ingest pass's scope was the 3 pages that name-checked the spec/plan paths literally, and grep for the literal path across the full vault is what caught it. Fixed: both lines now note the spec was removed by PR #138, cite `[[pr_138_remove-specs-plans-tracking]]`, and point at the skill's own SKILL.md as the current source. Both fixes are consistent with the pattern the other 4 already-touched pages use (a dated note pointing at PR #138, not silent deletion of the historical fact that a spec once existed). Vault-wide grep for `docs/coderails/specs`/`docs/coderails/plans` also hit `design/review-artifact-seam.md` and `design/task-evals-gate.md` (both cite the removed spec only in `sources:` frontmatter, i.e. historical provenance, not a live claim — no fix needed), 2 `investigation`-type pages dated 2026-07-07 (point-in-time snapshots, correctly pre-dating the removal — no fix needed per schema: investigations "may be superseded", not retroactively rewritten), and 13 `source`-type pages (immutable PR records citing what existed at merge time — no fix needed, sources are never edited to erase history). Standard checks on the 3 new + 4 updated pages: no orphans (all 3 new source pages have ≥3 inbound links plus an index.md row), no broken `[[links]]` (every wiki-page link target resolves; the one non-resolving link, `[[feedback_never_remove_hard_stops]]` in pr_134's source page, is explicitly marked "(memory)" in the prose, not a wiki-page claim), no staleness (all `last_updated` dates are 2026-07-11, today). One schema deviation flagged, not fixed this pass (deferred — a frontmatter-shape question, not a content-accuracy bug the ingest pass caused): all 3 new source pages (`pr_130-136_dashboard-right-rail-ux.md`, `pr_134_agentic-loop-retry-until-green.md`, `pr_138_remove-specs-plans-tracking.md`) use `origin:`/`date:` frontmatter fields instead of the schema-standard `created:`/`last_updated:`/`sources:` triad every other source page and `templates/source.md` use — verified via direct frontmatter grep against 4 older source pages and the template. Practical effect: these 3 pages are invisible to any future `last_updated`-based staleness check (the field doesn't exist to check). Recommend a follow-up pass adding `created`/`last_updated`/`sources: []` to all 3, or updating `templates/source.md` if `origin`/`date` is meant to become the new convention — the AGENTS.md evolution-note rule (schema changes go in AGENTS.md first) means this needs an explicit decision, not a silent lint-pass fix.

## [2026-07-11] ingest | PRs #139-141 merged: dashboard ASK-button UX + run-output clean view cluster (theme, not one-per-PR — #139 Enter-to-submit on the ask input; #140 `projectAssistantText()` clean-by-default projection of live run output + live-only raw/clean toggle; #141 CSS readability of the run-output box and the new toggle). Design fact recorded: the toggle's live-only scoping respects a pre-existing live(raw SSE)/settled(server-extracted via `extractResultText`, PR #124) asymmetry — caught by independent PR review, encoded as an explicit negative test rather than left implicit. Created: sources/pr_139-141_dashboard-ask-enter-clean-output.md. Updated: skills/dashboard.md (Run Output viewer section extended with two new subsections, sources/tags frontmatter, See Also), index.md (1 new source entry).

## [2026-07-11] lint | Scoped lint of the just-ingested #139-141 cluster (sources/pr_139-141_dashboard-ask-enter-clean-output.md, skills/dashboard.md's new sections, index.md's new entry). 1 finding, fixed same pass: [[pr_80-82_dashboard-stream-run-output-viewer]]'s caveat section (lines 140-141) made a present-tense claim — "the panel still renders a still-live run's output as opaque accumulated chunk text (unchanged, out of that fix's scope)" — that this cluster's PR #140 directly falsifies: live output is now `projectAssistantText`-projected clean by default, with a live-only raw toggle. Fixed by appending a strikethrough + "SUPERSEDED 2026-07-11" note in the same style as that page's existing PR #124 closure note, linking to [[pr_139-141_dashboard-ask-enter-clean-output]]; `last_updated` bumped 2026-07-08 → 2026-07-11. Also reconciled pr_139-141's own "Wiki pages updated" line for pr_80-82 (previously said "no content change needed," now describes the note added). No other findings in scope: no orphans (pr_139-141 has 3 inbound links — index.md + 2 within dashboard.md's body — plus the See Also entry); no broken `[[links]]` in the new page (all 3 targets resolve); no staleness (pr_139-141 and dashboard.md both dated 2026-07-11, today); [[pr_124_dashboard-run-output-result-extraction]] checked against the same supersession question and found NOT affected — this cluster only touches the live path, pr_124's settled-path extraction is unchanged and all its caveats remain accurate, so it was correctly left untouched. Unrelated vault findings (if any) deferred per this skill's scope instruction.

## [2026-07-12] lint | Full-vault health check (not scoped to one ingest). 167 pages across commands(12)/hooks(15)/skills(33)/design(19)/investigations(8)/sources(78). Structural integrity strong: 0 orphans (every page has ≥1 inbound `[[link]]`), 0 genuine broken links (all real wiki-page targets resolve; the only non-resolving targets are bash `[[ ]]` test snippets and doc-example placeholders inside skill/doc prose, plus `[[feedback_never_remove_hard_stops]]` in pr_134 which is explicitly labelled "(memory)" — an intentional external ref, not a wiki-page claim), 0 stale evergreen pages (no command/hook/skill/design page has `last_updated` older than the 30-day threshold; oldest is 2026-06-25). Coverage 1:1 for hooks (15/15) and commands (12/12) against plugin source. 2 findings fixed this pass, 2 reported for follow-up. FIXED (1) contradiction-with-newer-source: `design/spec-plan-progress-artifact-chain.md` line 40 still claimed "the shareable record is the committed spec under `docs/` (for a real product)" and cited `coderails/docs/superpowers/specs/` as committed repo artifacts — both now false. PR #138 (2026-07-11) gitignored `docs/coderails/specs/`+`docs/coderails/plans/`, and `docs/superpowers/` was removed entirely; verified there are now ZERO committed specs/ dirs anywhere in the repo (`git ls-files | grep specs/` empty). The prior 2026-07-11 lint's literal-string grep for `docs/coderails/specs` missed this because the page cited a *different* path (`docs/superpowers/specs/`) and a generic `docs/`. Reworded line 40 to state the shareable record is `coderails:handoff` or a wiki page, noting the removed dirs and citing `[[pr_138_remove-specs-plans-tracking]]`; bumped `last_updated` 2026-07-06→2026-07-12 and added pr_138 to `sources:`. NB the page's core claim — the agentic-loop's OWN `spec.md`/`plan.md` live uncommitted in `~/.claude/agentic-loop/` loop-state — was and remains correct and is a distinct mechanism from the #138-removed committed skill artifacts; only line 40's shareable-record aside was stale. FIXED (2) resolves the schema deviation the 2026-07-11 lint deferred: the 3 source pages `pr_130-136_dashboard-right-rail-ux.md`, `pr_134_agentic-loop-retry-until-green.md`, `pr_138_remove-specs-plans-tracking.md` used a non-schema `date:` field and omitted `created:`/`last_updated:`/`sources:`. Conformed all 3 to the schema/template shape (`created` + `last_updated` from the old `date`, `sources: []`), keeping their `origin:` provenance field. Chose schema-conform over schema-change because 75/78 sources + `templates/source.md` already use the triad — no evolution-note schema decision was warranted. All 78 sources now carry `created`+`last_updated`. REPORTED (not fixed — ingest's job, not lint's): 2 plugin skills have no wiki page — `fable-mode` (added PR-era 2026-07-08) and `loop-retro-promotion` (2026-07-09, `user-invocable: false`, dormant). `loop-retro-promotion` is mentioned in `sources/pr_118-123_self-improving-loops.md` and `skills/agentic-loop/SKILL.md` but has no `skills/` page; `fable-mode` is undocumented in the wiki entirely. Both are `/wiki-ingest` candidates, flagged for a follow-up ingest pass; neither is in index.md's skill list.

## [2026-07-12] lint | Second full-vault pass same day, script-assisted (deterministic link-graph script cross-checked against manual grep, then a subagent pass for content-judgment categories). 166 pages (commands 12, hooks 15, skills 33, design 19, investigations 8, sources 79 — one more source than the earlier pass today, from routine ingest activity between passes). Re-confirmed 0 orphans (inbound-link counting must include frontmatter `sources:` arrays and `index.md` catalog rows, not just body `[[wiki-links]]`, or 3 false orphans appear: `skills/handoff.md`, `sources/pr_19-30_self-containment-and-hardening.md`, `sources/pr_49_gate-function-rename.md` — all 3 verified referenced via index.md and/or other pages' `sources:` frontmatter). Re-confirmed the "dead link" list is entirely the known benign classes (POSIX `[[:space:]]`/bash-test-syntax snippets, `[[index]]`/`[[log]]` — real files just excluded from the page schema, `[[feedback_never_remove_hard_stops]]` — a memory file correctly marked "(memory)", `[[capabilities/send-gate]]` — a real page in the sibling `assistant-agent-wiki`, correctly annotated inline). 0 active `⚠️ CONTRADICTION` flags found (the word only appears in log.md's own historical narration and in skills/wiki-lint.md documenting the check itself). FIXED (3, all mechanical): (1) 3 `source`-type pages missing the `sources:` field entirely (distinct from this morning's `date:`→triad conversion, which was a different 3 pages) — `sources/session_2026-05-31_verify-loop-hardening.md`, `sources/session_2026-06-01_agentic-loop-delegate-all-impl.md`, `sources/session_2026-06-01_verify-loop-total-enforcement.md` — added `sources: []` (each page's own `origin:` field already names its direct-edit/commit provenance; no other wiki page informed them). `last_updated` left unchanged (schema-compliance addition, not a content update, and these are point-in-time session records). (2) `design/discipline-loop.md` named "agentic-loop" 5 times (loop_state_guard/loop_stall_guard/unregistered_loop_guard descriptions) without ever linking `[[agentic-loop]]` — added the link at first mention (line 144), `last_updated` bumped 2026-07-08→2026-07-12. (3) `design/agentic-loop-path-keying.md` named "agentic-loop orchestrator" without linking it despite the whole page being about that skill's execution mechanics — added `[[agentic-loop]]` at the "Where it is enforced" mention, `last_updated` bumped 2026-07-08→2026-07-12. REPORTED (not fixed, content-authorship scope): (1) `scripts/lib/git-common.sh` is referenced across 23 wiki files (index.md, design/enforcement-model.md, design/trust-floor.md, design/task-evals-gate.md, design/review-artifact-seam.md, 15+ sources/ PR records) covering trust-floor identity checks, main-branch fallback, `require::repo` GitHub validation, force-with-lease helpers — but unlike its sibling libs (`eval-artifact.sh`→task-evals-gate.md, `review-artifact.sh`→review-artifact-seam.md, `agentic_loop_path.sh`→agentic-loop-path-keying.md) it has no dedicated design page. Candidate for a new `design/git-common-lib.md` page; deferred as new-page authorship, not a lint-scope fix. (2) fable-mode/loop-retro-promotion missing skill pages — same 2 gaps this morning's pass already flagged, unchanged, not re-fixed. STALE (unchanged from every pass since 2026-07-06, all point-in-time by design per AGENTS.md): `investigations/install-cache-trap_2026-05-30.md`, `investigations/install-bash32-bad-substitution_2026-06-01.md`, `sources/session_2026-05-31_verify-loop-hardening.md`, `sources/session_2026-05-31_prompting-doc-alignment.md`, `sources/session_2026-06-01_verify-loop-total-enforcement.md`, `sources/session_2026-06-01_agentic-loop-delegate-all-impl.md`. 0 inbox backlog (no `inbox/` directory). PRs #145-149 (this session's own dashboard/grading-hardening tasks, merged today) not yet in `sources/` — expected, `/wiki-ingest` territory, not a lint finding (lint doesn't trigger ingest per this skill's Step 7 one-way pairing).

## [2026-07-13] lint | Full-vault pass, script-assisted link-graph + coverage diff against plugin source. 168 pages (commands 12, hooks 15, skills 33, design 19, investigations 8, sources 79, + index/log). Structural core re-confirmed clean: 0 orphans, 0 genuine broken links (same benign classes as prior passes — bash `[[ ]]`/`[[:space:]]` snippets, schema meta-text, the annotated `[[capabilities/send-gate]]` cross-vault ref and `[[feedback_never_remove_hard_stops]]` memory ref), 0 active `⚠️ CONTRADICTION` flags, hooks 15/15 and commands 12/12 parity, no inbox/ dir. FIXED (6): (1) contradiction-with-newer-source in `skills/task-evals.md` rule 3 — still stated end-state surfaces as "merged state, fresh clone, or deployed artifact" only, which PR #154 (2026-07-13, merged main `4486fdf`) superseded by admitting a locally built artifact driven directly (rule 6's pr-scope `artifact-path`); reworded to match SKILL.md. (2) Same page brought current with the rest of #153/#154: rule 5 gains the amended-after-verdict→fresh-grader extension, rule 6 names `artifact-path` and the `deployed` discriminator, tier-0 predicate gains the rule-6-names-outward reconciliation (user-facing = minimum tier 1, tier-2 predicate unwidened), verifier-contract section gains the regrade-on-amendment backstop (`amendments_at_grade`, `regraded_by` refusal, stated blind spots); `last_updated` 2026-07-12→2026-07-13. The lockstep #153/#154 edits had reached `design/task-evals-gate.md` (2026-07-13 01:39) but not this sibling skill page. (3) `commands/post-evals.md` "Honest ceiling" still said "the five anti-gaming rules" — six since PR #145; corrected with dating. (4) Same page's script list implied `post_evals.sh` has two subcommands — noted the third (`grade-loop`, PR #144, loop-scope, direct-invoked, documented on [[task-evals-gate]]); `last_updated` 2026-07-06→2026-07-13. (5) `design/routines.md` frontmatter said `last_updated: 2026-07-08` while its born-red `loop-retro-promotion-weekly` incident section (PRs #151+#152) was added 2026-07-13 00:33 — bumped to 2026-07-13. (6) `index.md` frontmatter said 2026-07-11 while its last content edit (pr_144-149 catalog row) was 2026-07-12 22:43 — bumped to 2026-07-12. Remaining "five anti-gaming rules" strings verified confined to immutable source records (pr_1-4, pr_7-10) where they are correct point-in-time history. NOTED (log hygiene, not rewritten — append-only): the 2026-07-12 second-lint entry above claims "PRs #145-149 not yet in sources/", but `sources/pr_144-149_agentic-loop-hardening-from-loop-engineering.md` was committed 16 minutes before that entry (22:25 vs 22:41) — written from stale context; no separate ingest log entry for that source page exists either. REPORTED (ingest's job, not lint's): (1) PRs #150 (docs sync), #151/#152 (routine deploy+invocability fix), #153/#154 (regrade backstop + rule-6/tier-0 artifact-path docs) all merged with design-page lockstep edits but NO `sources/` pages and no log ingest entries — `/wiki-ingest` backlog; the #151/#152 incident narrative currently lives only inside `design/routines.md`. (2) `fable-mode` and `loop-retro-promotion` skill pages still missing (third consecutive flag; 33/35 skill coverage). (3) `design/git-common-lib.md` candidate still open (second flag, 23 referencing files). STALE (unchanged, point-in-time by design): same 6 investigation/session pages as every pass since 2026-07-06. Process note: this pass's commit was blocked once by coderails' `test_gate` firing cross-repo on the wiki commit (the hook resolves `.claude/test_command` from cwd, which was the coderails checkout) and failing on `install_mode_sweep.test.sh` — root-caused to the session's hook environment missing `/opt/homebrew/bin` on PATH (no `gh` → install.sh preflight exits 1); the full suite passes 38/38 with `gh` on PATH, so coderails main is green and the block was environmental, not a regression.

## [2026-07-13] ingest | PRs #156 and #159 merged: DNV-presence check + discipline_catchup retirement/telemetry cluster (one hook-rethink loop; #155/#157/#158 are a concurrent session's PRs, referenced not duplicated here). PR #156 (`4fbb5fe`): `check_verify_loop.sh` gains a presence check closing the omission-beats-honesty inversion (missing `## Did Not Verify` header after `>= 3` files edited this turn now blocks, same as an untagged bullet); `dc_file_count` re-scoped session-cumulative → TURN-scoped and jq-hardened (closes the last of 3 known jq-slurp fragility instances); wired through PR #155's loop-demotion predicate. PR #159 (`1ac2166`): `discipline_catchup.sh` retired clean-break (file+test deleted, `hooks.json` UserPromptSubmit down to `inject_context.sh` only) on a null-result justification (flat 23-26% first-attempt miss rate unchanged since block-mode 2026-05-05); `event=<hook_event_name>` telemetry added to both remaining discipline hooks' log lines; `check_confidence_labels.sh` block message rewritten to match `check_verify_loop.sh`'s style; `plugin.json`/`marketplace.json` version lockstep repaired to `1.1.5` (PR #155 had bumped only one file). Same-day commit `109987b` hardened 3 review findings in the new tests (matcher-array-length, vacuous-version-pass, block-message stderr coverage) — folded into the PR #159 source page rather than a separate entry, since it's a same-PR-scope follow-up on `main`, not an independent PR. Created: `sources/pr_156_dnv-presence-check.md`, `sources/pr_159_retire-catchup-add-telemetry.md`. Updated: `hooks/check_verify_loop.md` (presence check, turn-scoped file_count, loop-demotion section, event= telemetry, History gen 5), `hooks/check_confidence_labels.md` (loop-demotion note referencing PR #155, block-message rewrite, event= telemetry), `hooks/discipline_catchup.md` (marked RETIRED with a dated banner — first use of this convention for a whole-page retirement, no prior "retired hook" pattern existed in the vault), `design/discipline-loop.md` (Stop-hook composition, gate-chain table, Advisory-vs-Enforced table row correction, shared-library consumer count 3→2, timeout-floor test hardening note, Cross-References), `design/install-and-cache-trap.md` (corrected the "version permanently pinned at 1.0.0, no semver" claim — actual state is semver-bumped, 1.1.5 live, multiple versioned cache directories observed, `claude plugin update` functional), `index.md` (hooks table: discipline_catchup row struck through and marked retired, check_confidence_labels/check_verify_loop rows updated, Stop-hook-order paragraph's "three discipline hooks" corrected to two, two new Sources entries, frontmatter date bump).

## [2026-07-13] lint | Scoped lint of the just-ingested PR #156/#159 cluster (the 2 new source pages + 5 updated pages: `hooks/check_verify_loop.md`, `hooks/check_confidence_labels.md`, `hooks/discipline_catchup.md`, `design/discipline-loop.md`, `design/install-and-cache-trap.md`, plus `index.md`). No orphans: both new source pages have inbound links from `index.md`'s Sources list plus `sources:` frontmatter references from at least 2 updated pages each. No broken `[[links]]`: every new cross-reference target (`[[pr_156_dnv-presence-check]]`, `[[pr_159_retire-catchup-add-telemetry]]`, `[[discipline_catchup]]`, `[[check_verify_loop]]`, `[[check_confidence_labels]]`, `[[agentic-loop]]`) resolves to an existing page. No staleness: all 7 touched pages carry `last_updated: 2026-07-13`. One deliberate convention decision, not a finding: `hooks/discipline_catchup.md` is the vault's first whole-page hook retirement — no prior page had gone from "live" to "describes something deleted," so the strikethrough-banner pattern used elsewhere for superseded *sections* (e.g. `design/task-evals-gate.md`) was extended to a full-page banner rather than deleting the page outright, consistent with AGENTS.md's "sources are never edited to erase history" norm for source-type pages, generalised here to a retired hook page. Cross-checked for contradiction with PR #155/#157/#158 (concurrent session's cluster, out of this ingest's scope): `design/discipline-loop.md`'s loop-demotion description is consistent with what `check_confidence_labels.sh`/`check_verify_loop.sh` actually contain on `main` as of this session (verified by reading both scripts directly) — no fix needed, but flagging that a full ingest of #155/#157/#158 themselves (their own source pages, their own dedicated wiki sections) is still owed and is NOT done by this pass; this pass only references them where the #156/#159 content directly touches the same code paths. REPORTED (not fixed, deferred to whoever ingests #155/#157/#158): those three PRs currently have no `sources/` pages of their own; `design/discipline-loop.md`'s loop-demotion material was written by this pass off direct script inspection, not off a #155 source page, since none exists yet — a future #155/#157/#158 ingest should reconcile against what's now in `discipline-loop.md` rather than duplicating it. Deferred per this skill's scope instruction (fix only what's in this cluster).

## [2026-07-13] lint | Full-vault pass, two-subagent independent re-verification (not trusting prior log entries — re-derived from source files) plus a live-coordination check against a concurrent in-session agent (`wiki-boundary-pass`) that was actively ingesting mid-pass. **Structural core: fully clean, independently re-confirmed.** 0 orphan pages (two separate implementations — bash+grep and Python regex — scanning all 169 pages for inbound `[[links]]`/index.md rows/`sources:` frontmatter references, zero candidates; the 3 pages a naive body-only-link scan would false-flag — `skills/handoff.md`, `sources/pr_19-30_self-containment-and-hardening.md`, `sources/pr_49_gate-function-rename.md` — are correctly excluded once frontmatter `sources:` arrays count as inbound). 0 genuine broken links (217 raw `[[...]]` hits vault-wide, 196 in log.md's own historical narration excluded as non-live, remaining 21 all resolve to the same benign classes documented in every prior pass — bash `[[ ]]`/`[[:space:]]` test syntax, schema meta-text, the annotated `[[capabilities/send-gate]]` and `[[feedback_never_remove_hard_stops]]` cross-vault/memory refs). 0 stale evergreen pages (all commands/hooks/skills/design pages `last_updated >= 2026-06-13`, i.e. within the 30-day window). 0 frontmatter schema violations (exhaustive sweep of all 169 pages for the `title`/`type`/`created`/`last_updated`/`sources`/`tags` sextet, zero missing fields — supersedes the need for further spot-checks). 0 active `⚠️ CONTRADICTION` flags. Hooks 15/15, commands 12/12 parity vs. plugin source reconfirmed.

**MID-FLIGHT INGEST OBSERVED, NOT DUPLICATED:** partway through this pass, `sources/pr_155-158_ceremony_noise_envelope_anchoring.md` (a 3-PR cluster: #155 loop-scoped warn demotion, #157 LOOP-STOP bundle warn-era prose, #158 envelope-anchored eval authoring) appeared on `main` (commit `fd8b9f7`, 14:38) along with content-complete updates to `design/task-evals-gate.md`, `design/discipline-loop.md`, `hooks/check_confidence_labels.md`, `hooks/check_verify_loop.md`, and `skills/agentic-loop.md` (through HEAD `172234e`, 14:41) — committed by a concurrent same-session agent (`wiki-boundary-pass`), not this pass. Read the new source page and confirmed it's well-formed (correct frontmatter triad, confidence-labelled claims, cites `hooks/scripts/check_confidence_labels.sh:62-77` and `check_verify_loop.sh:150-161,225-238` for its core mechanism claim) — no contradiction found with anything else in the vault. **No log.md ingest entry exists yet for this cluster** as of this pass; messaged the other agent to ask whether it was still active before touching any of the same files (no reply received in ~45s of polling, HEAD did not advance further) — deliberately left all 6 of those files untouched this pass to avoid a commit race or a duplicate/conflicting log entry. This closes the PR #157/#158 portion of the previously-reported ingest backlog (only #150-154 remain genuinely un-ingested now), but the closing log.md entry for #155-158 is still owed by whoever finishes that ingest.

**PERSISTENT BACKLOG, STILL TRUE (unchanged despite this session's #155-158 progress) — all ingest-scope, not lint-scope, per this skill's own convention of reporting rather than authoring new pages:**
1. **`skills/fable-mode.md` and `skills/loop-retro-promotion.md` still missing.** Both `skills/fable-mode/SKILL.md` and `skills/loop-retro-promotion/SKILL.md` exist in the plugin source; neither has a wiki page. Fourth consecutive lint pass to flag this (2026-07-12 ×2, 2026-07-13 ×2 including this one) — 33/35 skill coverage, unchanged.
2. **`design/git-common-lib.md` still missing.** `scripts/lib/git-common.sh` (16791 bytes, unchanged since 2026-07-06) is referenced across 29 wiki files as of this pass (up from the 23 last counted 2026-07-12) — trust-floor identity checks, main-branch fallback, `require::repo` GitHub validation, force-with-lease helpers — but has no dedicated design page unlike its siblings (`eval-artifact.sh`→[[task-evals-gate]], `review-artifact.sh`→[[review-artifact-seam]], `agentic_loop_path.sh`→[[agentic-loop-path-keying]]). Third consecutive flag.
3. **PRs #150–154 still un-ingested.** #150 (docs sync), #151/#152 (routine deploy+invocability fix, narrative currently lives only inside `design/routines.md`), #153/#154 (regrade backstop + rule-6/tier-0 artifact-path docs, already reflected in `skills/task-evals.md`/`design/task-evals-gate.md` via lockstep edits but with no dedicated source page recording provenance) — no `sources/` pages, no log ingest entries. Narrowed from the prior "#150-158" framing now that #155/#157/#158 have a source page (pending only its log entry, see above).
4. **PR #160 confirmed doc-only, correctly not source-paged.** `gh pr view 160 --json files` shows only `AGENTS.md`/`INSTALLATION.md`/`README.md`/`docs/REFERENCE.md` touched (7/2/4/2 lines), retiring stale `discipline_catchup` references left behind by PR #159's ingest — no behaviour change, consistent with how other pure doc-sync PRs have been treated (no dedicated source page).

STALE (unchanged, point-in-time by design per AGENTS.md, same 6 pages every pass since 2026-07-06): `investigations/install-cache-trap_2026-05-30.md`, `investigations/install-bash32-bad-substitution_2026-06-01.md`, `sources/session_2026-05-31_verify-loop-hardening.md`, `sources/session_2026-05-31_prompting-doc-alignment.md`, `sources/session_2026-06-01_verify-loop-total-enforcement.md`, `sources/session_2026-06-01_agentic-loop-delegate-all-impl.md`. 0 inbox backlog (no `inbox/` directory).

## [2026-07-13] ingest | PRs #155, #157, #158 merged: loop-scoped warn demotion + LOOP-STOP warn-era prose + envelope-anchored eval authoring (concurrent session's own cluster; #156/#159/#160 are a different concurrent session's PRs, referenced not duplicated here — see the #156/#159 ingest entry above). PR #155 (`a36fa9d`): `check_confidence_labels.sh` and `check_verify_loop.sh` gain the `als_loop_active_incomplete` predicate (new, `hooks/scripts/lib/loop_state_common.sh`) — a `Stop`-event block demotes to a model-visible `additionalContext` warn (`[discipline-warn(loop)] ...`) when the agentic-loop Skill has been invoked this session and the loop is not yet complete; `SubagentStop` never demotes. Non-exiting predicate, evaluated lazily only once a block is imminent; fail-toward-blocking (a failed `jq` emission falls through to the normal block rather than silently exiting 0). Demoted-path log line: `would_block=1 warned=1 blocked=0` — future retro mining should key on the `blocked=1` count, not `would_block=1`, since warns deliberately keep logging the latter by design. Motivating incident: loop f87a0a2e forced ~69 turn regenerations (49 confidence-label + 20 verify-loop blocks) in one crack-on loop. PR #157 (`fe66400`, doc-only, `skills/agentic-loop/SKILL.md`): rewrote Phase 0.5's warn/block framing to match #155's actual behaviour, clarified the `LOOP-STOP` declaration must be the FINAL line of the stopping turn (a prose clarification of the existing `loop_stall_guard` tail-1 contract, not a new hook rule), corrected the past-failure figure from "~8 blocks" to the real f87a0a2e numbers. PR #158 (`6eb8afc`, `skills/agentic-loop/SKILL.md` + `skills/task-evals/SKILL.md`): extends task-evals rule 4 (oracle independence) with a loop-scope precedence rule — the eval author's goal-state anchor is `progress.json`'s `authorising_prompt_raw` (the post-Phase-0 envelope, one canonical string); `spec.md`/`plan.md` restatements never override it. Wired into agentic-loop at four points: Phase -2 stub comment, Phase -2 mid-loop re-stub carry-forward (now covers `authorising_prompt_raw` alongside `loop_stop_counts`), Phase -1 update-on-adoption, Phase 2.7c cross-reference, Phase 13 `retro.json` `envelope` field sourcing. Deploy: installed plugin cache refreshed to 1.1.5 @ `1ac2166` (version lockstep repaired by the concurrent #159 session, not this cluster's own PRs). Loop evals graded GO (tier-2 frozen suite, `post_evals.sh grade-loop`); one recorded amendment (eval L7's fresh-clone surface reimplemented from `git archive` to a true `git clone`, proven green independently before folding back per rule-5 grader-independence). Accepted residuals: anomaly-line surfacing skipped (YAGNI, owner decision); cosplay-loop escape (a never-completing loop holds discipline at warn indefinitely — contained by `SubagentStop` always blocking + the log staying visible for retro mining, not structurally closed); bundle-line (labels+DNV+LOOP-STOP together) compliance is measured via the discipline log, not hook-guaranteed beyond `loop_stall_guard`'s existing tail-1 read. Created: `sources/pr_155-158_ceremony_noise_envelope_anchoring.md`. Updated: `design/task-evals-gate.md` (fixed pre-existing lockstep drift — schema-block copy said `tier_justification` "required when tier is 0", superseded since PR #10 which requires it at every tier; also extended rule 4 with PR #158's precedence rule), `skills/agentic-loop.md` (new "Envelope anchoring for loop-scope evals" section, new "Orchestrator discipline demotes to warn inside an active loop" section, LOOP-STOP FINAL-line + warn-era bundling note, phase-table Phase 0.5 row, frontmatter/cross-refs), `design/discipline-loop.md`/`hooks/check_confidence_labels.md`/`hooks/check_verify_loop.md` (cross-reference additions only — all three already accurately described #155's mechanism from the concurrent #156/#159 ingest pass; no content rewrite needed). sync-docs (non-`--semantic`, scoped to this cluster's own PRs): PR #155's own docs-sync rows in `AGENTS.md`/`README.md`/`docs/REFERENCE.md` verified accurate against hook source, no drift; found and fixed one small loop-introduced drift in `docs/REFERENCE.md`'s hook-library function table — `als_loop_active_incomplete` and its two new consumers (`check_confidence_labels.sh`/`check_verify_loop.sh`) were missing from the `loop_state_common.sh` row.

## [2026-07-13] ingest | PR #162 agentic-loop finishing-out.md sibling extraction — PR #162 (merged 2026-07-13T18:11Z, `b8fe61c`): direct sequel to [[pr_134_agentic-loop-retry-until-green|PR #134]]'s sibling-file extraction pattern (one-line markdown-link pointer in SKILL.md, detail in a new sibling file). Adds `skills/agentic-loop/finishing-out.md` (40 lines), bundling two loop-finish mechanics that were previously either missing or inline: (1) orchestrator-level [[verification-before-completion]] applied to the Phase 13 `LOOP-STOP: complete` declaration itself — SKILL.md's two pre-existing VBC references (Phase 3/3a) only discipline WORKERS' claims, so this closes the gap where nothing disciplined the ORCHESTRATOR's own final completion claim; scoped to that single declaration only, not per-unit merges, since Phase 12 already re-checks each merge independently and gating per-merge here too would duplicate that check; (2) the Phase 4b per-unit worktree-teardown paragraph, previously an inline command enumeration, re-pointed at [[finishing-a-development-branch]]'s Step 6 mechanics, adding one new caveat not previously stated inline — never `git worktree remove` the worktree that is the shell's own cwd, since the command fails from inside the worktree being removed, so `cd` to the main repo root first. Created: `sources/pr_162_agentic-loop-finishing-out.md`. Updated: `skills/agentic-loop.md` (new "Finishing-out" section mirroring the retry-until-green section, frontmatter `sources:`/`tags:`, Related-list bullet), `index.md` (new catalog bullet). Ingested per `AGENTS.md`'s `git.worktree: false` / `wiki.supervision: autonomous` config — committed directly, no PR.

## [2026-07-13] lint | Post-#162 finishing-out ingest cluster clean. Scoped to the PR #162 target set (`sources/pr_162_agentic-loop-finishing-out.md`, `skills/agentic-loop.md`, `index.md`, `log.md`) per the requesting agent's focus. **Dead links: 0** — all 51 `[[wiki-link]]` targets across the two new/updated pages resolve to real vault pages (`finishing-out` appears only as a frontmatter tag and a backticked plugin filename `finishing-out.md`, never as a page-ref link — correct, the sibling file lives in the plugin repo not the vault). **Contradictions with the retry-until-green precedent ([[pr_134_agentic-loop-retry-until-green]]): 0** — both pages describe the same progressive-disclosure sibling-file pattern (`retry-until-green.md` / `finishing-out.md`) attributed to the `learned-failure-modes.md` precedent; #162's "direct sequel" framing is consistent, no conflicting claims. **Orphans: 0** — `pr_162` source has 2 inbound links (`index.md`, `skills/agentic-loop.md`). **Frontmatter integrity:** all 19 `sources:` path entries on `skills/agentic-loop.md` resolve (SKILL.md → plugin repo, 18 source pages → vault); the new `pr_162` entry + `finishing-out`/`verification-before-completion` tags added correctly. **Merge-fact accuracy:** source page + `index.md:238` both state SHA `b8fe61c` / mergedAt `2026-07-13T18:11Z`, verified exact against `gh pr view 162`. `finishing-out.md` sibling confirmed present in plugin repo at 40 lines, matching the source page's stated size. Source-vs-skill consistency verified on both mechanics (VBC scoped to Phase 13 `complete` only; never-remove-cwd-worktree caveat). **Suggest (ingest-enrichment, not lint-scope — reported not acted on):** `skills/verification-before-completion.md` and `skills/finishing-a-development-branch.md` each gained a new relationship from #162 (orchestrator-level Phase 13 VBC wiring; Phase 4b Step 6 worktree-teardown reference respectively) but neither page mentions it — neither is a dead/missing link (finishing-a-development-branch already links `[[agentic-loop]]`; verification-before-completion has no Phase-13 mention to link), so these are candidate enrichments for a future ingest pass, not lint defects. Persistent backlog unchanged (fable-mode/loop-retro-promotion pages still missing, git-common-lib design page still missing, PRs #150-154 un-ingested) — out of scope for this focused pass.

## [2026-07-14] ingest | PR #169 mandatory model-per-task routing step (Phase 2.8) — PR #169 (merged 2026-07-14T19:31:26Z, `229903a`): new unconditional `agentic-loop` Phase 2.8 "Route: assign a model role per task" — model-agnostic capability roles (`fast-mechanical`→haiku, `default`→sonnet, `frontier`→fable/opus-alternate), the role→model table as the only thing a model release touches (durable role vocabulary elsewhere), `frontier`-FIRST (not escalated-to) for genuinely ambiguous investigations, fallback valves named in the plan/task `Model:` stamp never improvised by a worker, escalation safe by construction (PR gates are model-independent — routing is cost/latency, not correctness), assignment set recorded ONCE per loop in `progress.json.decisions_absorbed` as a `{phase: "2.8", decision: ...}` entry. Reconciles ~13 blanket sonnet/opus assertions across the skill (frontmatter description, Phases 2, 2.5, 3, 3a, 9, 10) — Phase 2/2.5/9 agents get their role assigned inline at their own spawn point (they run outside 2.8's per-task build-task routing); Phase 2.5's design-fork spawn inverted from "sonnet recon, escalate the synthesis to opus only if the tradeoff is genuinely close" to bounded-`default`-vs-genuinely-ambiguous-`frontier`-first. `writing-plans`: "What each task carries" grows from four to five mandatory elements — every task in every plan now carries a `Model:` stamp (role + rationale + optional fallback valve), no loop-only carve-out (Gary: "so I am gonna need it"); role definitions/table stay in agentic-loop's Phase 2.8, cross-referenced not duplicated. `AGENTS.md`: the "`model: sonnet` is advisory, not hook-enforced" bullet reworded to "Model-role routing ... advisory, not hook-enforced" — still NO enforcement hook, deliberately (cost/latency control, not correctness; a model-gate hook can't distinguish Phase 2.8's sanctioned role-vs-role judgement call from a disallowed spawn without a self-reported carve-out flag); hook-coverage claim made precise — the only `PreToolUse` matchers in `hooks/hooks.json` are `Bash` and `Write|Edit|MultiEdit`. **The number 2.8 is reused, not new:** the original Phase 2.8 (plan-writing, pre-PR #86) was merged into Phase 2.7 as sub-step 2.7b by PR #86 — that history is unchanged; PR #169 reuses the vacated number for this unrelated new phase. Design decisions from the loop: placement as unconditional Phase 2.8 (not a 2.7b sub-step, which is gated away exactly when a small loop needs it; not a Phase 3 preamble, which conflates decide-once with consume-per-spawn) so a <3-unit loop still routes its 1-2 workers; ONE atomic PR across all three files so they never contradict on `main`. Disambiguation noted: `frontier`'s "currently" value `fable` names the model Claude Fable 5, unrelated to the repo's separate `coderails:fable-mode` skill. Created: `sources/pr_169_model-routing-step.md`. Updated: `skills/agentic-loop.md` (new "Phase 2.8" section, reworded "2.7/2.8 merge note" reconciling the renumber, reworded "Model-role routing is advisory" section replacing the stale "`model: sonnet` is advisory" section, Phase 2.5/Delegation-rung/Key-decisions/frontmatter-description-note text updated for role vocabulary, frontmatter `sources:`/`tags:`/`last_updated`, Cross-references), `skills/writing-plans.md` (fifth `Model:` element added to "Key content", "formerly 2.8" refs annotated with the renumber pointer, new "Also now consumes Phase 2.8" paragraph, frontmatter/cross-refs), `design/enforcement-model.md` (the `model: sonnet` advisory-ceiling section reworded for the new vocabulary and precise hook-matcher claim, frontmatter/cross-refs), `index.md` (new Sources catalog entry, `last_updated`). Ingested per `AGENTS.md`'s `git.worktree: false` / `wiki.supervision: autonomous` config — committed directly, no PR; no `workflow.config.yaml` present in the vault, so `enforce_pr_workflow` does not gate this push either.

## [2026-07-14] ingest | PRs #163-168 dashboard-rethink cluster — one `dashboard-rethink` [[agentic-loop]] session (session `0767967d`, authorised 2026-07-13 after a live-dashboard walkthrough surfaced five concrete problems: half the deck buttons are theatre, two panels are dead weight, the loop panel shows a week-dead loop with a misleading label, gate state lags 2 minutes, headless runs return discipline-hook repair text instead of answers), six independent work units, all merged to `main` 2026-07-14. #163 (`9ab336c`, sonnet): `DashboardButton` gains an optional `hidden?: boolean` field + config validation (rejects non-boolean) + a render filter (`config.buttons.filter((b) => !b.hidden)`); applied live in the per-user `~/.claude/coderails-dashboard.json` (not in-repo) to trim the deck from 7 buttons to 3 visible (DEEP RESEARCH/WIKI QUERY/ASK), hiding 4 routine-trigger buttons that stay in config as live `buttonRef` targets. #166 (`db16d77`, sonnet, clean-break): removes the Memory.Trail panel/collector/slice and the dead Command-Deck run-history list end-to-end, plus the orphaned `memoryPaths` field from both `DashboardConfig` declarations (app + shared lib) and all fixtures/docs — verified zero remaining hits for `memoryPaths`/`MemoryTrail` anywhere under `skills/dashboard/app/src`. #165 (`ce79b08`, sonnet): loop collector rewrite — `LoopInfo` gains `title` (resolution chain `loop` field → `authorising_prompt_raw` → slug fallback), `lastUpdatedMs` (`last_updated` field wins, mtime fallback), and `units[]` with a `"done"|"in-flight"|"pending"` status union, replacing the old `unitTitles`/`done` boolean pair; both the current keyed-object and legacy array `progress.json` shapes parse. #168 (`967ad77`, opus, clean-break): DIRECTIVES becomes one card per **live** loop instead of a single tracked active loop — `LOOP_LIVE_WINDOW_MS = 60 * 60_000` (verified: `useDashboardState.ts:142`), `liveLoops()`/`stalledLoops()` partition the non-complete set on that window (both newest-first), a `useNow(30_000)` ticking-clock hook (verified: consumed by both `RailLeft.tsx:75` and `BottomHero.tsx:8`) keeps live/stalled current without a page reload; stalled loops render as a dim no-card sub-list; live cards get a `Live.N` header suffix, an evals-frozen footer, and a decisions sub-list; `BottomHero` follows `liveLoops(...)[0]`; `selectActiveLoop` retired clean-break. #164 (`a2bcb57`, sonnet fallback after 2 failed haiku attempts): `DEFAULT_GATES_POLL_MS` 120s→30s (verified: `collect/index.ts:74`) plus a new `GATES_RUNS_DEBOUNCE_MS = 3_000` (line 76) debounced additional refresh on runs-dir changes — a finished run now updates PR GATES within ~3s on top of the 30s poll floor, not up to 2 minutes. #167 (`16074a9`, sonnet): `CODERAILS_HEADLESS_RUN=1` set in exactly one place — `route.ts`'s `spawn(...)` call (verified: `route.ts:257`, spread-preserving env) — exempts `check_confidence_labels.sh`/`check_verify_loop.sh` from enforcement on the `Stop` event only (verified: both scripts gate on `hook_event = "Stop"` exactly); `SubagentStop` stays block-enforced regardless of the flag, pinning test H5 in both suites; rationale is that a headless `claude -p` run has no interactive turn left to satisfy a repair-turn block, so unexempted the gate would displace the run's actual answer with gate-repair text. A same-loop no-PR finding (t7, filed as `wu7-finding.md`, no wiki source page by design): slash-command-as-prompt runs show 0 model turns in run logs because the slash command executes as a CLI local command — the outer session genuinely takes no model turns; the write path is faithful, not buggy. Created: `sources/pr_163-168_dashboard-rethink.md`. Updated: `skills/dashboard.md` (new Deck-trim/Memory.Trail-removal/Multi-loop-Directives-panel/Gates-freshness sections, headless-exemption paragraph under the button/run model section, frontmatter `sources:`/`tags:`/`last_updated`, See-also links), `design/enforcement-model.md` (new "headless-run exemption" section, hook-map table rows annotated, frontmatter `sources:`, cross-references), `design/discipline-loop.md` (third Stop-hook condition documented alongside plain-block and loop-warn-demote, frontmatter `sources:`/`last_updated`, cross-references), `index.md` (new Sources catalog entry). Ingested per `AGENTS.md`'s `git.worktree: false` / `wiki.supervision: autonomous` config — committed directly, no PR. Note: this ingest ran in a wiki repo with a second session's uncommitted work in flight concurrently (PR #169 model-routing-step, files `design/enforcement-model.md`/`index.md`/`log.md`/`skills/agentic-loop.md`/`skills/writing-plans.md` + new `sources/pr_169_model-routing-step.md`) — both sessions' changes land in the same commit since neither touched the same lines/sections; verify against `git show origin/main` after push confirms both landed intact.

## [2026-07-14] lint | Post-#163-168 dashboard-rethink ingest clean, one gap found and fixed. Scoped to the ingest's own target set (`sources/pr_163-168_dashboard-rethink.md`, `skills/dashboard.md`, `design/enforcement-model.md`, `design/discipline-loop.md`, `index.md`) plus their direct cross-references, per this skill's convention. **Dead links: 0** across all five files (checked programmatically against every real page basename in the vault). **Merge-fact accuracy: verified exact** — all six PR merge SHAs (`9ab336c`/`db16d77`/`ce79b08`/`967ad77`/`a2bcb57`/`16074a9`) checked against `git log --oneline main` in the coderails repo, all match; the source page's note that #164 lands after #168 in merge order despite being the earlier work-unit number is confirmed true from the same log. **Source-claim accuracy: verified** — `AGENTS.md`'s hook-map rows (lines 99-100) and enforcement-ceilings bullet (line 175-183) match the wiki's description of the `CODERAILS_HEADLESS_RUN` Stop-only exemption exactly; `route.ts`'s inline comment (lines 250-256) and `spawn()` call (line 257, spread-preserving env) match the wiki's claim verbatim; both hook scripts' `if [ "${CODERAILS_HEADLESS_RUN:-}" = "1" ] && [ "$hook_event" = "Stop" ]` gate confirmed at `check_confidence_labels.sh:27` and `check_verify_loop.sh:81`. **Orphans: 0** — the new source page has 5 inbound links (index.md, log.md, dashboard.md, enforcement-model.md, discipline-loop.md). **Stale pages: 0** — all five touched files' `last_updated` is 2026-07-14 (today); no lingering "seven panels"/`memoryPaths`/`selectActiveLoop` references found anywhere outside the pages that correctly document their removal. **Missing cross-references: 1 found and fixed.** `hooks/check_confidence_labels.md` and `hooks/check_verify_loop.md` — the per-hook detail pages linked from `design/discipline-loop.md` (`[[check_confidence_labels]]`/`[[check_verify_loop]]`) and documenting the exact Stop-event gate chain PR #167 modified — had zero mention of the headless exemption despite covering the adjacent PR #155 loop-demotion condition on the same gate chain in detail. Fixed: added a "Headless-run exemption (PR #167, 2026-07-14)" section to each (mirroring the existing "Loop-scope demotion" section's style), added the exemption as an explicit step in each page's ordered-checks logic/table, updated frontmatter (`sources:`, `tags:`, `last_updated`), added `check_verify_loop.md`'s History list entry (generation 6), and added `[[pr_163-168_dashboard-rethink]]`/`[[dashboard]]` Related-list links to both. Re-checked both pages for dead links post-edit: 0. **Contradictions: 0** — the six PRs' clean-break claims (`memoryPaths`, Memory.Trail slice, `selectActiveLoop`) are consistent across `sources/pr_163-168_dashboard-rethink.md` and `skills/dashboard.md`; the two independent gates-freshness constants (`DEFAULT_GATES_POLL_MS`/`GATES_RUNS_DEBOUNCE_MS`) are described identically in both places without the conflation the source page's own caveat warns against. **Persistent backlog, unchanged, out of scope for this focused pass:** `skills/fable-mode.md`/`skills/loop-retro-promotion.md` still missing; `design/git-common-lib.md` still missing; PRs #150-154 still un-ingested (#155/#157/#158 now have a source page per the prior lint pass).

## [2026-07-14] ingest | PR #175 merged (09cb0d0): crack_on_gate.sh — new two-event discipline hook (UserPromptSubmit stamp + PreToolUse/AskUserQuestion deny) gating human-asks during a "crack on" envelope, raw-prompt-only detection (never transcript/context), session_id-keyed flag path deliberately bypassing agentic_loop_path.sh's resolver, four agentic-loop hard-stops untouched by design. PR #176 merged (fc122d3): inbox-brief dashboard button — first pure-dispatcher bypass button, spawns claude only to shell out to `bin/rachel "Read tasks/inbox-brief.md and follow it." < /dev/null` in cwd=assistant-agent. Created [[crack_on_gate]]; updated [[discipline-loop]], [[inject_context]], [[dashboard]], [[index]]. See [[sources/pr_175-176_crack-on-gate-and-inbox-brief-button]].

## [2026-07-14] lint | Post-#175-176 ingest clean, no fixes needed, one cross-cluster contradiction surfaced. Scoped to the ingest's own target set (`sources/pr_175-176_crack-on-gate-and-inbox-brief-button.md`, `hooks/crack_on_gate.md`, `design/discipline-loop.md`, `hooks/inject_context.md`, `skills/dashboard.md`, `index.md`) plus their direct cross-references, per this skill's convention; unrelated pre-existing findings deferred. **Dead links: 0** across all six files (checked programmatically against every real page basename in the vault, including piped `[[target|display]]` forms — two apparent hits on first pass were regex code-literal false positives, `[[:space:]]` inside the hook's own regex documentation). **Merge-fact accuracy: verified exact** — both merge SHAs (`09cb0d0`/`fc122d3`) match `git log --oneline main` in the coderails repo; `hooks/hooks.json`'s `UserPromptSubmit` array (`inject_context.sh` then `crack_on_gate.sh`, one matcher object, two hooks) and its new `PreToolUse` matcher scoped to `AskUserQuestion` both confirmed verbatim via `jq`; `examples/dashboard-config.json`'s `inbox-brief` button fields (`profile: "bypass"`, `bypassPermissions: true`, `cwd`, `command` string incl. `< /dev/null`) confirmed verbatim. `crack_on_gate.sh`'s regex, session-id-keyed flag path, and stamp/deny logic all confirmed against source at the line ranges `hooks/crack_on_gate.md` cites. **Orphans: 0** — `[[crack_on_gate]]` has 6 inbound links (index.md, log.md, discipline-loop.md ×2, inject_context.md, sources page, dashboard.md); the source page has 5 inbound links (log.md, discipline-loop.md, crack_on_gate.md, inject_context.md, dashboard.md). **Stale pages: 0** — all six in-scope files carry `last_updated: 2026-07-14` (today). **Missing cross-references: 0** found in-scope — the two other vault pages mentioning "crack on" by the trigger-phrase idiom (`skills/agentic-loop.md`, `skills/workflow-audit.md`) predate PR #175 and refer to the general authorisation phrase, not the new hook; out of scope per this pass's deferral instruction. **Contradiction (surfaced, not fixed — outside the six-file scope): `skills/workflow-audit.md`'s "Approval gate overrides loop autonomy" bullet claims its `AskUserQuestion` approval step is "a hard stop that applies even inside an agentic-loop session authorised for full autonomy ('crack on', ...)". `crack_on_gate.sh` as merged denies *every* `AskUserQuestion` call while the session's crack-on flag is stamped, with no carve-out for workflow-audit's gate — confirmed by reading the script (matcher is bare `tool_name = "AskUserQuestion"`, no caller/context check) and its test suite (`crack_on_gate.test.sh`'s "HARD-STOP PRESERVED" section only asserts Bash/Write/Task pass through; it never exercises workflow-audit's approval call). The in-scope pages' own "hard-stops untouched by design" claim is accurate as narrowly scoped — the four `LOOP-STOP` declarations genuinely aren't `AskUserQuestion` calls — but that framing doesn't cover workflow-audit's separate, pre-existing `AskUserQuestion`-based hard stop, which the new gate does appear to defeat in practice. Recommend the owner decide: either `crack_on_gate.sh` gains a documented carve-out (e.g. a payload field identifying the calling skill) or `skills/workflow-audit.md`'s claim is corrected to note the gate can now suppress it. Not fixed this pass — resolving it means a source-code or design-intent decision, not a wiki edit.** **Persistent backlog, unchanged, out of scope for this focused pass:** `skills/fable-mode.md`/`skills/loop-retro-promotion.md` still missing; `design/git-common-lib.md` still missing; PRs #150-154 still un-ingested.

## [2026-07-15] ingest | PR #179 merged (b8fbe19): opt-in LAN access for the dashboard via one env var, DASHBOARD_HOST. Unset/empty is byte-for-byte the pre-PR loopback-only behaviour. Set to a concrete host IP, both serving paths (start-dashboard.sh, runner/bin/dashboard-server.sh) bind that address instead of 127.0.0.1, and requestGuard.ts's new isAllowedHost() helper additionally exact-matches that one host for both Host and Origin, alongside loopback (always allowed) — DNS-rebinding/cross-origin still rejected. Both scripts fail loud (non-zero exit) on a wildcard bind or host:port form. com.coderails.dashboard.plist gains an EnvironmentVariables dict with an empty DASHBOARD_HOST entry. Security posture recorded as deliberate: the dashboard's unauthenticated command-exec surface (POST /api/run, workflow-audit Approve/Deny) is unchanged — the Host/Origin guard blocks hostile web pages/DNS-rebinding, not LAN devices; trusted-home-network-only by design, device auth out of scope. Created sources/pr_179_dashboard-lan-access.md. Updated skills/dashboard.md (stale "Binds 127.0.0.1 only" claim corrected, new "LAN access (opt-in)" subsection, CSRF-guard sentence reworded, frontmatter), index.md.

## [2026-07-15] lint | Scoped pass on the PR #179 ingest (`sources/pr_179_dashboard-lan-access.md`, `skills/dashboard.md` edits, `index.md`, `log.md`), per this skill's convention. **Dead links: 0** across the ingest's touched files, checked programmatically against every real page basename (piped `[[target|display]]` forms included). **Merge-fact accuracy: verified exact** — `gh pr view 179 --json mergedAt,mergeCommit` returns `b8fbe190511b00bc04ec4154d226e23e31d93661` / `2026-07-15T17:21:28Z`, matching the source page's PR-metadata table and index.md/log.md's short-SHA citations. **Orphans: 0** — `[[pr_179_dashboard-lan-access]]` had 2 inbound links pre-lint (index.md, dashboard.md), 3 after the fix below. **Stale-claim contradiction check (the scoped question): 0 found** — grepped the whole vault for `127.0.0.1`/`loopback`/`localhost` outside the ingest's own files; the only other hits are pre-existing, unrelated (a `start-dashboard.sh` invocation-shape note in an unrelated 2026-07-08 investigation, and CSRF/token-leakage prose in a streaming-viewer investigation) and don't claim loopback-exclusivity as a current architectural fact, so nothing else in the vault is now stale. `design/dashboard-runner.md` re-checked directly per the source page's own caveat — confirmed no bind/host content, genuinely a different `runner/` (intent-queue sweeper), not touched. **Missing cross-references: 1 found, fixed.** `skills/dashboard.md`'s new "Security posture" prose cites `[[enforcement-model]]`'s trust-boundary framing for the LAN opt-in, but `enforcement-model.md` had no reciprocal reference — every other page reusing that framing (e.g. PR #167's headless-run exemption) gets a named subsection + Cross-References entry there, and PR #179 was the one gap. Fixed: added `enforcement-model.md`'s "LAN access as a trust-boundary-preserving scope expansion (PR #179)" subsection, a Cross-References entry, and `sources`/`last_updated` frontmatter; added the reciprocal "Wiki pages updated" line to the source page. No other findings required an owner decision this pass.

## [2026-07-15] lint | Full-vault health pass. **Structural core clean.** 0 stale evergreen pages (all commands/hooks/skills/design `last_updated >= 2026-06-15`, inside the 30-day window). 0 orphan evergreen pages (every command/hook/skill/design basename has ≥1 inbound `[[link]]`, counting `sources:` frontmatter refs). 0 genuine dead links (the raw non-resolving `[[...]]` hits are the documented benign classes: bash `[[ ]]`/`[[:space:]]` test syntax, template placeholders `[[link]]`/`[[page_name]]`/`[[wiki-links]]`, and the intentional cross-vault/memory refs `[[capabilities/send-gate]]`, `[[feedback_never_remove_hard_stops]]`, `[[Case-insensitive retirement sweep]]`, which live in assistant-agent-wiki / the memory dir, not this vault). 0 active/unresolved `⚠️ CONTRADICTION` flags among source pages (all `⚠️` hits are deliberate resolved supersession or PR-number-collision notices). Parity vs. plugin source reconfirmed: commands 12/12, hooks 15/15 live scripts covered (plus the correctly-retired `discipline_catchup` page). Inbox: no `inbox/` dir → no ingest backlog. **Fixed this pass (wiki-layer, in-scope): 2.** (1) **Durable CONTRADICTION flag added** to `skills/workflow-audit.md`'s "Approval gate overrides loop autonomy" bullet. A prior pass (2026-07-14) surfaced this but left it in the log only, where it re-evaporates each pass. Ran the discriminating check this time: the "hard stop even under crack on" claim lives in the plugin SKILL SOURCE (`workflow-audit/SKILL.md` §7 "Approval gate — hard stop, no exceptions"), not just the wiki page's synthesis — so it is a genuine source-vs-hook product conflict, not wiki drift. `crack_on_gate.sh` denies EVERY `AskUserQuestion` call under a stamped crack-on flag (bare `tool_name` matcher, no carve-out; confirmed against script + test suite), mechanically suppressing the gate §7 calls unbreakable. Flagged durably on the page pointing at `[[crack_on_gate]]`; the underlying resolution (carve-out in the hook, or narrow §7's claim) is an owner source/design decision, NOT a wiki edit. (2) **Link-style normalization:** the vault's 3 remaining `[[sources/pr_...]]` path-prefixed body links (1 in `hooks/crack_on_gate.md`, 2 in `skills/dashboard.md`) normalized to the bare-basename convention used everywhere else (`[[pr_175-176_...]]`). Obsidian resolved both forms — this was style consistency, not a broken link. Frontmatter `sources:` array path entries left untouched (path form is correct there). Bumped `last_updated` to 2026-07-15 on the 3 touched pages. **Persistent backlog — REPORTED not fixed (ingest-class work, out of lint scope per Step 7's one-way rule; now recurred across ≥4 passes — escalating a dedicated ingest session as the fix):** `skills/fable-mode.md` and `skills/loop-retro-promotion.md` still missing (2 of 35 source skills have no dedicated page — both only mentioned in passing on `agentic-loop`/`routines`/`dashboard`/routing pages); `design/git-common-lib.md` still missing (`scripts/lib/git-common.sh` is a load-bearing shared lib with no design page); PRs #150-154 still un-ingested (no `sources/` pages). Note: the missing-cross-reference check this pass was a spot-check, not an exhaustive term-by-term sweep — adequate for a mature vault but not claimed as total.

## [2026-07-15] query-file-back | Query on run-output rendering / inbox-brief / run route / button-spawn model surfaced that [[dashboard]]'s Run Output viewer section is factually stale, not just incomplete: PRs #170-#174 (`rethink/docs-panel-drift`, `t9-no-truncation`, `t10-runoutput-modal`, `t11-mobile-responsive`, `t12-runoutput-gfm`; same `dashboard-rethink` loop as the already-ingested #163-168, all merged 2026-07-14) have zero wiki coverage. #172 replaced the inline `<pre>`+raw-toggle viewer documented in [[pr_139-141_dashboard-ask-enter-clean-output]] with a portal modal (`RunOutputOverlay.tsx`) rendering sanitized `react-markdown` (#174) — including an undocumented img→alt-text override closing a tracking-beacon/SSRF-from-viewer vector. #171 removed the 80-char title-truncation / two-line CSS clamp the wiki still describes as current. Confirmed `route.ts`'s literal `spawnImpl("claude", ...)` (not config-derived) as the real code enforcing "a button can only spawn claude" (the inbox-brief button's dispatcher design, from [[pr_175-176_crack-on-gate-and-inbox-brief-button]], depends on this). Also flagged open PR #179 (`dashboard-lan-access`, unmerged) as actively touching the `requestGuard.ts` Origin/Host localhost guard the wiki documents as a baseline invariant. Filed [[dashboard-run-output-rendering-gap_2026-07-15]] documenting the gap for a future `/wiki-ingest` pass; not ingested here (out of this query's scope). Updated [[index]].

## [2026-07-15] query-file-back | Query on retro.json + Phase 13 teardown + dashboard data sources/aggregation + existing cost/token/model tracking, asked ahead of a plan to add per-model cost tracking to retro.json + dashboard rollups. Headline: **no cost or per-model tracking exists anywhere in coderails** — zero prior wiki hits on cost/pricing/budget/spend, and `retro.json`'s verbatim schema (`session_id`, `created`, `loop_ordinal`, `envelope`, `loop_stop_counts`, `decisions_absorbed`, `disposition_record`, `evals`, `artifacts`, `hook_blocks`, `review_themes`, `raw_notes` — no `verdict` field by design) carries none. Surfaced a real, previously wiki-undocumented artifact by reading source directly: `skills/dashboard/app/src/lib/collect/usage.ts` (`collectUsage`), consumed by `collect/health.ts` for the SYSTEM VITALS token tiles — machine-wide (every `~/.claude/projects/*` transcript), two flat rolling windows (5h/week), de-duped by `message.id`, no per-model/per-loop/per-PR/per-session breakout, no dollar conversion. Confirmed Phase 2.8's model-role routing (PR #169) can't serve as a cost join key today: role assignment is a single free-text `decisions_absorbed` bullet covering all tasks, not per-task structured data, and is advisory-only (`AGENTS.md` states no `PreToolUse` hook gates `Agent`/`Task` spawns, so nothing verifies which model actually ran). Documented 6 cross-PR constraints a cost-tracking plan must respect: pick an explicit ownership model for any new field (orchestrator- vs. hook-owned, per [[loop-progress-fields]]'s established pattern, to avoid PR #98's two-writer race repeating); retro.json's no-verdict rule bars a self-issued efficiency judgement; the retro gate ([[loop_stall_guard]]'s `als_gate_retro_on_complete`) is presence+parse only, giving zero correctness checking to a new field; no join key exists between `usage.ts`'s token counts and Phase 2.8's declared-not-verified model role; `usage.ts` is machine-wide/session-scoped with no loop-boundary concept (`progress.json` doesn't even track a loop start time); `docs/coderails/specs|plans/` are gitignored (PR #138) so a plan document itself won't land as a permanent repo artifact. Filed [[retro-json-per-model-cost-tracking-gap_2026-07-15]]. Updated [[index]].

## [2026-07-15] lint | Scoped pass verifying the branch-protection reconciliation (coderails PR #182, which updated AGENTS.md and docs/routines.md to state GitHub branch protection is deliberately not enabled, a standing 2026-07-15 decision — not a pending gap). Checked [[enforce_pr_workflow]], [[no_edit_on_main]], and their direct cross-references [[enforcement-model]], [[discipline-loop]], [[index]] for any surviving claim presenting branch protection as an available/real/recommended backstop this repo relies on. **hooks/enforce_pr_workflow.md and hooks/no_edit_on_main.md are already fully reconciled** — both cite PR #182 verbatim with "no server-side lock/backstop in this repo, by design." design/enforcement-model.md and design/discipline-loop.md have zero branch-protection mentions — clean by omission, nothing to fix. **Found and fixed one stale instance:** index.md's PR #70 catalog entry (maintained content, not append-only log) still read "branch protection is the real lock" in present tense — the un-updated echo of the exact sentence already corrected in no_edit_on_main.md's PR #70 section. Reworded to "no server-side lock in this repo by design, PR #182" to match. Bumped index.md `last_updated` to 2026-07-15. **Checked and confirmed NOT in scope, left as-is:** index.md's separate "main-branch protection" mention (line 21) refers to the no_edit_on_main hook's protection of the main branch — an unrelated, still-true concept, not GitHub branch protection; a false-positive on the search term. commands/merge.md's three `protected()`/branch-protection mentions describe live, verified code in scripts/lib/git-common.sh and scripts/merge.sh (a generic conditional the script honours if a user's repo happens to have GitHub branch protection enabled) — accurate documentation of script capability, not a claim this repo relies on it; out of scope for this reconciliation. log.md and sources/ hits are append-only historical PR records (accurately describing what was true/believed at the time) — correctly untouched per lint convention. No other pre-existing lint drift investigated this pass (scoped run, full sweep not performed).

## [2026-07-15] ingest | Dashboard run-output modal — post-ship follow-ups cluster (coderails PR #181 merged ff99692, coderails PR #183 merged 0e87e16, assistant-agent PR #27 merged ef2783c): two independent same-day fixes to RunOutputOverlay.tsx. #181 — `.hud-markdown pre` gains `white-space: pre-wrap; overflow-wrap: anywhere; word-break: break-word;` so long lines/fenced code blocks wrap instead of overflowing horizontally; new hudMarkdownWrap.test.ts pins the CSS declarations (jsdom has no layout engine, so wrap itself isn't DOM-assertable). #183 + assistant-agent #27 — the inbox-brief button's modal output now shows the CLEAN brief Telegram received, not Rachel's raw execution trace: tasks/inbox-brief.md (assistant-agent) has Rachel end her turn with a machine-findable `BRIEF FILE: <path>` sentinel line after confirmed delivery; examples/dashboard-config.json's inbox-brief button command was rewritten so the OUTER spawned claude agent reads that file and makes its entire final message exactly its contents. Required because the modal renders the outer agent's `result` field, a separate LLM layer from Rachel — the sentinel alone couldn't reach it. Verified live: run 5cfc5ee0d5a32a82's modal output byte-matched (whitespace-normalised) the sent brief file. Also closed the wiki-drift the 2026-07-15 investigation ([[dashboard-run-output-rendering-gap_2026-07-15]]) flagged: skills/dashboard.md's Run Output viewer section described a retired inline `<pre>` viewer with a raw/clean toggle that no longer exists (removed by PR #172, same `dashboard-rethink` loop, previously un-ingested) — rewritten to describe the current RunOutputOverlay.tsx modal, PR #172's toggle retirement, and PR #174's react-markdown security layers (HTML-escaping sanitizer + img-to-alt-text override closing a tracking-beacon/SSRF vector), plus PR #171's title/description truncation removal. New sources/pr_181_183_dashboard-run-output-wrap-and-inbox-brief-clean-modal.md. Updated skills/dashboard.md (Run Output section rewrite, inbox-brief button paragraph, See also, frontmatter sources+tags), index.md. Companion update in assistant-agent-wiki's capabilities/inbox-brief.md (BRIEF FILE: sentinel section) — cross-vault, no dedicated assistant-agent-wiki source page (PR #27 is a 1-line diff already fully covered by this cluster's source page + the capability-page section).

## [2026-07-15] ingest | Per-loop cost & model tracking cluster (coderails PR #186 merged 18:45, PR #184 merged 18:56, PR #185 merged 19:11) — closes [[retro-json-per-model-cost-tracking-gap_2026-07-15]], filed earlier the same day. #186: new `hooks/scripts/lib/loop_cost.sh` (`dc_mine_token_usage`) — fail-open bash lib mining per-model token usage (dedup'd by `message.id`) across an agentic-loop orchestrator transcript plus every recursive worker/subagent transcript, priced via new dated `hooks/scripts/lib/model_prices.json` (2026-06-24, sonnet-5 intro rate $2/$10 through 2026-08-31); type-guards three nesting depths against whole-batch `jq -s` wipeout; widens `loop_state_common.sh`'s retro `schema_version` gate to accept any `>= 1` (forward-compatible). #184: `agentic-loop/SKILL.md` Phase 13 teardown gains a cost-mining sub-step — sources the miner, writes its output verbatim as `retro.cost` (own nested `schema_version` 1) and lifts `models_used` to top-level `retro.models_used` (never duplicated inside `cost`), bumping the retro's own `schema_version` to 2; pricing frozen once at teardown, never re-derived downstream; Phase 13's human-facing report gains a "Loop cost" bullet with a price-staleness age. `AGENTS.md` updated in lockstep (skills↔hooks seam paragraph + hook-event-map table). #185: new `skills/dashboard/app/src/lib/collect/cost.ts` (`collectLoopCost`) sums FROZEN `retro.json` `total_usd_estimate` across completed loops only (never re-prices) into two rolling windows — week (7d) and month (calendar) — rendering `null` (not `$0.00`) when no completed loops fall in-window; two new SYSTEM VITALS tiles ("Cost (Week)"/"Cost (Month)") in `RailLeft.tsx`, showing tokens+scope+staleness note. Model-identity-at-spawn attribution (Phase 2.8 role vs. actual model) remains unclosed — the miner's per-message `message.model` read is a stronger join than the investigation anticipated but still has no reconciliation against declared role. New source page [[pr_184_185_186_loop-cost-tracking]]. Updated [[loop_stall_guard]] (widened gate + schema_version 2), [[agentic-loop]] (Phase 13 cost sub-step), [[dashboard]] (two new cost tiles), [[retro-json-per-model-cost-tracking-gap_2026-07-15]] (marked RESOLVED, cross-linked), index.md (hooks table row).

## [2026-07-15] lint | Scoped pass verifying the loop-cost & model tracking ingest immediately above (coderails PRs #184/#185/#186). Cross-checked the new source page's technical claims directly against merged source on main: `hooks/scripts/lib/loop_cost.sh`'s `dc_mine_token_usage()` function name, `hooks/scripts/lib/model_prices.json`'s price table (identical field-for-field to the page's quoted excerpt), `loop_state_common.sh`'s `schema_version >= 1` gate widening, `skills/dashboard/app/src/lib/collect/cost.ts`'s `collectLoopCost()` export, and `agentic-loop/SKILL.md` Phase 13's cost-mining sub-step wording — all verified accurate, no drift. FIXED (3, all mechanical, each committed individually — this vault auto-commits per Edit): (1) `sources/pr_184_185_186_loop-cost-tracking.md` line 87 had a dangling link `[[dashboard-rethink-complete|dashboard-rethink loop]]` (that slug is a memory-file name, not a wiki page) — repointed to the real page, `[[pr_163-168_dashboard-rethink|dashboard-rethink loop]]`. (2) `index.md`'s Sources catalog section had no bullet for the new page — the hooks-table row referenced it but the running Sources list (every other ingested PR gets one) didn't; added a bullet matching neighbouring entries' style/detail. (3) `sources/pr_181_183_dashboard-run-output-wrap-and-inbox-brief-clean-modal.md` line 18 had a dangling wiki-link `[[RunOutputOverlay]]` — no such page exists (it's a component name documented inline inside `[[dashboard]]`, not a standalone page); de-linked to backtick-formatted `RunOutputOverlay.tsx` (matching the page's own code-symbol convention elsewhere), out-of-cluster but a one-line fix so resolved rather than deferred. CROSS-LINKS: verified `hooks/loop_stall_guard.md`, `skills/agentic-loop.md`, `skills/dashboard.md`, and `investigations/retro-json-per-model-cost-tracking-gap_2026-07-15.md` (marked RESOLVED, correctly annotated) all cite the new source page consistently — 0 contradictions across the cluster. ORPHANS: 0 — new page has 4 verified inbound links (log.md, the investigation page, loop_stall_guard.md, dashboard.md, agentic-loop.md) plus the now-added index.md bullet. STALE PAGES / full-vault scope: not re-audited this pass — scoped to the same-day ingest, consistent with this log's established convention. 0 inbox backlog (no `inbox/` directory).

## [2026-07-16] lint | Full-vault health pass (179 pages: 12 commands, 16 hooks, 33 skills, 19 design, 10 investigations, 89 sources). STRUCTURE CLEAN: 0 orphans (every page has ≥1 inbound link), 0 stale evergreen pages (all commands/hooks/skills/design have `last_updated` ≥ 2026-06-16; the 6 pages older than 30d are all immutable point-in-time `sources/`+`investigations/` records, stale-by-design not defects), 0 inbox backlog (no `inbox/` dir), 100% source coverage for commands (12/12) and hooks (15/15 scripts; `discipline_catchup.md` correctly marked RETIRED PR #159). CONTRADICTIONS: 1 genuine, now RESOLVED. The durable `⚠️ CONTRADICTION` flag on [[workflow-audit]] (surfaced 2026-07-14, "still live 2026-07-15", owner-decision-owed: workflow-audit's `AskUserQuestion` hard-stop gate vs [[crack_on_gate]]'s blanket AskUserQuestion deny) is now moot — the source was rewritten on 2026-07-15 (`workflow-audit/SKILL.md` §7 "Approval gate — hard stop, no exceptions" → "Proceed to creation": "no `AskUserQuestion`, no waiting on the owner", commit a6d0142, deliberately-named branch `worktree-workflow-audit-no-approval-gate`), removing the interactive gate entirely. The owner-decision-owed resolution (narrow the source claim) has happened. This left the wiki page lagging shipped source — genuine drift. FIXED (drift sweep, each Edit auto-committed): [[workflow-audit]] — page summary (line 16), pipeline diagram, the "Approval gate overrides loop autonomy" bullet, the stale CONTRADICTION flag (replaced with a RESOLVED note explaining the durable flag's disappearance rather than silently deleting it), the queue-mode section's false "interactive AskUserQuestion gate... remains the primary path, unchanged", and "approved"→"proposed" wording; index.md catalog row (line 112, "creates only on explicit owner approval") + the pr_27-39 source-summary (line 209, annotated the removed gate as "later removed 2026-07-15" without rewriting the historical PR record). Net: the interactive human-approval gate is retired; the queue-mode dashboard Approve-click is now the sole (opt-in) human gate — reflected consistently. DEAD LINKS: 5 fixed (all the swapped `[[memory-title|page-alias]]` anti-pattern — target was a memory-file name, not a wiki page): `[[Never remove agentic-loop hard-stops|discipline-loop]]` ×2 (hooks/crack_on_gate.md, sources/pr_175-176) repointed to the real invariant home [[agentic-loop]] + `feedback_never_remove_hard_stops` memory ref; `[[Case-insensitive retirement sweep|feedback]]` (crack_on_gate.md) de-linked to backticked `feedback_case_insensitive_retirement_sweep`; `[[feedback_never_remove_hard_stops]]` ×2 (sources/pr_134) converted to backtick memory-ref convention (dominant style: memories are backticked, not wiki-linked). SUGGESTIONS / DATA GAPS (Step 4, not lint edits — dedicated skill pages are wiki-ingest's job): (a) two shipped plugin skills have NO wiki skill page despite being cross-referenced across the vault — `fable-mode` (skills/fable-mode/, since 2026-07-08) and `loop-retro-promotion` (since 2026-07-09); (b) the 2026-07-15 workflow-audit approval-gate removal has no `sources/` ingest page — root cause of the drift fixed above; an ingest of that change would give the retirement a durable source record.

## [2026-07-16] lint | Full-vault health pass #2 (181 pages), scoped to provenance integrity after the prior pass closed the workflow-audit drift. STRUCTURE CLEAN: 0 orphans (0 of 89 `sources/` pages orphaned once frontmatter `sources:` refs are counted as inbound edges — a body-`[[link]]`-only orphan graph falsely reports nearly every source page and is a parser artifact, not a finding), 0 stale evergreen pages (all `commands/`/`hooks/`/`skills/`/`design/` within 30d; `sources/`+`investigations/` are immutable/point-in-time and stale-by-design — flagging them is a category error), 0 frontmatter defects across all 181 pages (every page carries the full required key set), 0 inbox backlog (no `inbox/` dir). CONTRADICTIONS: 0 — full-vault scan run this pass (grep for `⚠️ CONTRADICTION` across all content pages returns zero; every hit is log.md's own historical prose narrating past sweeps, plus the known-benign schema meta-text in wiki-lint/wiki-query pages — not flagged contradictions). INDEX RECONCILIATION: run page-by-page across all 179 catalogable pages this pass, NOT spot-checked — and this caught 3 real gaps an earlier 13-page spot-check had missed, proving the spot-check insufficient. FIXED (3 index bullets added to the `## Sources` list, each placed in PR-number order matching the list's existing convention, facts taken from the pages themselves not paraphrased from titles): [[pr_103_agentic-loop-path-session-fallback]] (#103, `bf02e8a` — session-id fallback probe closing the plugin-version re-keying gap [[agentic-loop-path-keying]] filed as owner-owed), [[pr_155-158_ceremony_noise_envelope_anchoring]] (#155/#157/#158, loop 1cf76302 — warn demotion + `authorising_prompt_raw` envelope-anchoring precedence rule), [[pr_175-176_crack-on-gate-and-inbox-brief-button]] (#175/#176, `09cb0d0`/`fc122d3` — crack_on_gate AskUserQuestion deny + Inbox Brief button). All 3 were genuinely ingested and well cross-linked elsewhere (1, 6 and 7 inbound refs respectively) — only their index bullet was absent, i.e. pure catalog drift, the exact class lint owns. Post-fix: 0 of 179 pages missing from index; all new outbound links verified to resolve. FINDING (1 genuine, now FIXED): dangling provenance to files deleted by [[pr_138_remove-specs-plans-tracking]] (2026-07-11 gitignored+deleted `docs/coderails/specs/` and `docs/coderails/plans/`). Verified all 4 cited spec paths absent from git (`git ls-files` = 0 hits each), so these were pointers to files that no longer exist. The #138 caveat sweep had landed on 6 of 9 non-source pages ([[brainstorming]], [[writing-plans]], [[task-evals]], [[dashboard]], [[agentic-loop]], [[review-artifact-seam]], and [[spec-plan-progress-artifact-chain]] — the latter exemplary, already carrying a correct dated supersession + wiki-link) but MISSED 3 — the classic multi-item-sweep-is-N-claims failure mode, ~2/3 true. FIXED: (1) [[task-evals-gate]] — `sources:` cited the deleted `2026-07-03-task-evals-design.md` with no caveat; replaced that entry with `sources/pr_138_remove-specs-plans-tracking.md` and added the same supersession note already used verbatim on its sibling [[task-evals]] ("This page's own content is now the durable record"). (2) [[queue-contract-cross-pr-audit_2026-07-07]] — body H2 still called the deleted `2026-07-06-assistant-link-panel-design.md` "**the normative contract**"/"source of truth for the `QueueFileEntry` shape", reading as a live pointer; added a dated ⚠️ banner redirecting to the durable homes ([[intent-queue-runner-contract]], [[assistant-link-send-gate-architecture]]). (3) [[dashboard-run-log-streaming-viewer-gap_2026-07-07]] — body finding #7 ("Design spec silence") cited the deleted observability spec; annotated inline as historical. On both investigations the original spec path is RETAINED in `sources:` with an inline `# deleted by PR #138` comment and `pr_138` added alongside — these are point-in-time records, so what informed the analysis is preserved rather than erased; only the live-pointer reading is corrected. `last_updated` bumped on all 3. DELIBERATELY NOT TOUCHED: 7 `sources/` pages (pr_1-4, pr_25, pr_28, pr_43-44-46, pr_50, pr_62_10, pr_96-98) still cite `docs/coderails/specs/*` — CORRECT as-is per the schema's immutable-PR-record rule; those specs existed at merge time and caveating them would corrupt the historical record. Do not re-flag. SUGGESTIONS (Step 4, unchanged from prior pass, still open — both are wiki-ingest's job, not lint's): (a) `fable-mode` and `loop-retro-promotion` remain shipped-but-undocumented skills; (b) the 2026-07-15 workflow-audit approval-gate removal still has no `sources/` ingest page.
## [2026-07-16] ingest | PR #192 merged (90f359e): frontier role re-pointed to opus at xhigh; effort joins the Model stamp. Owner-directed cost correction to PR #169's role table: auto-resolving `frontier` to fable (most expensive, ~2x opus) contradicted vendor model-selection guidance (complex agentic coding -> Opus, `xhigh` "best setting for coding/agentic"; Fable = next-gen intelligence at premium price) and made a silent cost decision the loop has no authority to make. Fable escalation now needs a named capability gap in the stamp + valve discipline. NEW effort dimension: every Model: stamp names role AND effort (frontier -> opus xhigh, max needs named reason; default -> sonnet high, medium allowed for bounded exact-recipe tasks only; fast-mechanical -> haiku, no effort param); effort tuning is the first lever, model escalation the second. Lockstep: agentic-loop SKILL Phase 2.8 + writing-plans stamp contract + AGENTS.md advisory-ceiling bullet. Gate trail: review artifact (no findings), tier-0 eval artifact GO with one on-record amendment (E3 removed pre-grading - validate-structure correctly refused a vacuous negative control identical to its check; folded into E2 evidence - the eval validator catching this live is itself evidence the gate works). Created: sources/pr_192_frontier-opus-effort-routing.md. Updated: skills/agentic-loop.md (table row + 2 new rule paragraphs), skills/writing-plans.md (stamp element 5), design/enforcement-model.md (role+effort wording), investigations/retro-json-per-model-cost-tracking-gap_2026-07-15.md (point-in-time supersession note), index.md. Note: the 3 in-flight improvement-loop plans (session-local) stamp roles not models - zero edits needed, the #169 indirection paying off.

## [2026-07-16] lint | Scoped pass on the PR #192 ingest (7 touched pages; full-vault structural pass ran earlier today, not repeated). CLEAN: all ingest wiki-links resolve except one (below); frontmatter complete on all 7; `origin:` field on the new source page matches source-page convention (pr_169 precedent); log ingest-entry format correct; full-vault `frontier` sweep confirms NO page outside the ingest set still implies frontier→fable (index pr_169 row is a historical PR record, now annotated); agentic-loop Phase 2.8 table/rules, writing-plans element-5 stamp, and enforcement-model ceiling bullet are mutually consistent (role AND effort, opus-at-xhigh, fable-as-justified-escalation). FIXED 4 (all ingest-scoped): (1) dead link [[pr_192_frontier-opus-effort-routing]] body pointed at nonexistent `[[loop-cost-tracking]]` — repointed to [[pr_184_185_186_loop-cost-tracking]] with alias; (2) [[enforcement-model]] got a PR-#192-driven body edit but its `sources:` lacked the pr_192 entry (agentic-loop and writing-plans both had it) — added; (3) [[retro-json-per-model-cost-tracking-gap_2026-07-15]] received the point-in-time supersession note without a `last_updated` bump or the superseding source in `sources:` — bumped to 2026-07-16 + added pr_192 (same treatment the pass-#2 lint applied to the 3 pr_138-annotated investigations); (4) index.md pr_192 catalog row was inserted mid-list after pr_169 (thematic adjacency) breaking the Sources list's PR-number-order convention — moved to list end after pr_184_185_186, and the pr_169 row's `frontier`→fable/opus-alternate clause annotated with a dated supersession pointer to [[pr_192_frontier-opus-effort-routing]] so the historical row can't be read as current. Declined as noise: writing-plans tags lack `effort-routing` (tags are freeform; not a defect). SUGGESTIONS (Step 4): (a) unchanged from earlier passes — `fable-mode` and `loop-retro-promotion` still lack wiki skill pages (wiki-ingest's job); (b) candidate connection: a short design/model-routing page consolidating the #169 role table + #192 effort/escalation rules now split across agentic-loop/writing-plans/enforcement-model would give the routing policy a single evergreen home — worth it only if a third routing PR lands.

## [2026-07-17] lint | Full-vault health pass (181 pages: 12 commands, 16 hooks, 34 skills, 19 design, 10 investigations, 89 sources). STRUCTURE CLEAN: 0 orphans (frontmatter `sources:` refs counted as inbound edges per the pass-#2 note — a body-link-only graph is a parser artifact), 0 stale evergreen pages (cutoff 2026-06-17; every commands/hooks/skills/design page is >= 2026-06-25 — the only sub-cutoff pages are immutable `sources/`+`investigations/` records, stale-by-design, NOT a finding), 0 frontmatter defects, 0 inbox backlog (no `inbox/` dir), 0 outstanding CONTRADICTION flags. DEAD LINKS: 0 real (the ~35 grep hits are bash `[[ -n ... ]]` tests, log.md prose, and schema placeholders like `[[wiki-links]]`/`[[page_name]]`; the 2 `[[capabilities/*]]` refs are deliberate, inline-labelled cross-vault pointers to assistant-agent-wiki). `discipline_catchup.md` re-confirmed correctly marked RETIRED (PR #159) — not a live-hook contradiction. FINDING (1 genuine, now FIXED): [[routines]] claimed "**The three shipped routines**" while the live config (`~/.claude/coderails-dashboard.json`, read this pass) defines **five** — the page had never absorbed `loop-retro-promotion-weekly` (added 2026-07-10) or `workflow-audit-weekly` (added 2026-07-17, PRs #195+#196, live-fired green). Verified against live config, not inferred. FIXED: section retitled to five, both routines documented with their real buttonRef/cadence/maxAgeSeconds/predicate/escalation values read from config; two consequential stale counts the same drift left behind also corrected — "all **three** shipped routines take this [buttonRef] path" -> "every shipped routine" (verified: 5/5 use buttonRef, 0 use skillCommand) and "the **two** weekly routines use 691200s" -> "all **four** weekly routines" (verified: 4 weekly, all 691200). last_updated bumped 07-13 -> 07-17. CLOSED A 3-PASS-OLD SUGGESTION: created [[loop-retro-promotion]] — flagged as shipped-but-undocumented by all three 07-16 passes and never actioned; writing it was forced this pass because the [[routines]] fix needed the link and a lint must not introduce its own dead link (it transiently did — caught by re-verifying every link I emitted, which is why that check exists). Written from `skills/loop-retro-promotion/SKILL.md`: graduation predicate (>=10 retros + >=1 lifecycle + >=1 decay), the "dormant run is a successful run — the log line IS the artifact" design, the recurrence-AND-repo-agnosticism promotion bar, and the two load-bearing delivery constraints (manifest lock to exactly one file w/ abort-with-cleanup; never raw `gh pr merge` because PreToolUse hooks do not fire headless — an instance of the [[enforcement-model]] ceiling). Indexed in the skill catalog. NEW DEFECTS RECORDED (both verified this pass, neither fixed — source/config defects, not wiki drift): (1) `loop-retro-promotion-weekly`'s artifact gate CAN NEVER PASS — its configured `promotion-runs.log` exists but is frozen at 2026-07-13, far outside the routine's own 8-day freshness window; the loop-state repo-key dir has drifted from the one pinned in config. Machine-path rot, **not a regression** — the commit that set it was correct when written, the checkout moved. Recorded as a dated warning on the new page. (2) the `{date}` token in both later routines' artifactPath resolves in LOCAL time while a `claude -p` run stamps UTC, so a correct run between 00:00-01:00 IST grades red against tomorrow's path — affects all four dated routines; recorded as a dated note on [[routines]]. PROBE HYGIENE: my first two ingest-coverage probes were broken (zsh ate `\b`; awk split only the first `_`-delimited number group), each producing a wall of false "UNINGESTED" hits incl. PRs I knew were covered — rewritten until they correctly resolved multi-PR filenames before any finding was drawn from them. Separately, the Step 6 commit initially ran the WRONG repo's test suite (sticky cwd -> coderails' `test_gate` fired on a doc-only wiki commit; 14 pre-existing install.sh failures, unrelated, coderails tree verified clean) and the chained heredoc for this log entry died with it — Step 5 was silently skipped and only caught by grepping for the marker afterwards. SUGGESTIONS (Step 4, ingest's job not lint's): (a) `fable-mode` is now the last shipped-but-undocumented skill (`skills/fable-mode/`, since 2026-07-08) — and it is unreferenced by AGENTS.md and README too, so the gap is upstream of the wiki: a behavioural skill sitting outside the documented catalog entirely; (b) ingest backlog is now the dominant gap — 10 merged PRs have no `sources/` page: #180, #182 (content did land in [[enforce_pr_workflow]]/[[no_edit_on_main]], only the source page is absent), #187, #189, #191, #193, #194 (deferral gate), #195/#196 (workflow-audit-weekly — this pass documented the routine from live config, but the PRs still lack their record), #197 (citation-gate comment scope); (c) the 2026-07-15 workflow-audit approval-gate removal still has no ingest page (carried from prior passes).
