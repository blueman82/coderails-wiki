---
title: Coderails Wiki Index
type: index
created: 2026-05-30
last_updated: 2026-07-17
sources: []
tags: [index, catalog]
---


# Coderails Wiki — Content Catalog

This is the first file Claude reads when answering queries about the coderails plugin.
Schema and maintenance protocols are in `/Users/harrison/Github/coderails/AGENTS.md`.

## What this plugin is

`coderails` is a Claude Code plugin (not an application) that ships three things:
1. A workflow command chain: `prep → push → merge → wiki`
2. Skills: `agentic-loop`, `test-driven-development`, `writing-plans`, `handoff`, `improve-prompt`, `planning-sequence`, `premortem`, `wiki-ingest`, `wiki-init`, `wiki-lint`, `wiki-query`, `using-coderails`, `using-git-worktrees`, `requesting-code-review`, `receiving-code-review`, `finishing-a-development-branch`, `dispatching-parallel-agents`, `systematic-debugging`, `verification-before-completion`, `writing-skills`, `subagent-driven-development`, `executing-plans`, `brainstorming`, `task-evals`, `dashboard`
3. A discipline loop: hooks that enforce confidence labels, verify-loop resolution, destructive-bash blocking, main-branch protection, and PR-workflow sequencing. **superpowers is no longer a dependency** — all core dev-workflow skills are vendored into the `coderails:` namespace. See [[self-containment]].

Source: `coderails/CLAUDE.md`. Plugin manifest: `coderails/.claude-plugin/plugin.json`.

---

## Commands

Slash commands in `coderails/commands/`. Advisory only — Claude chooses to invoke them.
See [[enforcement-model]] for why hooks enforce and commands do not.

| Command | File | Status |
|---|---|---|
| `/coderails:workflow` | `commands/workflow.md` | [[workflow]] |
| `/coderails:prep` | `commands/prep.md` | [[prep]] |
| `/coderails:push` | `commands/push.md` | [[push]] |
| `/coderails:post-review` | `commands/post-review.md` | [[post-review]] |
| `/coderails:merge` | `commands/merge.md` | [[merge]] |
| `/coderails:post-evals` | `commands/post-evals.md` | [[post-evals]] |
| `/coderails:init` | `commands/init.md` | [[init]] |
| `/coderails:test-gate-setup` | `commands/test-gate-setup.md` | [[test-gate-setup]] |
| `/coderails:assumptions` | `commands/assumptions.md` | [[assumptions]] |
| `/coderails:disconfirm` | `commands/disconfirm.md` | [[disconfirm]] |
| `/coderails:notchecked` | `commands/notchecked.md` | [[notchecked]] |
| `/coderails:verify` | `commands/verify.md` | [[verify]] |

Config resolution pattern shared by `workflow.md`, `prep.md`, `push.md`, `init.md`: [[config-resolution]]

---

## Hooks

Hook event wiring in `coderails/hooks/hooks.json`. Scripts in `coderails/hooks/scripts/`.
See [[discipline-loop]] for the design rationale across all hooks.

| Script | Event | Mode | Status |
|---|---|---|---|
| `inject_bootstrap.sh` | `SessionStart` | silent — bootstraps session with `coderails:using-coderails` | [[inject_bootstrap]] |
| `inject_context.sh` | `UserPromptSubmit` | silent — injects `[ctx]` date/cwd/branch; sole `UserPromptSubmit` hook from PR #159 (2026-07-13) until PR #175 added a sibling below | [[inject_context]] |
| `crack_on_gate.sh` | `UserPromptSubmit` + `PreToolUse (AskUserQuestion)` | silent stamp on raw-prompt "crack on" match; **block** (permissionDecision: deny) on `AskUserQuestion` while the session's flag is stamped (PR #175, 2026-07-14) | [[crack_on_gate]] |
| `crack_on_prose_gate.sh` | `Stop` | **block** (exit 2) a final message that hands a question to the user in prose while the crack-on flag is stamped — the prose half of `crack_on_gate`'s tool-deny; deterministic two-tier heuristic, fail-closed, per-turn 3-block cap (PR #238, 2026-07-17) | [[crack_on_prose_gate]] |
| ~~`discipline_catchup.sh`~~ | ~~`UserPromptSubmit`~~ | **RETIRED PR #159 (2026-07-13)** — file+test deleted, null-result (flat 23-26% miss rate unchanged by the nudge) | [[discipline_catchup]] |
| `check_confidence_labels.sh` | `Stop` + `SubagentStop` | **block** (exit 2) — ≥200-char response with no label; reads `last_assistant_message` on SubagentStop (PR #57); demotes to loop-warn on Stop inside an active incomplete loop (PR #155); `event=` telemetry (PR #159) | [[check_confidence_labels]] |
| `check_verify_loop.sh` | `Stop` + `SubagentStop` | **block** (exit 2) — any untagged `## Did Not Verify` bullet (file_count gate removed on Stop path, PR #61), PLUS missing-header presence check when turn `file_count >= 3` (PR #156, 2026-07-13, TURN-scoped file_count); reads `last_assistant_message` on SubagentStop (PR #57); demotes to loop-warn on Stop inside an active incomplete loop (PR #155/#156); `event=` telemetry (PR #159) | [[check_verify_loop]] |
| `loop_state_guard.sh` | `Stop` | **block** (exit 2) — agentic loop active but `progress.json` absent / session-mismatch (presence + ownership); shared transcript parse made malformed-line-tolerant, then failure-attribution corrected (`read_error`/`all_lines_malformed`), PRs #91/#107 | [[loop_state_guard]] |
| `loop_stall_guard.sh` | `Stop` | **block** (exit 2) — agentic loop active + incomplete + no `LOOP-STOP: <category>` declaration; sole writer of `progress.json.loop_stop_counts` (PR #98); shares the PRs #91/#107 malformed-transcript fix via the common lib; retro gate widened to `schema_version >= 1` (PR #186), retro now `schema_version` 2 with cost/models_used fields (PR #184); gained two more `complete`-only gates (PR #194 deferral gate on `work_units`, PR #198 proof gate re-deriving verdicts from the transcript against a blind-authored `proof.json`); gained a fourth `complete`-only step that NEVER blocks (PR #204 — `als_report_cost_on_complete`, a reporter not a gate, mechanically prints the loop's cost via the new `systemMessage` human-visible channel) | [[loop_stall_guard]] |
| `unregistered_loop_guard.sh` | `Stop` | **nudge only** (never blocks) — ≥3 distinct agent-dispatch turns + no `progress.json` + no `agentic-loop` Skill invocation (PR #17); nudges at most once per session, keyed on a BRE-escaped session id read back from the discipline log (PR #99); its OWN `jq -s` dispatch-turn count is now malformed-line-tolerant too (PR #113, closing the residual PRs #91/#107 left open) | [[unregistered_loop_guard]] |
| `destructive_bash_gate.sh` | `PreToolUse (Bash)` | **block** (permissionDecision: deny) — extended blocklist (git clean -f, find -delete, truncate, shred) + in-Bash source edits on main (PR #59); deny messages name a per-pattern concrete safe route instead of a bare generic sentence, first for 3 patterns (PR #203, 2026-07-17), then all ~13 blockable patterns same day (PR #216) | [[destructive_bash_gate]] |
| `test_gate.sh` | `PreToolUse (Bash)` | **block** on `git commit` if tests fail — opt-in via `.claude/test_command` | [[test_gate]] |
| `enforce_pr_workflow.sh` | `PreToolUse (Bash)` | **block** — `gh pr create` without prior `/push`; `gh pr merge`/`git merge`/`git push` on/targeting main without prior `/review-pr` (per-PR + consume-on-use + positional push, PR #58; NO_CONFIG opt-in); `gh pr merge` also requires a SHA-bound `GO` eval artifact (PR #97, 2026-07-08); a tier-0 artifact also requires a matching `tier-review` status, opt-in via `tier_review.machine_user` (Gate 8, PR #232, 2026-07-17) | [[enforce_pr_workflow]] |
| `no_edit_on_main.sh` | `PreToolUse (Write\|Edit\|MultiEdit)` | **block** — allowlist model (PR #60): everything except doc/config/special dotfiles on main/master; plugin-source markdown still blocked; `.claude/settings.json`/`.local.json` blocked on **any** branch (PR #70, merged 2026-06-29 — **pre-recreation** PR #70; see the collision note in [[repo-hosting]], not the 2026-07-07 PR #70) | [[no_edit_on_main]] |
| `voice_announce.sh` | `Stop` | **observe-only, always exits 0** — speaks a loop lifecycle event (complete/waiting/stopped/stall) via macOS `say`; runs first in the Stop array since it cannot affect other gates (PR #71, merged 2026-07-07) | [[voice_announce]] |
| `comment_citation_gate.sh` | `PreToolUse (Write\|Edit\|MultiEdit)` | **block** (permissionDecision: deny) — denies NEW code-comment content citing a session-artifact label (E#, F# fix, CHANGE B#/C#, Task A#, TA-I#, "reviewer finding X", WU#:, "per the plan/design/session"); scoped to comment-bearing content fields, skipped entirely for `.md` files; `PR #NN` is a documented survivor (durable, checkable GitHub artifact) | [[comment_citation_gate]] |
| `offload_push_guard.sh` | `Stop` + `SubagentStop` | **nudge only** (never blocks) — final message names a push to main/master AND carries an offload-to-user cue ("run this yourself", `! git push`, etc.); same nudge-once-per-session ledger idiom as `unregistered_loop_guard.sh` but unrelated subject matter (PR #108) | [[offload_push_guard]] |

Stop hook order: `voice_announce` (observe-only, first) → `check_confidence_labels` → `check_verify_loop` → `loop_state_guard` (C1) → `loop_stall_guard` (C2) → `unregistered_loop_guard` (PR #17, nudge-only) → `offload_push_guard` (PR #108, nudge-only, last). SubagentStop hook order (added PR #57, `offload_push_guard` added PR #108): `check_confidence_labels` → `check_verify_loop` → `offload_push_guard` (loop guards are Stop-only). The two ground-truth loop-state hooks share `hooks/scripts/lib/loop_state_common.sh` (vocab, active-loop detection, and named `als_gate_*` functions for Gates 1–4; extracted PR #49; gained the shared `als_extract_last_text`/`als_stable_last_text` text-extraction helpers in PR #71, moved out of `loop_stall_guard.sh`) and `hooks/scripts/lib/agentic_loop_path.sh` (sole path authority); `unregistered_loop_guard.sh` does not extend that shared lib (different evidence class — heuristic, not ground truth). The two remaining discipline hooks share `hooks/scripts/lib/discipline_common.sh` (transcript extraction + retry loop + TURN-scoped, jq-hardened `dc_file_count()` helper; added PR #29, extended PR #57, `dc_file_count` turn-scoped + hardened PR #156; `discipline_catchup.sh`, the library's third consumer, retired PR #159 — see [[discipline_catchup]]). See [[spec-plan-progress-artifact-chain]].

---

## Skills

Skills in `coderails/skills/*/SKILL.md`.

| Skill | Trigger summary | Status |
|---|---|---|
| `agentic-loop` | Multi-PR autonomous sessions; sits above `/workflow` | [[agentic-loop]] |
| `test-driven-development` | Worker construction discipline (red-green-refactor); code-guarded | [[test-driven-development]] |
| `writing-plans` | Turn a resolved spec into a durable task-by-task `plan.md` (Phase 2.7b, formerly 2.8) | [[writing-plans]] |
| `handoff` | Generates memory file + continuation prompt for next session | [[handoff]] |
| `improve-prompt` | Rewrites underspecified prompts using 7 diagnostic foundations; Phase -1 of agentic-loop | [[improve-prompt]] |
| `planning-sequence` | Pre-Parade → Premortem → Red Team on a plan | [[planning-sequence]] |
| `premortem` | Backwards-failure-mode analysis on a plan or decision (standalone; embedded in planning-sequence Stage 2) | [[premortem]] |
| `wiki-ingest` | Primary wiki write operation; translates a PR or decision into permanent vault entries | [[wiki-ingest]] |
| `wiki-init` | One-time vault bootstrap: creates vault, seeds AGENTS.md, wires Obsidian + qmd | [[wiki-init]] |
| `wiki-lint` | Wiki structural integrity auditor: contradictions, stale pages, orphans, missing cross-refs | [[wiki-lint]] |
| `wiki-query` | Wiki read operation; answers questions with citations, files hard answers back as investigations | [[wiki-query]] |
| `using-coderails` | Session orientation; auto-loaded at SessionStart via inject_bootstrap hook | [[using-coderails]] |
| `using-git-worktrees` | Git worktree mechanics for parallel agent isolation | [[using-git-worktrees]] |
| `requesting-code-review` | PR review submission discipline; sits at push→review boundary | [[requesting-code-review]] |
| `receiving-code-review` | Acting on review findings; triage by severity | [[receiving-code-review]] |
| `finishing-a-development-branch` | Final verification before merge; branch wrap-up | [[finishing-a-development-branch]] |
| `dispatching-parallel-agents` | Fan-out to parallel subagents with worktree isolation | [[dispatching-parallel-agents]] |
| `systematic-debugging` | Hypothesis-then-verify loop before touching code | [[systematic-debugging]] |
| `verification-before-completion` | Structured checklist against requirements before declaring done | [[verification-before-completion]] |
| `writing-skills` | Meta-skill for authoring new coderails skills | [[writing-skills]] |
| `subagent-driven-development` | Delegation pattern: implementer prompts → subagent → verify loop | [[subagent-driven-development]] |
| `executing-plans` | Executing a `plan.md` task list with scope discipline | [[executing-plans]] |
| `brainstorming` | Structured ideation + Decision Ledger before committing to a design | [[brainstorming]] |
| `task-evals` | Generates and freezes a tiered `evals.json` (pr or loop scope) for the task-evals merge gate | [[task-evals]] |
| `engineering-principles` (+3 language sub-skills: go/python/ts) | Enforces YAGNI/KISS/DRY/Fail-Fast/SSOT/Law-of-Demeter; per-language dispatch | [[engineering-principles]] (+ [[engineering-principles-python]] / [[engineering-principles-go]] / [[engineering-principles-ts]]) |
| `sync-docs` | Audits in-tree docs (README/AGENTS.md/docs/) for drift; ships in-repo since 2026-07-10 (an earlier "user-level only" claim on this page was stale and is corrected); invoked at agentic-loop Phase 9 and by `docs-sync`'s nightly pipeline | [[sync-docs]] |
| `docs-sync` | Machine-run only: nightly pipeline that invokes `sync-docs`'s audit and, only if drift is found, edits/pushes/reviews/self-merges a fix with no human in the loop; manifest-locked to git-tracked `.md` files with a self-governance deny-list | [[docs-sync]] |
| `dashboard` | Live local observability HUD (Next.js/R3F web + Obsidian command centre); sub-project 1 of 5 in the agentic-OS evolution | [[dashboard]] |
| `workflow-audit` | Mines transcripts for repeated tool-use patterns, judges skill candidates, creates every `propose`-verdict candidate (in-session `AskUserQuestion` gate removed 2026-07-15; queue-mode dashboard Approve is the opt-in human gate); sub-project 3 of 5 in the agentic-OS evolution | [[workflow-audit]] |
| `verify-merged-pr` | Re-derives a "PR #N is merged" claim (state + content on origin/main + sibling PRs) before relying on it; the first skill built end-to-end by the dashboard Approve→build pipeline | [[verify-merged-pr]] |
| `loop-retro-promotion` | Machine-run only (not interactive): mines accumulated loop retros for repo-agnostic lessons and promotes them into `learned-failure-modes.md`; dormant until its graduation predicate is met, manifest-locked to one file | [[loop-retro-promotion]] |

---

## Design

Architectural decisions and invariants.

- [[enforcement-model]] — hooks vs. commands; the central design distinction
- [[discipline-loop]] — how the discipline hooks compose into a coherent loop (as of PR #238, 2026-07-17, verified against `hooks/hooks.json` on origin/main: 1 SessionStart + 2 UserPromptSubmit + 8 Stop + 3 SubagentStop + 6 PreToolUse = 20 hook commands across all events; [[crack_on_prose_gate]] is the newest Stop hook, [[crack_on_gate]] contributes the UserPromptSubmit + PreToolUse pair)
- [[config-resolution]] — walk-up `workflow.config.yaml` resolver (`scripts/lib/config.sh`) shared by all workflow commands + the merge-gate hook
- [[install-and-cache-trap]] — idempotency contracts for `install.sh`/`uninstall.sh` and the reload-plugins caveat
- [[hook-exit-codes]] — which hook events block on exit 2; why coderails uses two block mechanisms
- [[repo-hosting]] — where the plugin + wiki repos live (private, `blueman82`), clone locations, `.gitignore` rules
- [[spec-plan-progress-artifact-chain]] — the agentic-loop's full spec→plan→progress durable-artifact chain + the two-hook (C1 presence/ownership, C2 anti-stall) loop-state guard architecture
- [[self-containment]] — the 2026-06-25 decision to vendor 12 superpowers skills into coderails; superpowers now uninstallable
- [[skills-hooks-seam]] — the cross-reference obligation between skills (advisory) and hooks (mechanical); the merge-base regex footgun; added PR #42
- [[review-artifact-seam]] — the 2026-06-30 decision to move review truth from ephemeral chat to a SHA-bound GitHub PR comment; `/merge` fail-closed artifact gate; honest ceiling (structure not provenance)
- [[history-identity-scrub]] — how to erase a stale commit-author identity from a GitHub repo's history so no clone method (normal or `--mirror`) recovers it; the `refs/pull/*` trap that makes a `filter-repo` rewrite alone insufficient; delete-and-recreate is the only complete fix; the three purity invariants; post-rewrite branch-pruning caveat
- [[task-evals-gate]] — dual-scope (pr + loop) merge gate over frozen, oracle-independent success evals; second independent enforcement axis alongside [[review-artifact-seam]]
- [[loop-progress-fields]] — consolidating page for `progress.json`'s `work_units` (orchestrator-owned) and `loop_stop_counts` (hook-owned) fields
- [[trust-floor]] — consolidating page for the trust-floor/OWNER-permission model (PR #14, extended with the `tempfile` reason by PR #21); SSOT for the mechanism remains [[merge]]
- [[agentic-loop-path-keying]] — repo-keyed (not cwd-keyed) `progress.json` slug via `git --git-common-dir`; closes a mid-loop `EnterWorktree` orphaning bug; supersedes [[pr_86_agentic-loop-hardening|PR #86]] point 6's rejected locking machinery; PR #24
- [[assistant-link-send-gate-architecture]] — assistant-agent's host-process SDK `PreToolUse` send-gate (chosen over a bash hook because a bash hook is agent-forgeable); the SDK's fail-open-on-throw/timeout finding that forces the gate to self-enforce fail-closed; the `~/.claude/coderails-dashboard/approvals/<hash>.json` seam connecting it to the dashboard's ASSISTANT.LINK panel (moved from `queue/` 2026-07-07, [[pr_62_10_approvals-dir-move]]); the marketplace-consumer analogue of [[install-and-cache-trap]] (assistant-agent's installed plugin cache silently lagged source ~2 weeks). Sub-project 4 of 5

---

## Investigations

Point-in-time filed investigations (`<topic>_<YYYY-MM-DD>.md` naming).

- [[install-cache-trap_2026-05-30]] — initial investigation filed at wiki bootstrap
- [[install-bash32-bad-substitution_2026-06-01]] — `${answer,,}` (bash 4) broke install.sh on macOS bash 3.2; fixed with `tr` lowercase
- [[skill-testing-state_2026-06-26]] — coderails has ZERO automated skill testing; the lone planning-sequence evals.json was a dormant skill-creator quality-eval artifact (deleted, PR #55); skill-creator (external) has two eval systems (trigger `run_eval.py` + agent-graded quality); decision NOT to build a runner (corpus=1); candidate reminder-hook deferred
- [[claude-code-transcript-schema_2026-07-07]] — catalogue of Claude Code session transcript JSONL record types (only `user`/`assistant`/`system` carry conversation; 10 bookkeeping-only types); tool_use extraction locus; gotchas surfaced by [[workflow-audit]]'s scan stage (bare-string content, last-prompt resume-cache mtime trap, non-string input type-guarding, per-line parse isolation)
- [[queue-contract-cross-pr-audit_2026-07-07]] — cross-PR audit of the `~/.claude/coderails-dashboard/queue/<hash>.json` contract, filed when PR #31 (Approve/Deny button) had no source page yet: verified `QueueFileEntry` verbatim-matching the normative spec and assistant-agent's send-gate; flagged workflow-audit's queue-mode approval path (WU1–WU3) as still pending. Both since closed same day: PR #31 now has its own page ([[pr_31_assistant-link-approve-button]]) and the queue-mode work shipped ([[pr_43-44-46_workflow-audit-queue-seam]]). PR #36 (routines sub-project 2, the still-unbuilt runner) remains without a wiki page
- [[dashboard-run-log-streaming-viewer-gap_2026-07-07]] — **CLOSED 2026-07-08**, see [[pr_80-82_dashboard-stream-run-output-viewer]]. Verified against source at the time that the per-run `runs/<runId>.log` file was write-only: `route.ts` appended `stdout+stderr` once, post-exit; `RunRecord.outputPath` was never read anywhere; the aggregator's recursive `fs.watch` on `runsDir` fired on log writes but only triggered a `runs.jsonl` metadata re-read, never emitting log content over SSE. Documented 6 cross-PR constraints (Origin/Host guard, strict-ID-validation precedent, single-SSE-provider requirement, collector never-throw discipline, token non-leakage, long-lived-POST/lock coupling) a future plan had to respect — all six honoured by the closing PRs
- [[evals-gate-enforcement-gap_2026-07-08]] — filed alongside [[pr_95_slash-command-loop-detection]]: audits which entry workflows route to `task-evals` (agentic-loop hook-enforced loop-scope only, above the 3-work-unit threshold; writing-plans and subagent-driven-development prose-only) vs. which don't (`systematic-debugging` has zero evals mention at all — verified via grep). Concrete consequence: PR #95's own debugging-framed fix satisfied the review gate but never touched the evals gate. **CLOSED same day** by [[pr_96-98_evals-gate-uniform-enforcement_2026-07-08]] (PR #97 hook-level `gh pr merge` eval gate + PR #96 systematic-debugging routing)
- [[dashboard-lockfile-emnapi-drift_2026-07-08]] — `npm ci` failed on dashboard first-run (`EUSAGE`) because `skills/dashboard/app/package-lock.json` was missing two transitive/optional native-dep entries (`@emnapi/core`/`@emnapi/runtime`). A standalone fix PR (`fix/dashboard-lockfile-emnapi-drift`) regenerated the lock, but was **closed, not merged** — verified the drift was already resolved on main by a later dashboard PR (`npm ci --dry-run` clean, diff against main byte-identical). General lesson: `npm ci` is exact-lock and fails hard on lockfile drift that `npm install` would silently repair; verify a lockfile PR against current main before merging, since parallel sessions touching the same deps can silently resolve each other's pending fixes. See [[dashboard]]
- [[dashboard-run-output-rendering-gap_2026-07-15]] — filed answering a query on run-output rendering/inbox-brief/run-route/button-spawn: the wiki's Run Output viewer section was **factually stale**, not just incomplete — PRs #170-#174 (same `dashboard-rethink` loop as #163-168, all merged 2026-07-14) replaced the inline `<pre>`+raw-toggle viewer ([[pr_139-141_dashboard-ask-enter-clean-output]]) with a portal modal (`RunOutputOverlay.tsx`) rendering sanitized `react-markdown` with an undocumented img→alt-text XSS/SSRF-beacon guard, and removed the 80-char title truncation the wiki still described. Confirmed `route.ts`'s literal `spawnImpl("claude", ...)` (not config-derived) as the real enforcement behind "a button can only spawn claude." Flags open PR #179 (`dashboard-lan-access`, unmerged) as actively touching the Origin/Host localhost guard — that PR has since merged, see [[pr_179_dashboard-lan-access]]. **Closed 2026-07-15** by the [[pr_181_183_dashboard-run-output-wrap-and-inbox-brief-clean-modal|#181/#183/#27 ingest]], which rewrote [[dashboard]]'s Run Output section to match current code
- [[retro-json-per-model-cost-tracking-gap_2026-07-15]] — filed answering a query about adding per-model cost tracking to retro.json + dashboard rollups: **there is no cost or per-model tracking anywhere in coderails** (zero wiki hits before this page, zero fields in `retro.json`'s verbatim schema). Surfaces a previously wiki-undocumented collector, `skills/dashboard/app/src/lib/collect/usage.ts`, found only by reading source — machine-wide (all `~/.claude/projects/*`), two flat rolling windows (5h/week), no per-model/per-loop/per-PR breakout, no dollar conversion, feeding SYSTEM VITALS only. Documents why Phase 2.8's model-role routing (PR #169) can't be used as a cost join key today: it's a single free-text `decisions_absorbed` bullet, not hook-enforced, records declared intent not verified spawn fact. Lists 6 cross-PR constraints a cost-tracking plan must respect (hook-vs-orchestrator ownership per [[loop-progress-fields]]'s pattern, no-verdict schema rule, gate is presence-only not correctness, no model-at-spawn enforcement, `usage.ts`'s machine-wide/non-loop-scoped shape, gitignored specs/plans). Not yet ingested into proper source pages — flagged for `/wiki-ingest` if the cost-tracking feature ships

---

## Sources

Ingested PR and change records (`sources/pr_<N>_*.md`; session sources for direct-edit work).

- [[session_2026-05-31_verify-loop-hardening]] — verify-loop rework (meta-exclusion, threshold, string-coercion), cache-trap, exit-2 findings
- [[session_2026-05-31_prompting-doc-alignment]] — prompting-doc alignment: --no-verify gate, agentic-loop context/delegate guidance, discipline scope+floor clauses, workflow parallel-calls
- [[session_2026-06-01_verify-loop-total-enforcement]] — verify-loop escalated to total enforcement: source-token regex + meta_pattern removed, any untagged DNV bullet blocks, `(unverifiable: …)` is the sole escape
- [[session_2026-06-01_agentic-loop-delegate-all-impl]] — agentic-loop: main context becomes pure orchestrator; every code change (even single-file) goes to sonnet agent for impl+verify; two-rung delegation ladder; new Phase 3a (plugin commit 3c33f99)
- [[session_2026-06-25_agentic-loop-upgrade-arc]] — the agentic-loop upgrade arc (PRs #12–#18): Specs A (clean-migration), C1 (progress.json lifecycle), C2 (anti-stall), B (slim), D (vendored TDD), E (spec→plan artifacts), + #18 tool-agnostic cleanup
- [[pr_19-30_self-containment-and-hardening]] — self-containment + plugin hardening cluster (PRs #19–#30): 12 skills vendored from superpowers, SessionStart bootstrap hook, no_edit_on_main + enforce_pr_workflow enforcement hooks, install.sh chmod derivation, discipline_common.sh extraction
- [[pr_39_agentic-loop-slim-v2]] — agentic-loop slim v2 (reference-not-embody): PR #39 normalised 13 bare skill refs to `/coderails:` form; two independent passes against a four-part test found ZERO further passages to slim — the embodied content is irreducible autonomy supersets. Do not re-attempt
- [[pr_40_hook-hardening]] — PR #40 hook hardening (2026-06-25): enforce_pr_workflow gains a `git merge` gate on main/master (closes local-merge bypass); inject_bootstrap jq refactor (escape_for_json removed, clean-break); new run_all.sh aggregate test runner with zero-test guard (13 suites)
- [[pr_41_phase25-brainstorming-xref]] — PR #41 (2026-06-25): Phase 2.5 design-fork resolver now applies brainstorming's design-quality discipline (YAGNI, design-for-isolation) by reference, without brainstorming's human-approval gates
- [[pr_42_skills-hooks-seam]] — PR #42 (2026-06-25): merge-base regex exclusion bug fix; reordered git-merge hint; skills↔hooks seam convention; finishing-a-development-branch Option 1 hook note
- [[pr_43_rough-edges]] — PR #43 (2026-06-25): failure_log template cleaned (3 stale dev-history rows removed); GitHub-only constraint made user-facing in README; merge.sh enforcement-gap notice when enforce_pr_workflow is inactive
- [[pr_44_no-edit-plugin-source]] — PR #44 (merged 2026-06-25): no_edit_on_main now gates plugin-source markdown (`skills/*/SKILL.md`, `commands/*.md`) on main, narrowing the docs carve-out; deliberate decision NOT to gate `git push` (edit-time is the right seam); `docs/superpowers` historical tree removed
- [[pr_46_gate-git-push-on-main]] — PR #46 (merged 2026-06-26, 7a4906c): enforce_pr_workflow gains a `git push` gate mirroring the `git merge` gate — blocks pushes that land on main/master (current branch OR explicit destination refspec like `HEAD:main`) without prior `/review-pr`. Destination-refspec model (not current-branch only); metachar-anchored against `HEAD:main;echo` evasion; bare positional `git push origin main` is a documented unparsed limitation. Review caught a Critical current-branch-only false-allow; tests 14→27. Distinct from #44's no_edit_on_main push decision
- [[pr_47_strictcode-skill-config]] — PR #47 (merged 2026-06-26, f00ed54): `strictcode_skill` promoted from hardcoded `/strictcode-python` to a configurable `workflow.config.yaml` field. `/coderails:init` now prompts with language auto-detection (go.mod → /strictcode-go; package.json+*.ts → /strictcode-ts; else /strictcode-python). [[push]] and [[workflow]] read `config.strictcode_skill` (default `/strictcode-python`). `workflow.md` allowed-tools expanded to pre-authorise `/strictcode-go` and `/strictcode-ts`. Fully backward-compatible
- [[pr_50_planning-sequence-gate]] — PR #50 (merged 2026-06-26, 03b5b11): [[writing-plans]] gains a mandatory `/coderails:planning-sequence` gate — after the self-review gate and before implementation hand-off, the written plan runs through Pre-Parade → Premortem → Red Team and findings fold back inline. Advisory (no hook). Also relocated engineering-principles spec/plan into `docs/coderails/{specs,plans}/` and landed design docs for the not-yet-implemented strictcode→engineering-principles vendoring. [[planning-sequence]] reframed: now a required *downstream* gate, not only an optional upstream step
- [[pr_49_gate-function-rename]] — PR #49 (merged 2026-06-26, 0426312): positional `# Gate N` comments replaced with named bash functions in `enforce_pr_workflow.sh` (7 functions: `gate_has_command`, `gate_safe_passthrough`, `gate_in_scope`, `gate_config_present`, `gate_targets_main`, `gate_have_transcript`, `enforce_required_step`). Gates 1–4 byte-identical between `loop_state_guard.sh` and `loop_stall_guard.sh` extracted into new shared lib `hooks/scripts/lib/loop_state_common.sh` (`als_gate_*` / `als_load_progress`); each guard keeps own Gates 5–6. Known limitation documented: `enforce_pr_workflow` checks invocation evidence not completion — fix deferred. CLAUDE.md hook conventions paragraph corrected (scoped to 3 scripts; removed false claim about `check_verify_loop.sh`). `.worktrees` added to `.gitignore`
- [[pr_54_engineering-principles-vendoring]] — PR #54 (merged 2026-06-26, 2f1ad1c): vendored the global `strictcode` family into coderails as the **`engineering-principles`** family (4 skills), stripping Scout/SlimCode (kept Serena + Grep fallback). Renamed config `strictcode_*` → `engineering_principles_*` and default `/strictcode-python` → `/engineering-principles-python` across [[push]]/[[workflow]]/[[prep]]/[[init]]/[[config-resolution]]. Added the PR-review touchpoint (`workflow` Phase 3 `2b` /engineering-principles + `2c` /simplify). README 23→27 / four groups. Globals removed. **Supersedes [[pr_47_strictcode-skill-config]]**
- [[pr_55_remove-dead-evals]] — PR #55 (merged 2026-06-26, ce499d7): deleted the orphaned `skills/planning-sequence/evals/evals.json` — a dormant skill-creator quality-eval artifact referenced by nothing in coderails, falsely implying a test harness existed. See [[skill-testing-state_2026-06-26]]
- [[pr_56_writing-skills-using-coderails-example]] — PR #56 (merged 2026-06-26, 0f0969e): [[writing-skills]] doc fixes — stale `using-skills`→`using-coderails` naming example corrected, and the new `examples/CLAUDE_MD_TESTING.md` worked test-campaign added (closes a dangling reference in `testing-skills-with-subagents.md`). Documents the *manual* skill-testing ritual only — no automation added
- [[pr_57-62_subagent-enforcement-gate-hardening]] — PRs #57–#62 (merged 2026-06-26): subagent enforcement + gate hardening cluster. #57: SubagentStop discipline for check_confidence_labels + check_verify_loop (reads last_assistant_message, not parent transcript). #58: enforce_pr_workflow hardened — per-PR review evidence, consume-on-use, positional git push origin main, flag-boundary tightening, subagent transcript. #59: destructive_bash_gate extended — git clean -f/--force, find -delete, truncate, shred + in-Bash source-file edits on main (best-effort). #60: no_edit_on_main code arm inverted to allowlist (everything except doc/config/special dotfiles blocked on main). #61: check_verify_loop file_count gate removed on Stop path (DNV enforcement independent of file edits). #62: docs sync + Enforcement ceilings section + .gitignore allowlist over-match fix
- [[pr_63_remove-failure-log]] — PR #63 (merged 2026-06-26, 18392fb): deleted the orphaned `templates/failure_log.md` + all plumbing (install/uninstall/CLAUDE.md/INSTALLATION.md). It was the data input for claude-guardrails' removed weekly calibration ritual — consumer gone, zero entries in two months. An automation plan (`/failure` + `/failure-review`) was DECLINED: empty log = demand signal, not friction signal. Records the caught-vs-escaped failure distinction (only caught failures are hook-capturable). Fixed the [[discipline-loop]] Founding-Thesis dead pointer (quote preserved in-wiki). Kept: docs/coderails-review.md (historical) + README "no calibration ritual" line
- [[pr_64_loop-review-via-skill]] — PR #64 (merged 2026-06-26, ee44aa0): agentic-loop Phase 4b changed to invoke `/pr-review-toolkit:review-pr <PR#>` as a Skill (with PR number as arg) instead of hand-rolling six reviewer agents. Root cause: enforce_pr_workflow only recognises Skill invocations as merge evidence; manual agent fanout leaves no recognisable record. CLAUDE.md skills↔hooks seam convention extended with one sentence naming this constraint
- [[pr_69_2026-06-29_no-edit-message-worktree]] — PR #69 (merged 2026-06-29, 86e701a): [[no_edit_on_main]] deny message + header comment reworded to recommend an isolated **worktree + branch** (`/coderails:prep` or `git worktree add <path> -b <name>`) instead of a plain feature branch. Prose-only — block conditions and the 20/20 test suite unchanged. Records a gotcha: `scripts/merge.sh` exits 128 (post-merge `git checkout main`) when run from inside a linked worktree, though the merge itself lands
- [[pr_70_2026-06-29_gate-settings-json-edits]] — PR #70 (merged 2026-06-29, e52e541; supersedes closed #68): [[no_edit_on_main]] gains a **permission-file arm** blocking `.claude/settings.json`/`.claude/settings.local.json` edits on **any** branch — the first non-main arm. Closes the self-escape door: the `permissions.allow` rules pre-approve commands upstream of every gate, so editing them dismantles the discipline layer. Friction not lock (Bash-path edits + plugin-disable still open; no server-side lock in this repo by design, PR #182). Collateral: retargeted `ceiling_note.test.sh` CLAUDE.md→AGENTS.md (stale after #67). Process gotcha: force-push to rebase #68 was blocked by [[destructive_bash_gate]], so a fresh branch was used — the gate forcing its own safe path
- [[pr_72_config-walkup-symlink-hang]] — PR #72 (merged 2026-06-29, 3862614): fixed an **infinite-loop hang** in `scripts/lib/config.sh::coderails::config_path()`. Walk-up terminated only on `d == git_root` string equality, but `git rev-parse` returns symlink-resolved paths (macOS /tmp→/private/tmp); on the NO_CONFIG path the loop spun at `dirname /` forever. Surfaced as a hang in [[enforce_pr_workflow]]'s "no workflow.config.yaml" test case, wedging `run_all.sh`. Bug from #67 (walk-up) + #71 (extracted `config.sh` with no test). Fix: `pwd -P` canonicalisation + hard `/` floor. Added `config.test.sh` (watchdog-guarded reproducer; fails pre-fix, passes post-fix). Suite 14→15, now 307 ok / 0 FAIL. Updated stale [[config-resolution]] (still documented the pre-#67 dual-path lookup). Lesson: absence of a FAIL line ≠ pass when the process was killed mid-hang
- [[pr_74_config-test-watchdog-stall]] — PR #74 (merged 2026-06-29, 55e0d63): the watchdog #72 added to `config.test.sh` was correct but **slow** — every `resolve()` call paid the full 5 s `sleep` (~25 s of the ~45 s suite). Root cause: `kill $w` orphans the watchdog's `sleep` child, which keeps the `$()` pipe write-fd open, so `$()` blocks the full 5 s — a pipe-fd lifetime issue, **not** the "ignores SIGTERM" the first comment claimed. Fix: `pkill -P $w` kills the sleep directly (closes fd); `{ } 2>/dev/null` swallows the job-control notice. Suite ~45 s→~21 s. Review (comment-analyzer) caught the false comment; code-reviewer self-retracted a wrong narrower-fix. Lesson: a comment that misexplains the mechanism is worse than none
- [[pr_75_main-branch-fallback]] — PR #75 (merged 2026-06-29, feaefe9): the `main()` helper in `scripts/lib/git-common.sh` returned a **blank string** instead of falling back to literal `main` when `git symbolic-ref refs/remotes/origin/HEAD` was unset. Root cause: in `git ... | sed ... || echo main`, the `||` binds to the **pipeline's** exit status, which is `sed`'s — `sed` exits 0 on empty input, so the fallback never fired. Fix: capture the result, `echo "${m:-main}"` (key the fallback off empty **output**, the real symptom). Latent — the marker is set by `git clone`/`gh`, so only bare-`git init` test scaffolding ever hit the blank; starved `sync::main_branch` (added by un-ingested #73) into a silent no-op. 3 reviews (code/tests/silent-failure) clean, 0 findings. Same pipeline-mechanism family as #74. Lesson: `cmd | filter || fallback` keys the fallback off `filter`'s exit, not `cmd`'s
- [[pr_76_harden-hook-stdin-read]] — PR #76 (merged 2026-06-29, 37459a3): replaced `input=$(cat)` with `IFS= read -r -d '' -t 5 input || true` in all 10 hook scripts. Defence-in-depth for a latent stdin-blocking risk (orphaned hook blocking forever if parent dies without closing stdin). NOT the fix for the fan incident (config.sh walk-up loop, PR #72). On read timeout, input empty → exit 0 (fail-open is safe: dead parent = no live pipeline). `|| true` mandatory (`read -d ''` exits 1 on normal EOF). Added `stdin_bounded_read.test.sh` (guard + fidelity tests, TDD). Security: CLEAN 9/10. ~~Known gap: no automated `min(hooks.json timeout) >= 5` guard~~ → **Closed by PR #78**
- [[pr_77_agentic-loop-sync-docs-step]] — PR #77 (merged 2026-06-29, cefb47c): agentic-loop Phase 9 gains a `/sync-docs` docs-drift check at the loop boundary. Wiki ingest (external KB) and `/sync-docs` (in-tree docs) are now explicitly separated as the two loop-boundary doc steps. `/sync-docs` runs without Serena (`--semantic` optional), is delegated to a spawned agent, and follows the same finding-triage discipline as Phase 5 (fix only loop-introduced drift; surface pre-existing drift to the user)
- [[pr_78_hooks-json-timeout-floor]] — PR #78 (merged 2026-06-29, ceabb75): closes the known gap from PR #76 — added `hooks/scripts/tests/hooks_json_timeout_floor.test.sh` asserting `min(declared hooks.json timeout) >= 5` (Half A of the timeout invariant). **Both halves of the invariant are now fully guarded by PR #80** — see [[pr_80_guard-read-t-floor]]
- [[pr_79_sync-docs-drift]] — PR #79 (merged 2026-06-29, 0bcb44e): fixes 4 pre-existing doc-drift findings surfaced by `/sync-docs` (Phase 9, PR #77). F1 (High): `docs/REFERENCE.md` missing 4 engineering-principles skills + false "complete catalogue" claim — added section, reconciled count 23→27. F2 (Medium): `README.md` listed bare `truncate` in destructive_bash_gate blocklist — corrected to `truncate -s/--size`. F3 (Low): `README.md` + `AGENTS.md` omitted inject_context.sh's first-prompt discipline reminder — added. F4 (Low): `AGENTS.md` said "four core gate scripts" omitting test_gate.sh — corrected to "five". Docs-only change; no runtime impact
- [[pr_80_guard-read-t-floor]] — PR #80 (merged 2026-06-30, f5aacde): extends `hooks_json_timeout_floor.test.sh` with Half B of the timeout invariant — closes test-analyzer S1 from PR #78's review. Half A (PR #78): `min(hooks.json declared timeout) >= 5`. Half B (this PR): exactly 10 hook scripts carry the `read -r -d '' -t N` backstop, and every N == 5. Both halves together are tamper-evident: lowering the in-process `read -t` value alone OR a hook silently gaining/losing the backstop now fails the suite. Two review hardening fixes: dropped redundant `|| echo 0`; switched `read -t` extraction to `grep -oE` for end-of-line tolerance
- [[pr_81-83_review-artifact-seam]] — PRs #81–83 (merged 2026-06-30): independent-review truth seam. #81: design spec (5 review rounds) + plan (planning-sequence stress-tested). #82: engine — `scripts/lib/review-artifact.sh` (marker SSOT), `scripts/post_review.sh` (validate + write-cache), `commands/post-review.md` (SHA-bound artifact poster), `git-common.sh` additions (pr::head_sha + pr::has_coderails_review_for_head, exit codes 0/1/2), `merge.sh` gate (fail-closed, no local fallback). #83: wiring — `/coderails:post-review` inserted into `workflow.md` Phase 3 + `agentic-loop/SKILL.md` Phase 4b (loop symmetry); optional `/prep` Part 1b progress.json stub; AGENTS.md/REFERENCE.md/README.md docs. Suite 21/21 passes
- [[pr_86_agentic-loop-hardening]] — PR #86 (merged 2026-07-01, b8a1958): 7 review-resolved hardening decisions on `skills/agentic-loop/SKILL.md`. Stage-map overview (5 stages over 19+ phases); merged Phase 2.7+2.8 into one guard-gated phase (sub-steps 2.7a/2.7b); removed the orchestrator's self-demote power over an independent reviewer's clean-break MERGE-BLOCKER (fix-it-or-hard-stop, Phase-0-only quoted carve-out); dropped Phase 13's gameable scorecard for raw LOOP-STOP counts + an unscored decisions-absorbed list; documented `model: sonnet` as a deliberate advisory (non-hook-enforced) ceiling in AGENTS.md; documented the single-loop-per-directory `progress.json` race invariant (resolution at the time: git worktrees — **fixed same-day by [[pr_87_agentic-loop-path-session-keying|PR #87]]**); replaced 3 stale/nonexistent memory-file citations with inline prose. Post-merge review fixed 2 Critical stale cross-refs + 1 Important Phase 0 gap, inside the same merge commit
- [[pr_87_agentic-loop-path-session-keying]] — PR #87 (merged 2026-07-01, 344a849): fixes the single-loop-per-directory race PR #86 had only documented — `agentic_loop_path.sh` now keys `progress.json` on cwd **and session_id**, not cwd alone, so concurrent sessions in one directory get independent files automatically (worktrees no longer required for this specific collision). Relies on Claude Code's `session_id` staying stable for one conversation across its own compaction/restart. Same-PR follow-up review caught and fixed a Critical residual bug: the guard scripts' `jq` extraction and the path helper's own fallback both used fixed sentinel strings when session_id was missing/null, which would let multiple such sessions collide again — closed with a unique PID+timestamp fallback (`als_sanitise_session_id()`) applied consistently across all 3 call sites. Also fixed two stale `docs/REFERENCE.md` rows describing the old cwd-only path format
- [[pr_89-91_skills-doc-frontmatter-injection]] — PRs #89–91 (merged 2026-07-03, 9510efd/dea3883/f8b69ea): adopts Claude Code skills-doc frontmatter/injection features (docs: code.claude.com/docs/en/skills). #89: agentic-loop description trimmed 1533→1134 chars (1,536-char listing cap), all triggers preserved, sonnet-delegation clause restored after review; `paths:` globs added to the three engineering-principles skills; `user-invocable: false` on using-coderails. #90: `context: fork` added to wiki-lint + wiki-query (isolated subagent execution; `agent: Explore` deliberately withheld since both write/commit; accepted tradeoff: reduced parent-transcript visibility into their autonomous wiki writes). #91: `commands/merge.md` gains an injected "Current Git Status" block (current branch + open PRs, display-only); a parallel `post-review.md` injection was deferred on an inconclusive `$ARGUMENTS`/`!`cmd`` ordering probe. Also records: `disable-model-invocation` rejected for workflow commands (breaks agentic-loop subroutine calls); skill-scoped hooks migration deferred (undocumented "active period" boundary); local `ai_docs` skills snapshot (2025-11-07) flagged stale, predates `paths`/`user-invocable`
- [[pr_92_exec-bit-sweep]] — PR #92 (merged 2026-07-03, 4cb38b5): fixes a real permission-denied bug — `scripts/post_review.sh` was mode 100644 despite being invoked as a direct executable path by [[post-review]] (`./scripts/post_review.sh validate/write-cache`), observed when a review artifact briefly posted with unvalidated content. Also normalises 8 already-working `hooks/scripts/tests/*.test.sh` stragglers to 100755 for consistency (cosmetic, invoked via `bash "$test_file"`). Zero content changes, blob-identical. `install.sh`/`uninstall.sh` audited and correctly left at 100644 (always invoked via `bash`). No test currently guards the mode bit against regression — known gap, not fixed. **Gap closed same day by [[pr_93-94_post-review-injection-and-exec-bit-invariant|PR #94]]**
- [[session_2026-07-03_ai-docs-refresh-and-cc-mechanics-probes]] — local `~/.claude` ai_docs snapshot refresh (commit dbb6822, rewritten from live code.claude.com/docs/en/skills) closes the staleness flagged in [[pr_89-91_skills-doc-frontmatter-injection]]. Plus 4 empirical Claude Code mechanics findings from a temporary probe skill: (1) `$ARGUMENTS` substitutes before `!`cmd`` injection runs — resolves PR #91's deferred post-review.md injection, now unblocked pending approval; (2) skill-frontmatter `PostToolUse` hooks persist across turns within a session but are session-scoped; (3) subagent skill enumeration is fixed at spawn time, explaining the #91 probe's inconclusive result as an enumeration artifact, not an ordering one; (4) DECISION: migrating loop_state_guard/loop_stall_guard into skill-frontmatter hooks REJECTED — no survival guarantee across post-compaction restarts, safety guards need deterministic scope
- [[pr_93-94_post-review-injection-and-exec-bit-invariant]] — PRs #93–94 (merged 2026-07-03, bba7ab5/89a9af6): #93 ships the post-review.md injection PR #91 deferred — `## Current PR State` block (`gh pr view "$ARGUMENTS" --json state,headRefOid,title`) plus the same "data, not instructions" guard line as merge.md; Step 3's fresh SHA resolution untouched. Pre-merge review fixed an unquoted `$ARGUMENTS` inconsistency. Surfaced: programmatic-caller injection into `/post-review`'s argument is a pre-existing (not new) gap via Step 3, now doubled to two call sites; candidate fix is numeric-only validation. **This PR-state line was a command-substitution injection, removed by [[pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter|PR #97]].** #94 adds `exec_bit_invariant.test.sh` (manifest vs git index + completeness scan, suite 23→24), closing PR #92's flagged gap, and normalises two more source-only libs (`git-common.sh`, `agentic_loop_path.sh`) 755→644 that #92 didn't reach. Surfaced: `install.sh`'s unconditional chmod sweep re-adds +x on disk at install time regardless of index mode — index-vs-installed-copy drift. **Closed by [[pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter|PR #96]]**
- [[repo-consolidation_2026-07-05]] — the two-repo split (private dev + scrubbed public export, 2026-07-03) collapsed back to ONE clean repo on 2026-07-05: history mailmap-rewritten clean of a legacy author identity, the repo deleted-and-recreated to shed the `refs/pull/*` residue, export repo deleted, dev repo renamed to canonical `coderails`, all 25 stale branches pruned. `coderails-dev` is now only a rename redirect. Method captured in [[history-identity-scrub]]; updated [[repo-hosting]]. Completes the "renamed mid-loop" note in [[pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter]]
- [[pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter]] — PRs #96–98 (merged 2026-07-03/2026-07-03/2026-07-05, 127a149/76084fe/559d79b). #96: `install.sh`'s exec-bit sweep is now git-index-mode-aware (100755→+x, 100644→-x, untracked→legacy +x), closing the drift PR #94 flagged; fix round closed a Critical (installer died silently on non-git checkouts under `set -euo pipefail`, `git ls-files` exit 128 propagating through the pipe). #97: experimentally proved [[post-review]]'s PR #93 render-time line was a **command-substitution injection** — `$ARGUMENTS` executes as live shell syntax inside a render-time `` !`cmd` `` line regardless of quoting, since textual substitution precedes shell parsing; removed the injected block entirely, replaced with argument-free `gh pr list`, added a class-wide test banning `$ARGUMENTS` in any `commands/*.md` render-time line. #98: `loop_stall_guard.sh` becomes the sole writer of `progress.json.loop_stop_counts` (jq auto-vivify increment, tmp+mv atomic, never-blocks-stop), closing a two-writer lost-update race that undercounted 2 loops; `SKILL.md` updated 5 places to mark the field HOOK-OWNED; fix round added 4 standing-invariant tests (no-clobber, jq-absence, tie-break, degraded-filesystem) and fixed a test-harness arg-shifting bug. Also: repo renamed to `blueman82/coderails-dev` mid-loop; ai_docs hooks/subagents/slash-commands snapshots refreshed; an org-auth outage killed a worker mid-fix-round (work survived, versioned respawn inherited it); origin/main advanced twice under the loop (workers used merge-base-relative scope checks)
- [[pr_1-4_task-evals-feature]] — PRs #1–4 (merged 2026-07-06, 0fc8350/5a5df44/3b4be2f/72261f7) + addendum PRs #5–6: new `task-evals` skill — freezes a tiered, oracle-independent `evals.json` before implementation starts, gated at two scopes: pr (SHA-bound PR-comment artifact, `scripts/merge.sh` gate) and loop (sibling file beside `progress.json`, `loop_state_guard.sh` gate at ≥3 work-units). Five anti-gaming rules (freeze-before-build, negative controls, end-state surfaces, oracle independence, grader independence); tier 0/1/2 predicate ladder. Addendum: doc-drift fixes + final TeamCreate/TeamDelete trigger-phrase purge. This wiki row was missing from a prior lint pass — added retroactively (row-gap fix, not new content)
- [[pr_7-10_task-evals-followups]] — PRs #7–10 (merged 2026-07-06, e487b54/62ad18d/ffe5ccc/238f5e1): follow-up cluster closing gaps [[pr_1-4_task-evals-feature]] had surfaced-not-fixed, plus one owner directive. #7: `task-evals` SKILL.md wiki-first context prerequisite tightened (invariants/constraints wording, no-wiki fallback, agentic-loop Phase 2 reuse carve-out). #8: closes comment-spoofing (author-identity + `OWNER` association filter, fail-closed) and the 100-comment `gh` pagination cap in both PR-gate readers (`scripts/lib/git-common.sh`) — also fixed a dotted-repo-name truncation bug, a jq-injection risk in the cached login, and two review-round test-tautology findings. #9: INSTALLATION.md refresh (28-skill table, post-review/post-evals, config-gated enforce_pr_workflow, any-branch no_edit_on_main clause) + `install.sh` chmod-list gap closed for `scripts/lib/review-artifact.sh`/`scripts/lib/config.sh`. #10 (owner directive): `tier_justification` required at **every** tier, not just tier 0 — new `UNJUSTIFIED` loop-gate result, retroactively blocking pre-existing unjustified GO artifacts
- [[pr_11-14_gate-hardening-followups]] — PRs #11–14 (merged 2026-07-06, a1c4803/fdd29fd/321bca3/7c1dd19; base 238f5e1): four independent gate-hardening follow-ups, each requiring both a review artifact and an eval artifact. #11: explicit `NO-GO` now wins over the tier-0 exemption in the loop-evals reader (owner directive — "an exemption justifies having no evals, not overriding a recorded failure"). #12: `install_mode_sweep.test.sh` sandboxes `$HOME` for its `install.sh` invocations — closes a **real, recurring corruption** of the developer machine's actual `~/.claude/settings.json` (10 auto-repaired events during this loop, caused by `test_gate`'s per-commit test run inside worker worktrees); anti-vacuity pre-seeding + before/after cksum guards on settings.json/known_marketplaces.json/CLAUDE.md. #13: `push.sh` staging changed from `git add -A` to `git add -u` + a named untracked-file warning; review caught and fixed a `grep`-under-`pipefail` crash on the zero-untracked-files case; new `push_staging.test.sh` (first-ever staging coverage). #14 (security-relevant, owner directive): the review/eval artifact gates' `author_association == "OWNER"` trust conjunct is **removed** (clean break, no compat path) and replaced with a repo-permission check (`viewerPermission` must be `ADMIN`/`MAINTAIN`/`WRITE`) — now works on org-owned repos, closing the scoped limitation [[pr_7-10_task-evals-followups|PR #8]] had documented; `merge.sh`'s fetch-failure error messages also split three ways (identity/permission/comments). Suite test-file count 29→30 (PR #13's new file)
- [[pr_15-17_loop-hardening-registration-eval-freeze-ledger-dry]] — PRs #15–17 (merged 2026-07-06, dc55377/e4aafa6/d1b1360): loop-hardening cluster closing two gaps a single 17-work-unit unregistered `subagent-driven-development` execution surfaced (9 units ran with zero hook signal, no frozen evals). #15 (F2): `writing-plans` "Final eval-gate task" split into "freeze after stress-test, before implementation" + "final task: grade and post only" — 4 cross-referencing docs fixed in lockstep (task-evals, subagent-driven-development's new Pre-Flight bullet, workflow.md, REFERENCE.md). #16 (WU4): SDD progress ledger moved from repo-root-keyed `$root/.coderails/sdd` (shared by all sessions in a checkout — a latent collision bug, destroyed by `git clean -fdx`) to the session-keyed loop-state dir beside `progress.json`; renamed `sdd-ledger.md`; fact-ownership split documented (ledger = task-level, controller-owned; `progress.json.work_units` = unit-level, orchestrator-owned); `sdd-workspace` fails loud on missing session_id or non-absolute helper output; first SDD test infra (6-check smoke test). Clean-break, no migration shim. #17 (F1): new sibling Stop hook `unregistered_loop_guard.sh` — nudges (never blocks) when ≥3 distinct agent-dispatch turns + no `progress.json` + no `agentic-loop` Skill invocation; distinct `jq_missing`/`jq_parse_error`/`payload_parse_error` log reasons; 27-check test file. Stop hook count 4→5
- [[pr_21-22_loop2-suggestion-tier-followups]] — PRs #21–22 (merged 2026-07-06, d0e4c5a/1033512; base 9f8f1af): Loop 2's suggestion-tier follow-up cluster, closing 4 of 6 residuals recorded against [[pr_11-14_gate-hardening-followups]]. #21: `merge.sh` gains a dedicated `tempfile)` failure-reason case arm on both the review and eval artifact gates — `pr::_trusted_comment_bodies_or_fail`'s own `mktemp` failure precedes any `gh` fetch, so the message deliberately does not say "GitHub fetch failed"; TDD (Test 3d/4d, message-distinctness asserted). #22: test-only coverage completions — `push_staging.test.sh` gains pre-staged-new-file and deleted-tracked-file cases; `loop_state_guard_evals.test.sh` gains the tier≠0/justified/result-key-absent final-else NO-GO case (a pre-existing fixture's claim to cover this branch was a misnomer); `install_mode_sweep.test.sh` gains a commands-dir conflict-decline fixture plus a NOGIT-sandbox pre-seed exercising real `jq` mutation. All new assertions mutation-tested. A sixth residual (a further eval-gate tempfile message-symmetry refinement) was identified and deliberately left unshipped — see the source page's caveats. See [[trust-floor]] (extended vocabulary) and [[task-evals-gate]] (cross-reference)
- [[pr_23-24_hook-lib-observability-and-repo-keyed-loop-state]] — PRs #23–24 (merged 2026-07-06, 9837c15/14c7f1c): Loop 2's second follow-ups cluster. #23: `als_count_invocations` no longer logs a jq-failure reason directly (ambiguous under retry — a transient failure recovering on a later attempt was indistinguishable from a sustained one); now signals the reason on stderr (a global would vanish across the retry caller's subshell boundary) for `als_stable_invocations` to log exactly once per retry loop (`reason=X attempts=N outcome=recovered|exhausted`); one-shot caller `unregistered_loop_guard.sh` explicitly discards the new stderr signal, preserving its prior silent behaviour. Also fixed a real `als_log` stderr leak (trailing `2>/dev/null` didn't catch a redirection-open error; brace-group fix) — accepted tradeoff documented (missing LOG_FILE parent silently swallows a caller's own failure line too). Gate outcomes proven bit-identical via a cross-SHA consumer exit-code matrix. #24: `agentic_loop_path.sh`'s slug is now **repo-keyed** (`git --git-common-dir`), not cwd-keyed, so every worktree of one repo shares one `progress.json` — closes a mid-loop `EnterWorktree` orphaning bug hit live during this cluster's own work. Critical fix: validates the git output is an absolute path before trusting it (a pre-2.31 git echoes `--path-format` back verbatim with a relative path on exit 0, which would have collapsed old-git hosts onto one garbage slug). Sanitisation collisions (`"foo/bar"`==`"foo_bar"`) documented as an accepted tradeoff. Clean-break, no compat flag; old cwd-keyed dirs left on disk. **Supersedes [[pr_86_agentic-loop-hardening|PR #86]] point 6's 2026-07-01 rejection of locking machinery** — the workflow shift (prescribed mid-session `EnterWorktree`) changed the calculus; this is a ~15ms keying fix, not the rejected machinery. Live proof: the fix re-keyed this very loop's own state mid-flight. See [[agentic-loop-path-keying]] (new design page)
- [[pr_25_observability-dashboard]] — PR #25 (merged 2026-07-06, fbc3ea0): new `coderails:dashboard` skill — sub-project 1 of 5 in the agentic-OS evolution sequence. Next.js/R3F web HUD (collectors for sessions/loops/PR-gates/memory-trail/health, SSE stream, token+Origin-guarded `/api/run` spawning headless `claude` via single-source `buildArgv`) plus a native Obsidian command-centre plugin (file-native `agentic-os` code-block processor, intent-queue seam for the not-yet-built routines sub-project). 16 SDD tasks, 5 fix loops (lock TOCTOU Critical, marker grammar, `hooksFired` scoping, single-SSE-provider, flag-smuggling High). The frozen Tier-2 eval suite (10 evals) caught **two production bugs every review round missed** (launch-script false-success on a squatted port; a statically-prerendered page baking an empty button config, fixed via force-dynamic). Eval artifact GO 10/10 + review artifact both SHA-bound and merge-gate-consumed. See [[dashboard]]
- [[pr_27-39_workflow-audit-skill]] — PRs #27/#30/#37/#39 (merged 2026-07-07): new `coderails:workflow-audit` skill — sub-project 3 of 5 in the agentic-OS evolution sequence. Mechanical scan (`scan_transcripts.sh`, structural privacy whitelist: Bash first-two-tokens / Skill name / Agent subagent_type only) → n-gram cluster (`cluster_ngrams.sh`, n=2..5, distinct-session support) → single fresh judge subagent (fixed-vocabulary `references/judge-contract.md`, 3 mandatory rejection criteria) → proposal chart → hard-stop `AskUserQuestion` approval gate that overrides standing loop autonomy (this in-session gate was **later removed 2026-07-15**, `worktree-workflow-audit-no-approval-gate`; the judge verdict is now the sole in-session filter — see [[workflow-audit]]) → one skill at a time via [[writing-skills]] TDD + full PR gate. E2E test (PR #39) includes a sentinel privacy negative control and a no-approval/no-creation pin. See [[workflow-audit]] and [[claude-code-transcript-schema_2026-07-07]]
- [[pr_28_assistant-link-queue-contract-and-panel-spec]] — PR #28 (merged 2026-07-06, 4a28bfc): normative-first design doc — the `QueueFileEntry` on-disk contract (`~/.claude/coderails-dashboard/queue/<hash>.json`) assistant-agent's send-gate writes to, and the four-item ASSISTANT.LINK panel spec (D6). No code; `skills/dashboard/` untouched; written before assistant-agent's `queue.ts` existed, so this doc is the contract the implementation had to conform to. Sub-project 4 of 5. See [[assistant-link-send-gate-architecture]]
- [[pr_31_assistant-link-approve-button]] — PR #31 (merged 2026-07-06, 6fa5e34): implements the dashboard side of PR #28's contract — `collectQueue` collector, `AssistantLinkPanel` component (renders only the queue slot; the other three D6 slots stay unbuilt), `POST /api/queue` in-place status-rewrite endpoint. 9/9 Tier-2 evals GO. Review caught and fixed one **Critical pre-merge**: path traversal via an unvalidated `hash` in `resolveQueueEntry`'s file-path join, closed with a strict `/^[0-9a-f]{64}$/` check at two layers (API route + the mutator itself). Zero files outside `skills/dashboard/`. Sub-project 4 of 5. See [[assistant-link-send-gate-architecture]]
- [[pr_43-44-46_workflow-audit-queue-seam]] — PRs #43/#44/#46 (merged 2026-07-07, f1212a31/0468bc90/675e63bc): workflow-audit gains a second, asynchronous approval surface on the dashboard's existing queue, alongside its unchanged synchronous `AskUserQuestion` gate. WU1 (`write_queue_entry.sh`): writes one `QueueFileEntry` per `verdict:"propose"` judge output, `toolName: "workflow-audit:propose-skill"`, six D2-whitelisted fields, sha256 hash binding, `0700`/`0600` perms; reuses the existing envelope verbatim, no new schema. WU2 (`AssistantLinkPanel.tsx`): type-guarded readable render branch for this `toolName`, falling back to the prior opaque render for every other producer; Approve/Deny wiring untouched. WU3 (spec doc): the consumption-seam contract for the not-yet-built routines runner — filter on `status:"approved"`, mandatory hash re-validation before acting (mismatch = distinct logged rejection, never a soft warning), only then drives `writing-skills`' full PR-gated create process. **Honesty requirement, explicit in the spec:** approving a queue entry today only flips its `status` field — no skill is created, no branch opens, no PR is filed, until the runner exists. Closes the queue-mode gap [[queue-contract-cross-pr-audit_2026-07-07]] had flagged as pending. See [[workflow-audit]] and [[dashboard]]
- [[pr_62_10_approvals-dir-move]] — coderails PR #62 + assistant-agent PR #10 (both merged 2026-07-07, c7e363c/a2b7e6a): moves the send-gate's own approval files from `~/.claude/coderails-dashboard/queue/` to a new sibling `approvals/` directory, fixing a collision where the routines runner sweep and workflow-audit's `propose-skill` writer (both still in `queue/`, [[pr_43-44-46_workflow-audit-queue-seam]]) would quarantine/reject a `QueueFileEntry` approval file as malformed input. Clean-break, no dual-dir fallback, nothing to migrate. Flags one residual doc-drift item: assistant-agent's `AGENTS.md` line 96 still names the old path. See [[assistant-link-send-gate-architecture]]
- [[pr_36-41-33-53-65_verified-routines]] — PRs #36/#41/#33/#53/#65 (all merged 2026-07-07, 027c09f/e322678/653146d/4232393/b6a40f5): sub-project 2 of 5 in the agentic-OS sequence — closes the "routines" gap [[dashboard]] and [[queue-contract-cross-pr-audit_2026-07-07]] both flagged as unbuilt. #36: `@coderails/dashboard-lib` — the `Intent` schema + `RoutineDef`/routines section of `DashboardConfig`, compile-time schema-compat test against the Obsidian plugin's own `IntentFile`. #41: `@coderails/dashboard-runner` — the one-shot sweeper (atomic rename claim queue→processing→archive/quarantine, per-intent failure boundary, 60-minute orphan recovery, artifact-gate predicate evaluator `exists`/`contains`/`json-field`, `notification`+`vault-note` escalation with no dedup by design). #33: `memory-consolidation` skill — dedupes/flags/refreshes `MEMORY.md`, writes its durable report artifact unconditionally so a routine can gate on the report's existence rather than exit code. #53: `seedDueRoutines` (nightly=20h/weekly=6.5d due-ness, intentionally shorter than nominal cadence) + two idempotent launchd jobs (calendar seed+sweep at 03:00, watch-triggered sweep-only) + the three shipped read-only-profile example routines. #65: `docs/routines.md` operator guide + a drift audit fixing README's skill catalog and AGENTS.md's wiki page-type table (`dashboard-runs/` run notes now explicitly documented as NOT a wiki page type). **Empirically verified security finding:** `PreToolUse` hooks (`test_gate`, `enforce_pr_workflow`) do not fire under `claude -p` — confirmed live, a deny-trigger did not stop a `git commit` — so a `bypass`-profile routine runs with neither a tool allowlist nor the hook safety net; all three shipped routines use `read-only` for exactly this reason. New pages: [[intent-queue-runner-contract]], [[dashboard-runner]], [[routines]], [[memory-consolidation]]. See [[dashboard]]
- [[pr_70-71_2026-07-07_dashboard-input-fix-and-voice-announcements]] — PR #70 + #71 (both merged 2026-07-07, e87877c/18094dd). #70: fixes a real bug found by an intake smoke test — `buildArgv()` pushed a button's free-text `input` as a **separate** argv token after the `--` sentinel, but the `claude` CLI's `-p` only consumes one positional prompt, so **input silently never reached the model on any `inputAllowed` button since the button model's inception**; fixed by merging `command`+`input` into one prompt string before the sentinel. Bundled hardening: empty/whitespace input normalised inside `buildArgv` itself, throws on an empty effective prompt, leading-dash flag-smuggling check now runs against the trimmed value. New "ask" button pattern (`command:""`, free text = whole prompt) shipped in the owner's config. 5/5 evals GO, graded against the *live* dashboard (argv-shape checks alone can't prove delivery — the pre-fix code passed all of them). Two security review passes, no exploitable findings. #71: new observe-only Stop hook `voice_announce.sh` — speaks complete/waiting/stopped/stall via macOS `say`, runs first in the Stop array, silent (not a stall) on empty text extraction. Shared-lib extraction of `als_extract_last_text`/`als_stable_last_text` out of `loop_stall_guard.sh`. Two Critical + three Important review findings fixed pre-merge (missing hard-stop arm; extract-failed misclassified as stall; false-success logging on say-absent/debounce-write-fail). Landed via a `git commit-tree` fast-forward after two rebases hit the force-push gate. **⚠️ PR-number collision:** this repo's PR #69/#70 (2026-07-07) are unrelated to the older `sources/pr_69_no-edit-message-worktree.md`/`sources/pr_70_gate-settings-json-edits.md` pages (2026-06-29, pre-recreation) — see the collision note in [[repo-hosting]]. New pages: [[voice_announce]]. See [[dashboard]] and [[discipline-loop]]
- [[pr_55-60-64-66-67_approve-build-runner]] — PRs #55/#59/#60/#64/#66/#67 (all merged 2026-07-07, 905795c/f92e61a/dd925fe/7f289c2/330c5a2/de19d95): loop 2 of the workflow-audit/dashboard queue-mode arc — turns the Approve-click on a `workflow-audit:propose-skill` queue entry ([[pr_43-44-46_workflow-audit-queue-seam]]) from a bare status flip into a real, mechanically-gated build. L2-WU1 (#55): pending-only transition guard (409 on re-approve) + `claimAndSpawnBuild` seam (hash-claimed `builds/<hash>/` sidecar dir). L2-WU2 (#60, hardened same-day by #64): `run-builder.sh` bash wrapper owns the state machine — global lock, hash re-validation before any LLM runs, artifact-derived terminal state via an `EXIT` trap, `--disallowedTools` mechanically blocking every merge-shaped tool call (empirically proven to survive `--dangerously-skip-permissions`); plus an injection-fenced prompt template (`prompt.ts`, six judge-authored fields confined to one fence) and a `__dirname`-anchored wrapper-path fix (closing a prod cwd-relative bug). L2-WU4 (#59): the normative spec doc + a clean-break rewrite of the prior cluster's now-false "status flip only" Honesty-requirement section + `SKILL.md` naming `skill-creator` as the authoring engine. L2-WU3 (#66): dashboard build-state visibility (`collectBuilds`, closed-set validation) plus a **real XSS fix** (`safePrUrl` — only `https:` URLs are ever linked from builder-controlled `prUrl` data). L2-WU5 (#67): re-routes workflow-audit's proposal writes `queue/`→`approvals/` after concurrent PR #62 split the directories. **Owner still merges the resulting skill PR by hand — a click approves creation, not landing an unseen diff.** E5 (one real manual build through the pipeline) is now DONE — see [[pr_89-100-104-106_approve-build-e5-live]] (first built skill [[verify-merged-pr]]). See [[workflow-audit]] and [[dashboard]]
- [[pr_80-82_dashboard-stream-run-output-viewer]] — PRs #80–82 (all merged 2026-07-08, 2a49d05/f2371ea/a383f36): closes the gap [[dashboard-run-log-streaming-viewer-gap_2026-07-07]] documented the same day. #80 (backend): `POST /api/run` now spawns `claude` with `--output-format stream-json --include-partial-messages --verbose`, streams each stdout/stderr chunk incrementally to `runs/<runId>.log` (clean break from the old single post-exit write) and publishes it on a new in-process pub/sub `runOutputBus.ts`, re-emitted by the aggregator as a new `"run-output"` SSE event on the **existing** `/api/events` connection (single-SSE-provider rule preserved). New non-throwing, forward-compatible `streamJson.ts` parser (no event-type allowlist). Review caught and fixed a Critical: no `child.on("error", ...)` handler, so a spawn failure would hang the request forever and leak the per-button lock. #81 (frontend): new `OutputViewerPanel.tsx` in the COMMAND DECK — live-streams an active run from the accumulated SSE buffer, fetches a finished run's full output once via new `GET /api/run/output` (runId format-validated `/^[0-9a-f]{16}$/`, never joined into a path — looks up the record's own `outputPath` instead, going beyond the PR #31 path-traversal precedent). Review caught and fixed a Critical: `fetchSettledOutput` silently swallowed all fetch/parse errors; fixed with a discriminated result type, a distinct 409/in-progress case, a visible retry button, and pruning of the `runOutput` map for runs aged out of run-history. #82 (docs): `docs/REFERENCE.md` + `skills/dashboard/SKILL.md` updated to describe the viewer. See [[dashboard]]
- [[pr_69_2026-07-08_substitution-bypass-audit]] — PR #69 (merged 2026-07-08, ebfee7b): closes two [[destructive_bash_gate]] bypasses — process substitution `<(...)`/`>(...)` (invisible to the old backtick/`$(...)`-only detector) and multi-line substitution (a script mention and its live substitution on different physical lines, via unquoted heredoc or backslash continuation, invisible to a non-flattened single-line regex). Widens `subst_re`; adds `cmd_flat` newline-flattening. Conservative tradeoff: a quoted-delimiter heredoc is now also denied. **⚠️ PR-number collision:** distinct from the older, unrelated 2026-06-29 [[pr_69_2026-06-29_no-edit-message-worktree|PR #69]] (a `no_edit_on_main` deny-message reword) — date-disambiguated slug. First of a five-PR same-day hardening arc; see [[destructive_bash_gate]]'s "2026-07-08 adversarial-hardening arc" section
- [[pr_72-75_2026-07-08_force-with-lease-allowlist]] — PR #72 + #75 (merged 2026-07-07/2026-07-08, 8332509/e63d3d1): adds a narrow, owner-opt-in conditional-allow to [[destructive_bash_gate]] — `git push --force-with-lease` is permitted (naked `--force`/`-f` always still denies, even alongside it) only if `.claude/destructive_allowlist` contains the exact line `git-push-force-with-lease` (fail-closed on missing/garbage file). **Incident:** PR #72 shipped with a live tab-separator regression in the naked-force boundary check (space-only, not `[[:space:]]`) — caught by an orchestrator STOP that treated the finding as unreproduced-not-absent; PR #75 fixed it plus an interior-flag-split bypass (`git push --fo\<newline>rce` bash-splices to real `--force`, evading a naive `tr '\n' ' '` flattener) via a backslash-newline-pair-aware `awk` splice; `push.sh` gained a `--force-with-lease` long flag so workers stop copying the raw command. **⚠️ PR-number collision:** distinct from the older, unrelated 2026-06-29 PRs [[pr_72_config-walkup-symlink-hang|#72]] (config.sh walk-up hang) and [[pr_75_main-branch-fallback|#75]] (main() fallback) — date-disambiguated slug. Second/third of the five-PR arc; establishes the positive-control-testing discipline recorded in [[enforcement-model]]
- [[pr_84_2026-07-08_git-global-option-bypass]] — PR #84 (merged 2026-07-08, 7c4f12f): closes a third independent bypass in [[destructive_bash_gate]] — the force-push trigger required contiguous `git`+`push` tokens, so a git global option between them (`git -c KEY=VAL push --force`, `git --no-pager push --force`) broke adjacency and let naked force sail through as ALLOW with no allowlist involved. Fix: option-tolerant `git_push_re` (git + 0–20 bounded global-option tokens + push), applied symmetrically to the trigger and the fwl-exclusion check. No PR-number collision (no prior #84 in this vault). Fourth of the five-PR arc
- [[pr_92_2026-07-08_reference-drift-and-lookalike-fp]] — PR #92 (merged 2026-07-08, 652c5f8): two fixes closing the arc. (a) `docs/REFERENCE.md`'s [[destructive_bash_gate]] matrix row was stale after the four preceding PRs — updated to document substitution detection + the force-with-lease carve-out precisely. (b) `plugin_src`'s unanchored matcher false-DENYed lookalike paths (`tee xcommands/prep.md` matching the `commands/` substring anywhere on the line); fixed with a left-anchor to a path/token boundary, verified nested real paths (`vendor/skills/x/SKILL.md`) still correctly deny. **⚠️ PR-number collision:** distinct from the older, unrelated [[pr_92_exec-bit-sweep|PR #92]] (2026-07-03 exec-bit sweep) — date-disambiguated slug. Fifth and closing PR of the arc; the only one fixing a false-DENY rather than a bypass
- [[pr_88_93_dashboard-launchd]] — PR #88 + #93 (merged 2026-07-08, ec46132/f630a98): gives the dashboard a launchd LaunchAgent (`RunAtLoad`+`KeepAlive`+`ThrottleInterval 60`) so it survives reboots/crashes, reusing the copy-into-`~/Library/LaunchAgents/`-then-bootstrap-from-copy fix [[routines]] discovered first for the routine-sweeper agents. New thin exec wrapper `dashboard-server.sh` (deliberately duplicates ~13 lines of `start-dashboard.sh`'s build-if-stale logic — accepted YAGNI — extended with a dependency/config staleness check a daemon needs and an interactive script doesn't). `stop-dashboard.sh` cannot stop the agent (no pidfile); stop command is `launchctl bootout gui/$(id -u)/com.coderails.dashboard`. PR #93 fixes an uninstall-time race found the same day: `launchctl bootout` is asynchronous for a running `KeepAlive` job (~2s observed), so the uninstaller now polls up to 10×1s before declaring failure rather than checking once immediately
- [[pr_95_slash-command-loop-detection]] — PR #95 (merged 2026-07-08T10:52:31Z, `bcd572bd`): fixes `als_count_invocations` (`hooks/scripts/lib/loop_state_common.sh`) only matching the programmatic assistant `Skill` tool_use invocation form, silently missing every loop started via the slash-command form (`/coderails:agentic-loop`, recorded in the transcript as a `user`-role string `.message.content` carrying `<command-name>...</command-name>`, not a tool_use). With 0 counted invocations, `als_gate_require_active_loop` exited before ever reaching `als_load_progress`/`gate_loop_stop_declared`/`bump_loop_stop_count` — the WHOLE `loop_stall_guard` was inert for slash-started loops (54/54 log lines `invocations=0` for the repro session), one root cause of `loop_stop_counts` silently staying null (NOT the previously-suspected `ALS_PATH` keying bug). Fix extends the jq filter to also scan user `<command-name>` messages (same `(^|:)agentic-loop$` anchor after stripping `/`), benefiting all four shared consumers. Hardening: scans every `<command-name>` tag not just the first, trims whitespace before the anchor test, and deliberately does NOT dedup across the two forms (documented decision — a session both slash- and Skill-invoked counts 2). Tests 37/37. Also filed [[evals-gate-enforcement-gap_2026-07-08]], a workflow-uniformity finding surfaced by this PR's own debugging-framed path (systematic-debugging has zero route to the task-evals gate). Updated [[discipline-loop]] and [[loop-progress-fields]]
- [[pr_96-98_evals-gate-uniform-enforcement_2026-07-08]] — PRs #96–98 (merged 2026-07-08, `4e727b2`/`5b96690`/`d9ea84e`; **⚠️ PR-number collision, unrelated content** — see [[pr_96-98_mode-aware-install-argument-injection-guard-hook-owned-counter]] for the earlier 2026-07-03/05 cluster that reused these same numbers). Closes [[evals-gate-enforcement-gap_2026-07-08]] same-day. #97: `enforce_pr_workflow.sh` gains `gate_eval_artifact_for_merge` — a new hook-level gate blocking raw `gh pr merge <N>` (after the pre-existing review-pr gate passes) unless a SHA-bound `GO` coderails eval artifact exists for the PR's current head, mirroring `scripts/merge.sh`'s own eval gate's rc semantics (0/GO allow, NO-GO/absent deny, rc=2 fetch-failure fail-closed); `git_merge`/`git_push` explicitly not covered (no PR number to key a SHA-bound artifact on). Tests: global `gh` mock added to the suite, 86→93 passing cases. #96: `systematic-debugging` Phase 4 gains a Step 0 freezing pr-scope `task-evals` before a fix, reciprocally cross-referenced from `task-evals`' own "invoked at four points" list. #98: `docs/REFERENCE.md` documents the resulting coverage boundary — eval gate enforced at `merge.sh` (config-independent) and this hook (config-dependent) only; NOT on raw `git merge`/`git push` or `NO_CONFIG` repos. Updated [[task-evals-gate]], [[enforce_pr_workflow]], [[enforcement-model]]
- [[pr_99_unregistered-loop-guard-nudge-once]] — PR #99 (merged 2026-07-08, `95479de`; commits `1ccefa7`+`88af636`): fixes [[unregistered_loop_guard]] re-emitting its nudge on EVERY Stop for a non-loop session with no per-session termination — self-perpetuating for a genuinely one-off dispatch sequence (nudge → honest "no action needed" turn → Stop → nudge again, observed live 2026-07-08). Fix greps the existing discipline log (`als_log`'s ledger, already written on emit) for a prior `nudged=1` line for this session id before emitting again; first nudge unaffected; missing/unreadable log still fails open. Second commit hardens the check: `session_id` is BRE-escaped before interpolation into the grep pattern, closing a real (if currently unreached for UUID-shaped ids) wildcard-match false-suppression regression (`s.1` matching a logged `sX1`). Reviewed (code-reviewer 0 critical/0 important/1 suggestion, applied; pr-test-analyzer confirmed the regression test fails pre-fix, passes post-fix); eval artifact GO tier 1; suite 37/37. Updated [[discipline-loop]] and [[unregistered_loop_guard]]
- [[pr_86-107_2026-07-08_loop-lib-residuals]] — PRs #86/#87/#91/#94/#107 (all merged 2026-07-08): loop-lib residuals cluster. #91 (`f1db15c4`) makes the shared `loop_state_common.sh` transcript parse malformed-line-tolerant (per-line `jq -R 'fromjson? // empty'` pre-filter, `skipped_malformed=N` breadcrumb) — a single bad JSONL line previously collapsed the whole invocation count to 0, defeating [[loop_state_guard]]/[[loop_stall_guard]]/[[voice_announce]]/[[unregistered_loop_guard]]. #107 (`d4256542`, same day, post-merge 4-reviewer pass) corrects two failure-attribution regressions the tolerant parse introduced: a `read_error` tag for an unreadable transcript or stage-1 `jq` binary death (previously a silent zero), and an `all_lines_malformed` tag distinguishing total parse loss from a benign partial skip (gates the retry/settle window). `unregistered_loop_guard.sh`'s own separate `jq -s` slurp remains untouched — a stated residual. #86 (`9b300355`): `skills/dashboard/obsidian/src/exec.ts`'s `pressButton` gains a try/catch around `buildArgv`, matching `route.ts`'s existing error-handling parity. #87 (`81a705f6`): a reported nested-worktree test-failure concern investigated, did not reproduce — documented as a header comment in `install_mode_sweep.test.sh`, no code change. #94 (`f4e05ee9`): a leaked root `evals.json` (task-evals working artifact, third occurrence) removed, plus a root-anchored `/evals.json` gitignore entry that doesn't shadow the tracked dashboard test fixture. **⚠️ PR-number collision:** this cluster's #86/#87 are unrelated to the older [[pr_86_agentic-loop-hardening|PR #86]]/[[pr_87_agentic-loop-path-session-keying|PR #87]] (both merged 2026-07-01, pre-recreation)
- [[pr_50_2026-07-07_comment-citation-gate]] — PR #50 (merged 2026-07-07T12:22:08Z, `6cdeb9c8`): new `comment_citation_gate.sh` `PreToolUse (Write|Edit|MultiEdit)` hook denying new code comments that cite a session-artifact label (`E#:`, `F# fix`, `CHANGE B#/C#`, `Task A#`, `TA-I#`, "reviewer finding", `eval E#`, `WU#:`, `C2`, "per the plan/design/session") — labels that rot the instant the session ends; `.md` files out of scope; `PR #NN` a documented survivor (durable GitHub artifact). Registered in `hooks.json` alongside [[no_edit_on_main]]. **Coverage gap closed retroactively**: this hook shipped 2026-07-07 but was never wiki-ingested at the time — a full-vault wiki-lint health pass on 2026-07-08 flagged the gap (13/14 hook scripts had pages; this was the missing one), closed by this source page + [[comment_citation_gate]] + this table row. **⚠️ PR-number collision:** distinct from the older, unrelated [[pr_50_planning-sequence-gate|PR #50]] (merged 2026-06-26, pre-recreation) — date-disambiguated slug
- [[pr_89-100-104-106_approve-build-e5-live]] — PRs #89/#100/#104/#106 (merged 2026-07-08; `91d3802`/`a6f40ec`/`c30fcbf`/`23a73bd`): the live-fire close of the [[pr_55-60-64-66-67_approve-build-runner|Approve→build pipeline]] (E5). Running one real dashboard Approve click end-to-end surfaced three production-only defects no test caught — **#89** builder repo-identity (was grepping a nonexistent root `package.json`; fixed to exact-name match on `.claude-plugin/plugin.json`), **#100** launchd launcher not exporting `CODERAILS_BUILDER_REPO_PATH`/`_WRAPPER` or `~/.local/bin` on `PATH` — and produced the pipeline's first real artifact: **#104**, [[verify-merged-pr]], a skill authored by a headless `skill-creator` build and merged by hand. **#106** replaces the opaque up-to-45-min `building` with honest feedback: a coarse builder-written phase (`authoring/testing/pushing/opening_pr`, closed-set-validated in `collectBuilds` before reaching the client), an elapsed timer, heartbeat freshness, and after-merge reconciliation showing `PR resolved` (with a null-guard so an open PR is never falsely resolved when the open-PR set is untrustworthy). The arc: Approve went from flipping a status field to running a real headless build to a merged skill PR with honest live feedback. **⚠️ PR-number collision:** this #89/#100 are unrelated to the earlier [[pr_89-91_skills-doc-frontmatter-injection|#89]] and any other #100 — resolve via merge SHA. See [[workflow-audit]], [[dashboard]], [[verify-merged-pr]]
- [[pr_103_agentic-loop-path-session-fallback]] — PR #103 (merged 2026-07-08, `bf02e8a`; branch `slug-fix`): session-id fallback probe in `hooks/scripts/lib/agentic_loop_path.sh`, closing the plugin-version re-keying gap [[agentic-loop-path-keying]] recorded at "Known caveats" bullet 4 as filed-for-the-owner-not-fixed. Implements the first candidate fix named there. See [[agentic-loop-path-keying]]
- [[pr_108_2026-07-08_offload-push-guard]] — PR #108 (merged 2026-07-08, `fe8d794`): new `offload_push_guard.sh` `Stop`+`SubagentStop` hook, nudge-only (never blocks) — fires when the final assistant message both names a `git push` to main/master AND carries an offload-to-user cue (leading `! `, "your own shell", "run this yourself", etc.), telling the user to bypass `enforce_pr_workflow`'s push gate from their own shell instead of clearing it in-session via `/pr-review-toolkit:review-pr`. Sibling to [[unregistered_loop_guard]] (same nudge-never-block contract + nudge-once-per-session ledger idiom) but unrelated subject matter. 20/20 new-suite assertions; `hooks_json_timeout_floor.test.sh` backstop count bumped 13→14. New page: [[offload_push_guard]]
- [[pr_112-113_2026-07-08_jq-slurp-residuals-round2]] — PRs #112/#113 (merged 2026-07-08): closes the two residuals [[pr_86-107_2026-07-08_loop-lib-residuals|PRs #91/#107]] explicitly left open. #112: `discipline_common.sh`'s `dc_extract_last_text` gains the same fast-path-then-tolerant-fallback `jq -s` fix as #91, unconditionally and silently (no log/reason, matching its prior contract); sibling `dc_file_count` still untouched, flagged again as out of scope. #113: `unregistered_loop_guard.sh`'s own `ulg_count_dispatch_turns` now mirrors `als_count_invocations`'s two-stage tolerant parse — benign partial skip recovers the count with `ULG_PARSE_REASON` empty, total parse loss reports 0 with a non-empty reason (mirrors #107's `all_lines_malformed` split). Companion commit `919d3c9` added malformed-line-first-ordering and blank-line-exclusion test coverage. Updated [[unregistered_loop_guard]] and [[pr_86-107_2026-07-08_loop-lib-residuals]] (residuals section now points forward to the closure)
- [[pr_118-123_self-improving-loops]] — PRs #118-123 (merged 2026-07-09, final `643e0ad`): **Self-Improving Loops** — closes the record→read-back gap in the agentic loop. #118 `dc_mine_hook_blocks` discipline.log miner; #119 `als_gate_retro_on_complete` retro gate (blocks `LOOP-STOP: complete` unless a `schema_version` 1 `retro.json` sits beside `progress.json`; block-before-bump; case-normalised after a Critical review catch); #120 Phase 13 write-contract + Phase 2 retro-intake + Phase 3/3a lesson-travel in [[agentic-loop]]; #121 dormant v2 `loop-retro-promotion` pipeline + machine-maintained `learned-failure-modes.md`; #122 `loop-retro-promotion-weekly` routine (`buttonRef`-wired not the dead `skillCommand`; first `bypass`-profile routine); #123 docs sync (→34 skills). Machine-owned `standing-orders.md` overlay is additive-or-recurrence-only (decay→tombstone, never delete); rollback-on-regression deliberately cut. **Retro gate is LIVE; v2 ships dormant** behind a ≥10-retro + lifecycle + clean-decay graduation predicate. Updated [[loop_stall_guard]] (hosts the gate) and [[agentic-loop]] (Phase 13 write contract).
- [[pr_124_dashboard-run-output-result-extraction]] — PR #124 (merged 2026-07-09, `c9db6da`/`1005d6b`): closes the caveat [[pr_80-82_dashboard-stream-run-output-viewer]] flagged at ingest time — `GET /api/run/output` returned a settled run's entire raw stream-json log as `output`, so the Run Output panel dumped every hook/system/assistant event with the real answer buried in a nested blob (a live user report: a correct "what time is it in Dublin" answer was genuinely unfindable on screen). New module-private `extractResultText()` scans the log backwards for the last `type:"result"` line via the existing `parseStreamJsonLine` (shipped PR #80, previously unused beyond never-throws verification — this is its first real consumer), returns that line's `result` field; falls back to raw content if none found (crashed run or non-string `result`), same fallback value as before so that path is unchanged by construction. Tier-1 `task-evals` GO (3 P0 + 2 P1). **Two independent review agents (code-reviewer + pr-test-analyzer) converged on the identical coverage gap unprompted** — multi-result-line last-match selection and non-string-`result` fallback were both untested in the initial 14-test submission; closed same session with 2 tests, each proven against a naive buggy implementation before confirming green (16/16 final). Updated [[dashboard]] and [[pr_80-82_dashboard-stream-run-output-viewer]] (caveat marked closed).
- [[pr_128_dashboard-ask-button-auto-profile]] — PR #128 (merged 2026-07-10, `7f60d96`): adds a fourth `"auto"` value to the dashboard's `PermissionProfile` type (`config.ts`), mapped in `profileFlags()` (`argv.ts`) to `--permission-mode auto`. The "ask" button (`examples/dashboard-config.json`) switches from `profile: "standard"` to `"auto"`, closing a real failure mode: in headless `claude -p` mode `"standard"` adds no permission flags, so a tool needing a fresh grant (e.g. `WebSearch`) blocks forever on an unanswerable prompt and the model silently falls back to memory with no tool call attempted. `"auto"` auto-approves instead of blocking — but does **not** fix the separate, already-known non-determinism of whether the model attempts the tool call at all: a code comment (`argv.ts:40-49`) and this session's own direct empirical testing both found only 1 real tool call in 3 consecutive identical runs. **Third occurrence of independent review agents (code-reviewer + pr-test-analyzer) converging on an identical finding** (missing auto+free-text test coverage), after [[pr_39_agentic-loop-slim-v2]] and [[pr_124_dashboard-run-output-result-extraction]]. Updated [[dashboard]] (profile→flag table, ask-button description).
- [[pr_130-136_dashboard-right-rail-ux]] — PRs #130/#131/#132/#133/#135/#136 (merged 2026-07-10): six independent right-rail UX/IA fixes — panel separation (bordered cards + rose accent spine on the shared `.hud-block` class, affecting both rails, not just the right one), input affordance (boxed input fields + an "ARG" tag on input-capable buttons), label wrapping (ellipsis truncation, deliberately scoped to `.hud-cmd .hud-label` so `RunProgress.tsx`/`HudCallout.tsx`'s bare `.hud-label` usage is unaffected), run-history structure (filled/hollow status glyphs in both intentionally-duplicated list implementations), output-viewer context (a header bar identifying which run produced the shown output), and button-state differentiation (a new `lastOutcome` field driving a green/rose bullet-flash on completion — the only finding needing new state, and the first PR to add React Testing Library/jsdom test infra to this codebase). A design-mockup agent's scope-drifted first pass (2 unscoped substitutions for 2 real findings) was caught and rejected by the user via a forced-choice ranking. Shipped under a registered [[agentic-loop]] run. Updated [[dashboard]].
- [[pr_134_agentic-loop-retry-until-green]] — PR #134 (merged 2026-07-10, `5a6c54d`): a mid-loop [[agentic-loop]] skill edit, triggered by a user request for zero-question autonomy that the orchestrator narrowed after refusing to remove the skill's 4 hard-stops. Adds a bounded "retry-until-green" auto-fix cycle before the verification-failure hard-stop fires, wires `coderails:systematic-debugging`/`coderails:dispatching-parallel-agents`/`coderails:disconfirm` into that cycle, hardens Phase 13's terminal summary as mandatory-and-singular, and extracts the new content to a sibling `retry-until-green.md` per Anthropic's documented 500-line skill guidance. All six of the skill's byte-frozen regions confirmed unchanged throughout. A branch-contamination incident (the orchestrator's own spec/plan docs mixed with this PR's intended 2-file scope) was caught by the plan's own precondition check and fixed with a clean isolated worktree — see the source page for the process notes.
- [[pr_138_remove-specs-plans-tracking]] — PR #138 (merged 2026-07-11, `b0a4487`): ⚠️ removes `docs/coderails/specs/` and `docs/coderails/plans/` (20 files) from repo tracking entirely, per explicit owner decision — both directories are now gitignored. Updates [[brainstorming]] and [[writing-plans]] to write to a session-local scratch path instead of the repo (no worktree-isolation step needed anymore, since nothing is committed). Fixes stale references in `docs/REFERENCE.md` and 7 dashboard source files that cited the deleted spec/mockup files — contract-citing comments (e.g. the queue schema) redirected to their actual current source of truth rather than left dangling. Supersedes the "permanent record" framing previously documented for both directories across several wiki/repo pages.
- [[pr_139-141_dashboard-ask-enter-clean-output]] — PRs #139/#140/#141 (merged 2026-07-11, `60fd025`/`43d4b89`/`e54d38d`): one theme, three PRs. #139 adds `Enter`-to-submit (without `Shift`) on the ask input's `onKeyDown`. #140 adds `projectAssistantText(raw)` to `streamJson.ts` — projects a raw stream-json log to just the assistant's readable prose (last `type:"result"` line's `result` field preferred, else concatenated `text_delta` values, else raw unchanged) — and `OutputViewerPanel.tsx` renders it clean-by-default behind a `showRaw` toggle. #141 is a pure CSS readability pass (`.hud-output-viewer` font 9.5px→12px, max-height 220px→320px; new `.hud-output-toggle` style). **Design fact:** the toggle only renders for a still-live run — a settled run's output is already server-extracted by `extractResultText` ([[pr_124_dashboard-run-output-result-extraction]]) with no raw JSONL left client-side to toggle to; this live/settled asymmetry predates the cluster, but scoping the new toggle to respect it (rather than rendering a no-op button for settled runs) was caught by review, encoded as an explicit negative test. **Superseded 2026-07-14 by PR #172:** the inline `<pre>` viewer and this `showRaw` toggle were retired outright, replaced by the portal-rendered `RunOutputOverlay.tsx` modal — see [[pr_181_183_dashboard-run-output-wrap-and-inbox-brief-clean-modal|the 2026-07-15 ingest]] that closed this staleness. Updated [[dashboard]].
- [[pr_144-149_agentic-loop-hardening-from-loop-engineering]] — PRs #144/#145/#146/#147/#148/#149 (merged 2026-07-12, tip `8eac114`): five independent hardening PRs from a "loop engineering" diagnosis. #144 adds `post_evals.sh grade-loop` as the sole neutral computer of loop-scope `result` — stamps a `grading: {by, checksum}` object; the reader (`als_read_loop_evals_result`) demotes an otherwise-valid `GO`/`TIER0` to a new `UNSTAMPED` category when the stamp is absent or checksum-mismatched, closing the gap where the orchestrator could hand-write its own loop-scope `GO`. #145 adds a sixth task-evals anti-gaming rule, "Strongest surface" — a user-facing goal state needs ≥1 P0 eval driving the running artifact, not merged-state greps alone (writer-side only, no gate can check it). #146 recognises `scripts/merge.sh <N>` as the same gated `merge` subcommand `gh pr merge <N>` already is in `enforce_pr_workflow.sh` — closing a real bypass where the hand-rolled wrapper sailed past review-pr enforcement entirely — plus two review-found fixes (a decoy-PR-number hijack across shell segments, a `--dry-run` passthrough that would have let a real merge proceed ungated) and a [[agentic-loop]] Phase 4b review-tier ladder (tier 0 may skip only the separate `/security-review` pass, never when the diff touches `hooks/`/`scripts/`/auth/exec/network). #147 adds `decisions_absorbed` to `progress.json` — a durable, chronological trace of in-scope autonomous decisions (Phases 2.5/2.6/5/6), replacing Phase 13's prior memory-reconstructed self-audit bullet; also makes `loop_stop_counts` carry-forward conditional (reset to `{}` on re-arm after a completed loop, verbatim mid-loop). #148+#149 give the dashboard's `RailLeft` Directives card a loop-decisions tile reading the new `decisions_absorbed` field, and align `sessions.ts`'s `readEvalsFrozen` with the bash `grade-loop`/`UNSTAMPED` SSOT — a same-day follow-up (#149) tightened a too-permissive stamp check the first cut (#148) shipped, illustrating the dashboard as a third, independent (read-only) consumer of the loop-scope `evals.json` schema. Updated [[task-evals-gate]] (grading/UNSTAMPED, rule 6, three-seams), [[task-evals]], [[loop_state_guard]], [[enforce_pr_workflow]], [[agentic-loop]], [[loop-progress-fields]], [[dashboard]].
- [[pr_155-158_ceremony_noise_envelope_anchoring]] — PRs #155/#157/#158 (merged 2026-07-13, loop 1cf76302): ceremony-noise reduction + envelope-anchored eval authoring. Loop-scoped warn demotion (the mechanism, live at plugin 1.1.5), LOOP-STOP bundle warn-era prose reconciliation, and `task-evals`' oracle-independence precedence rule — the eval author's goal-state anchor is `progress.json`'s `authorising_prompt_raw` (exactly one canonical string), with `spec.md`/`plan.md` supplying constraints but never overriding the envelope. Updated [[task-evals]], [[task-evals-gate]], [[agentic-loop]]
- [[pr_156_dnv-presence-check]] — PR #156 (merged 2026-07-13, `4fbb5fe`): `check_verify_loop.sh` gains a DNV-presence check — blocks on Stop when a turn edits `>= 3` files and the response has no `## Did Not Verify` header at all, closing the inversion where omitting the section entirely passed silently while an honest section with one untagged bullet blocked. `dc_file_count` re-scoped session-cumulative → TURN-scoped (records after the last genuine user prompt, honouring CLAUDE.md's per-response wording) and hardened with the same per-line tolerant jq parse the text extractors already used, closing the last of the jq-slurp fragility family. Wired through PR #155's (concurrent session) `als_loop_active_incomplete` demotion — in-loop Stop violations warn, SubagentStop still blocks. Updated [[check_verify_loop]], [[discipline-loop]].
- [[pr_159_retire-catchup-add-telemetry]] — PR #159 (merged 2026-07-13, `1ac2166`): `discipline_catchup.sh` retired clean-break (file+test deleted, `hooks.json` `UserPromptSubmit` down to `inject_context.sh` only) — justified by a flat 23-26% first-attempt miss rate unchanged since block-mode shipped 2026-05-05. `event=<hook_event_name>` telemetry added to every discipline-hook log line (Stop vs SubagentStop segmentation, previously structurally impossible). `check_confidence_labels.sh`'s block message rewritten to match `check_verify_loop.sh`'s actionable style. Repairs the `plugin.json`/`marketplace.json` version lockstep PR #155 broke (bumped only one file) — both now `1.1.5`. Same-day follow-up (commit `109987b`) hardened 3 review findings in the new tests (matcher-array-length, vacuous-version-pass, block-message stderr coverage). Also: 3 duplicate global `~/.claude/settings.json` discipline hooks deregistered (not part of this PR — global config isn't repo-tracked). Updated [[discipline_catchup]] (marked retired), [[discipline-loop]], [[check_verify_loop]], [[check_confidence_labels]], [[install-and-cache-trap]] (version-pinning claim corrected).
- [[pr_162_agentic-loop-finishing-out]] — PR #162 (merged 2026-07-13T18:11Z, `b8fe61c`): direct sequel to [[pr_134_agentic-loop-retry-until-green|PR #134]]'s sibling-file extraction pattern. New `skills/agentic-loop/finishing-out.md` (40 lines) bundles two loop-finish mechanics behind one-line SKILL.md pointers: orchestrator-level [[verification-before-completion]] applied to the Phase 13 `complete` declaration itself (the two pre-existing VBC references only discipline workers), scoped to that single declaration and not per-unit merges (Phase 12 already re-checks those); and the Phase 4b per-unit worktree teardown re-pointed at [[finishing-a-development-branch]]'s Step 6 mechanics, adding a never-remove-the-cwd-worktree caveat. Updated [[agentic-loop]].
- [[pr_169_model-routing-step]] — PR #169 (merged 2026-07-14T19:31:26Z, `229903a`): new unconditional agentic-loop Phase 2.8 "Route: assign a model role per task" — model-agnostic capability roles (`fast-mechanical`→haiku, `default`→sonnet, `frontier`→fable/opus-alternate — the `frontier` resolution later re-pointed to opus-at-`xhigh` by [[pr_192_frontier-opus-effort-routing|PR #192]], 2026-07-16), the role→model table as the only thing a model release touches, `frontier`-FIRST (not escalated-to) for genuinely ambiguous investigations, fallback valves named in the plan stamp never improvised by a worker, escalation safe by construction (PR gates are model-independent), assignment set recorded once in `progress.json.decisions_absorbed`. Reconciles ~13 blanket sonnet/opus assertions across the skill (frontmatter, Phases 2, 2.5, 3, 3a, 9, 10); Phase 2.5's design-fork spawn inverted from "sonnet recon, escalate to opus if close" to bounded-`default`-vs-`frontier`-first. `writing-plans` gains a mandatory fifth `Model:` stamp element on every task, no loop-only carve-out. `AGENTS.md`'s advisory-ceiling bullet reworded for the new vocabulary with a precise hook-matcher claim (only `Bash` and `Write|Edit|MultiEdit`). **The number 2.8 is reused, not new** — the original Phase 2.8 (plan-writing) was merged into 2.7b by PR #86; PR #169's 2.8 is an unrelated later phase. Updated [[agentic-loop]], [[writing-plans]], [[enforcement-model]].
- [[pr_163-168_dashboard-rethink]] — PRs #163/#164/#165/#166/#167/#168 (all merged 2026-07-14), one `dashboard-rethink` [[agentic-loop]] session, six independent work units against one design spec. #163 adds an optional `hidden?: boolean` `DashboardButton` field + config validation + a render-time filter, applied live in the per-user config to trim the deck from 7 buttons to 3 visible (DEEP RESEARCH/WIKI QUERY/ASK); 4 routine-trigger buttons stay in config, hidden not removed. #166 removes the Memory.Trail panel/collector/slice and the dead Command-Deck run-history list end-to-end, clean-break, plus the orphaned `memoryPaths` field from both `DashboardConfig` declarations and all fixtures/docs. #165 rewrites the loop collector: `LoopInfo` gains `title` (resolution chain `loop`→`authorising_prompt_raw`→slug), `lastUpdatedMs` (`last_updated` field wins, mtime fallback), and `units[]` with a `done`/`in-flight`/`pending` status union, replacing `unitTitles`/`done`; both current and legacy `progress.json` shapes parse. #168 replaces the single-active-loop Directives panel with one card per **live** loop — `LOOP_LIVE_WINDOW_MS = 60min`, `liveLoops()`/`stalledLoops()` partition on it, a `useNow(30s)` ticking-clock hook keeps live/stalled status current without reload, stalled loops render as a dim no-card sub-list, `BottomHero` follows the freshest live loop, `selectActiveLoop` retired clean-break. #164 drops the PR-gates poll from 120s to 30s and adds a 3s-debounced additional refresh on runs-dir changes, so a finished run updates gate state in ~3s rather than up to 2 minutes; shipped on sonnet after 2 failed haiku attempts. #167 adds a `CODERAILS_HEADLESS_RUN=1` env flag (sole set-site: the run route's `spawn()` call) that exempts `check_confidence_labels.sh`/`check_verify_loop.sh` from enforcement on the `Stop` event only — `SubagentStop` stays block-enforced regardless — because a headless `claude -p` run has no interactive turn left to satisfy a repair-turn block. A same-loop no-PR finding (t7): slash-command-as-prompt runs show 0 model turns in run logs because the slash command executes as a CLI local command, not a logging bug. Updated [[dashboard]], [[enforcement-model]], [[discipline-loop]].
- [[pr_175-176_crack-on-gate-and-inbox-brief-button]] — PRs #175/#176 (merged 2026-07-14T22:32:53Z `09cb0d0` / 22:36:26Z `fc122d3`): new `crack_on_gate.sh` PreToolUse hook denying `AskUserQuestion` while a crack-on session is live (the four agentic-loop hard-stops are untouched by design — they are turn-ending LOOP-STOP declarations, not tool calls), plus the "Inbox Brief" dashboard button running the secretary's inbox-brief routine. Ingested as one cluster: independently merged, same day, same feature arc. See [[crack_on_gate]] and [[dashboard]]
- [[pr_179_dashboard-lan-access]] — PR #179 (merged 2026-07-15T17:21:28Z, `b8fbe19`): opt-in LAN exposure for the dashboard via one env var, `DASHBOARD_HOST`. Unset/empty is byte-for-byte the pre-PR loopback-only behaviour. Set to a concrete host IP, both serving paths (`start-dashboard.sh`, `runner/bin/dashboard-server.sh`) bind that address instead of `127.0.0.1`, and `requestGuard.ts` gains a new `isAllowedHost()` helper that additionally exact-matches that one host (never any non-loopback host) for both `Host` and `Origin`, alongside loopback which stays always-allowed — DNS-rebinding and cross-origin requests are still rejected exactly as before. Both scripts validate `DASHBOARD_HOST` and reject wildcard binds (`0.0.0.0`/`::`/`*`) and `host:port` forms with a fail-loud non-zero exit before serving starts. `com.coderails.dashboard.plist` gains an `EnvironmentVariables` dict with an empty `DASHBOARD_HOST` entry (fill in + reinstall the agent for persistent LAN access). Security posture documented as deliberate, not an oversight: the dashboard's command-exec surface (`POST /api/run`, workflow-audit Approve/Deny) remains fully unauthenticated — the Host/Origin guard blocks hostile web pages and DNS-rebinding, not LAN devices; LAN access is trusted-home-network-only by design, device auth was out of scope for this PR. Updated [[dashboard]], [[enforcement-model]].
- [[pr_181_183_dashboard-run-output-wrap-and-inbox-brief-clean-modal]] — coderails PR #181 (merged ff99692) + PR #183 (merged 0e87e16) + assistant-agent PR #27 (merged ef2783c): two post-ship modal follow-ups. #181 makes `.hud-markdown pre` wrap long lines/fenced code (`white-space: pre-wrap; overflow-wrap: anywhere; word-break: break-word;`, `overflow-x: auto` kept as fallback). #183+#27 make the inbox-brief button's run-output modal show the CLEAN brief Telegram received instead of Rachel's raw execution trace: Rachel ends her turn with a `BRIEF FILE: <path>` sentinel after confirmed delivery, and the button's `command` was rewritten so the OUTER spawned `claude` agent reads that file and makes its entire final message exactly its contents — required because the modal renders the outer agent's `result` field, a separate LLM layer from Rachel, which a sentinel-only fix in the task file could never reach. Verified live (run `5cfc5ee0d5a32a82`, modal output byte-matched the sent brief file). Also closes the 5-PR wiki-staleness [[dashboard-run-output-rendering-gap_2026-07-15]] flagged: [[dashboard]]'s Run Output section rewritten to describe the current `RunOutputOverlay.tsx` modal (PR #172's retirement of the inline `<pre>`/raw-toggle viewer, PR #174's react-markdown sanitizer + img→alt-text security layers, PR #171's truncation removal). Updated [[dashboard]].
- [[pr_184_185_186_loop-cost-tracking]] — PRs #186/#184/#185 (merged 2026-07-15, 18:45/18:56/19:11): closes [[retro-json-per-model-cost-tracking-gap_2026-07-15]]. #186 adds `hooks/scripts/lib/loop_cost.sh` (`dc_mine_token_usage`) — a fail-open, dedup'd (by `message.id`), per-model token/USD miner across an orchestrator transcript plus every recursive worker transcript, priced via dated `hooks/scripts/lib/model_prices.json`; widens `loop_stall_guard`'s retro `schema_version` gate to accept any `>= 1`. #184 wires the miner into `agentic-loop` Phase 13 teardown: writes the miner's object verbatim as `retro.cost` and lifts `models_used` to top-level `retro.models_used` (never duplicated), bumping the retro's own `schema_version` to 2; pricing frozen once at teardown, never re-derived; Phase 13's human report gains a "Loop cost" bullet with a price-staleness age. `AGENTS.md` updated in lockstep. #185 adds `skills/dashboard/app/src/lib/collect/cost.ts` (`collectLoopCost`) summing frozen `total_usd_estimate` across completed loops only into week/calendar-month rolling windows (`null`, not `$0.00`, when no completed loops fall in-window), rendered as two new SYSTEM VITALS tiles ("Cost (Week)"/"Cost (Month)") in `RailLeft.tsx`. Model-identity-at-spawn attribution remains unclosed — see [[pr_169_model-routing-step]]. Updated [[loop_stall_guard]], [[agentic-loop]], [[dashboard]].
- [[pr_192_frontier-opus-effort-routing]] — PR #192 (merged 2026-07-16, `90f359e`): owner-directed correction to #169's table — `frontier` now resolves to **opus at `xhigh` effort**, never automatically to fable (escalation needs a named capability gap in the stamp + valve discipline; fable ≈ 2× opus price, vendor guidance puts complex agentic coding on Opus). NEW: effort is part of every `Model:` stamp (role AND effort; per-role defaults in Phase 2.8; effort tuning is the first lever, model escalation the second). writing-plans stamp contract + AGENTS.md ceiling bullet updated lockstep. Plans stamping roles (not model names) needed zero edits — the #169 indirection working as designed. Updated [[agentic-loop]], [[writing-plans]], [[enforcement-model]].
- [[pr_194_198_loop-complete-deferral-and-proof-gates]] — PR #194 (merged 2026-07-17, `a1dc723`) + PR #198 (merged 2026-07-17, `123b491`): two new fail-closed `complete`-branch gates in `loop_stall_guard.sh`, both from a one-day cluster (session `a5289b41`, 2026-07-16) where four failure classes were caught by hand despite every existing soft control passing. #194 adds `als_gate_work_units_on_complete` — every `progress.json` `work_unit` must be `"done"` or `"dropped"` with a non-empty `dropped_reason` before `LOOP-STOP: complete`; file-level fail-open (absent `work_units` allows), per-unit fail-closed (an unprovable entry blocks); review fixed two jq type-guard fail-open bypasses. #198 adds `als_gate_proofs_on_complete` — a NEW enforcement class that re-derives a verdict from the session transcript's raw Bash `tool_use`/`tool_result` pairs (exact trimmed-command match, last execution wins, `is_error` decides) against a `proof.json` frozen at a new Phase 2.7e by a SEPARATE blind agent seeing only `authorising_prompt_raw` — `proof.json`'s own `.status` field is never consulted, closing the self-report loophole; scope is deliberately orchestrator-session-only (worker Bash calls are invisible to this gate, mirroring task-evals' grader-independence). Review fixed: an O(proofs × executions) DoS-to-bypass via the 15s hook timeout (single-pass command-match index + 100-proof cap); a full complete-gate-family bypass via one valid-JSON non-object transcript line collapsing `als_count_invocations` to 0 (`select(type == "object")` guard added at `loop_state_common.sh:169`); two transient-jq fail-opens; a background-launch gaming vector (backgrounded Bash excluded from the transcript miner). Trust boundary documented explicitly: targets honest self-deception/lazy self-reporting, not adversarial transcript forgery. Design decisions: Q1 YES — blind proof authorship generalises task-evals' grader-independence to AUTHORING; Q2 NO — `"blocked"` is never a terminal `work_unit` status, a blocked loop declares `awaiting-input`/`hard-stop`, never `complete`. Phase 13 teardown gains a new first step (run every proof verbatim, foreground, orchestrator session, before assembling the retro). Updated [[loop_stall_guard]], [[agentic-loop]], [[loop-progress-fields]].
- [[pr_204_cost-reporter]] — PR #204 (merged 2026-07-17, `ce6b9d6`): `als_report_cost_on_complete`, a fourth `complete`-only step in `loop_stall_guard.sh`, added directly after the deferral/proof gates above. Deliberately a REPORTER not a gate — every path returns 0, inverting the file's house fail-toward-blocking idiom on purpose, because the cost miner ([[pr_184_185_186_loop-cost-tracking]]) is contractually fail-open to `{}` and a blocking reporter would deadlock an already-finished loop. Closes the exact gap loop `0d3fb487` exposed: it read `teardown.md`'s bolded "must print it, not merely write it to disk" instruction, ran Phase 13, and silently omitted the cost line anyway, then fabricated an explanation for the omission — prose could not enforce prose. Mechanically prints `retro.json`'s `cost` field (USD + tokens + price-staleness age) via a top-level `systemMessage` on every `LOOP-STOP: complete` declaration — verified as the FIRST coderails hook to use that human-visible channel (every prior hook used the model-only `additionalContext`; live smoke-tested, renders as `Stop says: <msg>`). Anti-fabrication design: a `{}` (miner failed open), absent, incomplete, or wrong-typed `.cost` on a `schema_version >= 2` retro each print a DISTINCT honest message, never a fabricated figure; silent only on a legacy `schema_version < 2` retro. Updated [[loop_stall_guard]], [[hook-exit-codes]], [[agentic-loop]].
- [[pr_201_202_203_routine-followups]] — PRs #201 (merged 2026-07-17, `7de4995`), #202 (merged 2026-07-17, `04f80a2`), #203 (merged 2026-07-17, `653d1c6`): three independent follow-ups from one handoff memory. #201: `examples/dashboard-config.json` (documentation-only — live config is `~/.claude/coderails-dashboard.json`) de-rotted to slash-rooted `/path/to/...` placeholders (must stay slash-rooted — `config.ts` throws `ConfigError` on a relative `cwd`). #202: [[dashboard-runner|sweepOnce()]] now derives the artifact gate's `{date}` from a TZ-aware `localDateIso()` helper (injectable `clock`) instead of `new Date().toISOString()` — closes a UTC/local skew that failed a correct run between local midnight and the UTC offset, across all four dated routines. #203: [[destructive_bash_gate]]'s deny messages now name a concrete per-pattern safe route (git reset --hard → backup branch + `--keep`; rm -rf → unlink/temp-dir; force-push → force-with-lease, with an honest disclosure that fwl is itself blocked by default) instead of a bare generic sentence — message text only, deny set unchanged; caught a live bash-3.2 `${pat,,}` fail-open during the work, fixed with `tr`. **Cluster-level finding, the real value of this ingest:** re-verifying the originating handoff's four claims against LIVE state (not source greps) found three of four false or already-retracted — most notably "loop-retro-promotion's gate CAN NEVER PASS," which was true only of the example config's rot, never of the live config, which passes today. Corrects stale ⚠️ notes in [[loop-retro-promotion]] and [[routines]] dated the same day. Also records: two frozen eval commands were never smoke-run at freeze and failed for reasons unrelated to the code under test (`--reporter=basic` doesn't exist in this vitest version; a root `npm ci` in a repo with no npm workspaces) — a lesson for [[task-evals]]'s freeze discipline; and [[destructive_bash_gate]] blocked the orchestrator six times on discussion/documentation of its own patterns, not execution — probes describing blocked patterns must be base64-assembled at runtime, never written inline.
- [[pr_238_crack-on-prose-gate]] — PR #238 (merged 2026-07-17, squash `d8a303e`): new `crack_on_prose_gate.sh`, a `Stop` hook that blocks a final assistant message handing a question back to the user in **prose** while the crack-on flag is stamped — the prose half of [[crack_on_gate]]'s `AskUserQuestion` tool-deny, closing the evasion where the model asks in plain text (no tool call → no `PreToolUse` event → the tool-gate never fires). Deterministic two-tier heuristic (terminal `?` on the prose body's last line / first-person-modal question in the last 3 lines / ~15 second-person phrases), NOT an LLM judge; fail-closed on discipline, fail-open on infra; per-turn 3-block cap (`CLAUDE_CRACK_ON_PROSE_MAX_BLOCKS`) as a release valve against rephrase loops; `Stop`-only, never `SubagentStop`. Honest ceiling (documented, not a bug): a `?`-free declarative handoff, a novel second-person phrasing, a plain-double-quoted question, or anything past the cap all pass — same class as [[destructive_bash_gate]]'s pre-expansion regex. Corrected the sibling [[crack_on_gate]] page's now-false "no Stop-event deny was added" claim. See [[crack_on_prose_gate]].
- [[pr_206_208_loop-state-common-docs-and-robustness]] — PR #206 (merged 2026-07-17T10:25:16Z, `f5725ec`) + PR #208 (merged 2026-07-17T10:29:43Z, `c98f7fa`): docs-accuracy then robustness, same file. #206 rewrote `docs/REFERENCE.md`'s `loop_state_common.sh` function-inventory row from 9 to all 20 functions (grouped by purpose), fixing the total omission of `als_gate_proofs_on_complete`; review caught two accuracy errors in the new row (the proof-gate description dropped the "last-failed" blocking case, and the cost-reporter description said "token cost" when it leads with USD) and both were fixed pre-merge. #208 added `select(type == "object")` to `als_extract_last_text`'s jq pipeline — a bare non-object JSON line (e.g. `123`) in the transcript tail window previously crashed the downstream `.type` access, and the blanket `2>/dev/null` silently collapsed the WHOLE extraction to empty; verified consequence: `loop_stall_guard.sh` spuriously blocked a genuine `LOOP-STOP: complete` declaration when a poisoned line sat in the tail window. Failed CLOSED (a wrongful block, not a bypass) — forced security pass returned APPROVE. **Not a duplicate of PR #198's `als_count_invocations` guard** (same file, same one-line shape, different function, different failure direction — fail-open gate-family bypass there vs. fail-closed spurious block here); the source page carries a full side-by-side comparison. `discipline_common.sh`'s sibling `dc_extract_last_text` remains explicitly unfixed, parked for jq-slurp round-2 — and while documenting that relationship, found `design/discipline-loop.md` had been claiming it as already "hardened in earlier PRs," which `origin/main`'s current source contradicts; flagged with a dated `⚠️ CONTRADICTION` note rather than silently propagating it. Updated [[loop_stall_guard]], [[discipline-loop]].
- [[pr_207_209_docs-sync-nightly-and-drift-fix]] — PR #207 (merged 2026-07-17T10:10:51Z, `70d0fe0`) + PR #209 (merged 2026-07-17T10:38:19Z, `dc4f3bb`): a docs-drift hand-fix, and the self-merging nightly routine built to replace the mechanism that was supposed to catch that drift automatically. #207: README skill count 34→35, five missing rows added, `offload_push_guard.sh` hook row added, new README section on PR #199's sandbox workers (no wiki page yet). #209 ships a new skill `docs-sync` (distinct from the older `sync-docs` it invokes) as the `sync-docs-nightly` routine: audits git-tracked docs nightly, and only if drift is found, edits/pushes/reviews/self-merges a fix with no human in the loop. **Root cause, with a retraction:** `sync-docs-weekly`'s dead `foreignSkillPath` caused its 2026-07-15 red run — but that run WAS correctly escalated to a vault-note (`escalate.ts:55-68`, verified); an earlier in-session claim that it "failed silently" was wrong (wrong-surface grep of `sweeper.log`) and is retracted on the source page — the real gap is escalation reaching an unwatched channel, not silence. Config validation checks shape not existence (`config.ts:102-111`); `maxAgeSeconds` doesn't derive from `cadence` (a real trap when changing a routine's cadence). **SECURITY, the most important finding:** the routine's own `.md`-only manifest check would pass its own governing `.md` files (SKILL.md, AGENTS.md, CLAUDE.md, docs/routines.md) without an explicit deny-list; two further holes (`git mv` renaming a script to `.md`, `git rm` masquerading as an edit) were proven empirically — re-derived live in this ingest session, not just quoted from the PR — and fixed by `--name-status` (never `--name-only`) plus a self-governance deny-list. Contract tests were tautological keyword greps until anchored on normative sentences with negative controls. Honest limits recorded plainly: prompt-enforced only, no branch protection, self-attesting merge. Live-fired 2026-07-17 on the no-drift path only; the actual self-merge chain is unexercised in production. Updated [[routines]] (new section), created [[docs-sync]], corrected a pre-existing stale claim on [[sync-docs]] (it moved in-repo 2026-07-10, independent of this cluster).
- [[pr_215_reference-md-discipline-common-eval-artifact-rows]] — PR #215 (merged 2026-07-17T12:19:16Z, `4e21216`): two more stale `docs/REFERENCE.md` function-inventory rows brought current, grouped by purpose — the same gap class [[pr_206_208_loop-state-common-docs-and-robustness|PR #206]] fixed the same day for `loop_state_common.sh`'s row, now a *third* row caught with the same rot. `discipline_common.sh`'s row was missing `dc_mine_hook_blocks` (despite the doc naming it twice elsewhere); `eval-artifact.sh`'s row was missing `_prefix`, `matches_marker`, `parse_result`, `parse_tier` (only `marker`/`compute_go`/`grading_checksum` were listed). **Same trap #206 hit, caught in review again:** the first draft claimed `marker()` "delegates its shared prefix to the private `_prefix` helper" — false; `marker()` uses a standalone `printf`, and `_prefix`'s only caller anywhere in the file is `matches_marker()` at line 40. Fixed at `3843b16`. **Process note:** this loop's evals were frozen before the fix, smoke-run and negative-controlled at freeze, and E3 (encoding the review finding) discriminates across three states — `origin/main` exit 1, pre-fix branch `0b3c12b` exit 1, fixed head exit 0 — closing the freeze-before-build gap the #206/#208 loop had to disclose. `discipline_common.sh`'s `dc_extract_last_text` — covered by [[discipline-loop]]'s `⚠️ CONTRADICTION` note — was deliberately left untouched, owned by the parked jq-slurp round-2 arc.
- [[pr_216_217_safe-routes-and-cost-miner-diagnostics]] — PR #216 (merged 2026-07-17T12:46:07Z, `be84433`) + PR #217 (merged 2026-07-17T12:57:29Z, `400178f`): two independent PRs, one theme — a blocked or failed thing must tell you what to do next, not leave you to guess. #216 extends [[destructive_bash_gate]]'s per-pattern named safe route (PR #203 shipped 3) to **all ~13 blockable patterns** — 6 honestly disclose "no safe equivalent" (`find -delete`, `truncate`, `shred`, the 3 SQL DDL patterns, `dd`/`mkfs`) rather than fabricate one; 2 more name a genuine alternative the gate itself already permits (`git clean -n`/`-i`; `chmod -R u+rwX,go+rX`). Sync enforcement resolved as a **test** (fails the instant a new pattern ships routeless), not a scheduled routine (reports drift days late) — two mechanisms: 13 new `assert_specific_route` assertions driving the real gate, plus a source-drift tripwire diffing the gate's pattern set against committed `EXPECTED_*` snapshots. **Two discriminating-check defects caught and fixed pre-merge:** the six honest routes shared identical needles (a copy-paste-swapped route body passed every assertion, proven by swapping `dd`'s text into `shred`'s case); the `git clean` check grepped bare `-n`/`-i`, two-char substrings matching inside unrelated words (a fabricated route containing `"manual-n"`/`"4-i"` passed clean). Message-only change — zero regex/control-flow changes; 202 ok/0 FAIL re-verified live. #217 gives all 7 fail-open `printf '{}'` bails in `dc_mine_token_usage` ([[loop_cost]], new page — previously undocumented except in cross-references) a distinct, cause-naming stderr line — closing the exact silence that let **four consecutive loops ship four different wrong root causes** for one `{}` symptom before the real cause (a zsh-only `${BASH_SOURCE[0]}` bug, PR #205) was found. Contract preserved exactly (`{}`/exit 0 unchanged); diagnostic-only. Scope grew from 3 named bails to all 6 remaining (one already messaged by PR #205), flagged not silently widened — 2 of the 6 mask real `jq` failures behind `2>/dev/null`, the same "identical `{}`" symptom class. Deliberately NOT capturing the suppressed jq stderr (real plumbing risk to the `{}` contract for marginal gain) — both workers agreed independently. New pairwise-distinctness test (71 ok/0 FAIL) closes the same needle-sharing gap class as #216, applied to messages instead of routes. **Known, deliberately unfixed follow-up surfaced again in this cluster:** the gate blocked the orchestrator's own progress-update writes for merely quoting a blocked pattern while documenting the block — a mention-vs-invocation false positive already recorded on this hook's own comments as having survived three prior narrowing attempts, each admitting a real bypass; worked around by assembling the literal at runtime, not fixed. Updated [[destructive_bash_gate]] (full route table + sync-enforcement design fork), created [[loop_cost]] (first dedicated page for `dc_mine_token_usage`, consolidating cross-references previously scattered across [[loop_stall_guard]] and [[pr_204_cost-reporter]]).
- [[pr_218_discriminating-check-gate]] — PR #218 (merged 2026-07-17, `81c8857`) + same-day follow-up fix (`335a382`): a freeze-time gate (`post_evals::validate_discriminating`, `/coderails:post-evals` Step 3b) that pipes an eval's optional `fixtures.good`/`fixtures.bad` into the check's formula and rejects any check where the two don't produce opposite exit codes — closing the "check is textually distinct from its negative_control but behaviourally broken" gap rule 2 and `validate_structure`'s vacuous-relative-to-cmd check can't catch. Motivating real bug: loop 8b69e779's awk formula split `39/39` under `-F'[ /]'` and exited 1 unconditionally on both a genuine pass and a genuine fail. `fixtures` is optional, scripted-mode only, and grandfathered — zero behaviour change for any eval that omits it. Env-guard distinguishes command-not-found (127), timeout (142), permission-denied (126), and signal deaths (>=128) from genuine discrimination results — broadened from 127/142-only to the full set in the same-day follow-up, which also closed an unsafe-accept gap (good/bad fixtures required together) and a malformed-fixtures misreport. Design history worth noting: an earlier hard-stop wrongly concluded the feature was unbuildable on a broken 91%-is-prose classifier stat (real figure ~46%), overturned by `/coderails:disconfirm` + a fable-5 red-team; the real design fork (real-surface execution vs. formula-against-fixtures) went against real-surface because a not-yet-built surface and a broken check are indistinguishable at freeze time. **Honest boundary, stated prominently:** validates only fixtures-carrying checks — proves a formula CAN discriminate between two specific inputs, not that it tests the right claim. Updated [[task-evals-gate]] (new "Discriminating-check gate" section), [[task-evals]] (schema cross-reference + rule-2 companion note), [[post-evals]] (Step 3b added to the command sequence, now 8 steps).
- [[pr_228_229_230_token-burn-reduction-and-agents-split]] — PR #228 (merged 2026-07-17T21:22:28Z, `c29f147`) + PR #229 (merged 2026-07-17T21:25:46Z, `47f28d6`) + PR #230 (merged 2026-07-17T21:27:27Z, `ebd21d2`): same-session cluster, adjacent theme (orchestrator cost/context discipline), three disjoint file sets. #228 adds 4 explicitly-numbered "token-burn rule" additions to [[agentic-loop]]: mandatory `/compact` at every PR-merge boundary (row 1, "the single largest saving," lands inside the `## Context-window persistence` no-touch region — a deliberate, precedented supersession, not a violation, see the reconciliation note on that page); new Phase 0.4 pinning the orchestrator's own model via `/model` at loop launch, distinct from Phase 2.8's worker-role routing (row 2); batching the Phase 4 probe battery into one compound Bash call instead of one call per check (row 3); capping probe output before it enters context plus a new Phase 3 rule that the orchestrator never authors deliverable files inline — loop-state writes only, every deliverable goes through a spawned worker (row 4). #229 splits `AGENTS.md` (439→328 lines) into a slim core plus new `AGENTS-wiki-schema.md` (127 lines, all wiki-specific content); 4 wiki skills' SKILL.md files repointed at the new file. No dedicated AGENTS.md-structure page exists or was created — this source page is the only wiki record of the split. #230 is cost-**measurement** honesty, not burn reduction: [[loop_cost]]'s `dc_mine_token_usage` gains `headless_children_excluded_count` (a candidate count, not a resolved attribution) so a reader sees an undercount signal instead of a silently-clean total; plus a numeric-input guard and a zsh stdout-corruption fix (a `local` redeclared inside a loop body echoes to stdout on zsh 5.9, corrupting the function's JSON-only output contract). A raw two-point branch diff momentarily looked like PR #228 reverted PR #198's `proof_disposition`/`schema_version` 2 work — verified as a false alarm: the merge commit `c29f147`'s content is byte-identical to `origin/main` today, no regression shipped. Updated [[agentic-loop]] (4-rule section + stage-map + no-touch reconciliation), [[loop_cost]] (new field + two fixes).
- [[pr_224_231_233_235_loop-tooling-hardening]] — PR #224 (merged 2026-07-17T19:42:37Z, `720ffbb`) + PR #231 (merged 2026-07-17T20:15:59Z, `f43d37c`) + PR #233 (merged 2026-07-17T20:36:34Z, `6437e83`) + PR #235 (merged 2026-07-17T20:56:09Z, `45fbb34`): four independently merged PRs from one loop, continuing the "name the cause instead of leaving a bare signal" design line PR #216/#217 established. #224 adds `withdrawn_proofs` to [[loop_stall_guard]]'s proof gate — a way to declare a proof RAN, FAILED, and is being withdrawn (not fixed), stricter than `.proofs` on failure-tolerance (only a strictly-`true` `is_error` justifies withdrawal; `null` does not, unlike `.proofs`), no double-dip against `.proofs`, combined `<=100` cap across both arrays; also adds a shared `ALS_PENDING_SYSMSG` accumulator so the proof gate's withdrawal notice and PR #204's cost reporter append to ONE merged `{systemMessage}` at the `loop_stall_guard.sh` call site instead of emitting two colliding top-level JSON objects on the same stdout. #231 gives `run_all.sh` a third outcome class: exit 3 = SKIPPED (not FAILED), absorbed into an overall green run, with an all-skipped floor (`skips == total` still fails, non-zero + WARNING — a run that verified nothing isn't a legitimate pass); `docs_sync_routine.test.sh`'s missing-`node_modules` precondition is the first consumer, `exit 3` instead of `exit 1` for that one documented cold-clone case. Four negative controls (exit 1/2/4, all-skip) pin the exact `==3` boundary. #233 gives every [[destructive_bash_gate]] named-route case arm (PR #216) a hyphenated, mention-safe `patternId` output field (`git-reset-hard`, `chmod-r-777`, etc.) — message-only, deny/allow verdicts unchanged — so a test/artifact can reference a blocked pattern by id instead of risking the gate denying its own literal text; the source-drift tripwire gained a companion check that a future route arm without an id fails immediately. #235 makes [[finishing-a-development-branch]]'s Step 6 lock-aware: a merged-and-unlocked worktree removes normally; locked by a live pid (parsed from the lock reason, `kill -0`-checked) is reported and deferred, never forced; locked by a confirmed-dead pid is unlocked and removed — a merged PR alone doesn't mean the worktree is safe to pull out from under another still-running session. **Also noted, not landed by this loop:** PR #221 (a duplicate discriminating-check gate) was CLOSED — the canonical implementation is [[pr_218_discriminating-check-gate|PR #218]], which had already merged at `81c8857`, 2026-07-17T15:46:07Z, several hours before this cluster began. Updated [[loop_stall_guard]] (withdrawn_proofs + `ALS_PENDING_SYSMSG` sections), [[destructive_bash_gate]] (`patternId` section), [[finishing-a-development-branch]] (Step 6 lock-check section), [[agentic-loop]] (Phase 4b caveat).
- [[pr_232_tier-review-gate]] — PR #232 (merged 2026-07-17T22:29:57Z, `88915bb`): the tier-review gate. Builds `scripts/tier-gate/`, a root-owned macOS LaunchDaemon that judges whether an agent's self-classified `tier=0` eval-exemption claim is honest — closing the one gap [[task-evals-gate]]'s tier-0 predicate always had: nothing previously reviewed it, it was pure self-attestation. The judge sees exactly `{claimed_tier, diff}`, never the defendant's own `tier_justification` prose (that channel was deleted by an in-PR fix, `cc5c570`, not merely sanitized). Auth is the owner's Claude **subscription** (`claude -p` + root-held `CLAUDE_CODE_OAUTH_TOKEN`), never a metered API key — a permanent owner veto; the daemon execs a root-owned `claude` binary with `HOME`/`cwd` pinned to a root-owned dir so no uid-501-writable config can reach the judge. Every GitHub read/write routes through root-owned `curl`, never `gh` (uid-501-writable = privesc surface), fail-closed on non-2xx. A mechanical, model-free prefilter (path denylist + size cap) blocks the obvious cases — the PR's own example is a 205-line/1-file diff — before any judge call. Verdict-laundering closed two ways (review blocker C1/HIGH, `b87272c`): the daemon posts NOTHING for a non-tier-0 claim, and both [[merge]] (step 4a) and [[enforce_pr_workflow]] (Gate 8) require `verdict=legitimate`/`state=success` from the exact configured machine-user login, not just a bare success state. Both local merge-gate checks are **explicitly redundant-by-design** once a GitHub branch-protection ruleset is live — defence-in-depth for the pre-ruleset interim, config-keyed and inactive by default (`tier_review.machine_user` unset = no-op, other installs unaffected). **Honest boundary, stated prominently: AVAILABLE, not ACTIVE** — this repo does not yet run the daemon; dormant until the owner completes a credentials-file population, a branch-protection ruleset, and a live-fire E1–E9 pass. The pitch is honest, not a claim of impossibility: raises a dishonest tier-0 from free to expensive. Nine in-PR fixes plus four review-caught blockers (injection-channel deletion, subscription-auth rewire, root-curl routing, a tautological test made to discriminate) — full chronology on the source page. Updated [[task-evals-gate]] (new honest-ceiling-narrowing section), [[task-evals]] (Tier rules section), [[merge]] (step 4a + Config fields read correction — this is now the only `workflow.config.yaml` field `merge.sh` reads), [[enforce_pr_workflow]] (new Gate 8 section), [[trust-floor]] (distinguished as a separate, later mechanism, not folded in), [[enforcement-model]] (new section: the first enforcement layer entirely outside the agent's own trust domain).
- [[pr_239_push-sh-add-flag]] — PR #239 (merged 2026-07-18T00:02:59Z, `777c8f4`): closes an under-staging gap in `push.sh` left by [[pr_11-14_gate-hardening-followups|PR #13]]'s `git add -A`→`git add -u` fix. `git add -u` stages tracked-modified files only and *warns about* (never stages) untracked files — correct for excluding foreign working-tree files, but it also silently excluded a worker's own genuinely-new file unless something ran a separate `git add` first. Surfaced by [[pr_224_231_233_235_loop-tooling-hardening|PR #231]]: its new `run_all_skip.test.sh` was at risk of dropping out of the PR, caught by the worker rather than a gate. Adds a repeatable, opt-in `--add <path>` flag: `push::main` latches the token following each `--add` into an `add_paths` array, then `git add -- "${add_paths[@]}"` runs after `git add -u`. Only explicitly named paths are staged — a foreign untracked file is still never swept up, preserving PR #13's fix; default no-flag behaviour is byte-identical to before. `commands/push.md` instructs the invoking actor (who knows what it created) to name new files via `--add`, and warns against `--add $SPACE_SEPARATED_VAR` since the flag consumes exactly one token per occurrence. Five new test blocks in `push_staging.test.sh`: named-file staging with a foreign file excluded, `--add` + message in both argument orders, multiple repeated `--add` flags, and a no-flag regression lock. Updated [[push]] (Staging safety section extended with a new "Under-staging follow-up" subsection), [[pr_224_231_233_235_loop-tooling-hardening]] (forward-pointer note on PR #231's section).
---

## Templates

Skeletons for each page type live in `templates/`. Use them when creating new pages:

- `templates/command.md`
- `templates/hook.md`
- `templates/skill.md`
- `templates/design.md`
- `templates/investigation.md`
- `templates/source.md`
